;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	橙口上 会話系個別リアクション口上ファイル
;
;	会話系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;会話系 REACT0～9
;========================================================================
;・1231版よりTFLAG:REACT負荷が導入され、選択したCOMが自然な行動かどうかが参照できるように
;・0127版以降のリアクション後の地の文は、基本的にREACT負荷で分岐するようになっています
;・TFLAG:REACT負荷(0=自然な行動/1=無理のない行動/2=無理をした行動)
;-------------------------------------------------
;　会話系はあまり汎用処理には向かないので、汎用リアクション(COM)に流さず個別リアクション(REACT)
;　で完結させてしまうのが望ましいと考えます
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 今の気分を聞く・個別リアクション
;────────────────────────────────────
;・ACT派生への代入は調教対象のリアクションに応じて@ACTION_APPLY2_0にて行われる
;-------------------------------------------------
@KOJO_REACT_K13_0
;-------------------------------------------------
;・TFLAG:ACT派生(0=歯切れ悪く答えた/1=勇気を出して強く当たった/2=皮肉/3=はっきりと今の気持ちを告げた
;・4=まあまあ悪くはない/5=気持ち良いことしたい)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,0 歯切れ悪く質問に答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が消極的従うで、リアクションが皮肉を言う以外の場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 0
	;自然な行動
	;=================================================
	;REACT負荷0, 質問にはっきり答えることができなかった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 歯切れ悪く質問に答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 出掛かった言葉を飲み込み答えを濁した
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,1 勇気を出して強く当たった
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが強気に応答するの場合
;REACT分類は"拒否"にセットされる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;自然な行動
	;=================================================
	;REACT負荷0, 厳しい言葉を浴びせた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 勇気を出して強く当たった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 目を逸らし口では強気に振る舞った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,2 皮肉「今の気分は、と聞かれても」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが皮肉を言うの場合
;REACT分類は"拒否"にセットされる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;自然な行動
	;=================================================
	;REACT負荷0, 余裕の笑みで肩をすくめ「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 少し考えてから「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, かろうじて皮肉交じりに答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,3 はっきりと今の気持ちを告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
	;自然な行動
	;=================================================
	;REACT負荷0, 一緒に居られることが嬉しいと素直に口にした
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, はっきりと今の気持ちを告げた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 機嫌を損ねないよう、取り繕って答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,4 まあまあ悪くはない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が平然と従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
	;自然な行動
	;=================================================
	;REACT負荷0, そう悪く思ってはいないと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「まあまあ悪くはない」と言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 無理をした行動
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		;実は積極			視線を外し、悪くはないよと呟いた
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				精一杯の強がりで、まあまあ悪くないと答えた
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,5 気持ち良いことしたい
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が受け入れるの場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
	;自然な行動
	;=================================================
	;REACT負荷0, もっと気持ち良くなりたいと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 気持ち良いことしたい気分だと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 媚びるように敢えて淫らな行為をねだった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT分類に属さないCOM毎の分岐
;-------------------------------------------------
[SKIPSTART]
通常の場合リアクションの地の文は@TRAIN_MESSAGE2と@REACTION_MESSAGEで処理されますが
以下の部分は@TRAIN_MESSAGE2の条件から漏れる為、@REACTION_MESSAGEでのみ処理されます
COM分岐に於いても当然TFLAG:REACT派生は機能しますし、@REACTION_MESSAGE内でもREACT負荷毎に書き分けられています
SELECTCASE GET_COM_RANK()
	;自然な行動
	CASE "自然な行動"
		PRINTFORMW 
	;無理のない行動
	CASE "無理のない行動"
		PRINTFORMW 
	;無理をした行動
	CASE "無理をした行動"
		;実はまんざらでもない
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
上記のような分岐を設ける事によって、本体側の変化に対応する事は可能ですが
テンプレに於いてデフォルトで設けるには手間がかかり過ぎると考える故に、基本的にREACT内のCOM分岐には枠を設けない方針です
汎用的な内容にしてもよし、また部分的に汎用リアクション(COM)に流しても良いと思います
まあREACT負荷以外にも拡張する方法はいくらでもあったりするので、その辺りの判断は各口上作者に委ねたいと思います
3BM/4MBでも書くから兎に角考えられる要素を全部入れてくれ！的な要望がもしあれば、その時はまた考えたいなと
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
;今の気分を聞くでのCOM沈黙するはTFLAG:REACT派生 == 1(質問に答えない)扱いとなりお仕置きポイントが加算される
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT0, 今の気分を聞く

;────────────────────────────────────
;REACT1, 自分への気持ちを聞く・個別リアクション
;────────────────────────────────────
;・ACT派生への代入は調教対象のリアクションに応じて@ACTION_APPLY2_1にて行われる
;-------------------------------------------------
@KOJO_REACT_K13_1
;-------------------------------------------------
;・TFLAG:ACT派生(0=嫌いじゃない…かも知れない…/1=そんなことあるわけない！/2=困るねぇ…好きじゃないといったら怒る？
;・3=もう酷いことしなければ、もっと好きになれるかもしれない/4=はっきりと好きと告げた?5=もっとすごいことしてくれば、もっと好きになれるかも)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「嫌いじゃない…かも知れない…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が消極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 0
	;自然な行動
	;=================================================
	;REACT負荷0, どうしても嫌いとは言えなかった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 本心を隠すように目を伏せ、「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「そんなことあるわけない！」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが強気に応答するの場合
;REACT分類は"拒否"にセットされる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;自然な行動
	;=================================================
	;REACT負荷0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 皮肉「困るねぇ…好きじゃないといったら怒る？」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが皮肉を言うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;自然な行動
	;=================================================
	;REACT負荷0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「もう酷いことしなければ、もっと好きになれるかもしれない」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うで、調教対象好意が少ないか、REACT分類が平然と従う
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
	;自然な行動
	;=================================================
	;REACT負荷0, 「もっと好きになりたいから、酷いことはしないで」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「もう酷いことしなければ、好きになれるかもしれない」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 機嫌を伺うように視線を向けた後、「酷いことばかりする人は嫌い」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, はっきり好きだと告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うで、調教対象好意が1000以上
;-------------------------------------------------
IF 口上記入チェック() && TFLAG:ACT派生 == 4
	;負荷2は弾いた方が何かと都合がよさそうなのでとりあえず
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(4) && TFLAG:REACT負荷 < 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;性交奉仕中
	;=================================================
	IF 口上記入チェック() && TEQUIP:性交奉仕中 && TFLAG:REACT負荷 < 2
		IF FUNC_K13_ISLOVER()
			PRINTFORML 「はぁん……❤　もっと言って……好きって、言ってぇ❤」
			PRINTFORML %CALLNAME:MASTER%が%CALLNAME%への好意を告げるたびに、%CALLNAME%の膣がキュウッと締まり、
			PRINTFORMW %CALLNAME:MASTER%の肉棒に甘くとろけるような刺激を与えてくる……
			PRINTFORMW 「私も好き❤　いっぱい愛してあげるから……いっぱい愛してぇ❤」
		ELSE
			PRINTFORML 「あんっ……そんな風に言われたら、お股がキュンキュンしちゃう❤」
			PRINTFORML %CALLNAME:MASTER%の言葉に反応して%CALLNAME%の膣が締まり、ペニスを刺激してくる……
			PRINTFORMW 「ほぉら、ちゃんと腰を振って？　休んじゃダメだからね……♪」
		ENDIF
		RETURN 1
	ENDIF

	;自然な行動
	;=================================================
	;REACT負荷0, 愛想よく笑って大好きだと答えた
	;=================================================
	IF 口上記入チェック() && GET_COM_RANK() == "自然な行動"
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML 「そうよね、当然よねぇ、うふふふ……❤」
				PRINTFORML %CALLNAME:MASTER%の言葉を聞いた%CALLNAME%は、すっかり顔を緩ませてニタニタと笑っている……
				PRINTFORMW ………………
				PRINTFORMW 「ちょっと、何見てるのよ？　……わ、笑うんじゃないわよ！　生意気ねっ！！」
			ELSE
				PRINTFORML 「うふふ、私も大好きよ……恋人さん❤」
				PRINTFORMW 「……もっと、何度でも好きって言ってくれていいのよ？」
			ENDIF
		ELSE
			PRINTFORML 「うふふ♪　ここまで懐いてくれると、私も嬉しくなっちゃうわね♪」
			PRINTFORML 「ほら、頭を撫でてあげる♪　こっちに来なさい？」
			PRINTFORMW %CALLNAME%は%CALLNAME:MASTER%の頭を、小さな手でワシワシと乱暴に撫で回した
		ENDIF
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, はっきりと好きと告げた
	;=================================================
	IF 口上記入チェック() && GET_COM_RANK() == "無理のない行動"
		PRINTFORML 「ふふ、嬉しい♪」
		PRINTFORMW 「%FUNC_K13_CALLNAME()%が私も好きでいてくれるなら、私も%FUNC_K13_CALLNAME()%に優しくしてあげる……♪」
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 作り笑いを浮かべて、好きだよと答えた
	;=================================================
	IF 口上記入チェック() && GET_COM_RANK() == "無理をした行動"
		PRINTFORML 「ふぅん？　別に無理しなくていいのに」
		PRINTFORMW 「……まあ、ご主人様の機嫌が悪くならないように頑張った所は、ちょっと評価してあげる」
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「もっとすごいことしてくれれば、もっと好きになれるかも」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が受け入れるの場合
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(5)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;自然な行動
	;=================================================
	;REACT負荷0, 「もっとすごいことしてくれば、もっと好きになれるかも」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「もっと好きになるように、もっと可愛がって欲しい」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 「好きになれるくらい、すごいことしてくれればいい」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT分類に属さないCOM毎の分岐
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
;自分への気持ちを聞くの場合はTFLAG:REACT派生 == 1(質問に答えない)扱いとなりお仕置きポイントが加算される
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT1, 自分への気持ちを聞く

;────────────────────────────────────
;REACT2, 性についての話をする・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K13_2
;-------------------------------------------------
;　派生する可能性のあるCOMはとりあえずREACT派生分岐を設けていますが
;　&& TFLAG:REACT派生 < 1などとして汎用リアクションで派生を拾う手もあります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめてと言った
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 気持ち良くして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良くして")
	;=================================================
	;お仕置きポイント加算 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;TEQUIPによって快SOURCEが入っている場合に派生
	;=================================================
	;REACT派生1, 快楽に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 状況に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 自慰し始める
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("自慰し始める")
	;=================================================
	;REACT派生1, 「勝手にオナニーするな」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 「勝手にオナニーしてるけどいいか」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT2, 性についての話をする

;────────────────────────────────────
;REACT3, 優しく慰める・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K13_3
;-------------------------------------------------
;　派生する可能性のあるCOMにはREACT派生分岐を設けていますが
;　&& TFLAG:REACT派生 < 1などとして汎用リアクションで派生を拾う手もあります
;-------------------------------------------------
;　とりあえず会話系COMを全て網羅してありますが
;　10番台の愛撫系COM、50番台以降のその他COM等も状況によっては選択可能になります
;　必要に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 無愛想に応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("無愛想に応答する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 愛想良く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("愛想よく応答する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 強気に応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("強気に応答する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 「ふーん」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 気持ち良くして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良くして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT3, 優しく慰める

;────────────────────────────────────
;REACT4, 厳しく脅す・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理無し。SOURCE_MESSAGEでの汎用処理のみ
;-------------------------------------------------
@KOJO_REACT_K13_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT4, 厳しく脅す

;────────────────────────────────────
;REACT5, 衣装の変更を要求する・個別リアクション
;────────────────────────────────────
;・調教対象のリアクションによって一枚脱ぐ、二枚脱ぐ、脱がないに分かれる
;・REACT分類が"消極的従う", "受け入れる"の場合は一枚脱ぐ
;・REACT分類が"積極的従う", "平然と従う"の場合は二枚脱ぐ
;・その他の場合は脱がない(PALAM恥情/反抗の数値によって、恥ずかしくて脱がない、嫌がって脱がないに分かれる)
;-------------------------------------------------
@KOJO_REACT_K13_5
;-------------------------------------------------
;・TFLAG:ACT派生(0=服を脱ぐように命令/1=服を着るように命令)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 服を脱ぐように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && TFLAG:ACT派生 == 0
	;-------------------------------------------------
	;全裸になった(アクセサリ・靴下を除く)
	;-------------------------------------------------
	IF 口上記入チェック() && (TEQUIP:MASTER:下半身下着 + TEQUIP:MASTER:上半身下着 + TEQUIP:MASTER:下半身上着 + TEQUIP:MASTER:上半身上着 + TEQUIP:MASTER:全身上着) == 0
		;REACT分類 受け入れる/積極的従う/平然と従う
		IF IS_COMGRONAME("受け入れる/積極的従う")
			IF TALENT:MASTER:オトコ
				PRINTFORML 「ふふ、美味しそうな身体してるわね♪」
				PRINTFORML %CALLNAME%は%CALLNAME:MASTER%の男性器を見て舌なめずりをした……
				PRINTFORMW 「ねえ……ひょっとして期待してる？」
			ELSE
				PRINTFORML 「ふふ、可愛い身体してるわね♪」
				PRINTFORML %CALLNAME%は%CALLNAME:MASTER%の体を舐め回すように観察した
				PRINTFORMW 「さてと……どうやって食べてあげようかしら？」
			ENDIF
			RETURN 1
		ELSEIF IS_COMGRONAME("平然と従う")
			IF TALENT:MASTER:オトコ
				PRINTFORMW 「む～……もうちょっと何か反応しなさいよ、つまらないわねぇ……」
				PRINTFORML 「まあ、それはそれとして、身体の方は美味しそうね♪」
				PRINTFORML %CALLNAME%は%CALLNAME:MASTER%の男性器を見て舌なめずりをした……
			ELSE
				PRINTFORMW 「む～……もうちょっと何か反応しなさいよ、つまらないわねぇ……」
				PRINTFORML 「まあ、それはそれとして、身体の方は可愛いくて素敵ね♪」
				PRINTFORML %CALLNAME%は%CALLNAME:MASTER%の体を舐め回すように観察した
			ENDIF
			RETURN 1
		;REACT分類消極的従う			仕方なく最後の一枚を脱ぎ、全裸になった
		ELSE
			IF TALENT:MASTER:オトコ
				PRINTFORML 「ふふ、美味しそうな身体してるわね♪」
				PRINTFORML %CALLNAME%は%CALLNAME:MASTER%の男性器を見て舌なめずりをした……
				PRINTFORMW 「怖がらなくていいわよ？　気持よくしてあげる……たぶんね？」
			ELSE
				PRINTFORML 「ふふ、可愛い身体してるわね♪」
				PRINTFORML %CALLNAME%は%CALLNAME:MASTER%の体を舐め回すように観察した
				PRINTFORMW 「怖がらなくていいわよ？　……美味しく食べちゃうだけだから♪」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF

	;=================================================
	;一枚脱いだ(消極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う/受け入れる")
		
		;-------------------------------------------------
		;緩やかな動きで服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;仕方なく服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;その他・条件なし
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;二枚脱いだ(積極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;-------------------------------------------------
		;全裸になった
		;-------------------------------------------------
		IF 口上記入チェック(0) && (TEQUIP:MASTER:アクセサリ + TEQUIP:MASTER:靴下 + TEQUIP:MASTER:下半身下着 + TEQUIP:MASTER:上半身下着 + TEQUIP:MASTER:下半身上着 + TEQUIP:MASTER:上半身上着 + TEQUIP:MASTER:全身上着) == 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;積極的に脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;手際よく脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;その他・条件なし
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;脱がなかった
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("拒否")
		;恥ずかしくて脱げなかった
		;IF PALAM:MASTER:恥情 > PALAM:MASTER:反抗
		;嫌がって脱がなかった
		;ELSE
		;ENDIF

		;脱衣拒否初回
		IF !VAR_K13:0
			VAR_K13:0 = 1
			PRINTFORML 「何よ、ご主人様の命令が聞けないっていうの？」
			PRINTFORMW 「仕方ないわね、それなら……」
			PRINTL 
			PRINTFORMW 「その服、無理矢理にでも引きちぎってやるわよっ！」
			PRINTFORML %CALLNAME%はそう言うなり、%CALLNAME:MASTER%に飛びかかってきた
			PRINTFORMW 一瞬のうちに、%CALLNAME:MASTER%の体がベッドの上に押さえつけられる
			PRINTFORML %CALLNAME%の小さな左手が%CALLNAME:MASTER%の首に押し当てられ、見た目からは想像できない強い力で締めあげてくる
			PRINTFORMW %CALLNAME:MASTER%はあまりの苦しさに足をバタつかせて暴れるが、%CALLNAME%の手はビクともしない
			PRINTFORMW 「ほら、暴れないの！　すぐ終わるから、じっとしてなさいっ！」
			PRINTFORMW %CALLNAME%は空いている右手を%CALLNAME:MASTER%の服の襟元に掛け、爪を立ててグッと力を込める……
			PRINTL 
			PRINTFORMW 「うーん……」
			PRINTFORMW 今にも服を破ろうかというところで、ピタリと%CALLNAME%の動きが止まる……%CALLNAME:MASTER%の首を締めあげたまま
			PRINTFORML 「よく考えてみたら、裸のまま過ごさせて風邪でも引かれたら困るのよね……」
			PRINTFORMW 「代わりの服なんて用意できないし……」
			PRINTL 
			PRINTFORML 「……ああ、もう！　面倒ねっ！」
			PRINTFORMW %CALLNAME%はようやく%CALLNAME:MASTER%の首から手を離すと、%CALLNAME:MASTER%の上から降りてベッドの上で立ち上がった
			PRINTFORML 「ほら！　服を破くのは勘弁してあげるから、さっさと体を起こしなさいっ！」
			PRINTFORMW あからさまに苛立った声で急かしてくる%CALLNAME%
			PRINTFORMW だが、首を締められ続けた%CALLNAME:MASTER%は激しく咳き込んでしまい、酸欠で意識も朦朧としていて動くことができない……
			PRINTFORMW 「起きろって言ってるでしょっ！　またご主人様の命令を無視する気なの？」
			PRINTFORMW %CALLNAME%は%CALLNAME:MASTER%の腰を横からゲシゲシと蹴りつけてきた……
		ELSE
			IF RAND:2
				PRINTFORML 「ふぅん、ご主人様の命令が聞けないのね？」
				PRINTFORMW 「だったら、私が責任を持って、命令が聞けるようになるまで躾けてあげる」
			ELSE
				PRINTFORML 「……あんまりイライラさせないでよね」
				PRINTFORMW 「私だって無闇に怒りたくはないのよ？　出来の悪いペットちゃん」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && TFLAG:ACT派生 == 1
	;=================================================
	;一枚着た(消極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う/受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;二枚着た(積極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;地の文では"嫌がって"という扱い
	;=================================================
	;着なかった
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("拒否")
		PRINTFORML 「ちょっと！　服を着るくらいいいじゃないの！？」
		PRINTFORMW 「ホント、聞き分けのないペットね……どうやって躾けてやろうかしら？」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 衣装の変更を要求する

;────────────────────────────────────
;REACT6, 合意を求める・個別リアクション
;────────────────────────────────────
;・調教対象のリアクションに応じてREACT分類毎に派生する
;-------------------------------------------------
@KOJO_REACT_K13_6
;-------------------------------------------------
;手間を厭わないのであれば、TALENT:MASTER:オトコ等で内部で分けるのもアリかもしれません
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 小さい声で了承する
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が消極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う")
	;自然な行動
	;=================================================
	;REACT負荷0, 覚悟を決めたように、小さい声で了承した
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷0, 小さい声で了承した	
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		;実は積極			恥ずかしさに消え入りそうな声で了承した
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				悔しさに顔を歪めながらも了承した
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 好きにすればいいと答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が平然と従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う")
	;自然な行動
	;=================================================
	;REACT負荷0, 別に嫌じゃないと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 好きにすればいいと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		;実は積極			したいならいいと言った
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				勝手にすればいいと答えた
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 意を決して捧げてもいいと告げる
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う")
	;自然な行動
	;=================================================
	;REACT負荷0, 初めてをあげられて嬉しいと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 意を決して捧げてもいいと告げた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, もう守れないならばせめて優しくされるようにと同意した
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 気持ち良くしてくれるんだったら初めてをあげてもいい
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が受け入れるの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
	;自然な行動
	;=================================================
	;REACT負荷0, 初めてを捧げるから気持ち良くして欲しいと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 気持ち良くしてくれるんだったら初めてをあげてもいいと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 初めてを捧げろと言うなら、辛さを忘れられるくらい気持ち良くしてと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 合意しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類がその他の場合(その他のREACT分類の場合、@ACTION_APPLY2_6にて"拒否"にセットされる)
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("拒否")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(4)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 合意を求める

;────────────────────────────────────
;REACT7, お仕置きと宣言する・個別リアクション
;────────────────────────────────────
;・宣言してもお仕置きが発生するとは限らない
;-------------------------------------------------
@KOJO_REACT_K13_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"気弱く応答する"は一定条件でREACT分類"許しを乞う"に派生する
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめてと言った
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 「ふーん」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う") 
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「ああ、もう！　散々手間かけさせておいて、今更怯えてるんじゃないわよ！」
		PRINTFORMW 「私がわざわざ面倒な思いをしてまで、きちんと躾けてやるって言ってるの！　少しは感謝しなさいっ！！」
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「あら、何を怯えることがあるの？」
		PRINTFORMW 「こうなることがわかってて反抗してたんでしょ？　違う？」
		RETURN 1
	ENDIF
ENDIF
;REACT7, お仕置きと宣言する

;────────────────────────────────────
;REACT8, 怒鳴る・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K13_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("沈黙する")
	IF FUNC_K13_ISLOVER()
		PRINTFORML 「……だ、黙ってないで何か言いなさいよ！」
		PRINTFORMW 「叱られる%FUNC_K13_CALLNAME()%が悪いのよ？　わかってるんでしょうね！？」
	ELSE
		PRINTFORML 「……なに？　せっかく叱ってあげたのに、『ごめんなさい』もないの？」
		PRINTFORMW 「いい度胸じゃない……どうしてやろうかしら？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「……何か言ったかしら？　ひょっとして、反論があるの？」
		PRINTFORMW 「反論があるなら聞くわよ？　……途中でイラついたら腕の一本や二本、引き千切っちゃうかもしれないけど♪」
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 0
		PRINTFORML 「……何か言ったかしら？　声が小さくて聞こえないわよ？」
		PRINTFORMW 「何よりもまず、『ごめんなさい』でしょ。小さい頃に習わなかったの？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「……あのねえ……許すも何もないでしょ？」
		PRINTFORMW 「わざわざ出来損ないの%FUNC_K13_CALLNAME()%のために叱ってあげたのよ？　ご主人様に感謝しなさい！」
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「……本当に反省してるのかしら？」
		PRINTFORMW 「まあいいわ。ダメならまた叱ってやればいいだけの話だしね♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 0
		PRINTFORML 「煩いわね！　ちょっとは黙っていられないの！？」
		PRINTFORMW 「せっかく叱ってあげたっていうのに、本当に恩知らずなペットね！！」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「煩いわね！　ちょっとは黙っていられないの！？」
		PRINTFORMW 「せっかく叱ってあげたっていうのに、本当に恩知らずなペットね！！」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		IF RAND:2
			PRINTFORML 「何を怯えてるのよ！？　それじゃあまるで私が悪いみたいじゃない」
			PRINTFORMW 「悪いのはお前の方でしょ？　私が出来損ないのお前を正しく躾けてやってるの！　違う！？」
		ELSE
			PRINTFORML 「ガタガタ震えてれば、許してもらえるとでも思ってるの？」
			PRINTFORMW 「かえってイラッとするのよ、そういう態度は！！」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML 「……ふふ、ごめんなさいね」
				PRINTFORMW 「たまには引き締めておかないといけないと思って……。傷ついちゃったかしら？」
			ELSE
				PRINTFORML 「……おっと、ついつい怒鳴っちゃった」
				PRINTFORMW 「ちょっと苛立ってたのよ。……もう落ち着いたから、大丈夫♪」
			ENDIF
		ELSE
			IF RAND:2
				PRINTFORML 「……ふふ、わかればいいのよ」
				PRINTFORMW 「私も鬼じゃないからね。ちゃんと反省するなら、今回は許してあげる♪」
			ELSE
				PRINTFORML 「……ふふ、悪かったわね？　いきなり怒鳴ったりして」
				PRINTFORMW 「別に私も%FUNC_K13_CALLNAME()%を怖がらせるために言ってるわけじゃないのよ？　……ね？」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT8, 怒鳴る

;────────────────────────────────────
;REACT9, 不気味に笑う・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K13_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「……私の顔を見ただけでそんなに怖がるなんて、失礼だと思わない？」
		PRINTFORMW 「正直、傷ついちゃうわね……これはお仕置きが必要かしら？」
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「何をそんなに怖がってるの？」
		PRINTFORMW 「まあ、震えたいなら好きに震えててもいいけど……私のやることは変わらないわよ？」
		RETURN 1
	ENDIF
ENDIF
;REACT9, 不気味に笑う
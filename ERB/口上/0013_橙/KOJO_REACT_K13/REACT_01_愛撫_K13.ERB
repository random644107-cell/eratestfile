;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	橙口上 愛撫系個別リアクション口上ファイル
;
;	愛撫系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;愛撫系 REACT10～19 & REACT27
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT分類(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT分類:愛撫系の場合、基本的に愛撫系COM(COM10~COM16)が選択肢となりますが
;・状況によってはその会話系、苦痛系、その他のものが選択肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める必要はありません
;・必要と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT及びACT派生内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、リアクションに関わらず、全て拾ってしまって汎用リアクションに流れません
;・要するに、想定されたシステムの破壊に繋がります
;-------------------------------------------------
;　基本的に地の文はTFLAG:REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐し、
;　各REACT分類内でTFLAG:REACT負荷(自然な行動/無理のない行動/無理をした行動)で派生
;　ACT_MESSAGE.ERBを参照しつつの作業をお勧めします
;-------------------------------------------------
;　取り敢えずACT派生毎に枠を切り分けてありますが、一つに纏めた上で汎用的な記述で手間を減らすという手もあります
;　正直、やり方はいくらでもあるので、以下に示す方法は一つの例と思ってください
;-------------------------------------------------

;────────────────────────────────────
;REACT10, 手で愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=愛撫/1=手淫/2=ペニスバンド手淫)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K13_10
;-------------------------------------------------
;・ACTTION_APPLY2の処理なし
;・地の文はACT派生毎にREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0においては可能性のあるCOMを一通り網羅しておきます
;必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック() && !TFLAG:ACT派生
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「感じてるんでしょ？　だったらもっと素直になりなさい」
			PRINTFORMW 「ちゃんとキャンキャン鳴いて、ご主人様を楽しませるのよ」
		;状況に恥ずかしがる
		ELSE
			PRINTFORML 「あら。ペットのくせに、いっちょ前に恥ずかしがっちゃって」
			PRINTFORMW 「もっとお馬鹿さんになれるように、私が責任を持って開発してあげないとね」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;嫌がった					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
		IF RAND:2
			PRINTFORML 「ふふ、オマンコ弄られただけで、そんなだらしない顔しちゃって……」
			PRINTFORMW 「随分とペットらしくなったじゃない。もっと、好きなだけ情けなく鳴いていいのよ？」
		ELSE
			PRINTFORML 「あはっ、可愛い声♪　すっかり素直になっちゃったわね」
			PRINTFORMW 「オマンコ弄られるの気持ち良いでしょ？　ちゃんとご主人様に感謝するのよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("気持ち良い！")
		IF RAND:2
			PRINTFORML 「ちょっと……そんな声出されたら、私まで変な気分になるじゃないの」
			PRINTFORMW 「まったく、生意気なペットね。もっと虐めてやろうかしら？」
		ELSE
			PRINTFORML 「うふふ、もう気持ち良いこと以外、何も考えられないって感じね？」
			PRINTFORML 「……でも、ご主人様を無視して気持ち良くなるのは許さないわよ？」
			PRINTFORML %CALLNAME%は%CALLNAME:MASTER%のクリトリスをきゅっ、と強く抓りあげた
			PRINTFORML 強烈な痛みと快感に、思わず%CALLNAME:MASTER%の体が跳ねる
			PRINTFORMW 「ほら、ちゃんと私の目を見なさい？　イクときは私に感謝しながらイクのよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1.派生2の場合はREACT分類:消極的従う扱い
	;=================================================
	;COM32, 暴れる(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
		;-------------------------------------------------
		;暴れ失敗(REACT分類:消極的従う)
		;-------------------------------------------------
		IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;縄拘束による暴れ失敗
			IF TFLAG:REACT派生 == 1
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 手淫
;ACT派生2, ペニスバンド手淫
;━━━━━━━━━━━━━━━━━━━━━━━━
;-------------------------------------------------
IF 口上記入チェック() && GROUPMATCH(TFLAG:ACT派生, 1, 2)
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「感じてるんでしょ？　だったらもっと素直になりなさい」
			PRINTFORMW 「ちゃんとアンアン喘いで、ご主人様を楽しませるのよ」
		;状況に恥ずかしがる
		ELSE
			PRINTFORML 「おちんぽ弄られるのが、そんなに恥ずかしいの？」
			PRINTFORMW 「ペットのくせに、無駄にプライドが高いのね。生意気だわ」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「こら！　大人しくしなさい！！　生意気ね！」
			PRINTFORMW 「せっかくご主人様がちんぽシゴいてやるって言ってるのに、何が不満なのよっ！？」
		;嫌がった					REACT分類"消極的従う"
		ELSE
			PRINTFORML 「ご主人様がわざわざ、ちんぽ気持ち良くしてやってるのよ？」
			PRINTFORMW 「感謝するならともかく、失礼な態度だと思わないのかしら？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「ふぅん？　私の手でシゴかれたくらいじゃ、反応しないっていうの？」
			PRINTFORMW 「その割には、私の手の中で大きくなってきてるわね。……どういうことかしら？」
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORML 「ペットのくせに、一々煩いわね！」
			PRINTFORMW 「あんまり生意気言うと、このまま%FUNC_K13_CALLNAME()%のコレを握り潰すわよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORML 「あら？　せっかく私が手でシゴいてやってるのに、生意気な態度ね」
		PRINTFORMW 「ペットなら、素直に感じる顔を見せて、私を楽しませなさい！」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		;IF TFLAG:REACT派生 == 1
		;派生:耐久					REACT分類"消極的従う"
		;ELSE
		;ENDIF
		IF RAND:2
			PRINTFORML 「ほら、素直に気持ち良くなっちゃいなさいよ」
			PRINTFORMW 「意地を張っても、良いことなんか一つもないよ？」
		ELSE
			PRINTFORML 「ご主人様がわざわざ、ちんぽシゴいてやってるのよ？」
			PRINTFORMW 「もう少し嬉しそうな声を上げたらどうなの？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
		IF RAND:2
			PRINTFORML 「ふふ、だらしない顔しちゃって……」
			PRINTFORMW 「このまま、もっとシコシコしてあげる。ちゃんとご主人様に感謝するのよ？」
		ELSE
			PRINTFORML 「ふふ、私の手の中でちんぽが可愛らしくビクビクしてるわよ？」
			PRINTFORMW 「そのまま気持ち良くなっていいから、もっと私に感じてる顔を見せなさい？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("気持ち良い！")
		IF RAND:2
			PRINTFORML 「ちょっと……そんな声出されたら、私まで変な気分になるじゃないの」
			PRINTFORMW 「まったく、生意気なペットね。もっと虐めてやろうかしら？」
		ELSE
			PRINTFORML 「うふふ、もう気持ち良いこと以外、何も考えられないって感じね？」
			PRINTFORMW 「でも、ご主人様を無視して気持ち良くなったらダメよ？　ほら、ちゃんと私の目を見なさい？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM32, 暴れる(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("暴れる")
		;暴れ失敗(REACT分類:消極的従う)
		IF GROUPMATCH(TFLAG:REACT派生, 1, 2)
			PRINTFORML 「こら！　抵抗するなんて許さないわよ！」
			PRINTFORMW 「%FUNC_K13_CALLNAME()%は大人しく、ちんぽシゴかれてひぃひぃ言ってなさいっ！」
		;暴れ成功
		ELSE
			PRINTFORML 「こら！　大人しくしなさい！！　生意気ね！」
			PRINTFORMW 「せっかくご主人様がちんぽシゴいてやるって言ってるのに、何が不満なのよっ！？」
		ENDIF
		RETURN 1
	ENDIF
	
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT10, 手で愛撫

;────────────────────────────────────
;REACT11, 胸愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=胸愛撫/1=乳首吸い/2=揉み合い/3=乳首責め)
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
;・地の文はACT派生2, 揉み合いとその他で分岐
;・ACTION_APPLY2に於いて、ACT派生2の場合は受容、拒否の判定処理あり
;・ACT派生2, 揉み合いにおいてREACT分類(消極的従う/平然と従う/積極的従う/受け入れる)以外は"拒否"扱い
;-------------------------------------------------
;　派生毎にまともにやると手間が尋常ではないので、ACT派生分岐の上層で汎用処理したり
;　各ACT派生内のCOM分岐等は一部のものに絞ってしまうのが良いと思います
;　またACT派生で区切らずに、大分岐でまとめてしまう手もありますが、手の込んだ事をやるには不向きであると考えます
;-------------------------------------------------
@KOJO_REACT_K13_11
;-------------------------------------------------
;決まったやりかたなどというものはないので、以下に示すものはあくまで一つの例ということで
;分岐の増減、COMに流すのか否かの判断は作者の判断に委ねます
;-------------------------------------------------

;*************************************************
;COM単体の分岐 ACT派生の上層で処理
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2は挙動が異なるので条件で弾く
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる") && TFLAG:ACT派生 !=2
	;=================================================
	;快感に対して恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1を書いた場合はREACT派生0, 状況に対して恥ずかしがる扱い
	;派生1を放置した場合はここで補完し、&& TFLAG:REACT派生 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;ACT派生2の場合はACT派生2分岐内のIS_COMGRONAME("拒否")で処理
	;=================================================
	;嫌がって妨害した(REACT分類6, 暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1 && TFLAG:ACT派生 !=2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1を書いた場合はREACT派生0, 嫌がった(REACT分類1, 消極的従う)扱い
	;派生1を放置した場合はここで補完し、&& TFLAG:REACT派生 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;強がって(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1を書いた場合はREACT派生0, 未熟なテクニックに対して(REACT分類:平然と従う)扱い
	;派生1を放置した場合はここで補完し、&& TFLAG:REACT派生 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;以下、ACT派生毎の分岐
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 胸愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 乳首吸い
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者に素質(幼稚/舌使い)がない場合は発生する確立は低い
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, 揉み合い
;━━━━━━━━━━━━━━━━━━━━━━━━
;奉仕系COMが選択肢となる場合あり
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;要求に応じなかった
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("拒否")
		;恥ずかしがって
		IF PALAM:MASTER:恥情 > PALAM:MASTER:反抗
			PRINTFORMW 
		;嫌がって
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-----------------------------------------------------------------
	;以下、COMを並列化
	;手間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;-----------------------------------------------------------------
	
	;=================================================
	;REACT分類4, 受け入れる＆COM, 快感を受け入れる/気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("快感を受け入れる/気持ち良い！")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;REACT分類2, 平然と従う＆COM, 不敵に笑う/平然とやる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う") && IS_NOWCOMNAME("不敵に笑う/平然とやる")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類2, 積極的従う＆COM, 積極的やる/愛情を込めてやる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う") && IS_NOWCOMNAME("積極的やる/愛情を込めてやる")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COM嫌がって妨害/哀願に派生したらCOMへ流す
	;&& TFLAG:REACT派生 < 1自体はなくてもREACT分類の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT分類2, 消極的従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う") && IS_NOWCOMNAME("嫌がる/快感を我慢する") && TFLAG:REACT派生 < 1
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生3, 乳首責め
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者に素質(幼稚/舌使い)がない場合は発生する確立が高い
;調教対象がオトコの場合はここにしか落ちない
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT11, 胸愛撫

;────────────────────────────────────
;REACT12, 口で愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=クンニ/1=フェラ)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K13_12
;-------------------------------------------------
;・地の文はACT派生毎にREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;・派生毎に区切ってありますが、例えばACT派生1のみ書いてあとは汎用に投げるという事もできます
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, クンニ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0においては参考として可能性のあるCOMを網羅しておきます
;必要に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;COM6, もっとハードにして(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
		;-------------------------------------------------
		;「この恥知らずの豚め！」(お仕置きポイント加算)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;状況に恥ずかしがる
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;嫌がった					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, フェラ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1は派生0と同じ種類のものを違う形で提示してみます
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;状況に恥ずかしがる
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;手間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;=================================================
	;REACT分類4, 受け入れる＆COM, もっとハードにして/快感を受け入れる/気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("もっとハードにして/快感を受け入れる/気持ち良い！")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類2, 平然と従う＆COM, 不敵に笑う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う") && IS_NOWCOMNAME("不敵に笑う")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COMが妨害/哀願に派生したらCOMへ流す
	;&& TFLAG:REACT派生 < 1自体はなくてもREACT分類の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT分類2, 消極的従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う") && IS_NOWCOMNAME("嫌がる/快感を我慢する") && TFLAG:REACT派生 < 1
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT12, 口で愛撫

;────────────────────────────────────
;REACT13, アナル愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生（0=アナル愛撫/1=前立腺刺激/2=アナルバイブ出し入れ）
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K13_13
;-------------------------------------------------
;・地の文はACT派生で分岐。ACT派生1, 2分岐内の派生なし
;・ACT派生0, アナル愛撫のみREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で派生処理される
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, アナル愛撫
;ACT派生1, 前立腺刺激(オトコ限定)
;ACT派生2, アナルバイブ出し入れ(オトコで地の文分岐)
;※とりあえず共通の文を表示し、一部必要に応じて分岐させる方針で
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0においては可能性のあるCOMを網羅しておきます
;必要に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
IF 口上記入チェック()
	;=================================================
	;COM7, 許しを乞う(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
		;派生1の場合はREACT分類:なし
		;-------------------------------------------------
		;意見を出すなんていい度胸だね！(お仕置きポイント加算)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「あら？　お尻で感じちゃうのは恥ずかしい？」
			PRINTFORMW 「ふふ。今に、恥ずかしいとか考える余裕もなくなるくらい、開発してあげる♪」
		;状況に恥ずかしがる
		ELSE
			PRINTFORML 「ふふ。お尻を弄られるのには、まだ抵抗があるみたいね？」
			PRINTFORMW 「でも、%FUNC_K13_CALLNAME()%は私のペットなんだから、私のすることは何でも受け入れなきゃダメよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「ちょっと、暴れないでよ！　上手く弄れないでしょ！？」
			IF PENIS(MASTER)
				PRINTFORMW 「あんまり我儘言ってると、その汚らしい玉を毟り取るわよ！！」
			ELSE
				PRINTFORMW 「あんまり我儘言ってると、その綺麗なお尻を引き裂くわよ！！」
			ENDIF
		;嫌がった					REACT分類"消極的従う"
		ELSE
			PRINTFORML 「ほらほら、暴れないの！」
			PRINTFORMW 「下手に暴れられると手元が狂って、%FUNC_K13_CALLNAME()%のケツ穴、血まみれになっちゃうわよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「ふぅん、反抗的ねえ……」
			PRINTFORMW 「そのわりに、%FUNC_K13_CALLNAME()%のケツ穴、きゅんきゅん締まって可愛く反応してるわよ？」
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORML 「そう、私の下手な愛撫じゃ感じられないっていうのね？」
			PRINTFORMW 「……あんまり生意気なことを言ってると、このまま%FUNC_K13_CALLNAME()%のケツ穴、ズタズタに引き裂くわよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORML 「へえ……随分と余裕じゃない」
		PRINTFORML 「だったら、そんな余裕もなくなるくらい、徹底的に開発してよがらせてやるわ」
		PRINTFORMW 「楽しみにしてなさい」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORML 「あら、そんなにお尻で感じるのは嫌？」
			PRINTFORMW 「ご主人様が、お尻でよがる姿を見せろと言ってるのよ？　ちゃんと素直に従いなさい！」
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORML 「ほら、もうちょっと力を抜きなさい！」
			PRINTFORMW 「あんまり意地を張っても、%FUNC_K13_CALLNAME()%が痛い思いをするだけだよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORML 「お尻でこんなに感じちゃうなんて、恥ずかしいペットね」
		PRINTFORML 「でも、私は%FUNC_K13_CALLNAME()%のみっともない姿、好きよ♪」
		PRINTFORMW 「ふふ……まだまだ、もっと、恥ずかしいコになりましょうね？」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORML 「ふふ、そんなに気持ちいいの？」
		PRINTFORMW 「情けない姿でお尻弄られてるのに、すっごく幸せそうな顔してるわよ……❤」
		RETURN 1
	ENDIF

	;=================================================
	;COM30, 痛みを我慢する
	;=================================================
	IF 口上記入チェック() && IS_NOWCOMNAME("痛みを我慢する")
		PRINTFORML 「ふふ、まだ痛いかしら？」
		PRINTFORML 「安心しなさい、そのうち気持ち良くなってくるから」
		PRINTFORMW 「……多分ね」
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT13, アナル愛撫

;────────────────────────────────────
;REACT14, アナル舐め・個別リアクション
;────────────────────────────────────
;・ACT派生なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K13_14
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;・ACT派生はないのでCOM分岐で完全に区切ります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類"消極的従う")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;快感に恥ずかしがる
	IF TFLAG:REACT派生 == 1
		PRINTFORML 「ぷはっ……。うふふ、もっと素直に感じちゃいなさい」
		PRINTFORMW 「お尻を舐められて感じちゃう恥ずかしいペットでも、私は大歓迎だからね？」
	;状況に恥ずかしがる
	ELSE
		PRINTFORML 「なあに？　恥ずかしいの？　我慢しなさい」
		PRINTFORMW 「私はご主人様なんだから、%FUNC_K13_CALLNAME()%の恥ずかしいトコロの臭いも味も、全部調べ尽くさないといけないの♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;嫌がって妨害した			REACT分類"暴れる"
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;嫌がった					REACT分類"消極的従う"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;強がって					REACT分類"消極的従う"
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;未熟なテクニックに対して	REACT分類"平然と従う"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類"平然と従う")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;派生:哀願					REACT分類"許しを乞う"
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;派生:耐久					REACT分類"消極的従う"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類"受け入れる")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORML 「あんっ……そんなに良さそうにされると、私も気合入っちゃうわ」
	IF RAND:2
		PRINTFORML 「ほらぁ、もっとサービスしてあげる……れろっ、れろぉ、じゅるるっ❤」
		PRINTFORMW %CALLNAME%の舌使いが激しくなり、%CALLNAME:MASTER%は腰が抜けそうな感覚に襲われた
	ELSE
		PRINTFORML 「ほらぁ、もっと好きなだけよがっていいのよ？　……んちゅっ、れろ、んぶっ、じゅるぅっ❤」
		PRINTFORMW %CALLNAME%の舌で尻穴の奥まで舐めしゃぶられ、%CALLNAME:MASTER%は声にならない声を上げて悶えた
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類"受け入れる")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORML 「ぷはっ……❤　うふふ、すっかりお尻責められるのが癖になっちゃったわね？」
	IF RAND:2
		PRINTFORML 「ほらぁ、もっとサービスしてあげる……れろっ、れろぉ、じゅるるっ❤」
		PRINTFORMW %CALLNAME%の舌使いが激しくなり、%CALLNAME:MASTER%は腰が抜けそうな感覚に襲われた
	ELSE
		PRINTFORML 「ほらぁ、もっと好きなだけよがっていいのよ？　……んちゅっ、れろ、んぶっ、じゅるぅっ❤」
		PRINTFORMW %CALLNAME%の舌で尻穴の奥まで舐めしゃぶられ、%CALLNAME:MASTER%は声にならない声を上げて悶えた
	ENDIF
	RETURN 1
ENDIF
;REACT14, アナル舐め

;────────────────────────────────────
;REACT15, キス・個別リアクション
;────────────────────────────────────
;・ACT派生なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;その他反応
;	COM:自慰し始める
;-------------------------------------------------
@KOJO_REACT_K13_15
;-------------------------------------------------
;・地の文はREACT分類(積極的従う/受け入れる/平然と従う/許しを乞う/消極的従う)で分岐
;・TEQUIP:性交中は体位毎、TEQUIP:性交奉仕中はTCVAR:MASTER:焦らし度で派生する
;・可能性のあるものを一通り置いておきます。必要に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT15, キス

;────────────────────────────────────
;REACT16, 性器を擦り合う・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生（=0,貝あわせ/=1,素股/=2,ペニスバンド素股）
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K13_16
;-------------------------------------------------
;・地の文はACT派生毎にREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う, 消極的従う)で分岐
;・必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 貝あわせ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 素股
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, ペニスバンド素股
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT16, 性器を擦り合う

;────────────────────────────────────
;REACT17, パイズリする・個別リアクション
;────────────────────────────────────
;・ACT派生なし
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K13_17
;-------------------------------------------------
;・地の文はCOM:腰を振る、REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;・愛撫系COMを網羅。必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし時には選択不可
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	;=================================================
	;イきそう状態で焦らし終了/焦らし無し
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 < 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT17, パイズリする

;────────────────────────────────────
;REACT18, 足コキする・個別リアクション
;────────────────────────────────────
;・ACT派生なし
;-------------------------------------------------
;想定されるCOM
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K13_18
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;・必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「せっかくご主人様が気持ちよくしてやってるのに、随分と失礼な態度ね？」
		PRINTFORMW 「あんまり舐めた態度をとってるようなら、このまま蹴り潰すわよ？」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「何を我慢してるのよ」
		PRINTFORMW 「踏まれて感じるのが悔しいの？　そんなプライド、私のペットには必要ないわ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
	IF RAND:2
		PRINTFORML 「踏まれてそんな声出すなんて、恥ずかしいペットね……」
		PRINTFORMW 「ほらほら、もっとご主人様の下で情けなくよがって見せなさい！」
	ELSE
		PRINTFORML 「そんなに足でされるのが気持ちいいの？」
		PRINTFORMW 「さっきから顔が緩みっぱなしで、面白いことになってるわよ？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("気持ち良い！")
	IF RAND:2
		PRINTFORML 「こら！　いくらなんでも喜びすぎよっ！」
		PRINTFORML %CALLNAME%は突然ぐっと足に掛ける力を強めると、ペニスを押しつぶすかのように強く踏み付けてきた
		PRINTFORMW 「これじゃ、私が%FUNC_K13_CALLNAME()%に奉仕してるみたいじゃないの！」
		PRINTFORMW 「どっちがご主人様だか、わかったものじゃないわ！」
	ELSE
		PRINTFORML 「ちょっと踏まれただけでそこまでよがるなんて、とんだ変態ちんぽね」
		PRINTFORMW 「まったくもう……私まで感じてきちゃうじゃないの❤　どうしてくれるのよっ！」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「あら？　ひょっとして踏み潰されると思っちゃった？　そんなことしないわよ♪」
		PRINTFORMW 「……本当に踏み潰したらどんな反応してくれるのかしら？」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「あら？　痛かった？　ごめんなさいね」
		PRINTFORMW 「安心しなさい。ちゃんと、痛くても気持ち良くなれるように調教してあげるから♪」
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「せっかくご主人様が足で気持よくしてやってるのに、随分と失礼な態度ね」
		PRINTFORMW 「お前はアンアン喘いでいればいいの。あんまり私を怒らせるようなら、本気で踏み潰すわよ？」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「ふふ、嫌がってるわりには気持ち良さそうね」
		PRINTFORMW 「素直にお願いしてもいいんだよ……『もっと踏んでください』って？」
		RETURN 1
	ENDIF
ENDIF

;REACT18, 足コキする

;────────────────────────────────────
;REACT19, 指挿入れ
;────────────────────────────────────
;・TFLAG:ACT派生(0=指挿入れ/1=Ｇスポット刺激/2=バイブ出し入れ)
;-------------------------------------------------
;想定されるCOM
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K13_19
;-------------------------------------------------
;地の文はACT派生0のみREACT分類で分岐
;その他はTALENT:MASTER:ふたなりであるか否かで派生するのみ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 指挿入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う, 消極的従う)で分岐
;・愛撫系COMを網羅。必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;COM10, 恥ずかしがる(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
		;-------------------------------------------------
		;快感に恥ずかしがる
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;派生1の場合はREACT分類:暴れる扱い
	;=================================================
	;COM11, 嫌がる(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
		;-------------------------------------------------
		;嫌がって妨害した(REACT分類:暴れる)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		;-------------------------------------------------
		;快感を感じながら(REACT分類:消極的従う)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類:平然と従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1の場合はREACT分類:許しを乞う扱い
	;=================================================
	;COM14, 快感を我慢する(消極的従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;-------------------------------------------------
		;哀願(REACT分類:許しを乞う)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「耐久」扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, Ｇスポット刺激
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は調教対象がふたなりか否かで分岐
;・REACT分類による分岐はなく、内容は汎用的なもの
;-------------------------------------------------
;とりあえず最小限の構成にしておきます。必要に応じて分岐を追加して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;膣内から前立腺をこねくり回される感覚に%CALLNAME:MASTER%の男性器は膨張し、
	;渦を巻くような強烈な射精感がこみ上げてくる…
	;=================================================
	;調教対象がふたなり
	;=================================================
	IF 口上記入チェック(0) && TALENT:MASTER:ふたなり
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%はＧスポットを指でえぐられる感覚に
	;髪を振り乱して悶えた…
	;=================================================
	;調教対象にペニス無し
	;=================================================
	IF 口上記入チェック(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, バイブ出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は調教対象がふたなりか否かで分岐
;・REACT分類による分岐はなく、内容は汎用的なもの
;-------------------------------------------------
;とりあえず最小限の構成にしておきます。必要に応じて分岐を追加して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;膣内から前立腺をこねくり回される感覚に%CALLNAME:MASTER%の男性器は膨張し、
	;渦を巻くような強烈な射精感がこみ上げてくる…
	;=================================================
	;調教対象がふたなり
	;=================================================
	IF 口上記入チェック(0) && TALENT:MASTER:ふたなり
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%はＧスポットをバイブにえぐられる感覚に
	;髪を振り乱して悶えた…
	;=================================================
	;調教対象にペニス無し
	;=================================================
	IF 口上記入チェック(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;────────────────────────────────────
;REACT27, 押し倒す・個別リアクション
;────────────────────────────────────
;TFLAG:ACT派生（=0,押し倒す/=1,押し倒し終了）
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:気弱く応答する/無愛想に応答する/愛想よく応答する/強気に応答する/許しを乞う/気持ち良くして
;苦痛系反応
;	COM:怯える
;-------------------------------------------------
@KOJO_REACT_K13_27
;-------------------------------------------------
;地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;ACT派生毎の分岐はなしかつ、内容的にはACT派生0のもの
;ACT派生1の場合は整合性が崩れると思われる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 押し倒す
;━━━━━━━━━━━━━━━━━━━━━━━━
;この箇所は個別のCOM毎に記述するには適さないと思われるので
;REACT分類で分けておきます
;-------------------------------------------------
IF 口上記入チェック() && !TFLAG:ACT派生
	;=================================================
	;REACT分類受け入れる
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("受け入れる")
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML 「嬉しそうな顔しちゃって……私に躾けられるのがクセになっちゃったのかしら？」
				PRINTFORMW 「お望み通り、いっぱい愛して、いっぱい鳴かせてあげる❤」
			ELSE
				PRINTFORML 「あら？　組み敷かれてるのに、そんなに可愛い顔しちゃうのね❤」
				PRINTFORMW 「……なんだか私まで興奮してきちゃった♪　滅茶苦茶に苛めちゃうからね❤」
			ENDIF
		ELSE
			PRINTFORML 「組み敷かれて悦んじゃうなんて、恥ずかしい子ね♪」
			PRINTFORMW 「いいわよ……後悔するくらい、滅茶苦茶に嬲ってあげる♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("積極的従う/平然と従う")
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML %CALLNAME:MASTER%を押し倒した%CALLNAME%は、%CALLNAME:MASTER%の首筋をザラザラの舌でなぞるように舐め上げた
				PRINTFORMW 「んふ……美味しい❤　このまま、全部食べ尽くしてあげるわね？」
			ELSE
				PRINTFORML 「うふふ……押さえ込んじゃった♪　もう逃げられないわね？」
				PRINTFORMW 「それじゃ、いっぱい愛してあげるから、いい声で鳴いてよね❤」
			ENDIF
		ELSE
			PRINTFORML 「ふふ……抵抗しないってことは、私の好きにしていいのね？」
			PRINTFORMW 「お望み通り、たっぷりと躾けてあげる♪」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;REACT分類許しを乞う
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("許しを乞う")
		IF BASE:MASTER:体力 <= 500
			IF FUNC_K13_ISLOVER()
				PRINTFORML 「ふふ、もうすっかりバテバテだものね。……休ませてほしいのよね？」
				PRINTFORMW 「もちろん却下よ♪　無理やりイカされたときの、情けない声、聞かせてちょうだいね……❤」
			ELSE
				PRINTFORML 「あら、もう疲れちゃった？　許してほしいの？」
				PRINTFORMW 「ダメに決まってるでしょ。私が満足するまで、無理矢理にでも付き合わせてあげる♪」
			ENDIF
		ELSE
			IF FUNC_K13_ISLOVER()
				PRINTFORML 「そんなに嫌がらなくたっていいでしょ？　傷ついちゃうじゃない！」
				PRINTFORMW 「どっちにしても、私の好きなようにヤラせてもらうわよ！」
			ELSE
				PRINTFORML 「ふふ、そんなに怯えなくてもいいのよ？　そこまで酷いことはしない予定だから」
				PRINTFORMW 「……まあ、気分次第で予定は変わるけどね♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類消極的従う
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("消極的従う")
		IF FUNC_K13_ISLOVER()
			PRINTFORML 「あら？　あんまり乗り気じゃなさそうね？」
			PRINTFORMW 「まあ、どっちにしても私がしたいようにさせてもらうから、覚悟しなさい♪」
		ELSE
			PRINTFORML 「ふふ、そんなに怯えなくてもいいのよ？　そこまで酷いことはしない予定だから」
			PRINTFORMW 「……まあ、気分次第で予定は変わるけどね♪」
		ENDIF
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 押し倒し終了
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1,押し倒しと処理が共通なのでちょっとアレな感じの箇所
;何もせずにRETURN 1だけ返すのもアリな気もします
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
ENDIF
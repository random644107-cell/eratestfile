;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	橙口上 性交奉仕系個別リアクション口上ファイル
;
;	性交奉仕系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;性交奉仕系
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT分類(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT分類:性交奉仕系の場合、奉仕系COM(COM40~COM44)を中心の選択肢となりますが
;・状況によってはその他のものが選択肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める必要はありません
;・必要と思うものを見極め、絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、全て拾ってしまって汎用リアクションに流れません
;・調教対象の反応に関わらず、同じリアクションしかしなくなってしまいます
;・要するにこれは、想定されたシステムの破壊に繋がります
;-------------------------------------------------
;　REACTの地の文はACT_MESSAGE.ERBのTRAIN_MESSAGE2_{TFLAG:ACT}で処理されます
;　地の文は受容の場合はTFLAG:REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)
;　拒絶した場合はREACT分類(暴れる/逃げる)とREACT分類:拒否に分かれます
;　各REACT分類内でTFLAG:REACT負荷(自然な行動/無理のない行動/無理をした行動)で派生
;　REACT分類が(受け入れる/積極的従う/平然とやる)の場合は、TCVAR:MASTER:イきそう、TCVAR:MASTER:焦らし度等で細かく描写されます
;　ACT_MESSAGE.ERB REACTION_MESSAGE.ERBを参照しながらの作業をお勧めします
;-------------------------------------------------
;各REACTでは想定される反応を網羅してあります。必要に応じて分岐を増減して使用して下さい
;COMによってはREACT派生でREACT分類が変化する事に注意
;各COMの派生は基本的にREACT派生1を条件決めうちの小分岐にし、REACT派生0はその他扱いで条件なしとしています
;手間を掛けたくない場合はその他のみを書いて、派生に関わらず当該箇所を補完する事ができます
;またIF 口上記入チェック(0) && TFLAG:REACT派生 < 1とする事で派生した場合は汎用リアクションへ流すなどという事も可能です
;派生は将来増える可能性もあるので != 1などとするよりは < 1とした方が無難であると考えます
;-------------------------------------------------
;・COM20.腰を振るについて…
;・COM20, 腰を振るはACTによってREACT分類が変化
;・逆レイプの場合はREACT分類:受け入れる。その他性交奉仕系TではREACT分類:積極的従う扱いとなる
;・この変化は関数@SEX_INITIATIVEにて行われる
;-------------------------------------------------



;-------------------------------------------------
;性奉共通
;以下のコマンドで個別のREACTがなければ呼び出す
;　正常位させる、後背位させる、対面座位させる、背面座位させる
;なお、逆レイプの場合はREACTの挙動が異なるため、この部分は呼び出されない
;★個別REACTを書くときは条件判定と呼び出しの順番に気を配ること★
;-------------------------------------------------
@KOJO_REACT_K13_性奉共通
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;・TEQUIP:性交奉仕中が立っている際にREACT分類が暴れるだと拒絶扱いになる(即ち、性交奉仕系ACT以外でも拒絶処理となる場合がある)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
;個別REACTで優先処理

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
;個別REACTで優先処理

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		IF !VAR_K13:30
			PRINTFORML 「あん、ふぁあんっ❤　イキそう？　でも、まだ出しちゃダメっ！」
			PRINTFORML %CALLNAME%は%CALLNAME:MASTER%のペニスの根本に尻尾をキツく巻きつけ、射精できないようにしてきた
			PRINTFORMW 「ちゃんと奉仕できたら解いてあげる♪　私のオマンコ、全力でズコバコ突きまくりなさいっ❤」
			;尻尾フラグ
			VAR_K13:30 = 1
		ELSE
			PRINTFORML 「あっ❤　はあぁんっ❤　イイわよ、その調子ぃ❤　もっと腰振って、気持ち良くしてぇっ！」
			PRINTFORMW 「満足させてくれたらぁ、思いっきり気持ちよーく私の中でお漏らしさせてあげるっ❤」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		IF !VAR_K13:30
			PRINTFORML 「あはっ、気持ち良さそうね❤　……でも、射精はまだダーメ♪」
			PRINTFORML %CALLNAME%は%CALLNAME:MASTER%のペニスの根本に尻尾をキツく巻きつけ、射精できないようにしてきた
			PRINTFORMW 「ふふっ♪　簡単には出させてあげないわ♪　もっともっと、濃ゆい精をいっぱい溜めなさい❤」
			;尻尾フラグ
			VAR_K13:30 = 1
		ELSE
			PRINTFORML 「あんっ❤　あぅんっ❤　ねえ、射精したい？　中出ししたい？」
			PRINTFORMW 「でもまだダメ♪　もっと我慢してぇ、あんっ、いっぱいキンタマに精液溜めるのよ❤」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORML 「はぁんっ❤　必死で腰振っちゃって、そんなに気持ちいいの？」
	PRINTFORML 「せっかく私のオマンコを使わせてあげてるんだからぁ……」
	PRINTFORMW 「いっぱい腰を振って、濃ゆい精をご主人様に捧げるのよ❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORML 「あはっ♪　そんなに気持ち良さそうな声上げちゃって、可愛い子ね❤」
	PRINTFORMW 「いいのよ？　そのまま獣みたいにご主人様を犯しまくって、孕ませるくらいビュルビュル種付け射精しても❤」
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 「あんっ❤　はあんっ❤　いいわよ、その調子で、もっと激しく突いてぇっ❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 「あっ！　ひゃぁんっ！？　イイよぉっ、感じちゃうっ❤」
	PRINTFORMW 「そのまま、もっと激しくシてぇっ❤　」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("愛情を込めてやる")
	IF FUNC_K13_ISLOVER()
		IF RAND:2
			PRINTFORML 「ふぁあっ！？　あっ❤　イイっ❤　すっごくイイよぉ❤❤」
			PRINTFORMW 「今は恋人でいいからぁ、ああんっ❤　もっと、愛してぇ❤　もっと、感じさせてぇっ❤❤」
		ELSE
			PRINTFORML 「あっ、ひぃんっ、んぁああっ❤❤　そんな風にされたらぁっ、あっ！　感じちゃうっ❤」
			PRINTFORMW 「あはぁ❤　子宮が、熱いぃ……っ❤　本気で孕んじゃいそぉっ❤❤」
		ENDIF
	ELSE
		IF RAND:2
			PRINTFORML 「ふぁあっ！？　あっ❤　イイっ❤　すっごくイイよぉっ❤❤」
			PRINTFORMW 「こんなの知ったらぁ❤　ああんっ❤　本気で離れられなくなるぅっ❤　ふあああっ❤❤」
		ELSE
			PRINTFORML 「あっ、ひぃんっ、んぁああっ❤❤　そんな風にされたらぁっ、あっ！　私ダメになっちゃうぅっ❤」
			PRINTFORMW 「ひぃんっ❤　子宮が、熱いよぉ……っ❤　もっと、犯してぇ……っ❤❤」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;性奉共通

;────────────────────────────────────
;REACT95, 正常位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K13_95
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;・TEQUIP:性交奉仕中が立っている際にREACT分類が暴れるだと拒絶扱いになる(即ち、性交奉仕系ACT以外でも拒絶処理となる場合がある)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:処女喪失
	;処女のときの共通処理
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;個別REACTがなければ性奉共通に飛ぶ
CALL KOJO_REACT_K13_性奉共通
;REACT95, 正常位させる

;────────────────────────────────────
;REACT96, 後背位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K13_96
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他・ACT開始時の拒絶
	;=================================================
	IF 口上記入チェック()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:処女喪失
	;処女のときの共通処理
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;個別REACTがなければ性奉共通に飛ぶ
CALL KOJO_REACT_K13_性奉共通
;REACT96, 後背位させる

;────────────────────────────────────
;REACT97, 対面座位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K13_97
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:処女喪失
	;処女のときの共通処理
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("積極的やる")
	PRINTFORML 「にゃあんっ❤　ソコ突かれるの、好きぃっ！　気持ちイイっ❤」
	PRINTFORMW 「その調子で、もっと突いてっ、犯しまくってぇ……っ❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORML 「にゃぁあんっ❤　気持ちイイところぉ、ちんぽで押されてるっ❤　はぁああっ❤」
	PRINTFORMW 「こんな気持ち良くされたら、赤ちゃんの部屋が疼いちゃう……本当に孕んじゃいそぉっ❤❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;個別REACTがなければ性奉共通に飛ぶ
CALL KOJO_REACT_K13_性奉共通
;REACT97, 対面座位させる

;────────────────────────────────────
;REACT98, 背面座位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K13_98
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		CALL FUNC_K13_CANCEL_SEX()
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:処女喪失
	;処女のときの共通処理
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;個別REACTがなければ性奉共通に飛ぶ
CALL KOJO_REACT_K13_性奉共通
;REACT98, 背面座位させる

;────────────────────────────────────
;REACT99, アナルセックスさせる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ａ性交系から/3=Ｖ性交奉仕系から/4=Ｖ性交系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K13_99
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞でも童貞喪失にはならない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && A_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT99, アナルセックスさせる

;────────────────────────────────────
;REACT103, 逆レイプ・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:悲鳴を上げる/暴れる/怯える
;性交系反応
;	COM:腰を振る
;奉仕系反応
;	COM:消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K13_103
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・他の性交奉仕系と異なり、調教対象は拒絶できない
;・COM20,腰を振るはREACT分類:受け入れるにセットされる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;・COM:嫌がる/暴れるはREACT分類:暴れる(即ち、暴れが通る状況)にはならない
;-------------------------------------------------
;・逆レイプは挙動及びCOMの傾向がちょっと異なります
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
IF TCVAR:処女喪失
	;処女のときの共通処理
	CALL FUNC_K13_LOST_VERGIN_TARGET()
	RETURN 1
ENDIF

;逆レイプは拒絶できないので、拒絶系の分岐は設置しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		IF !VAR_K13:30
			PRINTFORML 「ああんっ❤　そんなに腰突き上げちゃって、もう出したくてたまらないのね？」
			PRINTFORML 「でもダメよ、まだ我慢しなさいっ！」
			PRINTFORML %CALLNAME%は%CALLNAME:MASTER%のペニスの根本に尻尾をキツく巻きつけ、射精できないようにしてきた
			PRINTFORMW 「もっと精液いっぱい溜めるまで、射精はおあずけ……❤　頑張ってキンタマ働かせるのよっ♪」
			;尻尾フラグ
			VAR_K13:30 = 1
		ELSE
			PRINTFORML 「ひゃんっ❤　はぁんっ❤　うんっ、イイわよ、その調子でもっと突き上げてぇっ！」
			PRINTFORMW 「満足させてくれたら出させてあげるからぁっ、射精我慢したガチガチちんぽで、もっとズコズコ突いてぇっ❤❤」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック() && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		IF !VAR_K13:30
			PRINTFORML 「あはっ、そんなに可愛い顔して、イキたい？　イキたいわよねぇ？」
			PRINTFORML 「でもダーメ♪　まだ射精させてあげないっ♪」
			PRINTFORML %CALLNAME%は%CALLNAME:MASTER%のペニスの根本に尻尾をキツく巻きつけ、射精できないようにしてきた
			PRINTFORMW 「もっともっと、可愛い表情にしてあげる❤　射精したくて泣いちゃうくらい、焦らしてあげるからね❤」
			;尻尾フラグ
			VAR_K13:30 = 1
		ELSE
			PRINTFORML 「あんっ❤　はあんっ❤　イキたい？　精液いっぱい出したい？」
			PRINTFORML 「でも私、射精我慢させられて切なそうな%FUNC_K13_CALLNAME()%の表情見ると、すっごく興奮しちゃうのよね♪」
			PRINTFORMW 「だから……ご主人様が%FUNC_K13_CALLNAME()%のちんぽでオナニーするところ、ああんっ❤　黙って見てなさい❤」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("許しを乞う")
	;派生1の場合はREACT分類:なし
	;=================================================
	;意見を出すなんていい度胸だね！(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック() && TFLAG:REACT派生 == 1
		PRINTFORML 「何よ？　ご主人様がわざわざオマンコしてあげてるっていうのに、不満なの？」
		PRINTFORMW 「……生意気なこと言うじゃない。そのちんぽ、今すぐズタズタに引き裂いてやろうかしら？」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「せっかくご主人様がオマンコ貸してあげてるのに、何を馬鹿なこと言ってるのよ？」
		IF TALENT:MASTER:オトコ
			PRINTFORMW 「雄なら雄らしく、ご主人様に中出しすることだけ考えてパコパコ腰振ってなさい」
		ELSE
			PRINTFORMW 「せっかくそんなモノが付いてるんだから、素直に射精することだけ考えてパコパコ腰振ってなさい」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック()
		PRINTFORML 「んぁっ❤　はぁんっ❤　こら、顔を隠すんじゃないの！」
		PRINTFORMW 「ほら、ご主人様の下で情けなくよがってる姿、ちゃんとご主人様に見せなさいっ！」
		RETURN 1
	ENDIF
ENDIF

;逆レイプの場合、派生1、REACT分類:暴れるになる事はないので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORML 「あはっ❤　すっごくイイ表情してるわよ？」
	PRINTFORMW 「イイ顔見せてくれたご褒美にぃ……ああんっ❤　いっぱい腰振って、ちんぽ虐めてあげるっ❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORML 「ああんっ❤　そんなに気持ち良いの？　ふふっ♪」」
	PRINTFORMW 「それじゃあもっと激しくしてあげるっ❤　んぁっ、あっ❤　どう？　イイでしょ？」
	RETURN 1
ENDIF

;逆レイプの場合はREACT分類:受け入れるになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック() && IS_NOWCOMNAME("腰を振る")
	PRINTFORML 「あっ❤　ひゃあんっ❤　イイよぉ、もっと突き上げてぇっ！！」
	PRINTFORMW 「私も、ほらぁっ❤　ナカをきゅって絞めてあげるからぁ、気持ちよーくお漏らししちゃいなさいっ❤」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;逆レイプの場合暴れが通る事はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生2,制止による暴れ失敗扱い。派生1は汎用リアクションへ
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる") && TFLAG:REACT派生 == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT103, 逆レイプ


;━━━━━━━━━━━━━━━━━━━━━━━━
;性奉時に拒否した場合の共通処理
;━━━━━━━━━━━━━━━━━━━━━━━━
@FUNC_K13_CANCEL_SEX
IF TALENT:処女
	PRINTFORML 「何よ！　ご主人様がヤラせてやるって言ってるのに、断るつもり！？」
	PRINTFORML 「一体何のために、股間にちんぽをぶら下げてるつもりよっ！！」
	PRINTFORMW 「せっかく私が……！」
	TCVAR:状態変化 = 8
	TCVAR:状態変化持続 = 6
	BASE:理性 = MAX(0, BASE:理性 - 800)
	BASE:苛立ち = MIN(MAXBASE:苛立ち, BASE:苛立ち + 800)
ELSE
	PRINTFORML 「何よ！　ご主人様がヤラせてやるって言ってるのに、断るつもり！？」
	PRINTFORMW 「一体何のために、股間にちんぽをぶら下げてるつもりよっ！！　使えないペットね！」
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性奉で調教者が処女喪失したときの共通処理
;━━━━━━━━━━━━━━━━━━━━━━━━
@FUNC_K13_LOST_VERGIN_TARGET
PRINTFORMW 「…………っ！」
PRINTFORMW %CALLNAME:MASTER%のペニスを咥え込んだ%CALLNAME%の膣からは、処女の証である純潔の血が流れ落ちた……
PRINTL 
PRINTFORMW 「んっ、く……はぁ……」
PRINTFORMW 「……ちょっと……何をボサッとしてるのよ」
PRINTFORMW 「ご主人様の命令よ。さっさと腰を振って……そのちんぽで私のナカを突きなさい」
PRINTFORMW %CALLNAME%は平然と振舞おうとしているようだが、破瓜の痛みを隠せずにいる
VAR_K13:9 = 1

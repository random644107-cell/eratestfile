;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	パチュリー口上 調教終了イベント口上ファイル
;
;	調教終了イベント口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 調教終了口上
;========================================================================
;・@EVENTCOMENDから呼び出される
;・口上主が調教終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に口上を表示
;・ARG:0の場合は固有の地の文なし。口上表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと口上表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:日常制御	調教終了時状況(1=通常/2=精力0/3=一回休み/4=失神した)
;-------------------------------------------------
@KOJO_EVENT_K8_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 調教対象が疲れている、2, 調教対象が失神した)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・失神による調教終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・調教対象消耗度に応じて疲労/失神に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→口上の表示
;・EVENT201, 介抱(日常イベント)の箇所を書く場合は、上手く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が疲れているまたは失神した
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && (ARG == 1 || ARG == 2)
	PRINTFORMW 「ちょっとやりすぎちゃったかしら」
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・通常の調教終了時の口上
;・地の文なし。PRINTLによる空行の後に口上表示
;・枠毎にイベント番号を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;大満足ボーナスを得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:今回で大満足
	;=================================================
	;調教対象の精力0
	;=================================================
	IF 口上記入チェック(100) && FLAG:日常制御 == 2
		PRINTFORMW 「我ながら完璧ね」
		PRINTFORMW 「これで暫く魔力に困ることもないでしょう」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「成果は上々ね」
		PRINTFORMW 「私にはきっと調教の才能があるんだわ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「今回はこのくらいにしといてあげるわ」
		RETURN 1
	ENDIF
ENDIF

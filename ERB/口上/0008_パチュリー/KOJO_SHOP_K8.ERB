;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	パチュリー口上 ショップイベント口上ファイル
;
;	ショップから呼び出される口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;//暫定版@/L(2015/01/15)

;========================================================================
;ショップイベント口上
;========================================================================

;────────────────────────────────────
;EV101, 衣装変更
;────────────────────────────────────
;・ショップから衣装変更を行った場合に表示
;-------------------------------------------------
@KOJO_EVENT_K8_101(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=
;　一の位…（0, もう着てる、1, 着替えた、2, 恥ずかしくて着られない、3, 嫌がって着ない、9, 持ってない）
;　十の位…（0, 通常、10, 半裸、20, 全裸、30, 下着無し）
;　百の位…コスプレのために保留
;　千の位…（0, 調教者, 1000, 調教対象）
;　例（調教対象が恥ずかしがって全裸にできなかった＝1022）
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;着替えたか着替えるのをやめた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GROUPMATCH(ARG % 10, 0, 1)
	SELECTCASE ARG / 10
		;調教者が
		;通常になった
		CASE 0
			PRINTFORMW 「この服が一番落ち着くわ」
		;半裸になった
		CASE 1
			PRINTFORMW 「下着姿を歓ぶ者も居るらしい」
		;全裸になった
		CASE 2
			PRINTFORMW 「魔力は肌で直接感じてこそよ」
		;調教対象が
		;通常になった
		CASE 100
			PRINTFORMW 「どうせ脱がすのだけれども」
		;半裸になった
		CASE 101
			PRINTFORMW 「ここでは半裸で不自由はしないの」
		;全裸になった
		CASE 102
			PRINTFORMW 「奴隷に服は不要よ」
		CASEELSE
			RETURN 0
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;EV102, 調教者変更
;────────────────────────────────────
;・調教者を交代させたときに表示
;-------------------------------------------------
@KOJO_EVENT_K8_102(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 口上主が他のキャラに任せた、1, 口上主が調教者になった)
;旧調教者は「%CALLNAME:SHOP_OLDTARGET%」、新調教者は「%CALLNAME:SHOP_NEWTARGET%」で参照できる
;口上主はTARGETで参照
;-------------------------------------------------
;・反抗刻印は調教者毎に保存されるので、MARK:MASTER:反抗刻印などとしてしまうと0にしかならないので注意
;・これは屈服刻印も同様です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;他の調教者に交代
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 0
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさまが調教者になった
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 1
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;EV110, 魔力使用
;────────────────────────────────────
;・魔力を消費して何らかの特典を購入したときに表示
;-------------------------------------------------
@KOJO_EVENT_K8_110(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 魔力使用前、1, 調教技巧、2, 知識を得る前、3, 調合知識取得、4, 魅惑取得、5, 禁断の知識取得、6, 道具使い取得、
;	7, 技術を得る前、8, 舌使い取得、9, 器用な指先取得、10, 縛り上手、11, サド消去/サド取得、12, 汚れ無視取得、 38, 着衣プレイ好き、39, 腰使い)
;イベント口上：アイテム
;引数内訳…
;ARG=(13, アイテム練成前、14, ローター練成、15,バイブ練成、16, アナルビーズ練成、 17, ペニスバンド練成、 18, アナルバイブ練成
; 19, クリキャップ練成、 20, ニプルキャップ練成、21, オナホール練成)
;イベント口上：責め具錬成
;引数内訳…
;ARG=(22, 責め具練成前、23, アイマスク練成、24, 鞭練成、 25, 縄練成、 26, ボールギャグ練成、27,  針練成、28, 浣腸セット練成
; 29, 三角木馬練成、 30, 大鏡練成、 31, 首輪練成、 32, 道具をカスタマイズした)
;その他イベント
;引数内訳…
;ARG=(33, ふたなり化/戻す、34, 巨乳化/戻す、35, 貧乳化/戻す、36, 精力の限界値上昇)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま魔力使用前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 0
	PRINTFORMDL パチュリーは小悪魔を呼び出した…
	PRINTFORMDL 「調教の効率化はお任せください！」
	PRINTFORMDL 「その対価として、魔力をほんのちょっぴり頂きますがね」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさまの調教技巧がアップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま知識を得る前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま素質を取得
;━━━━━━━━━━━━━━━━━━━━━━━━
;・必要に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 口上記入チェック(0) && (INRANGE(ARG, 3, 12) || GROUPMATCH(ARG, 38, 39))
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさまアイテム錬成前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 13
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさまアイテム練成
;━━━━━━━━━━━━━━━━━━━━━━━━
;・必要に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 口上記入チェック(0) && INRANGE(ARG, 14, 21)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま責め具練成前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 22
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま責め具錬成
;━━━━━━━━━━━━━━━━━━━━━━━━
;・必要に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 口上記入チェック(0) && INRANGE(ARG, 23, 31)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま道具をカスタマイズ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 32
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさまふたなり化orふたなり消去
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 33
	;=================================================
	;ふたなり化
	;=================================================
	IF 口上記入チェック(0) && TALENT:TARGET:ふたなり
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;ふたなり消去
	;=================================================
	IF 口上記入チェック(0) && !TALENT:TARGET:ふたなり
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま巨乳化or通常に戻す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 34
	;=================================================
	;巨乳化
	;=================================================
	IF 口上記入チェック(0) && TALENT:TARGET:巨乳
		PRINTFORMDW 
		RETURN 1
	ENDIF
	
	;=================================================
	;元に戻す
	;=================================================
	IF 口上記入チェック(0) && !TALENT:TARGET:巨乳
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;パチュリーさま貧乳化or通常に戻す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 35
	;=================================================
	;貧乳化
	;=================================================
	IF 口上記入チェック(0) && TALENT:TARGET:貧乳
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;元に戻す
	;=================================================
	IF 口上記入チェック(0) && !TALENT:TARGET:貧乳
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;EV111, ご主人様として新しい調教者を雇った
;────────────────────────────────────
@KOJO_EVENT_K8_111(ARG)
;-------------------------------------------------
;・新しい調教者がTARGETになっているため、口上主はTARGETではないので注意
;・口上主を参照したい場合はFLAG:ご主人様で
;-------------------------------------------------
;戻り値（1,表示、2,表示して加入したキャラの加入口上をスキップ）
;-------------------------------------------------
IF 口上記入チェック(100)
	;//NO:TARGETで雇った相手を参照することが可能です
	;CSTR:(FLAG:ご主人様):呼び方リスト = 元の名前_呼ばせたい名前/%CSTR:(FLAG:ご主人様):呼び方リスト%
	;人任せ
	PRINTFORMW 「何か楽して調教を進める方法は無いものかしら」
	PRINTFORML
	PRINTFORMDW ………
	PRINTFORMDW ……
	PRINTFORMDW …
	PRINTFORML 
	RETURN 1
ENDIF

;────────────────────────────────────
;EV112, パチュリーさまが調教者として加入した
;────────────────────────────────────
@KOJO_EVENT_K8_112(ARG)
;-------------------------------------------------
;・ご主人様の名前は「FLAG:ご主人様」で参照できる
;・ご主人様が雇用口上で戻り値2を返していると表示されない
;・口上主はTARGET
;-------------------------------------------------
IF 口上記入チェック(100)
	PRINTFORMW 「魔力を分けてくれるなんて随分と気前がいいのね」
	PRINTFORMW 「悪いようにはしないから安心していいわ」
	RETURN 1
ENDIF

;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	パチュリー口上 奉仕系アクション口上ファイル
;
;	奉仕系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;奉仕系 ACT50～57
;========================================================================
;・TFLAG:ACT(50=手で愛撫を強制/51=口で愛撫を強制/52=パイズリ/53=素股/55=足舐め強制/56=イラマチオ/57=胸愛撫強制)
;・TFLAG:ACT分類 == 5に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "奉仕")で拾う事が可能
;・調教対象に、調教者の身体を愛撫するように命令する
;・基本的には命令なので、この段階では従うかどうか確定していない(ACT56,イラマチオは拒否できない)
;-------------------------------------------------
;・ACT54,足コキは12/04/08版で粛清
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT51, 口で愛撫を強制
;────────────────────────────────────
;・クンニ/フェラチオ/顔面騎乗/顔面騎乗アナル/
;・調教対象に、口による愛撫を強制する
;・調教者がふたなりの場合、調教対象が仰向けなら1/2で顔面騎乗、そうでなければフェラ強制になる
;・調教者にペニスがなく、調教対象が仰向けなら顔面騎乗になる
;・顔面騎乗が選ばれていたとき、顔面騎乗アナルに変化することがある
;・全ての条件から漏れた場合は通常のクンニとなる
;・TFLAG:お仕置きフラグが立っていると発生しない
;・押し倒し中には発生しない
;・TEQUIP(ボールギャグ/三角木馬)装着中は発生しない
;・調教者がペニスバンドを装着している場合は発生しない
;・性交中/性交奉仕中には発生しない
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱いになる
;-------------------------------------------------
@KOJO_ACT_K8_51
;-------------------------------------------------
;・TFLAG:ACT派生(0=クンニ/1=フェラ/2=顔面騎乗/3=顔面騎乗アナル)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, クンニ強制
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && !TFLAG:ACT派生
	IF 口上記入チェック(100)
		PRINTFORMW 「真心を込めて奉仕なさい」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, フェラチオ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 顔面騎乗
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「口だけで私を気持ちよくできたら」
		PRINTFORMW 「もっと強く押し付けてあげるわ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 顔面騎乗アナル
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ACT派生 == 3
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「つべこべ言わずに私の尻を舐めなさい」
		PRINTFORMW 「服従する歓びを噛みしめるの」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT55, 足舐め強制
;────────────────────────────────────
;・調教者が足を舐めろと命令してくる
;・異常系から発生する場合あり
;・ターン方針が"ソフト"だと発生しない
;・本日の方針が"イチャイチャ"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP(ボールギャグ/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教者に足がない場合は発生しない
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱い
;-------------------------------------------------
@KOJO_ACT_K8_55
;-------------------------------------------------
;・地の文に分岐なし。@TARGET_DESCRIPTIONで状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 足舐め強制
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;残念ながらパチュリーの足は汚くないので
		;只奴隷を労うためのアクションである
		PRINTFORMW 「私の足でも舐めて落ち着きなさい」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT56, イラマチオ
;────────────────────────────────────
;・命令するのではなく、強引にペニスを口に突っ込む
;・調教対象の反応は未定だが、拒絶する事はできない
;・加虐系から発生する場合あり
;・ターン方針が"ノーマル", "ソフト"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP(ボールギャグ/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教者にペニスがない場合は発生しない(ペニスバンドによるイラマチオは無い)
;-------------------------------------------------
@KOJO_ACT_K8_56
;-------------------------------------------------
;・自慰中であれば解除される
;・地の文に分岐なし。地の文は↓
;・%CALLNAME:TARGET%は%CALLNAME:MASTER%の頭をつかんで、乱暴に%CALLNAME:MASTER%の喉まで突き入れた…
;-------------------------------------------------
;・調教者の勃起度合いはTCVAR:勃起で参照できる
;・TCVAR:勃起(0=まだ柔らかい/1=徐々に硬度を増しつつある/2=勃起した/2以上=反り返った)的な感じで
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, イラマチオ
;━━━━━━━━━━━━━━━━━━━━━━━━
;必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

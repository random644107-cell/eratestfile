;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	アリス口上 PRIORITY1, エンディング口上ファイル
;
;		エンディングイベント口上のファイル
;
;		PRIORITY1の条件を満たした場合にのみ表示
;
;Version		Reason
;1.70			新規作成
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV2, エンディングイベント口上
;========================================================================
@KOJO_PRI1_MS_EVENT_K14_2(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;PRIORITY1, エンディング
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && KFLAG:(FLAG:ご主人様):100 == 1 && FIRSTEVENT(0)
	[SKIPSTART]
	;現状では達成するの至難。ギミック仕込んで何とかって感じ
	;=================================================
	;調教者が依存
	;=================================================
	IF 口上記入チェック(0) && CFLAG:TARGET:依存度 > CFLAG:MASTER:依存度
	ENDIF
	
	;普通に進めるとだいたいコレ
	;=================================================
	;調教対象が依存
	;=================================================
	IF 口上記入チェック(0) && CFLAG:TARGET:依存度 < CFLAG:MASTER:依存度
		;15000以上になるとちょっと難しい
		;-------------------------------------------------
		;調教対象の依存度15000以上
		;-------------------------------------------------
		IF 口上記入チェック(0) && CFLAG:MASTER:依存度 > 15000
		ENDIF
		;その他
	ENDIF
	[SKIPEND]
	
	;-------------------------
	;汎用処理
	;-------------------------
	CALL ENDING_K14
	KFLAG:(FLAG:ご主人様):102 = 1
	RETURN 1
ENDIF

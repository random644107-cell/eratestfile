;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	アリス口上 加虐系個別リアクション口上ファイル
;
;	加虐系リアクション口上をまとめたファイル
;
;Version		Reason
;1.71			@口上記入チェック仕様に
;1.67			TEQUIPが解除される場合の処理を追加
;1.65			TFLAG:REACT負荷 == 2の場合COMに流れるように
;1.62			@IS_NOWCOMNAMEの新設に伴いSELECTCOMをIS_NOWCOMNAMEに変更
;1.60			ACT分類単体ファイルとして独立
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;加虐系
;────────────────────────────────────

;*************************************************
;REACT60, スパンキング・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_60
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(55) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF 0
	ELSEIF AFFECTION("相思相愛")
		PRINTFORMW 「ほら、お尻叩かれると興奮しちゃうんでしょう…？」
		PRINTFORM 「隠さなくてもいいのよ…私、そんな%CNAME(MASTER, TARGET)%が大好きだから
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("溺愛")
		PRINTFORMW 「中々いい声で鳴くじゃない」
		PRINTFORMW 「本当は%CNAME(MASTER, TARGET)%も愉しんでるんでしょう…？ ほら、正直にいいなさいよ」
	ELSEIF AFFECTION("眷愛")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、気がついてるかしら？」
		PRINTFORMW 「甘い声が混じってきてるるわよ…」
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「ほらほらっ…！ まだまだこんなものじゃ済まないわよっ！」
		PRINTFORMW 「徹底的に仕込んであげるわ… 覚悟しなさい！」
		PRINTFORMDW アリスのあまりにも過酷な仕打ちに、%CALLNAME:MASTER%は声を出す事も出来ない…
	ELSE
		PRINTFORMW 「泣いても叫んでもやめないわよ…」
		PRINTFORMW 「ほら、鳴きなさいよ！ 豚みたいに鳴いてみせなさい！」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(50) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「いつ聞いてもいい声で鳴くわね…」
		PRINTFORM 「%CNAME(MASTER, TARGET)%、それは誘ってるのかしら
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「本当に煩い豚ね…」
		PRINTFORMW 「いいわ、二度と耳障りな泣き声を出せないようにしてあげる…」
	ELSE
		PRINTFORMW 「さぁ、鳴きたいだけ鳴いていいわよ？」
		PRINTFORMW 「それで止めるつもりはないけどね。フフ…」
	ENDIF
	RETURN 1
ENDIF
;REACT60, スパンキング

;*************************************************
;REACT61, 鞭・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_61
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(55) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ふふっ… なんて甘美な鳴き声なのかしら…堪らないわね…」
		PRINTFORMW 「私、凄い興奮しちゃってる…」
	ELSEIF AFFECTION("溺愛")
		PRINTFORMW 「うぅ… これ、堪らないわね…」
		PRINTFORMW 「私、凄い興奮しちゃってる…」
	ELSEIF AFFECTION("眷愛")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、気がついてるかしら…？」
		PRINTFORMW 「甘い声が混じってきてるるわよ…」
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「ほらほらっ…！ まだまだこんなものじゃ済まないわよっ！」
		PRINTFORMW 「徹底的に仕込んであげるわ… 覚悟しなさい」
		PRINTFORMDW アリスの仕打ちは苛烈を極め、%CALLNAME:MASTER%は声を出す事も出来ない…
	ELSE
		PRINTFORMW 「泣いても叫んでもやめないわよ…」
		PRINTFORMW 「ほら、鳴きなさいよ！　豚みたいに鳴いてみせなさい！」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(55) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「いつ聞いてもいい声で鳴くわね…」
		PRINTFORM 「%CNAME(MASTER, TARGET)%、それは誘ってるのかしら
		CALL PRINT_SYMBOL_K14
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「本当に煩い豚ね…」
		PRINTFORMW 「いいわ、二度と耳障りな泣き声を出せないようにしてあげる…」
	ELSE
		PRINTFORMW 「さぁ、鳴きたいだけ鳴いていいわよ？」
		PRINTFORM 「それで止めるつもりはないけどね…
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF
;REACT61, 鞭・個別リアクション

;*************************************************
;REACT62, 針・個別リアクション FIRSTTIME(262)
;*************************************************
;=================================================
@KOJO_REACT_K14_62
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ふふっ…なんて甘美な鳴き声なのかしら…これ、堪らないわね…」
		PRINTFORMW 「私、凄い興奮しちゃってる…」
	ELSEIF AFFECTION("溺愛")
		PRINTFORMW 「うぅ…これ、堪らないわね…」
		PRINTFORMW 「私、凄い興奮しちゃってる…」
	ELSEIF AFFECTION("眷愛")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、気がついてるかしら…」
		PRINTFORMW 「甘い声が混じってきてるるわよ…」
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「ねぇ、忠告してあげるけど、コレ動くと危ないわよ？」
		PRINTFORMW 「無事でいたいなら大人しくする事ね。 ウフフ…」
		PRINTFORMDW アリスの仕打ちは苛烈を極め、%CALLNAME:MASTER%は声を出す事も出来ない…
	ELSE
		PRINTFORMW 「ほら、動くと危ないわよ…？」
		PRINTFORMW 「余計な怪我したくないなら、大人しくなさい…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(55) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「いつ聞いてもいい声で鳴くわね…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、それ誘ってるのかしら♪」
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「本当に煩い豚ね…」
		PRINTFORMW 「いいわ、二度と耳障りな泣き声を出せないようにしてあげる…」
	ELSE
		PRINTFORMW 「さぁ、鳴きたいだけ鳴いていいわよ？」
		PRINTFORMW 「それで止めるつもりはないけどね♪」
	ENDIF
	RETURN 1
ENDIF
;REACT62, 針

;*************************************************
;REACT63, 縄・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_63
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("許しを乞う")
	CALL 加虐系解除処理_K14
	RETURN 1
ENDIF

;他のSELECTCOMは汎用リアクションで
;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORM 「%CNAME(MASTER, TARGET)%、とっても縄が似合ってるわよ…
		CALL PRINT_SYMBOL_K14
		PRINTFORMW 「ゾクゾクしちゃう… めちゃめちゃに虐めたくなるわね…」
	ELSE
		PRINTFORMW 「%RAND_SPLIT("さぁ、これでもう動けないわよ/さて、お愉しみはこれから。ね")%…」
	ENDIF
	RETURN 1
ENDIF
;REACT63, 縄

;*************************************************
;REACT64, アイマスク・個別リアクション
;*************************************************
;アイマスク、ボールギャグ等のTEQUIP系は装着直後のREACTよりも
;それ以降のACT等でいかに上手く処理するかが肝な感じだけど
;まぁ考えれば考えるほど面倒なんで、なんともな感じ
;-------------------------------------------------
;=================================================
@KOJO_REACT_K14_64
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("許しを乞う")
	CALL 加虐系解除処理_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ふふ……その格好とってもいいわね…」
	ELSE
		PRINTFORMW 「視界を奪われた気分はいかが…？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ほら、怯えないで…」
		PRINTFORMW 「私はちゃんと此処にいるから」
	ELSE
		PRINTFORML 「そんなに怯える事ないわよ…？」
		PRINTFORMW 「別に痛めつけようって訳じゃないんだから」
		PRINTFORM 「とりあえずのところは、ね
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF
;REACT64, アイマスク

;*************************************************
;REACT65, ボールギャグ・個別リアクション FIRSTTIME(265)
;*************************************************
;=================================================
@KOJO_REACT_K14_65
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("許しを乞う")
	CALL 加虐系解除処理_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「涎垂れ流してはしたないわね…」
		PRINTFORM 「いいわ、私が全部啜ってあげる
		CALL PRINT_SYMBOL_K14
		;フェードアウト
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("キス"), 1))
		CLEARLINE 1
	ELSEIF AFFECTION("溺愛")
		PRINTFORMW 「ふふ、なんて無様なのかしら」
		PRINTFORMW 「涎垂れ流してはしたないわね…」
	ELSEIF AFFECTION("眷愛")
		PRINTFORMW 「ふふ…なんて無様なのかしら」
		PRINTFORMW 「涎垂れ流してはしたないわね…」
	ELSEIF AFFECTION("嫌悪") || T_COND("怒り") || TFLAG:お仕置きフラグ
		PRINTFORMW 「あはっ、いい格好。まるで家畜ね」
		PRINTFORMW 「あら、何か言いたいの？ でも残念…♪」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%が何を言ってるのか、私には分からないの　うふふっ」
	ELSE
		PRINTFORMW 「涎垂れ流しちゃって、無様な事この上ないわね」
		PRINTFORMW 「まぁでも、%CNAME(MASTER, TARGET)%にはお似合いの姿かしら？ フフ…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	SELECTCASE RANDREACT(2, 0)
		CASE 0
			PRINTFORM 「あら、残念ね。唸ってるだけじゃ何も分からないわ
			CALL PRINT_SYMBOL_K14
		CASE 1
			PRINTFORMW 「無駄な足掻きもそのへんにしておいたら？」
			PRINTFORMW 「そんな状態で泣こうが喚こうが、私には何が何だかサッパリだし。フフ…」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	SELECTCASE RANDREACT(3, 1)
		CASE 0
			PRINTFORMW 「あら、怯えなくていいのよ？」
			PRINTFORMW 「これからたっぷり可愛がってあげるつもりだから」
			PRINTFORMW 「ただし…、私なりのやり方で、ね」
		CASE 1
			PRINTFORMW 「そうね、仕方無い面もあるのは分かってるつもりだけど…」
			PRINTFORMW 「そうあからさまに怯えられるのは、あまりいい気はしないのよ」
			PRINTFORMW 「少なくても今は、ね……」
		CASE 2
			PRINTFORMW 「%CNAME(MASTER, TARGET)%が小動物みたいに怯えてる姿も、時には悪くないものね？」
			PRINTFORMW 「ま、いつもそんな感じでいられたら、うんざりするとは思うけど、ね」
	ENDSELECT
	RETURN 1
ENDIF
;REACT65, ボールギャグ

;*************************************************
;REACT66, 罵倒・個別リアクション
;*************************************************
;罵倒は加虐系に分類されるが、内容的には会話系と同一（会話系から罵倒に派生する場合もあり）なので
;REACT内で完結させた方が無難ではある
;COMで処理するならGET_ACTM(TFLAG:ACT)を使えば派生に対応できる
;-------------------------------------------------

;=================================================
@KOJO_REACT_K14_66
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("沈黙する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ちょ、ちょっと。軽い冗談なのに…」
		PRINTFORMW 「ねぇ、機嫌なおしてよ…」
	ELSE
		PRINTFORMW 「黙ってないで何か言ったらどうなの…？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気弱く応答する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「なによ、傷ついたの…？」
	ELSE
		PRINTFORMW 「何かしら？ 聞こえないわよ…？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;無愛想に応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("無愛想に応答する") && TFLAG:REACT負荷 < 2
	SELECTCASE RANDREACT(4, 0)
		CASE 0
			PRINTFORMW 「生意気ね…」
		CASE 1
			PRINTFORMW 「可愛くないわね……」
			PRINTFORMW 「もっとも、当然の反応だとは思うけど」
		CASE 2
			PRINTFORMW 「あら、いい度胸ね？」
			PRINTFORMW 「ま、その位じゃないと張り合いがないし、ね。フフ…」
		CASE 3
			PRINTFORMW 「ま、当然の反応だとは思うけど…」
			PRINTFORMW 「あまり好きじゃないのよ、そういうの」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;愛想良く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛想よく応答する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORM 「この変態…
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「すっかり飼いならされた感じね…」
	ENDIF
	RETURN 1
ENDIF

;無理をした行動、もうやめろと強く言ったの場合はCOMに流す
;━━━━━━━━━━━━━━━━━━━━━━━━
;強気に応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("強気に応答する") && TFLAG:REACT負荷 < 2 && TFLAG:REACT派生 < 1
	SELECTCASE RANDREACT(5, 1)
		CASE 0
			PRINTFORMW 「随分威勢が良いわね？ ま、この際一つ言っておくけど……」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の為にならないわよ、そういうの」
		CASE 1
			PRINTFORMW 「元気がいいわね。何かいい事でもあったのかしら？」
			PRINTFORMW 「ま、いいわ。今だけは言わせといてあげる……」
		CASE 2
			PRINTFORMW 「……へぇ、面白いわ」
			PRINTFORMW 「ただ、虚勢を張ったところで何の意味もないのが悲しいところだけど、ね」
		CASE 3
			PRINTFORMW 「…言うわね。ちょっと面白かったわよ」
			PRINTFORMW 「でも調子に乗ってると、痛い目にあわせるから…」
		CASE 4
			PRINTFORMW 「生意気…ね」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("皮肉を言う") && TFLAG:REACT負荷 < 2
	SELECTCASE RANDREACT(3, 2)
		CASE 0
			PRINTFORMW 「皮肉は嫌いよ…」
		CASE 1
			PRINTFORMW 「どうせなら、もう少しヒネリの効いた皮肉を期待したいものだけれど…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%には無理な相談かしら、ね？」
		CASE 2
			PRINTFORMW 「皮肉、ねぇ？ ま、そうね、私に言わせると……」
			PRINTFORMW 「今の%CNAME(MASTER, TARGET)%の境遇こそが皮肉、って感じだけどね」
	ENDSELECT
	RETURN 1
ENDIF

;無理をした行動時は弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;気持ち良くして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良くして") && TFLAG:REACT負荷 < 2
	;=================================================
	;お仕置きポイント加算
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		SELECTCASE RANDREACT(3, 5)
			CASE 0
				PRINTFORMW 「あら、物足りなかったのかしら…？」
			CASE 1
				PRINTFORMW 「なによ、まだ足りないっていうワケ？」
			CASE 2
				PRINTFORMW 「それ、遠まわしに私にケチつけてるつもり？」
		ENDSELECT
		PRINTFORMW 「今言ったこと、後悔%RANDTEXT_K14("するわよ", "させてやるから", "しないようにね")%……」
		RETURN 1
	ENDIF
	
	;相思相愛
	IF AFFECTION("相思相愛")
		SELECTCASE RANDREACT(2, 3)
			CASE 0
				PRINTFORM 「うふっ、いやらしいわね
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「私にあんな事言われた返事がそれなの？」
				PRINTFORM 「ま、別にいいけど、ね
				CALL PRINT_SYMBOL_K14
		ENDSELECT
	;その他
	ELSE
		SELECTCASE RANDREACT(2, 4)
			CASE 0
				PRINTFORMW 「プライドってものが無いのかしら…？」
				PRINTFORMW 「浅ましいわね……」
			CASE 1
				PRINTFORMW 「%CNAME(MASTER, TARGET)%の頭の中はそれしかないのね？」
				PRINTFORMW 「ま、そうね。その方が私にとって都合が良いのは確かではあるけど」
				PRINTFORMW 「それってちょっとどうなの？ って、気もするわ。フフ…」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;不敵に笑う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("不敵に笑う") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		SELECTCASE RANDREACT(2, 5)
			CASE 0
				PRINTFORMW 「なによそれ？ 反抗期？」
			CASE 1
				PRINTFORMW 「面白いわね……可愛くないけど」
				PRINTFORMW 「ただ、まぁ…、その位じゃないと張り合いがないのは確かではあるわね」
		ENDSELECT
	ELSE
		SELECTCASE RANDREACT(2, 6)
			CASE 0
				PRINTFORMW 「ふふ…上等じゃない」
				PRINTFORMDW アリスは%CALLNAME:MASTER%に不敵に笑い返してきた…
			CASE 1
				PRINTFORMW 「そんな顔ができるって事は、まだ余裕がありそうね…」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		SELECTCASE RANDREACT(2, 7)
			CASE 0
				PRINTFORM 「あら、いい声ね
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「ねぇ……この程度で悲鳴をあげる事ないと思うんだけど？」
		ENDSELECT
	ELSE
		SELECTCASE RANDREACT(2, 8)
			CASE 0
				PRINTFORMW 「うるさい豚ね…」
			CASE 1
				PRINTFORML 「ひとつ言っておくけど…」
				PRINTFORMW 「私、あまり騒がしいのは好きじゃないのよ」
				PRINTFORMW 「大人しくして頂戴」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ちょっと、怯える事ないでしょう？」
	ELSE
		PRINTFORMW 「ふふ…もっと酷い事したくなるわね…」
	ENDIF
	RETURN 1
ENDIF

;勝手にオナニーするな！に派生したら弾く
;━━━━━━━━━━━━━━━━━━━━━━━━
;自慰し始める
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("自慰し始める") && TFLAG:REACT派生 < 1
	IF AFFECTION("相思相愛")
		PRINTFORM 「あらあら、仕様のない人ね
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「そんなの許可した覚えはないわよ…」
	ENDIF
	RETURN 1
ENDIF

;*************************************************
;REACT67, 三角木馬・個別リアクション
;*************************************************
;=================================================
@KOJO_REACT_K14_67
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;拒絶処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("許しを乞う")
	CALL 加虐系解除処理_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;痛みを我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ふふ…%CNAME(MASTER, TARGET)%最高よ。なんていやらしいのかしら…」
		PRINTFORM 「腰くねらせちゃって、随分気に入ったみたいね…
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「ふふっ…情けない格好…」
		PRINTFORM 「無様で醜悪で最悪な感じね…
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	PRINTFORMW 「うふっ、いいわ……その調子よ」
	PRINTFORM 「もっといい声で鳴かせてあげるわ
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	PRINTFORMW 「あら、怯えなくていいのよ？」
	PRINTFORM 「これからたっぷり可愛がってあげるつもりだから
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;*************************************************
;REACT68, 浣腸器＋プラグ・個別リアクション RANDREACT(0)
;*************************************************
;・TFLAG:ACT派生（= 0,注入/= 1,排出/= 2,追加）
;-------------------------------------------------
;=================================================
@KOJO_REACT_K14_68
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;浣腸液注入＆追加
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GROUPMATCH(TFLAG:ACT派生, 0, 2)
	;=================================================
	;痛みを我慢する RANDREACT(0)
	;━━━━━━━━━━━━━━━━━━━━━━━━
	IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する") && TFLAG:REACT負荷 < 2
		IF AFFECTION("相思相愛")
			SELECTCASE RANDREACT(2, 0)
				CASE 0
					PRINTFORMW 「ほら、いっぱい入ったわよ…」
					PRINTFORM 「苦しいのと気持ちいいの、%CNAME(MASTER, TARGET)%はどっちなのかしら
					CALL PRINT_SYMBOL_K14
				CASE 1
					PRINTFORMW 「こんな事されてるのに興奮してるんでしょう？」
					PRINTFORM 「嫌そうな素振りも見せかけだって、ちゃんと判ってるんだから…
					CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
			ENDSELECT
		ELSE
			SELECTCASE RANDREACT(2, 1)
				CASE 0
					PRINTFORMW 「ねぇ、お腹辛いの？ それとも気持ちいいの？」
					PRINTFORMW 「ふふふ…」
				CASE 1
					PRINTFORMW 「まったく無様な事この上ないわね？」
					PRINTFORMW 「まぁ…、%CNAME(MASTER, TARGET)%には良く似合ってるけど、ね」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;悲鳴を上げる
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
		IF AFFECTION("相思相愛")
			PRINTFORMW 「ねぇ、いつも思うのだけど…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%のその悲鳴って、私を誘うためにわざとやってるんでしょ？」
			PRINTFORM 「まぁ結局乗せられちゃうから、お互い様ってとこなのかしら、ね
			CALL PRINT_SYMBOL_K14
		ELSE
			PRINTFORML 「うふっ、いいわ……その調子よ」
			PRINTFORM 「もっといい声で鳴かせてあげるわ
			CALL PRINT_SYMBOL_K14
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;怯える
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
		PRINTFORMW 「何も怯える事ないでしょう？」
		PRINTFORMW 「私は気持ちよくさせてあげるつもりなんだから。フフ…」
		RETURN 1
	ENDIF
ENDIF

;何もしないでRETURNしてEV20に渡す。※この状況の場合はCOMの口上を表示させたくない
;━━━━━━━━━━━━━━━━━━━━━━━━
;浣腸液排出＆調教対象がＡ絶頂（若しくはＡ責め射精）する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && (TFLAG:ACT派生 == 1 && TRUECHECK(GET_EX_K14("MA責め射精"), GET_EX_K14("MA絶頂")) > 0)
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;浣腸液排出
;記入チェック
;LOCAL = 0 ;off
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
;ENDIF

;-------------------------------------------------
;加虐系REACT拒絶処理（哀願を受け入れてのTEQUIP解除）
;-------------------------------------------------
@加虐系解除処理_K14
SELECTCASE TENSION(TARGET, 8)
	CASE "高い"
		PRINTFORMW 「なによ、\@ IS_NOWCOMNAME("怯える") ? そんなに怯える事ない%RAND_SPLIT("んじゃない/と思うけど")% # そんなに止めて欲しい%RAND_SPLIT("訳/ワケ/の")% \@？」
		PRINTFORMW 「まぁでも…、仕方無いわね。止めてあげるわ」
	CASE "一般"
		PRINTFORMW 「はいはい、\@ IS_NOWCOMNAME("怯える") ? 判ったわ。そんなに怯えられちゃ仕方無いわね… # 判ったわよ。止めればいいんでしょう？ \@」
	CASE "低い"
		PRINTFORMW 「…判った。\@ IS_NOWCOMNAME("怯える") ? そんなに怯えるなら、止めてあげる%RAND_SPLIT("わ/")% # そこまで言うなら仕方無いわね… \@」
ENDSELECT
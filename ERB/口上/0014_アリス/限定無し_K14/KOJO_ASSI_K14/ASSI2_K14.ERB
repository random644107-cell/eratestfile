;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	アリス口上 助手アクション口上
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;脱衣サポート口上
;────────────────────────────────────
;・調教者アクションの前に助手による強制脱衣が発生したときに表示
;・いかなる場合でも「口上主=ASSI」となって呼び出される
;・口上主が調教者でご主人様が助手の場合にも呼び出され、この場合一時的に「ご主人様=TARGET」となる
;・この口上の時点ではまだ、TEQUIPは更新されていない
;-------------------------------------------------
@KOJO_DATUI_ASSI_K14
;-------------------------------------------------
;関連変数…
;強制脱衣：TFLAG:102　自主脱衣：TFLAG:103
;=(-1,強制脱衣に抵抗、1,上着、2,下半身上着、3,上衣、4,上半身全部、5,下半身全部、
;　6,アクセサリー、7,アクセサリー以外全て、8,全裸まで、9,下着だけ)
;助手脱衣：TFLAG:110 = 12のときに発生
;-------------------------------------------------
RETURN 0

[SKIPSTART]
以下、コメントアウト
;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣に抵抗された
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 < 0
	;ご主人様も何か脱ごうとしてる
	IF TFLAG:自主脱衣 && TFLAG:110 == 12
	;通常
	ELSE
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ご主人様も何か脱ごうとしてる
;-------------------------------------------------
;・主にセックス系行動の予備動作で発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 > 0 && TFLAG:自主脱衣 && TFLAG:110 == 12
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・アクセサリーのみ
;-------------------------------------------------
;・主に靴下。他には手袋や袖など
;・脱がしたうちに入らない！となりがちなのでこれだけは部位別テンプレを用意
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 == 6
	;全裸になった
	IF ZENRACHECK(1)
	;それ以外
	ELSE
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・アソコ見せろ
;-------------------------------------------------
;・今回の脱衣で性器が見えるようになった場合に表示
;・ぱんつ見せろを経由せず一気に脱がせる場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && P_DATUICHECK(1) == 3
	;調教対象がオトコ
	IF TALENT:MASTER:オトコ
	;調教対象がふたなり
	ELSEIF TALENT:MASTER:ふたなり
	;調教対象が女
	ELSE
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・おっぱい見せろ
;-------------------------------------------------
;・今回の脱衣でおっぱいが見えるようになった場合に表示
;・ブラ見せろを経由せず一気に脱がせる場合あり
;・調教対象がオトコだと表示しない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:オトコ
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・ぱんつ見せろ
;-------------------------------------------------
;・今回の脱衣でパンティ（キャラによってドロワーズ・トランクス）が見えるようになった場合に表示
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && P_DATUICHECK(1) == 2
	;調教対象がオトコ
	IF TALENT:MASTER:オトコ
	;調教対象がふたなり
	ELSEIF TALENT:MASTER:ふたなり
	;調教対象が女
	ELSE
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・ブラ見せろ
;-------------------------------------------------
;・今回の脱衣でブラ（キャラによってスポーツブラ・サラシ）が見えるようになった場合に表示
;・調教対象がオトコだと表示しない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:オトコ
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・全裸に
;-------------------------------------------------
;・「全裸まで剥いだ」だけではなく、「今剥いだので全裸になった」という意味
;・「上着しか残っていない状態で上着を剥いだ」なども含まれる
;・「アソコ見せろ」～「ブラ見せろ」を書いた場合は記入する必要はない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && ZENRACHECK(1)
	;調教対象がオトコ
	IF TALENT:MASTER:オトコ
	;調教対象がふたなり
	ELSEIF TALENT:MASTER:ふたなり
	;調教対象が女
	ELSE
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・何か剥いだ
;-------------------------------------------------
;・愛撫系の予備動作になりやすい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣
[SKIPEND]
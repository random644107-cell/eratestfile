;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	アリス口上 会話系アクション口上ファイル
;
;	会話系アクション口上をまとめたファイル
;
;Version		Reason
;1.71			@口上記入チェック仕様に。ACT1/2/9に加筆。その他細かな修正
;1.62			一部条件の見直し。誤字、脱字修正。加筆等
;1.60			ACT分類単体ファイルとして独立
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;会話系 ACT0～9
;========================================================================
;・TFLAG:ACT(0=今の気分を聞く/1=自分への気持ちを聞く/2=性についての話をする/3=優しく慰める/4=厳しく脅す/5=衣装の変更を要求する/6=合意を求める/7=お仕置きと宣言する/8=怒鳴る/9=不気味に笑う)
;・TFLAG:ACT分類 == 0に該当
;・ACT9, 不気味に笑うは異常系ACT分類から派生する場合がある
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "会話")で拾う事が可能
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 今の気分を聞く
;────────────────────────────────────
;・今の気分について聞いてくる
;・調教対象の反応に応じてREACT以降でACT派生への代入が行われる
;・ターン方針が"異常"なら発生しない
;-------------------------------------------------
@KOJO_ACT_K14_0
;-------------------------------------------------
;APPLY2で代入が行われるものがACT派生というのもなんだかな感
;いずれ整理されるべき類の代物
;-------------------------------------------------
;会話系ACTはちょとアレしないとだ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && FIRSTACT(0)
	PRINTFORMW 「あら、機嫌悪そうね」
	PRINTFORMW 「ん…当たり前？ ま、そうよね。フフフ……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;その調教の最初のアクション
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:経過時間 == 1
	IF AFFECTION("相思相愛")
		PRINTFORMW 「ねぇ、気分はどう？ と、いうか…」
		PRINTFORMW 「私が話しかけてるんだから、もっと嬉しそうな顔しなさいよ！」
	ELSE
		PRINTFORMW 「正直、%CNAME(MASTER, TARGET)%の今の気分とかに興味ないの%RAND_SPLIT("よね/…")%」
		PRINTFORMW 「私のやりたいようにさせて頂くわ。ウフフ…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が自慰中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GETBIT(TEQUIP:自慰中, 0)
	LOCALS:0 = \@ GET_KFLAG_K14("相思相愛") ? %RAND_SPLIT("ふふふ/ふふ/ふふっ/クスクス")% # %RAND_SPLIT("フフフ/フフ/フフッ")% \@
	SELECTCASE RANDACT(20)
		CASE 0
			PRINTFORMW 「どう？ 今の気分は。って……」
			PRINTFORMW 「お楽しみ中にこんなこと聞くのは無粋だったかしら。ねぇ？」
		CASE 1
			PRINTFORMW 「あらあら、精が出るわねぇ？ って……」
			PRINTFORMW 「この場合の言葉の意味、%CNAME(MASTER, TARGET)%はちゃんと理解できるかしら」
		CASE 2
			PRINTFORMW 「\@ GET_KFLAG_K14("相思相愛") ? ふふ # あらあら \@、楽しそうで何よりね\@ !GET_KFLAG_K14("相思相愛") ? ぇ？ # \@」
			PRINTFORMW 「ま、正直……、今わたしにちょっかい出されるのは迷惑なんでしょうけど」
			PRINTFORMW 「でも、黙って見てるのもアレだし……、ついつい弄りたくなっちゃうのよね」
	ENDSELECT
	CALL FADEOUT(10, @"「%LOCALS:0%……」")
	CALL FADE_CLINE(1)
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性交奉仕系で膣内射精の後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:前回中出し == 2
	PRINTFORMW 「なによ、やり遂げたような顔しちゃって…」
	PRINTFORMW 「でも…、ふふ。私の膣内は良かったみたいね」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性交系で膣内射精の後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:前回中出し == 1
	PRINTFORMW 「%CNAME(MASTER, TARGET)%に中出し…最高ね♪」
	PRINTFORMW 「ねぇ、私に征服された気分はいかがかしら？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;イキそう
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
	;=================================================
	;イキそうなのを我慢している
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:我慢
		PRINTFORMW 「あら、頑張るわね？」
		PRINTFORM 「%CNAME(MASTER, TARGET)%がいつまで耐えられるか、試してみるのも悪くないわね
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;焦らし中
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:焦らし度 > 1
		PRINTFORMW 「ねぇ、焦らされてる気分はどう？」
		PRINTFORMW 「簡単にはイかせないわよ、悪いけど。フフ…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「\@ GET_KFLAG_K14("相思相愛") ? ふふ # フフ \@、イキそうなのね？」
	PRINTFORM 「でも、簡単にイかせては面白くないのよね
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ボールギャグ装着中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TEQUIP:ボールギャグ
	PRINTFORMW 「ふふ、良い格好ね。今の気分はいかがかしら？」
	PRINTFORMW 「と言っても、今の%CNAME(MASTER, TARGET)%には答えられないでしょうけど、ね♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アイマスク装着中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TEQUIP:アイマスク
	PRINTFORMW 「視界を奪われた気分はどうかしら？ フフ…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;顔面騎乗中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
	PRINTFORMW 「ねぇ……、私にこんな事されてる気分はどうかしら？」
	PRINTFORMW 「でもこの状態では答えられないかしら、ね？ フフフ…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;三角木馬使用中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TEQUIP:三角木馬
	PRINTFORMW 「私だって思うのよ？ こんな状況で気分なんて聞くべきじゃないって」
	PRINTFORMW 「フフフ…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;勃起
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && PENIS(MASTER) && TCVAR:MASTER:勃起度 >= 1000
	IF AFFECTION("相思相愛")
		PRINTFORMW 「もうギンギンになってるわね…」
		PRINTFORMDW アリスは%CALLNAME:MASTER%のペニスを指でつつき始めた…
		PRINTFORMW 「ビクンビクンしちゃって…　ふふふ…」
	ELSE
		PRINTFORMW 「多少はサマになったってとこかしら」
		PRINTFORMW 「萎えきったものほど、興醒めするものはないし。ねぇ？」
		PRINTFORMW 「フフフ…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;萎えた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && PENIS(MASTER) && (TCVAR:MASTER:勃起度 < TCVAR:MASTER:前の勃起度)
	IF AFFECTION("相思相愛")
		PRINTFORMW 「私の前で萎えるとか、許さないわよ…」
	ELSE
		SELECTCASE RANDACT(2, 110)
			CASE 0
				PRINTFORMW 「ただでさえ貧相なのに、そんなに萎えさせちゃって…」
				PRINTFORMW 「ホント、見栄えがしないわね」
			CASE 1
				PRINTFORMW 「…まったく。お粗末過ぎて悲しくなるわね？」
				PRINTFORMW 「本当、使えないわ」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「ねぇ、%RAND_SPLIT("調子はどうかしら/気分はいかが？/今の気分はどう？")%」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT1, 自分への気持ちを聞く
;────────────────────────────────────
;・自分のことをどう思うか聞く
;・調教対象の反応に応じてREACT以降でACT派生への代入が行われる
;・ターン方針が"異常"なら発生しない
;・話題『好意』が既出となり、以後、同一調教中では抑制される
;-------------------------------------------------
@KOJO_ACT_K14_1
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && FIRSTACT(0)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;その調教の最初のアクション
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:経過時間 == 1 && !GET_KFLAG_K14("相思相愛")
	PRINTFORMW 「ねぇ…、ちょっと聞かせて欲しいんだけど」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%、私の事どう思ってるのかしら？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;相思相愛かつイベントでフラグが立っている
;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさんはこういうのが似合いすぎて困る
;ここは状況描写を捏造してもいいかも試練
;-------------------------------------------------
IF 口上記入チェック(100) && GET_KFLAG_K14("相思相愛")
	LOCALS = \@ TENSION(TARGET, 8) == "高い" ? っ！ # \@
	;kawaii
	SELECTCASE RANDACT(4, 0)
		CASE 0
			PRINTFORMW 「いまさら答えは聞くまでも無いけど、一応聞いておくわ」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%…、私の事どう思ってる？」
		CASE 1
			PRINTFORMW 「判りきった事でも、時々言葉にするのって大切なのよね」
			PRINTFORMW 「ほら、聞いてあげるから早くいいなさいよ%LOCALS%」
		CASE 2
			PRINTFORMW 「何で今頃そんな事とか、くだらない事を言%RAND_SPLIT("うのはやめておくのね/ったら殴るわよ")%…」
			PRINTFORMW 「こういうのって、一度言えば済むって話じゃないのよ。わかる？」
		CASE 3
			PRINTFORMW 「ほらっ！ ぼさっとしてないで、サッサと答えなさいよ」
			PRINTFORMW 「どうせ…、答えは判りきってるけどね%LOCALS%」
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「%CNAME(MASTER, TARGET)%は私の事どう思ってるのかしら？」
	PRINTFORMW 「ねぇ、聞かせて頂戴…」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT2, 性についての話をする
;────────────────────────────────────
;・調教者が性についての知識を語り始める
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・調教者に性知識が無いと発生しない
;・話題『知識』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_ACT_K14_2
;-------------------------------------------------
;正直、微妙なコマンドだなぁと思うｗ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && FIRSTTIME(0)
	PRINTFORMW 「ねぇ、%CNAME(MASTER, TARGET)%はこういう話について聞いた事あるかしら？」
	PRINTFORMW 「私も良く知ってる訳じゃないんだけど――」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;前回COMが恥ずかしがる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("恥ずかしがる")
	PRINTFORMW 「%CNAME(MASTER, TARGET)%みたいな恥ずかしがり屋\@ TALENT:MASTER:小柄 ? の子供 # \@に、こういうのはどうかって気もするけど」
	PRINTFORMW 「は？ 何の話かって？ 決まってるでしょ」
	CALL FADEIN(10, "「セックスの話よ！」")
	WAIT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が小柄		口上的には子供扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(80) && TALENT:MASTER:小柄
	SELECTCASE RANDACT(3, 2)
		CASE 0
			PRINTFORMW 「正直、%CNAME(MASTER, TARGET)%のような子供に聞かせるべきか悩ましいけど…」
			PRINTFORMW 「でも、エッチな話に興味ない訳じゃないでしょう？」
		CASE 1
			PRINTFORMW 「こういうのって、お子様にはちょっと刺激が強すぎるかしら…」
			PRINTFORMW 「ってまぁ、今更過ぎる話かも。ね」
		CASE 2
			PRINTFORMW 「ねぇ、%CNAME(MASTER, TARGET)%も聞きたいでしょう？ お姉さんのエッチな話…」
			PRINTFORMW 「ふふ、とぼけたって駄目。本当は満更でもない癖に…ね」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;前回REACT分類で派生
	SELECTCASE PREVCOMGRONAME()
		CASE "積極的従う", "受け入れる", "平然と従う"
			PRINTFORMW 「%CNAME(MASTER, TARGET)%なら、こういう話も結構いけそうだと思うんだけど」
			PRINTFORMW 「たとえば、たとえば――」
		CASE "消極的従う"
			PRINTFORMW 「%CNAME(MASTER, TARGET)%には正直、もっと積極的になって貰いたいのよねぇ」
			PRINTFORMW 「じゃぁ、こういう話を聞かせてあげましょうか――」
		CASE "許しを乞う"
			PRINTFORMW 「%CNAME(MASTER, TARGET)%を見てると、ちょっと困らせてやりたくなるのよねぇ」
			PRINTFORMW 「ま、そんな訳だから観念して聞くのよ。私のイヤラシイ話――」
		CASEELSE
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の年頃なら、こういう話に興味ないはずないんだけど」
			PRINTFORMW 「あら、とぼける気？ 往生際が悪いわね」
			PRINTFORMW 「まぁいいわ、聞かせてあげる。ちょっとエッチな話を、ね――」
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT3, 優しく慰める
;────────────────────────────────────
;・調教者が優しい言葉で慰めてくる
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ハード", "異常"だと発生しない
;・話題『慰め』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_ACT_K14_3
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 性交奉仕系で膣内射精の後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:前回中出し == 2
	PRINTFORMW 「あら、落ち込まなくていのよ」
	PRINTFORMW 「私の膣内にこんなに出しちゃって…よかったのよね？」
	PRINTFORMW 「ねぇ、もっと素直になって…ふふふ」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性交系で膣内射精の後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:前回中出し == 1
	PRINTFORMW 「心配しないで…」
	PRINTFORMW 「デきちゃったら、ちゃんと責任とってあげるから♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;イキそう
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
	;=================================================
	;イキそうなのを我慢している
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう > 5
		PRINTFORMW 「我慢なんてしないでイってしまいなさい」
		PRINTFORMW 「でも、耐えてる姿はちょっと可愛いかも…ふふふ」
		RETURN 1
	ENDIF
	
	;=================================================
	;焦らし中
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:焦らし度 > 1
		PRINTFORMW 「どう？ 焦らされるのは辛いかしら？」
		PRINTFORML 「私の気が済んだら、イかせてあげてもいいわよ」
		PRINTFORMW 「フフフ……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「イキそうなのね？」
	PRINTFORMW 「待ってて…すぐにイかせてあげるから♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;勃起
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
	PRINTFORMW 「ふふっ…恥ずかしがらなくていいのよ」
	PRINTFORMW 「私を前にしてそうなっちゃうのは…」
	PRINTFORMW 「自然な事なんだから♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	IF GET_KFLAG_K14("相思相愛")
		PRINTFORMDW アリスは%CALLNAME:MASTER%の頭を優しく撫でた…
		PRINTFORM 「ふふ、よしよし…
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「別にとって食おうって訳じゃないんだから」
		PRINTFORMW 「安心していいわよ…」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT4, 厳しく脅す
;────────────────────────────────────
;・厳しい言葉で脅す
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ソフト", "異常"だと発生しない
;・話題『脅す』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_ACT_K14_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && FIRSTACT(0)
	PRINTFORMW 「覚えておいて頂戴」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%をどうするか、全て私次第だって事」
	PRINTFORMW 「この意味が分からない程、馬鹿じゃない事を期待してるわ…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性交奉仕系で膣内射精の後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:前回中出し == 2
	PRINTFORMW 「まだよ、まだ足りないわ！」
	PRINTFORMW 「最後の一滴まで搾り取ってあげるから、覚悟して頂戴」
	CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ……」")
	CLEARLINE 1
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性交系で膣内射精の後
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:前回中出し == 1
	PRINTFORMW 「\@ IS_PREVCOMNAME("中に出さないで！") ? 無理やり # %CNAME(MASTER, TARGET)%に \@中出し、最高ね。フフ」
	PRINTFORM 「%CNAME(MASTER, TARGET)%がちゃんと孕むまで、毎日膣内に注いであげるわ
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;イキそう
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
	;=================================================
	;イキそうなのを我慢している
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう > 5
		PRINTFORMW 「無駄な抵抗はやめたらどう…？」
		PRINTFORMW 「どうせすぐイっちゃうんだから」
		RETURN 1
	ENDIF
	
	;=================================================
	;焦らし中
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:焦らし度 > 1
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「あら、イキそうなの？」
				PRINTFORMW 「でも駄目よ、もう少し我慢なさい」
			CASE 1
				PRINTFORMW 「イきたくてたまらない、って顔ね？」
				PRINTFORMW 「このままずっと焦らしてたら%CNAME(MASTER, TARGET)%、どうなっちゃうかしら。ねぇ？」
		ENDSELECT
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ……」")
		CLEARLINE 1
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;勃起
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
	PRINTFORMW 「何勝手におっ勃てているのかしら」
	PRINTFORMW 「本当…、不出来なイチモツ、ね…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アイマスク装着中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TEQUIP:アイマスク
	PRINTFORMW 「このまま%CNAME(MASTER, TARGET)%を放置したらどうなるかしら？」
	PRINTFORMW 「ふふ、ちょっと面白そうね」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「大人しくしてれば、悪いようにはしないわ」
	PRINTFORMW 「無駄な抵抗はやめた方が賢明よ」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT5, 衣装の変更を要求する
;────────────────────────────────────
;・調教者が衣装の変更を要求してくる
;・調教対象の反応に応じて、何枚脱ぐか、あるいは脱がないのかにREACT以降で派生する
;・TFLAG:ACT派生(0=服を脱ぐように命令/1=服を着るように命令)
;・TEQUIP:押し倒し中には発生しない
;・TEQUIP:縄が真になっていると発生しない
;-------------------------------------------------
@KOJO_ACT_K14_5
;-------------------------------------------------
;・TFLAG:ACT派生(0=服を脱ぐように命令/1=服を着るように命令)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 服を脱ぐように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「さ、服を脱いで頂戴」 
		PRINTFORM 「%CNAME(MASTER, TARGET)%の全て、見せてもらうわ
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;勃起
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
		PRINTFORMW 「なによ、もう膨らませちゃってる訳？ 気の早いことね」
		PRINTFORMW 「ま、それは結構だから…さっさと服を脱%RAND_SPLIT("ぎなさい/いで")%」
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が調教対象を溺愛
	;=================================================
	IF 口上記入チェック(50) && AFFECTION("溺愛")
		PRINTFORMW 「さぁ、脱いで…、私に全部見せて頂戴」
		PRINTFORM 「ふふっ、興奮しちゃうわ
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF

	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「じゃぁ、脱いでもらおうかしら」
	PRINTFORMW 「恥ずかしがる事なんて無いわ」
	PRINTFORM 「ゆっくり…、私によく見えるように。ね
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	PRINTFORMW 「さて、服を着てもらおうかしら？」
	PRINTFORMW 「別に理由なんてないわ。ただ、そういう気分なのよね」
	PRINTFORMW 「分かったらさっさとして頂戴…」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 合意を求める
;────────────────────────────────────
;・初めてを貰ってもいいかと聞く
;・初めて＝処女or童貞(両方の場合もある)
;・既に合意を得ている場合は発生しない
;・調教対象が処女でも童貞でもない場合は発生しない
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・話題『合意』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
;MASTERの性別毎にACT派生があったりはしないんで
;!PENISI(MASTER)なりPENIS(MASTER)分岐内でTALENTで別けるなり
;TALENT毎に条件を書き分けるなりする必要がある
;というかまぁ、面倒なら別に分けなくても良い
;-------------------------------------------------
@KOJO_ACT_K14_6
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;面倒なんで最上層でまとめてしまう
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && FIRSTACT(0)
	PRINTFORMW 「ねぇ、ひとつお願いがあるのだけれど…」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%の初めて、私にくれないかしら？」
	SIF TALENT:MASTER:小柄
		PRINTFORMW 「と言っても…、子供に理解できるか微妙なとこかしら、ね」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 調教対象がオトコの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TALENT:MASTER:オトコ
	;=================================================
	;調教対象への好感度1000以上
	;=================================================
	IF 口上記入チェック(100) && AFFECTION("眷愛")
		PRINTFORMW 「ねぇ…、お願い」
		PRINTFORMW 「私、%CNAME(MASTER, TARGET)%の初めての女になりたいの…」
		PRINTFORMDW アリスは頬を紅潮させ、熱っぽい眼差しで%CALLNAME:MASTER%に迫ってきた…
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「そうえば、%CNAME(MASTER, TARGET)%ってまだいわゆる、その…童貞、だったわよね？」
	PRINTFORMW 「だったら、あの、私と……って。いい加減、察して欲しいものだけど」
	SIF TALENT:MASTER:小柄
		PRINTFORMW 「%CNAME(MASTER, TARGET)%みたいなお子様には、無理な相談だったかしら、ね…」
	SIF !TALENT:MASTER:小柄
		PRINTFORMW 「あまり恥をかかせないで頂戴…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 調教対象がふたなりの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TALENT:MASTER:ふたなり
	;=================================================
	;調教対象への好感度3000以上
	;=================================================
	IF 口上記入チェック(100) && AFFECTION("盲愛")
		;調教対象が処女かつ童貞
		IF ALLSAMES(1, TALENT:MASTER:処女, TALENT:MASTER:童貞)
			PRINTFORMW 「ねぇ…、お願い」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の初めて、私に頂戴…」
			PRINTFORMW 「ん、そうね。どっちの事だと思う…？　フフ…」
		;その他
		ELSEIF TRUECHECK(TALENT:MASTER:処女, TALENT:MASTER:童貞) > 0
			PRINTFORMW 「ねぇ…お願いよ」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の初めて、私に頂戴…」
			PRINTFORMDW アリスは頬を紅潮させ、熱っぽい眼差しで%CALLNAME:MASTER%に迫ってきた…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象が処女若しくは童貞
	;=================================================
	IF 口上記入チェック(100) && TALENT:MASTER:処女
		;処女かつ童貞
		IF TALENT:MASTER:童貞
			PRINTFORMW 「ねぇ、ひとつお願いがあるのだけれど」
			PRINTFORMW 「%CALLNAME:MASTER%の初めて、私にくれないかしら？」
		;処女
		ELSE
			PRINTFORMW 「ねぇ、ひとつお願いがあるのだけれど」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の初めて、私にくれないかしら？」
		ENDIF
		SIF TALENT:MASTER:小柄
			PRINTFORMW 「…って。%CNAME(MASTER, TARGET)%みたいなお子様には理解できないかしら、ね…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「ね、貰っちゃってもいいかしら？」
	PRINTFORMW 「何をかって？ ……やれやれ、まったく」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%、意外と鈍いのね……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 調教対象が女の子の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && !PENIS(MASTER)
	;=================================================
	;調教対象への好感度3000以上
	;=================================================
	IF 口上記入チェック(100) && AFFECTION("盲愛")
		PRINTFORMW 「ねぇ…、お願い」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%の初めて、私に頂戴…」
		PRINTFORMDW アリスは頬を紅潮させ、熱っぽい眼差しで%CALLNAME:MASTER%に迫ってきた…
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「ね、貰っちゃってもいいかしら？」
	PRINTFORMW 「何をかって？ ……やれやれ、まったく」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%、意外と鈍いのね……」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT7, お仕置きと宣言する
;────────────────────────────────────
;・これからお仕置きすると言い出す
;・このACTが発生している時点でTFLAG:お仕置きフラグは立っている
;・ターン方針が"ソフト", "異常"だと発生しない
;・TEQUIP:押し倒し中時には発生しない
;-------------------------------------------------
@KOJO_ACT_K14_7
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;ここはKOJO_TRAIN_MESSAGEで地の文と連携させるべき
;それと行動予約から発生するものに関してはKFLAG枠を設けるべき
;まぁ次回以降に…
;-------------------------------------------------

;口先だけというのは嫌いなので宣言するからには実行する！
TCVAR:行動予約 = 60

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 前COMが「無愛想に応答する/強気に応答する/嫌がる/暴れる/拒否する」のいずれか
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("無愛想に応答する/強気に応答する/嫌がる/暴れる/拒否する")
	PRINTFORMW 「まだ自分の立場が判ってないようね？ まったく、やれやれだわ」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%の場合ことばで伝えるより…、もっと直接的な方法がいいのかしら。ねぇ？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;前COMが「気持ち良くして」（この恥知らず！）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("気持ち良くして")
	PRINTFORMW 「恥知らずで浅ましいわね…」
	PRINTFORMW 「これはちょっと躾け直す必要があるかしら」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;前COMが「もっとハードにして」（この豚め！）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("もっとハードにして")
	PRINTFORMW 「呆れた…」
	PRINTFORMW 「この変態マゾ豚には、相応の仕打ちが必要みたいね…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;前COMが「許しを乞う」（許してもらえませんでした）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("許しを乞う")
	PRINTFORMW 「身の程を弁えなさい…」
	PRINTFORMW 「今の%CNAME(MASTER, TARGET)%には、許しを乞う権利などないのよ」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;前COMが「自慰し始める」（人の話を聞け！）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("自慰し始める")
	PRINTFORMW 「何を勝手に始めちゃってるのかしら」
	PRINTFORMW 「これはお仕置きが必要かしらね」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「身の程を弁えない人形には…」
	PRINTFORMW 「どうやらお仕置きが必要みたい、ね」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT8, 怒鳴る
;────────────────────────────────────
;・大声で怒鳴る
;・ターン方針が"ソフト", "ノーマル"では発生しない
;・TFLAG:お仕置きフラグに関わらず、発生する可能性がある（お仕置き中にさらに怒鳴る可能性等）
;-------------------------------------------------
@KOJO_ACT_K14_8
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 前COMが「無愛想に応答する/強気に応答する/嫌がる/暴れる/拒否する」のいずれか
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_PREVCOMNAME("無愛想に応答する/強気に応答する/嫌がる/暴れる/拒否する")
	PRINTFORMW 「ホント、逆らってばかりでイライラするわ…」
	PRINTFORMW 「いい加減、頭にくるのよ！ そういうの！」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, その他・条件なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「本当…、可愛くないのよ%CNAME(MASTER, TARGET)%」
	PRINTFORMW 「その間抜けな顔もイライラするわ！」 
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT9, 不気味に笑う
;────────────────────────────────────
;・不気味な微笑を浮かべる
;・異常系から発生する場合もある
;・今日の方針が"イチャイチャ"だと発生しない
;・話題『嗤う』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_ACT_K14_9
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 不気味に笑う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	LOCALS = %RAND_SPLIT("ウフフフフ/フフフフフ")%
	;=================================================
	;アイマスク装着中
	;=================================================
	IF 口上記入チェック(100) && TEQUIP:アイマスク
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		SIF ANIME_CONFIG("FADE")
			CLEARLINE 1
		PRINTFORMDW 視界を奪われた%CALLNAME:MASTER%の耳に、%CALLNAME%の不気味な嗤い声が届く…
		RETURN 1
	ENDIF
	
	;=================================================
	;異常系から発生
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT分類 == 7
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		SIF ANIME_CONFIG("FADE")
			CLEARLINE 1
		PRINTFORMDW アリスの不気味な嗤い声が部屋に響いている…
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	SELECTCASE RANDACT(2, 10)
		CASE 0
			PRINTFORMDW %CALLNAME%はその表情に妖しい笑みを湛えつつ、
			PRINTFORMDW 時折クスクスと嗤いながら、%CALLNAME:MASTER%に絡みつくような視線を投げかけている…
		CASE 1
			PRINTFORMDW %CALLNAME%の不気味な嗤い声が部屋中に響き渡る…
			PRINTFORMDW そう、可憐な少女の姿をしていても彼女は魔女なのだ
			PRINTFORMDW 改めて思い知らされ、%CALLNAME:MASTER%は戦慄した…
	ENDSELECT
	RETURN 1
ENDIF
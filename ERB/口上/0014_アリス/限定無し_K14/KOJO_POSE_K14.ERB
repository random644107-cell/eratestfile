;*************************************************
;姿勢変更
;*************************************************
;とりあえず枠だけ
;書くかどうかはなんとも言えないところ
;-------------------------------------------------
@KOJO_POSE_K14(ARG, ARG:1, ARG:2)
;ARG = 姿勢
;ARG:1 = 位置前後
;ARG:2 = 位置上下
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;位置上下(自由)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG:2 == 0
	;=================================================
	;姿勢変化あり
	;=================================================
	IF 口上記入チェック(0) && ARG >= 0
		;=================================================
		;自由
		;=================================================
		IF 口上記入チェック(0) && ARG == 0
		ENDIF
		
		;=================================================
		;仰向け
		;=================================================
		IF 口上記入チェック(0) && ARG == 1
		ENDIF
		
		;=================================================
		;座り
		;=================================================
		IF 口上記入チェック(0) && ARG == 2
		ENDIF
	
		;=================================================
		;四つん這い
		;=================================================
		IF 口上記入チェック(0) && ARG == 3
		ENDIF
		
		;=================================================
		;起立
		;=================================================
		IF 口上記入チェック(0) && ARG == 4
		ENDIF
		
		;=================================================
		;膝立ち
		;=================================================
		IF 口上記入チェック(0) && ARG == 10
		ENDIF
	ENDIF
	
	;=================================================
	;姿勢変化なし
	;=================================================
	IF 口上記入チェック(0) && !ARG
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;位置上下(上に乗った)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG:2 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;位置上下(上に乗せた)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG:2 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;位置上下(上下変化なし)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !ARG:2
	;=================================================
	;仰向け
	;=================================================
	IF 口上記入チェック(0) && ARG == 1
		PRINTFORMW
		IF ARG:1 >= 0
			SELECTCASE ARG:1
				CASE 0
				CASE 1
				CASE 2
			ENDSELECT
		ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;座り
	;=================================================
	IF 口上記入チェック(0) && ARG == 2
	ENDIF
	
	;=================================================
	;四つん這い
	;=================================================
	IF 口上記入チェック(0) && ARG == 3
	ENDIF
	
	;=================================================
	;起立
	;=================================================
	IF 口上記入チェック(0) && ARG == 4
	ENDIF
	
	;=================================================
	;膝立ち
	;=================================================
	IF 口上記入チェック(0) && ARG == 10
	ENDIF
	
	;=================================================
	;自由
	;=================================================
	IF 口上記入チェック(0) && ARG == 0
	ENDIF
ENDIF
[SKIPEND]
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	アリス口上 その他調教イベント口上ファイル
;
;	その他調教ターン中イベントをまとめたファイル
;
;version		Reason
;1.71			@口上記入チェック仕様に
;1.69			KOJO_TRAIN_K14.ERBからEVENT21/22/23/24及び刻印口上を切り分け
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;EV21, 大満足口上
;────────────────────────────────────
;・調教者の大満足ボーナスを取得した場合に表示
;-------------------------------------------------
@KOJO_EVENT_K14_21(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま大満足
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;愛想よく応答する
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("愛想よく応答する")
		PRINTFORM 「ふふ、愛想がいい子は大好きよ
		CALL PRINT_SYMBOL_K14
		SIF GET_COM_RANK() == "無理をした行動"
			PRINTFORMW 「ちょっとワザとらしい気もするけど、ね」
		PRINTFORMDW アリスは\@ TFLAG:REACT負荷 > 2 ? 満面の # \@笑みを湛え、%CALLNAME:MASTER%を見つめている
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる/気持ち良い！
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる/気持ち良い！")
		PRINTFORM 「素直で可愛いわね
		CALL PRINT_SYMBOL_K14
		PRINTFORMDW アリスは満面の笑みを湛え、%CALLNAME:MASTER%を見つめている
		RETURN 1
	ENDIF
	
	;=================================================
	;中に出して！
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出して！")
		PRINTFORMW 「ふふっ、中出しをおねだりだなんて……」
		PRINTFORM 「なんて可愛いのかしら…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;悲鳴を上げる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
		IF AFFECTION("相思相愛")
			PRINTFORMW 「なんて甘い声で、鳴くのかしら……」
			PRINTFORMW 「ねぇ、もっと……、もっと聞かせて」
		ELSE
			PRINTFORMW 「いい声で鳴くわね……」
			PRINTFORMW 「…たまらないわ。その情けない顔も素敵よ？」
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ…」")
			CLEARLINE 1
		ENDIF
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;=================================================
	;愛情を込めてやる
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("愛情を込めてやる")
		IF AFFECTION("相思相愛")
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の心が伝わってくるわ…」
			PRINTFORMDL アリスは蕩けきった表情を浮かべ、体を摺り寄せてきた…
			PRINTL
			PRINTFORM 「好きよ…、大好き…\@ CFLAG:TARGET:依存度 > CFLAG:MASTER:依存度 ? っ！ # \@
			CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
			PRINTFORMDW アリスは潤んだ瞳で%CALLNAME:MASTER%を見つめている…
		ELSE
			PRINTFORMW 「こんなになって…、私で、わたしなんかに…」
			PRINTFORMDW アリスは蕩けきった表情を浮かべ、体を摺り寄せてきた…
			PRINTFORM 「ふふ、 悪くないわ。こういうの…
			CALL PRINT_SYMBOL_K14
		ENDIF
		RETURN 1
	ENDIF
	
	;SELECTCOM == 15は上層で処理
	;=================================================
	;REACT分類が積極的従う(積極的やる、なすがまま等)
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("積極的従う/受け入れる")
		PRINTFORMW 「いいわよ……、とってもいい感じ…」
		PRINTFORMDW アリスは潤んだ瞳で%CALLNAME:MASTER%を見つめている…
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;その他を処理
	;---------------------------------------------
	PRINTFORMW 「ふふ、いい感じね」
	PRINTFORM 「%CNAME(MASTER, TARGET)%、悪くないわよ
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;EV22, 状態変化口上
;────────────────────────────────────
;・状態異常が付いたときに表示
;・TCVAR:状態変化が調教者、TCVAR:MASTER:状態変化が調教対象
;・状態変化が起こると3ターン持続する
;-------------------------------------------------
;・TCVAR:状態変化持続を一律3ターンとか()
;-------------------------------------------------
@KOJO_EVENT_K14_22(ARG)
;-------------------------------------------------

;*************************************************
;EV22_0, 調教者状態変化
;*************************************************
;T_COND TARGETが指定した状態なら真を返す
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[疲弊]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("疲弊") && !TFLAG:一回休み
	;状態変化持続ターンを2に
	TCVAR:状態変化持続 = 2
	;2/3の確率で表示無し RESULTに0を返す
	IF RAND:3
		RETURN 0
	;1/3の確率で表示
	ELSE
		SELECTCASE TENSION(TARGET, 8)
			CASE "高い"
				PRINTFORMW 「疲れたけど…心地よい疲れって感じね…♪」
			CASE "一般"
				PRINTFORMW 「ちょっと疲れたわね…」
			CASE "低い"
				PRINTFORMW 「ふぅ……疲れたわね……」
		ENDSELECT
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[衰弱]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("衰弱") && !TFLAG:一回休み
	TCVAR:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「はぁ……はぁ……」
		PRINTFORMDW アリスは肩で息をしながら、疲れきった表情を見せた
		PRINTFORMW 「ちょっと……ヤりすぎちゃったわね…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[無気力]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("無気力")
	TCVAR:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		SELECTCASE TENSION(TARGET, 8)
			CASE "高い"
				PRINTFORMW 「良く判らないけど…急に何もかも嫌になったわ…」
			CASE "一般"
				PRINTFORMW 「なんか…何もする気にならないわね…」
			CASE "低い"
				PRINTFORMW 「なんか興醒めね……」
		ENDSELECT
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[朦朧]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("朦朧")
	TCVAR:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「んっ…！ 頭がクラクラするわ…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[情欲]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("情欲")
	;情欲は3ターンのままで
	;TCVAR:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		SELECTCASE TENSION(TARGET, 8)
			CASE "高い"
				PRINTFORML 「くっ……！ もう駄目…、我慢できない……！」
				PRINTFORML 「徹底的に可愛がってあげるから覚悟して…」
			CASE "一般"
				PRINTFORML 「だめ、こんなの…抑えないと…」
				PRINTFORML 「でも、%CNAME(MASTER, TARGET)%をめちゃめちゃにしたくて、私…」
			CASE "低い"
				PRINTFORML 「私…自分でも怖いくらい興奮してる…」
		ENDSELECT
		PRINTFORMDW %CALLNAME:MASTER%を見つめるアリスの瞳は、妖しく潤んでいる…
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[怒り]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("怒り")
	IF AFFECTION("相思相愛")
		TCVAR:状態変化持続 = 1
	ELSEIF AFFECTION("溺愛")
		IF RAND:3
			TCVAR:状態変化持続 = 2
		ELSE
			TCVAR:状態変化持続 = 1
		ENDIF
	ELSEIF AFFECTION("眷愛")
		IF RAND:3
			TCVAR:状態変化持続 = 3
		ELSE
			TCVAR:状態変化持続 = 2
		ENDIF
	ELSE
		TCVAR:状態変化持続 = 3
	ENDIF
	IF RAND:3
		RETURN 0
	ELSE
		;アリスさま満足度派生処理
		SELECTCASE TENSION(TARGET, 8)
			CASE "高い"
				PRINTFORML 「悪くない気分だったけど…すっかり台無しにしてくれたわね？」
				PRINTFORMW 「その代償は高くつくから、覚悟して…」
			CASE "一般"
				PRINTFORML 「お願いだから、あまり怒らせないで頂戴」
				PRINTFORMW 「どこまで自分を押さえられるか、あまり自信が無いわ…」
			CASE "低い"
				PRINTFORMW 「私を怒らせても良い事は無いわよ…」
		ENDSELECT
		RETURN 2
	ENDIF
ENDIF

;100%
;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[退屈]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && T_COND("退屈")
	;調教の経過時間
	SELECTCASE TFLAG:経過時間
		;長い
		CASE IS >= 60
			PRINTFORMW 「流石に、興がさめてきたわね……」
			TCVAR:状態変化持続 = 3
		;普通
		CASE IS >= 30
			PRINTFORMW 「いまひとつ、興が乗らないわ……」
			TCVAR:状態変化持続 = 2
		;短い
		CASEELSE
			PRINTFORMW 「なんか、イマイチ、ね……」
			TCVAR:状態変化持続 = 1
	ENDSELECT
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[鬱屈]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && T_COND("鬱屈")
	TCVAR:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		RETURN 2
	ENDIF
ENDIF
;TALENT:狂気がある場合にのみ1/5の確率で鬱屈化
[SKIPEND]

;*************************************************
;EV22_1, 調教対象状態変化
;*************************************************
;M_COND MASTERが指定した状態なら真を返す
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[疲弊]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("疲弊") && !TFLAG:一回休み
	TCVAR:MASTER:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「ちょっとお疲れみたいね？」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[衰弱]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("衰弱") && !TFLAG:一回休み
	TCVAR:MASTER:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「…酷い有様ね。ちょっとやりすぎたかしら…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[無気力]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("無気力")
	TCVAR:MASTER:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「…気に入らないわね。その気の抜けた態度…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[朦朧]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("朦朧")
	TCVAR:MASTER:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORML 「足下がふらついてるわ。一体どうしちゃったのかしら、ねぇ？」
		PRINTFORMW 「フフフ…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[情欲]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(33) && M_COND("情欲")
	;アイマスク装着中
	IF TEQUIP:アイマスク
		PRINTFORMW 「は？ なによ。目隠しされてるのに鼻息荒くしちゃって」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、興奮しちゃってる訳？」
	;その他
	ELSE
		PRINTFORM 「ちょっと生意気ね？ この
		CALL PRINTFONTSTYLE(COLOR2_K14("ALICE"), "私", 1)
		PRINTFORMW をそんな目で見つめるなんて」
	ENDIF
	SIF AFFECTION("相思相愛")
		PRINTFORMW 「でもそうね…、悪い気はしないわ。ふふっ」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[怒り]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("怒り")
	IF AFFECTION("相思相愛")
		TCVAR:MASTER:状態変化持続 = 1
	ELSEIF AFFECTION("調教完了")
		IF RAND:3
			TCVAR:MASTER:状態変化持続 = 2
		ELSE
			TCVAR:MASTER:状態変化持続 = 1
		ENDIF
	ELSEIF AFFECTION("調教済み")
		IF RAND:3
			TCVAR:MASTER:状態変化持続 = 3
		ELSE
			TCVAR:MASTER:状態変化持続 = 2
		ENDIF
	ELSE
		TCVAR:MASTER:状態変化持続 = 3
	ENDIF
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「あら、機嫌悪そうね？」
		PRINTFORMW 「ただ、あまり賢明な態度とは言えないわよ。そういうの…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[退屈]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("退屈")
	TCVAR:MASTER:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「なんかムカつくわね、その態度…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[鬱屈]化
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && M_COND("鬱屈")
	TCVAR:MASTER:状態変化持続 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「ちょっと様子がおかしいわね…」
		RETURN 2
	ENDIF
ENDIF

;────────────────────────────────────
;EV23, 調教対象能力値アップ口上
;────────────────────────────────────
;・調教中に調教対象の能力値が上昇した際に表示
;-------------------------------------------------
@KOJO_EVENT_K14_23(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(今回上昇したABL番号/各ARG番号はABLのそれに相当)
;
;ARG:0 従順/1 欲望/2 技巧/3 Ｃ感覚/4 Ｖ感覚/5 Ａ感覚/6 Ｂ感覚/7 奉仕精神/8 露出癖/9 レズっ気/10ＢＬっ気/11マゾっ気
;12自慰中毒/13精液中毒/14レズ中毒/15苦痛中毒/16拘束中毒
;
;ARG:20 会話/21 愛撫/22 道具/23 性交/24 羞恥/25 奉仕/26 加虐/27 異常
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[従順]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 0
	;見づらいので改行
	PRINTL 
	PRINTFORMW 「素直が１番ね」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[欲望]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	PRINTL 
	PRINTFORMW 「そうよ…　もっと欲望に正直になりなさい…」
	PRINTFORM 「私が、全部受け止めてあげる%RANDTEXT_K14("から", "わ")%
	IF RAND:3
		PRINTW 」
	ELSE
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[技巧]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 2
	PRINTL 
	SELECTCASE RANDEVENT(2, 0)
		CASE 0
			PRINTFORMW 「ちょっと上手くなった感じかしら？」
		CASE 1
			PRINTFORMW 「多少は使える感じになったかしら、ねぇ？」
			IF !RAND:3
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「フフ……」")
				CLEARLINE 1
			ENDIF
	ENDSELECT
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;Ｃ/Ｖ/Ａ/Ｂ感覚のいずれかがアップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GROUPMATCH(ARG, 3, 4, 5, 6)
	PRINTL
	SELECTCASE RANDEVENT(2, 0)
		CASE 0
			PRINTFORMW 「少し感じやすくなったみたいね？」
		CASE 1
			PRINTFORMW 「感度が良くなったみたいね…？」
	ENDSELECT
	RETURN 2
ENDIF

[SKIPSTART]
以下全てOFFの上コメントアウトしておく
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[Ｃ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 3
	PRINTL 
	PRINTFORMW 「感度が良くなったみたいね…？」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[Ｖ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 4
	PRINTL 
	PRINTFORMW 「感度が良くなったみたいね…？」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[Ａ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 5
	PRINTL 
	PRINTFORMW 「感度が良くなったみたいね…？」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象[Ｂ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 6
	PRINTL 
	PRINTFORMW 「感度が良くなったみたいね…？」
	RETURN 2
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV24, 調教者能力値アップ口上
;────────────────────────────────────
;・調教中に調教者の能力値が上昇した際に表示
;-------------------------------------------------
@KOJO_EVENT_K14_24(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(今回上昇したABL番号)
;
;ARG:0 従順/1 欲望/2 技巧/3 Ｃ感覚/4 Ｖ感覚/5 Ａ感覚/6 Ｂ感覚/7 奉仕精神/8 露出癖/9 レズっ気/10ＢＬっ気/11マゾっ気
;12自慰中毒/13精液中毒/14レズ中毒/15苦痛中毒/16拘束中毒
;
;ARG:20 会話/21 愛撫/22 道具/23 性交/24 羞恥/25 奉仕/26 加虐/27 異常
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[欲望]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	;見づらいので改行
	PRINTL 
	PRINTFORMW 「もっと私を愉しませて…」
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[技巧]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[Ｃ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[Ｖ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[Ａ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[Ｂ感覚]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 6
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[レズっ気]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 9
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[自慰中毒]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 12
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[愛撫]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 21
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;アリスさま[加虐]アップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 26
	PRINTL
	SELECTCASE ABL:TARGET:加虐
		CASE IS >= 9
			PRINTFORML 「ちょっと自分が判らなくなってきたわ」
			PRINTFORMW 「でも、もう止められそうにない…」
		CASE IS >= 6
			PRINTFORMW 「自分にこんな面があるなんて知らなかったわ…」
		CASE IS >= 3
			PRINTFORMW 「あまり良い声で鳴かれると、やめられなくなりそう…」
		CASEELSE
			PRINTFORML 「自分でもよくわからないけど」
			PRINTFORMW 「なんか興奮するわね、これ…」
	ENDSELECT
	RETURN 2
ENDIF

;────────────────────────────────────
;EV29, 屈服・苦痛刻印による反抗刻印上限処理
;────────────────────────────────────
;・反抗刻印Lv上限の処理
;・反抗刻印は10-(苦痛刻印+屈服刻印)/2を超えない
;-------------------------------------------------
@KOJO_EVENT_K14_29(ARG)
;-------------------------------------------------
;%CALLNAME:TARGET%の与える快楽\@ MARK:MASTER:苦痛刻印 ? と苦痛 # \@が%CALLNAME:MASTER%から反抗の気力を奪っていく…
;反抗刻印は{MARK:反抗刻印}になった
;-------------------------------------------------
;ものは試しにちょっと書いてみる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;反抗刻印上限処理
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;階層イベント。3つ見たらELSE以降で処理とかそんな感じで
	SELECTCASE ORDEREVENT(0)
		CASE 0
			PRINTFORMW 「少しは素直になったみたいだけど……、まだまだこれからってとこね」
		CASE 1
			PRINTFORML 「%CNAME(MASTER, TARGET)%も多少はマシになったかしら？」
			PRINTFORMW 「…って、私の気のせいかも知れないけど」
		CASE 2
			PRINTFORMW 「愚鈍な%CNAME(MASTER, TARGET)%でも、逆らっても無駄って事に気付いたかしら。ねぇ？」
		CASEELSE
			PRINTFORML 「無益な抵抗を続けたところで、何も得るものは無い」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%がそれに気付けない程、馬鹿じゃなければいいんだけどね？」
			;フェードイン
			CALL FADEIN(13, @"「だってそれは……、%CNAME(MASTER, TARGET)%自身の為でもあるんだから」")
			;フェードアウト
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ ALI(0) ? ふふふ # ウフフ \@…」")
			SIF ANIME_CONFIG("FADE")
				CLEARLINE 1
	ENDSELECT
	RETURN 1
ENDIF

;========================================================================
;刻印取得口上
;========================================================================
;・調教中に刻印を取得させた場合に表示。どのレベルで口上表示するかは加減自由なので個々で調整するのが望ましい
;・記入チェックが=1の場合、同時に複数取得したときはこのファイルで一番上のものだけを表示する
;・=2の場合、取得した刻印の口上すべてを上から順に表示する
;・刻印取得による素質変動の前に表示するため、素質取得/喪失するかはこの時点でのTALENTの有無で判定できる
;-------------------------------------------------
@KOJO_MARK_K14(ARG:0, ARG:1, ARG:2, ARG:3, ARG:4)
#DIM 記入チェック
#DIM すべて順に表示
すべて順に表示 = 0	;刻印取得口上「すべて順に表示」のための初期化処理
;-------------------------------------------------
;引数内訳…
;ARG:(刻印番号)=(今回取得したレベル)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;[トラウマ]取得
記入チェック = 1	;0.非表示/1.同時に取得した刻印をこれ以下表示しない/2.すべて順に表示する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:4
	SETBIT すべて順に表示, 4
	SELECTCASE ARG:4
		;トラウマLv1~Lv9
		CASE 1 TO 9
			;初回
			IF FIRSTMARK(0)
				PRINTFORMW 「――なるほど、ねぇ。%CNAME(MASTER, TARGET)%は%ACTSTR(CFLAG:MASTER:トラウマ)%がトラウマ、と」
				PRINTFORMW 「%RAND_SPLIT("あら？ 誤魔化したってもう遅いわよ/嘘ついたって駄目")%。%CNAME(MASTER, TARGET)%って判りやすいのよ」
				PRINTFORML 「まあでも――」
				;フェードアウト、少し早め
				CALL FADE(8, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ ALI(0) ? ふふっ # フフッ \@…」")
				;FADE絡みのコードをすっきりさせる関数をこしらえたので、今後、この手のやつは不要になる
				SIF ANIME_CONFIG("FADE")
					CLEARLINE 1
				PRINTFORMW 「ちょっと面白くなってきたかしら。ねぇ？」
			;初回以降はRANDMARKで
			ELSE
				;ORDERMARKでも面白いのかも知れない
				SELECTCASE RANDMARK(4, 0)
					CASE 0
						PRINTFORMW 「あら、%ACTSTR(CFLAG:MASTER:トラウマ)%が随分堪えたみたいね？」
					CASE 1
						PRINTFORMW 「%ACTSTR(CFLAG:MASTER:トラウマ)%がそんなに嫌だったのかしら」
					CASE 2
						PRINTFORMW 「%CNAME(MASTER, TARGET)%……、%ACTSTR(CFLAG:MASTER:トラウマ)%が嫌なのね」
						PRINTFORMW 「だからって、私は手加減するつもりないんだけど。ふふ…」
					CASE 3
						PRINTFORMW 「へぇ、そう。%CNAME(MASTER, TARGET)%、%ACTSTR(CFLAG:MASTER:トラウマ)%が苦手なの…」
						PRINTFORMW 「まぁでも…、私に知られたのは不味かったわね？ %RAND_SPLIT("クスクス/くすくす/ふふふ")%…」
				ENDSELECT
				;初回を含めてトラウマLv1から5までは被り無し
				;Lv6からはRANDMARK次第といったところだが、連続して同じものが表示される可能性は一度だけということで妥協
				;そもそも全てのLv書くならFIRSTもRANDも要らないし。まぁなんとかFIRSTMARKの出番を作りたかったというのが本音かも知れない
				;初回はSELECTCASE ARG:4のCASE 1使えとかツマラナイ事は言いっこなしで
			ENDIF
		;トラウマLv10…[臆病]がない場合、それを得る
		;              既に[臆病]があり[狂気]がない場合、[幼稚]がある場合は100%、ない場合は1/3の確率で[狂気]を得る
		;              [狂気]の取得判定に漏れ、かつ[幼稚]を持たない場合、[幼稚]を得る(幼児退行する)
		;              現状、[狂気]と[幼稚]はどちらに転ぶか先に判定する術は無い(要望があれば…)。とりあえず頭がおかしくなると解釈してください
		CASE 10
			;微妙なところだな。10000だと低すぎる気はする
			IF CFLAG:MASTER:依存度 > 12000
				;調教対象が臆病/狂気/幼稚の内1つ以上を所持
				IF TRUECHECK(TALENT:MASTER:臆病, TALENT:MASTER:狂気, TALENT:MASTER:幼稚) > 1
					PRINTFORM 「ま、仕方無いわね\@ AFFECTION("相思相愛") ? … # \@。
					IF AFFECTION("相思相愛")
						PRINTFORMW ちゃんと責任はとってあげるわ」
						PRINTFORMW 「ずっと私の傍で可愛がってあげるから、%CNAME(MASTER, TARGET)%は何も心配しなくていいのよ…」
					ELSE
						PRINTFORMW %CNAME(MASTER, TARGET)%自身も望んだ結果でしょう？」
						PRINTFORMW 「これからも、私の人形として可愛がってあげるから…」
						CALL FADEIN(10, "「――安心していいわよ」")
						CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「ウフフ…」")
						SIF ANIME_CONFIG("FADE")
							CLEARLINE 1
					ENDIF
				;調教対象が臆病/狂気/幼稚の内1つを所持
				ELSE
					PRINTFORMW 「いつかこんな事になるんじゃないかって思ってた。だけど…」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%自身も望まぬ限り、こうはならなかったはずよ」
					PRINTFORMW 「ま、何であれ…、私は%CNAME(MASTER, TARGET)%の事、\@ AFFECTION("相思相愛") ? これからもずっと愛してあげるわ # 見捨てたりはしないから \@…」
				ENDIF
			ELSE
				IF AFFECTION("相思相愛")
					PRINTFORML 「なんていうか……」
					PRINTFORMW 「取り返しのつかない事をしてしまった気がするわ…」
				ELSE
					PRINTFORML 「う、これは流石に……」
					PRINTFORMW 「私、やりすぎてしまったかしら…」
				ENDIF
			ENDIF
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 4
	ENDSELECT
	SIF GETBIT(すべて順に表示, 4) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[苦痛刻印]取得
記入チェック = 1	;0.非表示/1.同時に取得した刻印をこれ以下表示しない/2.すべて順に表示する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:0
	SETBIT すべて順に表示, 0
	SELECTCASE ARG:0
		;苦痛刻印Lv2
		CASE 2
			PRINTFORMW 「%CNAME(MASTER, TARGET)%にはもっと痛みに慣れてもらわないと、ね」
		;苦痛刻印Lv5
		CASE 5
			PRINTFORMW 「少しは苦痛にも慣れてきたかしらね」
		;苦痛刻印Lv7
		CASE 7
			PRINTFORMW 「ねぇ、痛いのが好きになってきたんじゃない…？」
			PRINTFORMW 
		;苦痛刻印Lv9…[気丈]がある場合、それを失う
		;CASE 9
		;	PRINTFORMW 「あら…？　心が折れちゃったみたいね」
		;	PRINTFORMW 「それでいいのよ…　気丈な人形なんて可愛くないもの…♪」
		;苦痛刻印Lv10
		CASE 10
			PRINTFORM 「ふふ…、もう立派な変態マゾ、ね…
			CALL PRINT_SYMBOL_K14
			PRINTFORML 「あら、気にする事ないと思うわよ？ %CNAME(MASTER, TARGET)%には相応しいと思うし」
			CALL FADEIN(8, "「私にしたって…」")
			PRINTFORMW 「嫌いじゃないしね？ そういうの。ふふ…」
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 0
	ENDSELECT
	SIF GETBIT(すべて順に表示, 0) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[反抗刻印]取得
記入チェック = 1	;（0, 非表示、1, 同時に取得した刻印をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG:3
	SETBIT すべて順に表示, 3
	SELECTCASE ARG:3
		;反抗刻印Lv2
		CASE 2
			PRINTFORMW 「それがどういう結果に繋がるのか、よく考えた方が身の為よ…」
		;反抗刻印Lv5
		CASE 5
			PRINTFORMW 「あくまでも私に逆らうつもりかしら…？」
		;反抗刻印Lv7
		CASE 7
			PRINTFORMW 「そんな態度がいつまでも許されると思ったら大間違いよ」
		CASE 8, 9
			PRINTFORMW 「本当、許し難いわね。%CNAME(MASTER, TARGET)%…」
		;反抗刻印Lv10
		CASE 10
			PRINTFORMW 「許し難いわね、その態度…」
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 3
	ENDSELECT
	SIF GETBIT(すべて順に表示, 3) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[屈服刻印]取得
記入チェック = 1	;（0, 非表示、1, 同時に取得した刻印をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:2
	SETBIT すべて順に表示, 2
	SELECTCASE ARG:2
		;屈服刻印Lv2
		CASE 2
			PRINTFORMW 「ちょっとは素直になったかしら」
		;屈服刻印Lv5
		CASE 5
			PRINTFORMW 「\@ ALI(0) ? ふふっ # フフッ \@、良い傾向ね」
		;屈服刻印Lv7…[反抗的]がある場合、それを失う
		CASE 7
			PRINTFORMW 「ようやく逆らっても無駄なのが判ったみたい、ね…？」
		;屈服刻印Lv10…[プライド高い]がある場合、それを失う
		CASE 10
			PRINTFORMW 「そうよ……、つまらないプライドなんて捨ててしまいなさい」
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 2
	ENDSELECT
	SIF GETBIT(すべて順に表示, 2) && 記入チェック == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[快楽刻印]取得
記入チェック = 1	;（0, 非表示、1, 同時に取得した刻印をこれ以下表示しない、2, すべて順に表示する）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 記入チェック && ARG:1
	SETBIT すべて順に表示, 1
	SELECTCASE ARG:1
		;快楽刻印Lv2
		CASE 2
			PRINTFORMW 「いくら言葉で否定しても、誤魔化しきれるものではないわ…」
		;快楽刻印Lv5…[快感の否定]がある場合、それを失う
		CASE 5
			PRINTFORM 「フフッ、躰は正直ね…
			CALL PRINT_SYMBOL_K14
		;快楽刻印Lv7…[抑圧]がある場合、それを失う
		CASE 7
			PRINTFORMW 「そうよ…、自分をもっと解放して…」
		;快楽刻印Lv10…[快感に素直]がない場合、それを得る
		CASE 10
			PRINTFORMW 「そうよ、それでいいの…」
			PRINTFORM 「これから、もっと可愛がってあげるわ…
			CALL PRINT_SYMBOL_K14
		;表示しない
		CASEELSE
			CLEARBIT すべて順に表示, 1
	ENDSELECT
	SIF GETBIT(すべて順に表示, 1) && 記入チェック == 1
		RETURN 1
ENDIF

;刻印取得口上「すべて順に表示」のための終端処理
RETURN すべて順に表示 != 0

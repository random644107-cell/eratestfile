;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	アリス口上 調教終了イベント口上ファイル
;
;	調教終了イベント口上をまとめたファイル
;
;Version		Reason
;1.71			@口上記入チェック仕様に。その他細かな修正
;1.60			単体ファイルとして独立
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 調教終了口上
;========================================================================
;・@EVENTCOMENDから呼び出される
;・口上主が調教終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に口上を表示
;・ARG:0の場合は固有の地の文なし。口上表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと口上表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:日常制御	調教終了時状況(1=通常/2=精力0/3=一回休み/4=失神した)
;-------------------------------------------------
@KOJO_EVENT_K14_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 調教対象が疲れている、2, 調教対象が失神した)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・失神による調教終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・調教対象消耗度に応じて疲労/失神に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→口上の表示
;・EVENT201, 介抱(日常イベント)の箇所を書く場合は、上手く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	PRINTFORMW 「――やれやれ。その様子じゃもう無理みたいね」
	PRINTFORMW 「まあいいわ。ここで終わりにしましょう」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が失神した FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 2 && TALENT:TARGET:ご主人様
	PRINTFORMW 「やりすぎちゃったみたいね…」
	PRINTFORMW 「今日は終わりにしましょう」
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・通常の調教終了時の口上
;・地の文なし。PRINTLによる空行の後に口上表示
;・枠毎にイベント番号を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;特定条件で真
;━━━━━━━━━━━━━━━━━━━━━━━━
;OPを経過済みかつ相思相愛初回時のイベントを経過した場合一度のみ表示
;------------------------------------------------
IF GET_KFLAG_K14("相思相愛") && KFLAG:(FLAG:ご主人様):50 == 1
	;初回を処理した後はRETURN 0で下位に流す
	IF FIRSTEVENT(20)
		PRINTFORMW 「こんな時間がいつまで続くかしら…」
		PRINTFORMW 「私ね、思うの…このまま時が止まってしまえばいいのに、って…」
		PRINTFORMDW アリスは微かに寂しげな表情を浮かべながら、%CALLNAME:MASTER%に身を摺り寄せてきた…
		PRINTFORMW 「あ、うん……ぎゅっとして…そう」
		PRINTFORMW 「ん……しあわせ…よ」
		PRINTFORMDW 二人は抱き合ったまま、深い眠りに落ちていった…
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;大満足ボーナスを得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:今回で大満足 && TALENT:TARGET:ご主人様
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(10)
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、中々見込みがあるわよ…」
		PRINTFORMW 「その調子で、せいぜい頑張ることね」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	IF GET_KFLAG_K14("相思相愛")
		PRINTFORM 「うふっ、とってもよかったわ…
		CALL PRINT_SYMBOL_K14
		PRINTFORMW 「じゃ、一緒に寝ましょうか」
		PRINTFORMDW 二人はベッドで抱き合ったまま、深い眠りに落ちていった…
	ELSE
		PRINTFORMW 「ふふ、今日はよかったわよ」
		PRINTFORM 「次も期待してるわね…
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;相思相愛（好意3000以上・好感度3000以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GET_KFLAG_K14("相思相愛")
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「時間が経つのが速くて恨めしい…」
	PRINTFORMW 「もっと、こうしていたいのに…」
	PRINTFORMDW %CALLNAME:MASTER%がなだめるうちに、アリスは笑みを浮かべながら安らかな寝息をたてはじめた…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教完了（好意2000以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("調教完了") && TALENT:TARGET:ご主人様
	PRINTFORMW 「物足りない顔をしてるわね」
	PRINTFORMW 「駄目よ、そんな顔したって。私も暇じゃないんだから」
	PRINTFORMW 「大丈夫…また、すぐに可愛がってあげるわ」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;溺愛（好感度2000以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("溺愛") && TALENT:TARGET:ご主人様
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(40)
		PRINTFORMDW アリスは%CALLNAME:MASTER%に溺れはじめたようだ…
		PRINTFORMW 「%CNAME(MASTER, TARGET)%、いいわ……もう食べちゃいたいくらいよ…」
		PRINTFORMW 「んっ…ちゅっ…ん、ん…ん…」
		PRINTFORMDW アリスは調教が終わった後も、%CALLNAME:MASTER%の唇を貪り続けた…
		RETURN 1
	ENDIF
	
	;その他
	PRINTFORMW 「フ、フフッ…%CNAME(MASTER, TARGET)%、本当に可愛いわね…」
	;コンフィグでFADEがONの場合
	IF ANIME_CONFIG("FADE")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"　　　　　%KOJO_RAND_K14(("キス"), 1)%")
		CLEARLINE 1
	;OFFならPRINT
	ELSE
		PRINTFORMW 「%KOJO_RAND_K14(("キス"), 1)%」
	ENDIF
	PRINTFORMDW %CALLNAME:MASTER%の口内を蹂躙し、満足したアリスは部屋を後にした…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教済み（好意1000以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("調教済み") && TALENT:TARGET:ご主人様
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(50)
		PRINTFORMW 「なぁに？　そんな寂しそうな顔して」
		PRINTFORMW 「心配しなくても、私は何処にも行かないし」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%も何処にも行けはしないわ」
		PRINTFORMW 「次回を楽しみにしてて頂戴♪」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「そうよ…素直にしてれば悪いようにはしないわ…」
	PRINTFORMW 「続きはまた今度、ね……私も楽しみにしてるから♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;嫌悪（好意-100未満）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("嫌悪")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(60)
		PRINTFORMW 「あら、怖い顔…穏やかじゃないわね」
		PRINTFORMW 「でも嫌いじゃないわ、そういうの」
		PRINTFORMDW アリスは邪悪な笑みを浮かべながら、%CALLNAME:MASTER%に近づき耳元で囁いた…
		PRINTFORMW 「心が折れた%CNAME(MASTER, TARGET)%が、どんな顔するのか…」
		PRINTFORMW 「今から楽しみね……フフフフフ……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「そんなに睨まないで頂戴」
	PRINTFORMW 「そんな顔されると、私……」
	PRINTFORMW 「自分を抑えられなくなっちゃうから……」
	PRINTFORMW 「フフフフフ……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;恐怖（好意200未満・トラウマLv3以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("恐怖")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(70)
		PRINTFORMW 「すっかり怯えてるわね…」 
		PRINTFORMW 「でもね…そんな顔されると、私……」
		PRINTFORMW 「もっと、もっと酷い事したくなっちゃうの…♪」
		PRINTFORMW 「フフフフフ……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「そんなに怯えられると、もっと虐めたくなるんだけど？」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%…分かっててやってるのかしら、ねぇ？」
	PRINTFORMW 「フフッ…次回を楽しみにしてて頂戴……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;反抗（好意0以下・反抗刻印Lv1以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("反抗")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(80)
		PRINTFORMW 「使えないわね。 興醒めだわ…」
		PRINTFORMW 「逆らってどうにかなる状況じゃないってのが、分からないのかしら？」
		PRINTFORMW 「愚鈍、ね……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「まだ逆らうつもりなのね……」
	PRINTFORMW 「自分の立場が悪くなるだけなのが、分からないのかしら？」
	PRINTFORMW 「愚鈍、ね……」
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;目覚め（好意500以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教されるのもむしろ好きになってきたかも知れない…
;-------------------------------------------------
IF 口上記入チェック(0) && AFFECTION("目覚め")
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;順応（好意100以上・好感度100以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
;・丁寧に調教を進められて、思ったより嫌じゃないかも…
;------------------------------------------------
IF 口上記入チェック(0) && AFFECTION("順応")
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;序盤（条件なし）
;━━━━━━━━━━━━━━━━━━━━━━━━
;・ここまでの条件にヒットしなかった場合
;------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(110)
		PRINTFORMW 「とりあえずこんなところかしら」
		PRINTFORMW 「慌てる必要もないものね」
		RETURN 1
	ENDIF
	
	;-------------------------
	;その他
	;-------------------------
	PRINTFORMW 「時間はいくらでもあるわ…」
	PRINTFORMW 「じっくり慣らしていってあげる。楽しみにしてて頂戴……」
	RETURN 1
ENDIF
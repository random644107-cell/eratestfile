;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	レミリア口上 調教終了イベント口上ファイル
;
;	調教終了イベント口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 調教終了口上
;========================================================================
;・@EVENTCOMENDから呼び出される
;・口上主が調教終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に口上を表示
;・ARG:0の場合は固有の地の文なし。口上表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと口上表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:日常制御	調教終了時状況(1=通常/2=精力0/3=一回休み/4=失神した)
;-------------------------------------------------
@KOJO_MALE_EVENT_K10_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 調教対象が疲れている、2, 調教対象が失神した)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・失神による調教終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・調教対象消耗度に応じて疲労/失神に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→口上の表示
;・EVENT201, 介抱(日常イベント)の箇所を書く場合は、上手く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG == 1
;	PRINTFORMW 
;	RETURN 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が失神した
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 2
	;初回
	IF FIRSTEVENT(1)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「あら…ちょっとやりすぎちゃったかしら？」
			PRINTFORMDW ニヤリと笑い、レミリアはそのまま部屋を後にした…
		;アライメント負
		ELSE
			PRINTFORMW 「もう…まだまだこれからなのに…」
			PRINTFORMW 「これじゃあ私を満足させるのはいつになるのかしら…？」
		ENDIF
	;二回目以降
	ELSE
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「いけない、またやっちゃたわ…」
			PRINTFORMW 「力加減って難しいのね」
			PRINTFORMDW レミリアは笑みを浮かべながら、横たわるあなたを見ている
		;アライメント負
		ELSE
			PRINTFORMW 「…駄目ね」
			PRINTFORMW 「もっと、肉体的に鍛える必要があるのかしら？」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・通常の調教終了時の口上
;・地の文なし。PRINTLによる空行の後に口上表示
;・枠毎にイベント番号を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;大満足ボーナスを得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:今回で大満足
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(10)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「中々良かったわよ、ふふ…」
			PRINTFORMDW 妖しく笑いながら、レミリアはあなたの頬を優しく撫でる
		;アライメント負
		ELSE
			PRINTFORMW 「いいわ…もっと、もっとよ」
			PRINTFORMDW レミリアは恍惚の目であなたを見ている
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象の精力0
	;=================================================
	;IF 口上記入チェック(0) && FLAG:日常制御 == 2
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「良く頑張ったわね…」
			PRINTFORMW 「御褒美に、もっともっと躾けてあげなくちゃね…」
			PRINTFORMDW レミリアの瞳がギラリと光った
		;アライメント負
		ELSE
			PRINTFORMW 「いいわね、その調子よ」
			PRINTFORMW 「その頑張りで、もっと私を喜ばせて頂戴…！」
			PRINTFORMDW レミリアは体をゾクゾクッと震わせながら歓喜の声を上げている
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;以下、好感度による派生
;*************************************************

;EVENT11あたりで任意のKFLAGを立てて条件にしてやると話の辻褄は合わせやすいです
;━━━━━━━━━━━━━━━━━━━━━━━━
;相思相愛（好意3000以上・好感度3000以上）
;-------------------------------------------------
;・互いに好き合っている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("相思相愛")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(20)
		PRINTFORMW 「今回はここまでね」
		PRINTFORMW 「もう、そんな目で見ないの…」
		PRINTFORMW 「あなたがそうである様に、私も常にあなたを求めてる」
		PRINTFORMW 「互いがそうである以上、私たちは誰にも邪魔する事はできないわ」
		PRINTFORMW 「だから会えなくなるなんて事は無いの」
		PRINTFORMW 「良い子だから、少しの間だけ待っててちょうだい」
		PRINTFORMDW そう言いつつ部屋を後にするレミリアだが、背中の翼はうなだれていた…
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「もう…私も完全にあなたに依存しちゃってるわね…」
		PRINTFORMW 「この時間が永遠に続けばいいのに…」
		PRINTFORMW 「一時的とはいえ別れるのは惜しいけど、またね…ん…」
		PRINTFORMDW レミリアはあなたの唇に自分の唇を重ねた後、部屋を後にした
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
オリジナルで記入されていない箇所なので、コメントアウトしておきます@/L
;好感度の伸び的に溺愛の後になると思われます
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教完了（好意2000以上）
;-------------------------------------------------
;・調教対象が口上主にもうメロメロ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("調教完了")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(30)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;溺愛（好感度2000以上）
;-------------------------------------------------
;・調教者の方が調教対象にはまってしまい、深かったり倒錯した愛を注いでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("溺愛")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(40)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教済み（好意1000以上）
;-------------------------------------------------
;・調教対象が口上主に調教されるのを幸せに感じている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("調教済み")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(50)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;嫌悪（好意-100未満）
;-------------------------------------------------
;・調教者に対し嫌悪感を抱いている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("嫌悪")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(60)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・心に傷を作られ、ご主人様の姿を見るだけで怯えてしまう
;・トラウマの種は「%ACTSTR(CFLAG:MASTER:8)%」で表示できる
;　例）PRINTFORMW 「もう%ACTSTR(CFLAG:MASTER:8)%は嫌ですって？」
;━━━━━━━━━━━━━━━━━━━━━━━━
;恐怖（好意200未満・トラウマLv3以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("恐怖")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(70)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;反抗（好意0以下・反抗刻印Lv1以上）
;-------------------------------------------------
;・心を開かない奴隷に苛つくご主人様
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("反抗")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(80)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;目覚め（好意500以上）
;-------------------------------------------------
;・調教されるのもむしろ好きになってきたかも知れない…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("目覚め")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(90)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;順応（好意100以上・好感度100以上）
;-------------------------------------------------
;・丁寧に調教を進められて、思ったより嫌じゃないかも…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("順応")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(100)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;序盤（条件なし）
;-------------------------------------------------
;・ここまでの条件にヒットしなかった場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0)
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(110)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
[SKIPEND]
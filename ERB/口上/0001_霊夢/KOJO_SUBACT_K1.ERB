;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 脱衣・追加アクション口上ファイル
;
;	調教者が調教指示以外に行う行動のファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;//暫定仕様@/L

;────────────────────────────────────
;脱衣アクション口上
;────────────────────────────────────
;・調教者アクションの前に強制脱衣や自主脱衣が発生したときに表示
;・この口上の時点ではまだTEQUIPは更新されていない
;・初回分岐は「初めて見た/見せた」とは限らない。
;　ショップメニューで脱がせたことがあるかもしれないし、調教の流れで完全に脱がせずに露出だけしている場合もある。
;　もしかすると助手だったときに見たことがあるかもしれない。そういう意味では簡易的なものである。
;　厳密な意味での「初見」フラグを用意するかどうかは要望次第ということで。
;-------------------------------------------------
@KOJO_DATUI_K1
;-------------------------------------------------
;関連変数…
;強制脱衣：TFLAG:102　自主脱衣：TFLAG:103
;=(-1, 強制脱衣に抵抗、1, 上着、2, 下半身上着、3, 上衣、4, 上半身全部、5, 下半身全部、
;　6, アクセサリー、7, アクセサリー以外全て、8, 全裸まで、9, 下着だけ)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣に抵抗された FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:強制脱衣 < 0
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTDATUI(0)
		RETURN 1
	ENDIF
	
	;=================================================
	;自分は脱いだ
	;=================================================
	IF 口上記入チェック(0) && TFLAG:自主脱衣
		PRINTFORMW 
		RETURN 1
	ENDIF
	;=================================================
	;勃起中 今大きくなってるかららめぇ
	;=================================================
	IF 口上記入チェック(100) && PENIS(MASTER) && TCVAR:MASTER:勃起度 >= 1000
		CALL FADEOUT(12, "「躍起になって隠しても」")
		PRINTFORMW 「もう%RAND_SPLIT("勃起し/大きくなっ/準備でき/元気になっ")%てるの、服の上からでも」
		CALL FADEIN(8, "「バレバレよ？」")
		CALL FADEOUT(8, "「くす」")
		RETURN 1
	ENDIF
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「ぐぬぬ…」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;二人とも何か脱いだ FIRSTDATUI(10)
;------------------------------------------------
;・主にセックス系行動の予備動作で発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:強制脱衣 > 0 && TFLAG:自主脱衣 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTDATUI(10)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が[情欲]状態
	;=================================================
	IF 口上記入チェック(0) && T_COND("情欲")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMDW これからの行為に期待してか、%CALLNAME:TARGET%は%CALLNAME:MASTER%をチラチラと観察しながら服を脱いでいった
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・アクセサリーのみ FIRSTDATUI(20)
;------------------------------------------------
;・主に靴下。他には手袋や袖など
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:自主脱衣 == 6 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;全裸になった（脱ぐ前は靴下以外裸とかそういう感じ）
	;=================================================
	IF 口上記入チェック(0) && TFLAG:自主脱衣 && ZENRACHECK(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORML 「これは邪魔になるわね…」
		PRINTFORMDW %CALLNAME:TARGET%が袖を外すと、%CALLNAME:MASTER%はつい声を漏らしてしまった
		PRINTFORMW 「つけたままが良かったかしら？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・まんまんうｐ FIRSTDATUI(30)
;------------------------------------------------
;・今回の脱衣で性器が見えるようになった場合に表示
;・ぱんつ見せを経由せず一気に脱ぐ場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:自主脱衣 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTDATUI(30)
		;昼
		;錯誤の為修正
		;IF TIME == 1
		IF TIME == 0
		;夜
		ELSE
			PRINTFORMW 「明かりは消さない方がいいわよね」
		ENDIF
		PRINTFORMW 「こんなところ、あんただから見せるんだから」
		PRINTFORMDW %CALLNAME:TARGET%の恥丘には薄っすらと陰毛が生え、その茂みは彼女が幼い少女ではなくひとりの女性なのだと無言の主張をしていた
		RETURN 1
	ENDIF
	
	;=================================================
	;ちんこうｐ
	;=================================================
	IF 口上記入チェック(100) && PENIS(TARGET)
		PRINTFORMW 「これでこれからどうされちゃうか…わかるわよね？」
		PRINTFORMW %CALLNAME:TARGET%は一物を見せつけながら、%CALLNAME:MASTER%にイヤらしい視線を向けている
		RETURN 1
	ENDIF
	
	;=================================================
	;すでに胸が見えてる
	;=================================================
	IF 口上記入チェック(100) && B_DATUICHECK(0) == 1
		SIF !TEQUIP:性交奉仕中 && TCVAR:MASTER:勃起度 >= 1000
			PRINTFORMDL %CALLNAME:TARGET%はいきり立つ%CALLNAME:MASTER%の股間を凝視しながら下着を下していった
			;PRINTFORMDL %CALLNAME:TARGET%は脈動する%CALLNAME:MASTER%の股間を凝視しながら下着を下していった
			;PRINTFORML 「何脱いだだけで射精しそうになってるのよ\@ TALENT:MASTER:早漏 ? 早漏 #  \@」
		SIF IS_CONDITION(MASTER, "情欲")
			PRINTFORML 「そんなにかぶりつくように見られると、その、困ったわね」
		IF IS_CONDITION(TARGET, "情欲")
			PRINTFORML （濡れてるの解っちゃうわよね）
			;PRINTFORMDW %CALLNAME:TARGET%の秘唇から内腿を蜜が伝う
			PRINTFORMDW %CALLNAME:TARGET%の内腿を蜜が伝う
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMDW 下着を下ろすその一瞬、つい秘裂に目を奪われる
		PRINTFORMW 「どう、かしら？…な、なんとか言いなさいよ」
		IF TEQUIP:MASTER:自慰中
			PRINTFORMW 「手、激しくなってる」
			IF IS_CONDITION(TARGET, "情欲")
				CALL FADEOUT(8, "（ごくっ）")
				;うちの霊夢さんは初心だからくぱぁが似合わないことに注意
			ELSEIF TCVAR:MASTER:イきそう
				PRINTFORML 「そんなに激しくして大丈夫？って、なんだか駄目そうね。」
			ELSE
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%は手を口元にあてながら%CALLNAME:MASTER%の自慰を凝視している
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・おっぱいうｐ FIRSTDATUI(40)
;------------------------------------------------
;・今回の脱衣でおっぱいが見えるようになった場合に表示
;・ブラ見せを経由せず一気に脱ぐ場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:自主脱衣 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && !TALENT:巨乳 && FIRSTDATUI(40)
		PRINTFORMDW サラシの下の二つのふくらみは、今、女性と少女の境にあるようだ
		PRINTFORMW 「この頃ね、大きくなってきたの」
		;ソースは早苗 おっぱいが嫌いな女子は居るのであろうか
		PRINTFORMW 「男子は皆大きい胸が好きだって早苗が言ってたけど、あんたはどう？」
		RETURN 1
	ENDIF
	
	;=================================================
	;すでに性器が見えてる
	;=================================================
	IF 口上記入チェック(0) && P_DATUICHECK(0) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	;=================================================
	;溺愛　挟んでくれる程度の進行具合
	;=================================================
	IF 口上記入チェック(100) && AFFECTION("溺愛")
		PRINTFORMW 「挟んで欲しい？んっふっふ…どうしようかしら？」
		RETURN 1
	ENDIF
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「あんたにえっちな目で見られるのは\@ AFFECTION("相思相愛", TARGET) ? 嬉しいけど # 満更でもないけど \@…やっぱり恥ずかしいわ…」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・ぱんつ見せ FIRSTDATUI(50)
;------------------------------------------------
;・今回の脱衣でパンティ（キャラによってドロワーズ・トランクス）が見えるようになった場合に表示
;・下半身下着の無いキャラは書く必要なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:自主脱衣 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTDATUI(50)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・ブラ見せ FIRSTDATUI(60)
;------------------------------------------------
;・今回の脱衣でブラ（キャラによってスポーツブラ・サラシ）が見えるようになった場合に表示
;・上半身下着の無いキャラは書く必要なし
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:自主脱衣 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTDATUI(60)
		;あなたをしゃべらせないほうがよりよい
		PRINTFORMDW 色気がない。
		PRINTFORMW 「うっさい馬鹿」
		PRINTFORMW （色気のある下着…アリス辺りが詳しいかしら…）
		PRINTFORMDW 色気がない。だがそれがいい…！
		PRINTFORMW （どうしたもんかしらね。この馬鹿亭主）
		RETURN 1
		
		;後日、てんこちゃんにスポブラについて相談する天然ドＳ霊夢さん
		;胸についての相談されて調子コキまくるてんこちゃん
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・全裸に FIRSTDATUI(70)
;------------------------------------------------
;・「全裸まで脱いだ」だけではなく、「今脱いだので全裸になった」という意味
;・「上着しか残っていない状態で上着を脱いだ」なども含まれる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:自主脱衣 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%は羞恥に紅潮しながらも、一糸纏わぬ姿となった
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・アクセサリーのみ FIRSTDATUI(80)
;------------------------------------------------
;・主に靴下。他には手袋や袖など
;・脱がしたうちに入らない！となりがちなのでこれだけは部位別テンプレを用意
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 == 6
	;=================================================
	;全裸になった
	;=================================================
	IF 口上記入チェック(0) && ZENRACHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・ちんこ見せろ FIRSTDATUI(90)
;------------------------------------------------
;・今回の脱衣で男性器が見えるようになった場合に表示
;・ぱんつ見せろを経由せず一気に脱がせる場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:強制脱衣 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	;=================================================
	;今大きくなってるかららめぇ
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
		PRINTFORMW 「あんたのちんちん、見せて貰うわよ」
		PRINTFORMW 「…元気ね♪」
		RETURN 1
	ENDIF
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「ほら、いいから脱ぎなさい！脱げ！」
		SIF IS_CONDITION(TARGET, "情欲")
			PRINTFORML 「脱げーっ！！」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・まんまん見せろ FIRSTDATUI(100)
;------------------------------------------------
;・今回の脱衣で女性器が見えるようになった場合に表示
;・ぱんつ見せろを経由せず一気に脱がせる場合あり
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:強制脱衣 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:オトコ
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「ほら、いいから脱ぎなさい！」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・おっぱい見せろ FIRSTDATUI(110)
;------------------------------------------------
;・今回の脱衣でおっぱいが見えるようになった場合に表示
;・ブラ見せろを経由せず一気に脱がせる場合あり
;・調教対象がオトコだと表示しないので、オトコ限定口上では書く必要が無い
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:オトコ
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・ぱんつ見せろ FIRSTDATUI(120)
;------------------------------------------------
;・今回の脱衣で下半身下着が見えるようになった場合に表示
;・調教対象が下半身下着の無いキャラ限定の口上であれば書く必要がない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:強制脱衣 && P_DATUICHECK(1) == 2
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		IF TCVAR:MASTER:勃起度 >= 1000
			PRINTFORMW 「もう硬くなってるみたいね？下着を押し上げてて、丸わかりよ」
			PRINTFORMW 「すぐ、%RAND_SPLIT("楽にしてあげるから/柔らかくしてあげるわ")%」
		ELSE
			PRINTFORMW 「まだ、おっきくなってないみたいね？」
			PRINTFORMW 「すぐ硬くして、それから柔らかくしてあげるわ」
		ENDIF
		
		;ふたなりあなたでそちんの場合 霊夢さんは比較対象知らない
		;大丈夫、えのきには勝ってるわ。しめじには、いや、まあ、その
		;ふたなり霊夢さんだいなりおとこあなた
		;PRINTFORMW 「その下着、可愛いわね」
		;PRINTFORML 「でも、下着の中身はもっと可愛いわよ」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・ブラ見せろ FIRSTDATUI(130)
;------------------------------------------------
;・今回の脱衣でブラ上半身下着が見えるようになった場合に表示
;・調教対象がオトコだと表示しないので、オトコ限定口上では書く必要が無い
;・調教対象が上半身下着の無いキャラ限定の口上であれば書く必要がない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:オトコ
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTDATUI(130)
		PRINTFORMW 「その下着、可愛いわね」
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・全裸に FIRSTDATUI(140)
;------------------------------------------------
;・「全裸まで剥いだ」だけではなく、「今剥いだので全裸になった」という意味
;・「上着しか残っていない状態で上着を剥いだ」なども含まれる
;・「おっぱい見せろ」～「ぱんつ見せろ」を書いた場合は記入する必要はない（上層の分岐に食われてここまで落ちてこない為）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:強制脱衣 && ZENRACHECK(1)
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		IF TCVAR:MASTER:勃起度 >= 1000
			PRINTFORMW 「これで全部ね」
		;皮の鎧
		ELSE
			PRINTFORMW 「もう一枚あったわね」
			CALL FADEOUT(8, "くすっ")
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;自主脱衣・何か脱いだ FIRSTDATUI(150)
;------------------------------------------------
;・自慰・奉仕系の予備動作になりやすい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:自主脱衣 && !IS_NOWASSIACTNAME(, "脱衣")
	;=================================================
	;調教者が[情欲]
	;=================================================
	IF 口上記入チェック(0) && IS_CONDITION(TARGET, "情欲")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;強制脱衣・何か剥いだ FIRSTDATUI(160)
;------------------------------------------------
;・愛撫系の予備動作になりやすい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:強制脱衣
	;=================================================
	;調教者が[情欲]
	;=================================================
	IF 口上記入チェック(0) && IS_CONDITION(TARGET, "情欲")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;========================================================================
;追加アクション口上
;========================================================================
;・調教者が追加行動を取ったときに汎用的に表示
;・書く場合、アクション口上の後に続けて違和感のない形で書かれることが望ましい
;-------------------------------------------------

;脱衣後に限らず追加行動の箇所
;要はアナル舐め手淫や逆レイプ乳首コリコリ等に用いる
;脱衣からのコンボをするならACT手淫側に前回行動参照分岐を作る
;────────────────────────────────────
;EXTRA1, Ｃ愛撫
;────────────────────────────────────
;・追加で愛撫または手淫を行う
;・調教対象にペニスがない場合は愛撫、ある場合は手淫
;-------------------------------------------------
@KOJO_ACT_EXTRA_K1_1
;-------------------------------------------------
IF 口上記入チェック(0) && TCVAR:MASTER:勃起度 <= 1000
	RETURN 1
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 愛撫する
;-------------------------------------------------
;・調教対象にペニスがない場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !PENIS(MASTER)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 手淫する
;-------------------------------------------------
;・調教対象にペニスがある場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && PENIS(MASTER)
	RETURN 1
ENDIF

;────────────────────────────────────
;EXTRA2, Ｖ愛撫
;────────────────────────────────────
;・追加でヴァッジ愛撫を行う
;・調教対象がオトコの場合発生しない
;・調教対象が処女の場合は発生しない
;-------------------------------------------------
@KOJO_ACT_EXTRA_K1_2
;-------------------------------------------------
IF 口上記入チェック(0)
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;EXTRA3, Ａ愛撫
;────────────────────────────────────
;・追加でアナル愛撫を行う
;-------------------------------------------------
@KOJO_ACT_EXTRA_K1_3
;-------------------------------------------------
IF 口上記入チェック(0)
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ愛撫
;────────────────────────────────────
;・追加で乳首責めを行う
;-------------------------------------------------
@KOJO_ACT_EXTRA_K1_4
;-------------------------------------------------
IF 口上記入チェック(0)
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;媚薬等使用口上
;────────────────────────────────────
;・調教者が媚薬やローションを使用したときに汎用的に表示
;・「アクション口上」で記入チェックを=2とした場合は表示されない
;・書く場合、アクション口上の後に続けて違和感のない形で書かれることが望ましい
;-------------------------------------------------
@KOJO_USE_K1
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ローション/媚薬同時使用
;------------------------------------------------
;・主に性交系コマンドから発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && GETBIT(TFLAG:媚薬等使用口上, 0) && GETBIT(TFLAG:媚薬等使用口上, 1)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ローション使用
;------------------------------------------------
;・玩具・性交系コマンド等から発生する
;━━━━━━━━━━━━━━━━━━━━━━━━
;パイズリのお供としても活用される
IF 口上記入チェック(0) && GETBIT(TFLAG:媚薬等使用口上, 0)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;媚薬使用
;------------------------------------------------
;・愛撫系コマンド等から発生する
;・キスで発生した場合は口移しとなるため、そのケースはキスのアクション口上内で処理するほうがよい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && GETBIT(TFLAG:媚薬等使用口上, 1)
	PRINTFORMW 
	RETURN 1
ENDIF
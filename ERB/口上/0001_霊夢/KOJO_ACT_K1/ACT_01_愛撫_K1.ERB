;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 愛撫系アクション口上ファイル
;
;	愛撫系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;愛撫系 ACT10～19 & 27
;========================================================================
;・TFLAG:ACT(10=手で愛撫/11=胸愛撫/12=口で愛撫/13=アナル愛撫/14=アナル舐め/15=キス/16=性器を擦り合う/17=パイズリする/18=足コキする/19=指挿入れ/27=押し倒す)
;・TFLAG:ACT分類 == 1に該当
;・ACT18, 足コキは異常系から派生する場合あり
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "愛撫")で拾う事が可能
;・調教者が調教対象を愛撫する。この口上の時点で調教者はすでに手を出している
;・媚薬を使用するケースがある
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT10, 手で愛撫(ACT10_0, 愛撫する/ACT10_1, 手淫する/ACT10_2, ペニスバンド手淫)
;────────────────────────────────────
;・Ｃ中心に愛撫する
;・調教対象にペニスがある場合、及びペニスバンド装着時には派生コマンドになる
;・調教対象が自慰中の場合は解除される
;・調教者の手に汚れがある場合不快SOURCEが変動する
;・調教者技巧、愛撫、器用な指先素質に応じて勃起度上昇
;-------------------------------------------------
@KOJO_ACT_K1_10
;-------------------------------------------------
;・TFLAG:ACT派生(0=愛撫/1=手淫/2=ペニスバンド手淫)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_1, 手淫する
;━━━━━━━━━━━━━━━━━━━━━━━━
;・男性器への愛撫を行う、つまり手コキ
;・他の派生と比べてSOURCE:MASTER:快Ｃは+200%
;・調教者技巧、愛撫、器用な指先素質に応じて勃起度上昇
;-------------------------------------------------
;必要に応じて、TEQUIP:オナホールやTCVAR:MASTER:勃起度
;PANTSCHECK(1)等で分岐を設ける事もできます。
;まともにやると手間が尋常じゃないので、効率よく記述する事をお勧めします
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;=================================================
	;初回
	;=================================================
	;試しに助手を呼んでみた所、ダブル手コキでもこの箇所が発生
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「触ってもいいかしら？ううん。あんたに拒否権はないわ、触らせなさい！」
		PRINTFORMW 「\@ TEQUIP:MASTER:下半身下着 ? …これかしら？ # \@…ぶにぶにしてて、硬くなってきてる…変な感触ね」
		PRINTFORML
		;処女性の補強。今一つしっくりこない
		;CALL FADEOUT(8, "（うひゃあ……）")
		PRINTFORMDW 次第に硬度を増していく陰茎に%CALLNAME:TARGET%は目を丸くしている
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	;IF 口上記入チェック(0) && ONCEACT(0)
	;	;下半身服装がある場合
	;	IF PANTSCHECK(1)
	;		PRINTFORMW 
	;	;その他
	;	ELSE
	;		PRINTFORMW 
	;	ENDIF
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;調教対象がイキそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;-------------------------------------------------
		;前から愛撫
		;-------------------------------------------------
		;IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
		;	;焦らし度によって分岐
		;	SELECTCASE TCVAR:MASTER:焦らし度
		;		;焦らし中
		;		CASE IS > 1
		;		;焦らし終了
		;		CASE 1
		;		;焦らし無し
		;		CASEELSE
		;	ENDSELECT
		;	RETURN 1
		;ENDIF
		
		;-------------------------------------------------
		;後ろから愛撫
		;-------------------------------------------------
		;IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
		;	;焦らし度によって分岐
		;	SELECTCASE TCVAR:MASTER:焦らし度
		;		;焦らし中
		;		CASE IS > 1
		;		;焦らし終了
		;		CASE 1
		;		;焦らし無し
		;		CASEELSE
		;	ENDSELECT
		;	RETURN 1
		;ENDIF
		
		;位置前後分岐を埋めるなら必要なし。埋めないならここで補完させましょう
		;不要な分岐は削除、もしくはコメントアウトすることで負荷は軽くなります
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(100)
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					;焦らしそのものが多発するので拡充確率操作
					IF !RAND:3
						PRINTFORMW 「ちょっと意地悪しちゃおうかしら…んふふ」
						IF !RAND:3
							PRINTFORM 「
							PRINTFORMD 大好きな%CALLNAME:TARGET%さんで%RAND_SPLIT("抜きたい/いきたい")%です
							PRINTFORM って言えたら、続けてあげるわ
							CALL HEARTBW
							IF !RAND:3
								PRINTFORML
								PRINTFORMW 「私の方がずっと、焦らされてたんだから…」
								PRINTFORMDW %CALLNAME:TARGET%は手の動きを緩め、拗ねたようにおねだりしてきた
							ENDIF
						ENDIF
					ELSE
						PRINTFORML 「このまま手で出しちゃうのも、勿体無い気がしてきたわ」
						;快感を受け入れる場合不自然になりがちな為調整コメントアウト
						;PRINTFORMW 「あんたはどうされたい？どこで射精したいかしら」
						PRINTFORMW 「あんたはどうされたい？射精したい？」
					ENDIF
				;構文の変更
				;焦らし終了
				;CASE 1
				;焦らし無し
				;CASEELSE
					;RETURN 0
				;焦らし終了焦らし無し
				CASEELSE
					IF IS_CONDITION(TARGET, "情欲")
						PRINTFORML 「%RAND_SPLIT("そろそろ/もう限界")%かしら？もう限界よね」
						PRINTFORML 「ほら、いっちゃいなさい」
						PRINTFORMW 「いくとこ見せて」
						;子供のつくりかたは知っていても射精についての性知識は無い。という条件は苦しい 口上用フラグの設置を検討する事
						;「いっちゃえ」 「いっちゃえいっちゃえ」 と 「いっちゃえいっちゃえ」 「いっちゃえ」 テンポに推敲を要する三連
						PRINTFORMW 「いっちゃえ」
						PRINTFORMW 「いっちゃえ」
						PRINTFORMW 「いっちゃえ」
						RETURN 1
					ENDIF
					IF TALENT:サド
						PRINTFORML 「ほらこのまま射精していいわよ」
						PRINTFORML 「ぴゅっぴゅって皮の中に精液出して気持ちよくなっちゃいなさい」
						PRINTFORMDW %CALLNAME:TARGET%は皮で亀頭を包みながら%CALLNAME:MASTER%を絶頂へ導いた
					ELSE
						PRINTFORMW 「%RAND_SPLIT("そろそろ/もう限界")%かしら？%RAND_SPLIT("いっていいわよ/よく頑張ったわね")%」
						;アナル舐め手淫との擦り合わせが未だ
						;PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の耳を甘噛みしながら手の動きを速めた
						
						;PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%のうなじに口付けながら手の動きを速めた
					ENDIF
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;連続実行
	;=================================================
	;IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
	;	;-------------------------------------------------
	;	;顔面騎乗中
	;	;-------------------------------------------------
	;	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
	;		;前から愛撫
	;		IF TCVAR:MASTER:位置前後 == 1
	;			PRINTFORMW 
	;		;後ろから愛撫
	;		ELSE
	;			PRINTFORMW 
	;		ENDIF
	;		RETURN 1
	;	ENDIF
	;	
	;	;-------------------------------------------------
	;	;その他
	;	;-------------------------------------------------
	;	IF 口上記入チェック(0)
	;		;前から愛撫
	;		IF TCVAR:MASTER:位置前後 == 1
	;			PRINTFORMW 
	;		;後ろから愛撫
	;		ELSE
	;			PRINTFORMW 
	;		ENDIF
	;		RETURN 1
	;	ENDIF
	;ENDIF
	;=================================================
	;無条件顔面騎乗
	;=================================================
	IF 口上記入チェック(100) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 「あんたのも気持ち良くしてあげるわ」
		PRINTFORMW 「ごしごししてあげるから、あんたは私のをおかずにしてなさい」
		PRINTFORMW 「こんなに甲斐甲斐しいお嫁さんに尻に敷かれるなんて…あんたって果報者よね」
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;屈服刻印
	;=================================================
	;いまひとつ使いこなせない為コメントアウト
	;【屈服:30】性交
	IF 口上記入チェック(20) && GETBIT(CFLAG:刻印, 30)
		PRINTFORML 「これが私の中に入ったのよね」
		;頻発させるものではない為言及に条件の追記が必要
		;【屈服:25】膣内射精
		IF 口上記入チェック(50) && GETBIT(CFLAG:刻印, 25)
			;ふたなり時矛盾の恐れ
			;自慰にも転用できそうな条件
			PRINTFORMW （こんな感じだったかしら？）
			PRINTFORM 「こんな感じで%RAND_SPLIT("/ぬっぷぬっぷ/強く")%したらあんたすぐ果てちゃって、しっかり種付けしてくれ%RAND_SPLIT("ちゃった/た")%わよね
			CALL HEARTBW
			PRINTFORMDL %CALLNAME:TARGET%の口元が意地悪そうに吊り上がる
			PRINTFORMW 「また、したい？」
		ENDIF
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		SIF 口上記入チェック(20) && TALENT:器用な指先
			PRINTFORMW 「あんたの好きな事。してあげるわ」
		SIF TALENT:サド
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の唇に指をねじ込むと、唾液で濡れた指で陰茎を扱いた…
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			SELECTCASE RAND:4
				CASE 0
					PRINTFORML 「ね。気持ちいいかしら？%RAND_SPLIT("意地張らないでもいいじゃない/隠しても駄目よ？/気持ちいいわよね？/んふふ…")%」
					PRINTFORML 「あんた%RAND_SPLIT("の事だ/って判りやすい/、顔に出る")%もの…わかるわよ♪」
				CASE 1
					PRINTFORML 「おちんちん、一杯%RAND_SPLIT("しゅっしゅ/しこしこ/ごしごし")%してあげるわ」
					PRINTFORMW 「大事な所だもの。しっかりと磨いてあげないとね」
					IF !EQUIP:下半身下着
						PRINTFORML 「あんた%RAND_SPLIT("って、こういうの/こういうの、")%好きでしょ？」
						;PRINTFORMDW %CALLNAME:TARGET%はドロワーズを被せながら丹念に%CALLNAME:MASTER%の陰茎を磨いている
						PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の陰茎を下着で包んできた
						PRINTFORMW 「元気になってる♪下着、破かないでよ」
					ENDIF
				CASE 2
					PRINTFORML 「………」
					CALL FADEOUT(8, "    じーっ")
					CALL FADEIN(8, "    くすっ")
					PRINTFORMDW %CALLNAME:TARGET%は無言で%CALLNAME:MASTER%の表情に訊ねながら陰茎を扱いている
				CASEELSE
					PRINTFORMW 「%RAND_SPLIT("ふふっ/くす")%。気持ち良さそうね\@ AFFECTION("溺愛") ? ♪ # ？ \@」
					PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の表情をじっくりと観察しながら丁寧に陰茎を扱いている
			ENDSELECT
		;コメントアウトしておきます@/L
		;ELSE
		;後ろから
		ELSE
			SELECTCASE RAND:5
				CASE 0
					PRINTFORMDW %CALLNAME:TARGET%はほっぺたをくっつけながら、%CALLNAME:MASTER%への責めを続けている
					PRINTFORMW 「こうやってあんたの顔を間近で見ながらするのも、いいものね」
				CASE 1
					PRINTFORML 「どう？\@ EXP:MASTER:自慰経験 ? 自分でするより #  \@気持ちいいかしら？」
					PRINTFORM 「
					PRINTFORMD %CALLNAME:TARGET%のおてて気持ちいい
					PRINTFORML って言えたら、参考にしてあげてもいいわよ」
				CASE 2
					PRINTFORMDW 背中に%CALLNAME:TARGET%の胸の感触が伝わってくる
					;サド成分の補強 見えなくとも%CALLNAME:TARGET%の挙動を理解している%CALLNAME:MASTER%
					IF TALENT:サド
						PRINTFORMD %CALLNAME:TARGET%の口元は歪み、
						PRINTFORM 当ててるのよ
						PRINTFORMDW と言わんばかりにニヤついている
					ENDIF
				CASE 3
					;首筋責め+独占欲を出す
					;ここに水音を入れて視界不良を補填したい
					PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の首筋を吸いたてながら奉仕を続けている
				CASEELSE
					PRINTFORMDW %CALLNAME:TARGET%は片手で陰茎を扱きながら、もう片方の手で%CALLNAME:MASTER%の手を握ってきた
					PRINTFORMW 「こうすると、あんたが感じてるのがよく伝わってくるわ♪」
				ENDSELECT
			ENDIF
			;コメントアウトしておきます@/L
			;処置ありがとうございます
			;地の文に頼った方がいいかもしれないのでオミット
			;PRINTFORMDW %CALLNAME:TARGET%は後ろから抱き着くように腕を回し%CALLNAME:MASTER%の股間をまさぐってきた
			;PRINTFORMDW 背中に%CALLNAME:TARGET%の胸の感触が伝わってくる
		RETURN 1
	ENDIF
ENDIF
;ACT10_1, 手淫する

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_2, ペニスバンド手淫
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教対象がペニスバンド装着時に発生
;・快ＣSOURCE的にはTFLAG:ACT派生 == 1か否かでしか見てないっぽいので、事実上手淫ではなく愛撫と同等？
;・快Ｖも見てないような感じ。そして部位の汚れも参照できないんじゃないかなと
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(1)
		;下半身服装がある場合
		IF PANTSCHECK(1)
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_2, ペニスバンド手淫

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_0, 愛撫する
;━━━━━━━━━━━━━━━━━━━━━━━━
;・女性器への愛撫を行う
;・SOURCE的には快Ｂと快Ｃは同等
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(2)
		;下半身服装がある場合
		IF PANTSCHECK(1)
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象がイキそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;-------------------------------------------------
		;前から愛撫
		;-------------------------------------------------
		IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;後ろから愛撫
		;-------------------------------------------------
		IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;位置前後分岐を埋めるなら必要なし。埋めないならここで補完させましょう
		;不要な分岐は削除、もしくはコメントアウトすることで負荷は軽くなります
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_0, 愛撫する

;────────────────────────────────────
;ACT11, 胸愛撫
;────────────────────────────────────
;・胸愛撫/乳首吸い/乳の揉み合い/乳首責め
;・特定の条件で「乳首吸い」「乳の揉み合い」「乳首責め」へ派生する
;・調教対象がオトコの場合は必ず「乳首責め」になり、他に派生する事はない
;-------------------------------------------------
@KOJO_ACT_K1_11
;-------------------------------------------------
;・TFLAG:ACT派生(0=胸愛撫/1=乳首吸い/2=揉み合い/3=乳首責め)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_0, 胸愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
;・全ての派生条件から漏れると通常の胸愛撫になる
;・調教者の欲望+Ｂ感覚が3以上の場合1/2の確率で揉み合いに派生
;・それから漏れた場合にのみ、通常の胸愛撫となる
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;前から愛撫
	IF TCVAR:MASTER:位置前後 == 1
		PRINTFORMW 
	;後ろから愛撫
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_1, 乳首吸い
;━━━━━━━━━━━━━━━━━━━━━━━━
;・前回ACTが胸愛撫の場合乳首系になる
;・その際、調教者に幼稚か舌使いがあると3/4、なければ1/4で乳首吸いとなる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;上半身服装が要押し退け			服をまくりあげて
	IF BRACHECK(1, 1)
		PRINTFORMW
	;その他
	ELSE
		PRINTFORMW
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_2, 乳の揉み合い
;━━━━━━━━━━━━━━━━━━━━━━━━
;・胸の揉み合いを要求する。この時点での調教対象の反応は未定
;・乳首系の条件から漏れた場合、調教者の欲望+Ｂ感覚３以上なら、1/2で胸の揉み合いになる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;上半身服装が邪魔			服の上から
	;=================================================
	IF 口上記入チェック(0) && BRACHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;上半身上着が要押し退け		上着をまくり上げ
	;=================================================
	IF 口上記入チェック(0) && UPPERJACKET(1) == 1
		;下着もまくり上げた
		IF UPPERUNDER(1) == 1
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;上半身上着が要押し退け		下着の裾から手を忍ばせ
	;=================================================
	IF 口上記入チェック(0) && UPPERUNDER(1) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_3, 乳首責め
;━━━━━━━━━━━━━━━━━━━━━━━━
;・前回ACTが胸愛撫の場合乳首系になる
;・その際、調教者に幼稚か舌使いがあると1/4、なければ3/4で乳首責めとなる
;・調教対象がオトコの場合は乳首責めにしかならない
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
	;=================================================
	;調教対象がオトコ
	;=================================================
	IF 口上記入チェック(0) && TALENT:MASTER:オトコ
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT12, 口で愛撫
;────────────────────────────────────
;・クンニする/フェラチオする
;・下半身の衣装が残っていても発生することがある。調教者にTALENT:壁抜けがない場合はずらして実行する
;・ターン方針が"異常"なら発生しない
;・TEQUIP(バイブ/オナホール/ペニスバンド/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教対象にペニスがあると、必ずフェラになる
;-------------------------------------------------
@KOJO_ACT_K1_12
;-------------------------------------------------
;・TFLAG:ACT派生(0=クンニ/1=フェラ)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_0, クンニする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;顔面騎乗、アライメントが負の場合、TALENT:サド等で地の文が分岐する場合あり
	;必要に応じて拡張すると良いでしょう
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度派生処理
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1 ;焦らし中
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;連続実行のネストを開放するならここは必ず書いておきましょう
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;LOWERJACKET(1) == 1「まくりあげて」LOWERUNDER(1) == 1「ずらして」等で拡張するのも良いでしょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_1, フェラチオする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;顔面騎乗、アライメントが負の場合、TALENT:サド等で地の文が分岐する場合あり
	;必要に応じて拡張すると良いでしょう
	;-------------------------------------------------
	;初回
	;-------------------------------------------------
	IF 口上記入チェック(100) && FIRSTACT(0)
		;この辺りはある程度進行しないとお目にかかれない
		[SKIPSTART]
		PRINTFORMW 「口でしてあげるから、出しなさい」
		;迷走中;知り合いの名前を出してみたものの、くどいかもしれない
		PRINTFORMW 「夫婦では口でするのは当たり前だって妖夢が言ってたのよ」
		;妖夢は前フリ「幽々子様は鳴門」に繋げる
		PRINTFORMDW と、なると幽々子様からの又聞きか
		PRINTFORML 
		[SKIPEND]
		;ちんちんを間近観察
		CALL FADEIN(6, "（うひゃぁ…）")
		PRINTFORMW 「これを、お口で…するのよね…」
		CALL FADEOUT(6, "ごくっ…")
		CALL FADEIN(4, "「す…するわね？」")
		CALL FADEOUT(6, "「あむっ！」")
		PRINTFORMW 「んっ、んっんっ…じゅぅっ…ぢゅっ……」
		;一生懸命さを出す為に延長。コツをつかんでいきテンポを整え音が規則的に。…を短くしていく
		PRINTFORMW 「んじゅっ……ぢゅぅっ……んっんっ…んっんっんっんっ…」
		;息継ぎが下手
		CALL FADEOUT(6, "「…ぷあっ」")
		RETURN 1
	ENDIF
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;焦らし度派生処理
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の表情を見るや、奉仕を止めてしまった
				PRINTFORMDW %CALLNAME:TARGET%の目は意地悪そうに笑っている…
			;焦らし終了
			CASE 1
				IF IS_CONDITION(TARGET, "情欲")
					CALL FADEOUT(8, "（出して、出して）")
					[SKIPSTART]
					;喋らせない方が良さそう
					PRINTFORM 「せーし飲ませて
					CALL HEARTBW
					[SKIPEND]
				ELSE
					CALL FADEOUT(12, "じゅうっ…ちゅぱ…ぢゅっ…ぢゅーっ…")
					PRINTFORMW 「飲んであげるから、射精しちゃいなさい」
					CALL FADEOUT(6, "「あむっ！」")
					PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の陰茎を深く咥えると強めに吸いたてた…
					IF !RAND:3
						PRINTFORMDW やり場に困る%CALLNAME:MASTER%の視線は%CALLNAME:TARGET%の細い肩に吸い込まれた
						PRINTFORMDW その細い線は%CALLNAME:MASTER%の中の雄に%CALLNAME:TARGET%の雌を意識させた…
					ELSEIF !RAND:2
						PRINTFORMDW %CALLNAME:TARGET%の艶やかな黒髪が%CALLNAME:MASTER%の視線を捉える…
					ELSE
						PRINTFORMDW 一心不乱に肉棒を吸いたてる%CALLNAME:TARGET%の表情は長い髪に覆われて見えない…
					ENDIF
				ENDIF
			;焦らし無し
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%はリボンを揺らしながら%CALLNAME:MASTER%の陰茎を喉奥まで使って奉仕している…
		ENDSELECT
		RETURN 1
	ENDIF
	
	;イきそうは上流にある+三回連続はまず出ないので、他のプレイに移行し射精させる事を意識した方がいいのかもしれない
	;イかせるつもりでない形も相性がいいかもしれない
	;=================================================
	;フェラ連続実行
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;連続実行フェラ %CALLNAME:MASTER%情欲
		;-------------------------------------------------
		IF 口上記入チェック(50) && IS_CONDITION(MASTER, "情欲")
			CALL FADEOUT(8, "（変な声出しちゃって）")
			CALL FADEIN(8, "（可愛いんだから）")
			RETURN 1
		ENDIF
		;-------------------------------------------------
		;連続実行フェラ %CALLNAME:TARGET%情欲
		;-------------------------------------------------
		IF 口上記入チェック(50) && IS_CONDITION(TARGET, "情欲")
			[SKIPSTART]
			;瓶コーラが存在するので旧型パッケージだと強弁すればかっぱえびせんも幻想郷に存在しうる。プレイヤーさんが困るので無し
			PRINTFORMW （あー…やめらんないわこれ…）
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%のかっぱえびせんサイズのペニスにむしゃぶりついている…
			;悲鳴との連携を考えると無しではないが、痛いので無し
			PRINTFORMW （あんまり美味しくないわね…噛めば肉汁出るかしら…？）
			[SKIPEND]
			PRINTFORMDW %CALLNAME:TARGET%は美味しそうに%CALLNAME:MASTER%の陰茎を味わっている
			RETURN 1
		ENDIF
		
		;連続実行のネストを開放するならここは必ず書いておきましょう
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(100)
			PRINTFORML 「続けるわね？あむっ…」
			PRINTFORMDW %CALLNAME:TARGET%は献身的に%CALLNAME:MASTER%の一物を口で奉仕している…
			RETURN 1
		ENDIF
	ENDIF
	
	;-------------------------------------------------
	;顔面騎乗中
	;-------------------------------------------------
	IF 口上記入チェック(100) && TRUECHECK(TEQUIP:顔面騎乗) > 0
		PRINTFORMW 「私の%RAND_SPLIT("/おまんこ/大事なところ/あそこ")%でおちんちんこんなにしちゃって…」
		CALL FADEOUT(4, "「んふふ」")
		CALL FADEIN(12, "「しょうがないわね」")
		PRINTFORMW 「私でこんなにしたんだから…責任とお醤油は取ってあげないと…%RAND_SPLIT("/いけないわ")%ね」
		;アングル的に%CALLNAME:MASTER%には見えないので無し
		;PRINTFORMDW %CALLNAME:TARGET%は嬉しそうに%CALLNAME:MASTER%の陰茎を咥え込んだ
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;LOWERJACKET(1) == 1「まくりあげて」LOWERUNDER(1) == 1「ずらして」等で拡張するのも良いでしょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		;地の文
		;%CALLNAME:TARGET%は%CALLNAME:MASTER%のペニスを握りゆっくりと上下させている…
		;%CALLNAME:TARGET%は%CALLNAME:MASTER%の股間に顔を埋めてはちきれんばかりに怒張した男性器を口で愛撫し始めた…
		;口上ここ
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMDW %CALLNAME:TARGET%が%CALLNAME:MASTER%の股に顔を埋めると、垂れる黒髪が%CALLNAME:TARGET%の表情を隠す
				PRINTFORMDW 見えないアングルが、却って%CALLNAME:MASTER%を乱した
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%が%CALLNAME:MASTER%を高めようと懸命に頭を動かすたびに、リボンがひらひらと舞った…
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT13, アナル愛撫
;────────────────────────────────────
;・アナル愛撫/前立腺刺激/アナルバイブ出し入れ
;・アナルバイブ装備中は、アナルバイブ出し入れに派生
;・連続実行の場合、前立腺刺激に派生
;・イきそうor快Ｃが5000以上の場合、調教者の愛撫スキルに応じて前立腺刺激に派生
;・勃起度が低い場合、調教者の愛撫スキルに応じて前立腺刺激に派生
;・要強制勃起の場合、前立腺刺激に派生
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ソフト", "異常"だと発生しない
;・TEQUIP(三角木馬/ペニスバンド/アナルビーズ)装着中には発生しない
;・性交奉仕中には発生しない
;-------------------------------------------------
@KOJO_ACT_K1_13
;-------------------------------------------------
;・TFLAG:ACT派生（0=アナル愛撫/1=前立腺刺激/2=アナルバイブ出し入れ）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_0, アナル愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORML 「うーん…多分ここで合ってるわよね」
		PRINTFORMDL %CALLNAME:TARGET%は行き当たりばったりに%CALLNAME:MASTER%の菊門を責めてきた
		RETURN 1
	ENDIF

	;=================================================
	;弾切れかつちんこ勃ってない
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 <= 1000 && BASE:MASTER:射精 == 0
		PRINTFORML 「弾切れしたら許してもらえると思ったかしら？」
		PRINTFORML 「%RAND_SPLIT("私はまだ満足してないわよ/もう少し付き合いなさい/許さないわよ/許さ・な・い・わ・よ♪")%」
		RETURN 1
	ENDIF

	;=================================================
	;顔面騎乗中
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;下半身服装が要押し退け
	;=================================================
	IF 口上記入チェック(0) && PANTSCHECK(1, 1)
		;下半身上着をまくりあげ
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;下着越しに
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		SELECTCASE ABL:MASTER:Ａ感覚
			CASE 0 TO 2
				PRINTFORML 「痛い？大丈夫、すぐよくなるわ」
				CALL FADEOUT(4, "「…多分」")
			CASEELSE
				PRINTFORMW 「お尻で気持ちよくなっちゃうなんて、あんたって%RAND_SPLIT("/本当")%」
				PRINTFORMW 「どうしようもない%RAND_SPLIT("変態/わ")%ね？」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_1, 前立腺刺激(アナル愛撫)
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がオトコの場合のみに派生
;ふたなりの前立腺は尿道、つまり膣の上にあるとし、Ｇスポット刺激に
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「ああ此処ね？大体解ってきたわ」
		RETURN 1
	ENDIF
	;=================================================
	;勃起強制
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 <= 1000
		PRINTFORMW 「ほら！おちんちん勃たせなさい！」
		PRINTFORMW 「…%RAND_SPLIT("あんたのお尻って便利ね/あんたのおちんちんって便利ね/便利ねこれ")%」
		RETURN 1
	ENDIF
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「うーん、便利というか単純というか…これはこれである意味、男らしいのかしらね？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_2, アナルバイブ出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・アナルバイブ装備中は、アナルバイブ出し入れになる
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
	;調教対象がオトコ
	IF TALENT:MASTER:オトコ
		PRINTFORMW 「お尻ほじられて%RAND_SPLIT("はひはひ言う/あふんあふん悶える")%のはいいけど…」
		PRINTFORMW 「無駄撃ちしたら、お仕置きするわよ」
		RETURN 1
	;その他
	ELSE
		;PRINTFORMW 
		RETURN 0
	ENDIF
	;RETURN 1
ENDIF

;────────────────────────────────────
;ACT14, アナル舐め
;────────────────────────────────────
;・アナル舐め
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ソフト", "異常"だと発生しない
;・TEQUIP(アナルバイブ/アナルビーズ/浣腸器＋プラグ/ペニスバンド/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・TCVAR:MASTER:焦らし度が真になっている場合は発生しない
;・舌による前立腺刺激への派生は「そんなところまで舌が届くわけないじゃないかｗｗｗ」という事で粛清されています
;・現時点でACTが派生する事はないが、ACT改変の可能性に備え、TFLAG:ACT派生 == 0で指定
;-------------------------------------------------
@KOJO_ACT_K1_14
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT14_0, アナル舐め
;━━━━━━━━━━━━━━━━━━━━━━━━
;口淫は水音に留めあまり喋らせない
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が自慰中
	;=================================================
	IF 口上記入チェック(0) && GETBIT(TEQUIP:自慰中, 1)
		;調教者がペニスバンド身に付けている
		IF TEQUIP:調教者ペニスバンド
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;顔面騎乗中
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象が自慰中
	;=================================================
	IF 口上記入チェック(0) && GETBIT(TEQUIP:自慰中, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;下半身服装が要押し退け
	;=================================================
	IF 口上記入チェック(0) && PANTSCHECK(1, 1)
		;下半身上着をまくりあげ
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;下着越しに
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT15, キス
;────────────────────────────────────
;・キス/媚薬キス
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ハード", "異常"だと発生しない
;・調教者にTALENT:壁抜けが無い場合、TEQUIP:ボールギャグ装着中は弾かれる
;-------------------------------------------------
@KOJO_ACT_K1_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15, キス 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && FIRSTACT(0)
	;=================================================
	;お互いに初めて
	;=================================================
	IF 口上記入チェック(0) && EXP:MASTER:キス経験 == 0 && EXP:TARGET:キス経験 == 0
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象が初めて
	;=================================================
	IF 口上記入チェック(0) && EXP:MASTER:キス経験 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が初めて
	;=================================================
	IF 口上記入チェック(100) && EXP:TARGET:キス経験 == 0
		PRINTFORMW 「ちゅ…」
		;くちづけは平仮名の方が雰囲気が出る
		PRINTFORMDW 横暴極まりない行為とはあまりにも違う、ふれるような優しいくちづけ
		PRINTFORMW 「これで一線、越えちゃったわね」
		PRINTFORMDL 頬を染め口元を押さえる仕草がしおらしい…
		RETURN 1
	ENDIF

	;"初回"のネストを開放し、各条件の分岐を放置するならここで補完しましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_0, キス
;━━━━━━━━━━━━━━━━━━━━━━━━
;!GETBIT(TFLAG:媚薬等使用, 1)等の条件を削除して!TFLAG:ACT派生でまとめてしまっても良いと思います
;無理に全ての箇所を埋める事はないので、必要に応じて使い分けて下さい
;-------------------------------------------------
IF 口上記入チェック(100) && !TFLAG:ACT派生 && !GETBIT(TFLAG:媚薬等使用, 1)
	;=================================================
	;性交中（調教者挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交中
		;体位を派生処理
		SELECTCASE TEQUIP:性交中
			;正常位					頭をつかんで情熱的なキスをした…
			CASE 1
				PRINTFORMW 
			;後背位					振り向かせ唇を奪った…
			CASE 2
				PRINTFORMW 
			;騎乗位					下から突き上げながら情熱的に口づけした…
			CASE 3
				PRINTFORMW 
			;対面座位				ぎゅっと抱きしめるとそのまま唇を奪った…
			CASE 4
				PRINTFORMW 
			;背面座位				振り向かせそのまま唇を奪った…
			CASE 5
				PRINTFORMW 
			;アナルセックス			アヌスを犯しながらキスしてきた…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;性交奉仕中（調教対象挿入）
	;=================================================
	IF 口上記入チェック(100) && TEQUIP:性交奉仕中
		;焦らし度による派生			TCVAR:MASTER:イきそうで囲んではいないので、CASEELSEは通常の状態を意味する
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				;PRINTFORMW 
				RETURN 0
			;焦らし終了
			CASE 1
				IF IS_CONDITION(TARGET, "情欲")
					PRINTFORMW 「んっ！」
					PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の唇に自らの唇を合わせるや、腰を激しく動かし始めた
					PRINTFORMDW %CALLNAME:MASTER%の洩らした嬌声は、%CALLNAME:TARGET%の口内に飲み込まれ、替えに唾液を注がれた
				ELSE
					PRINTFORMW 「我慢できたご褒美、してあげるわ」
					PRINTFORMW 「ええ。私がしたいのよ♪」
					PRINTFORMDW %CALLNAME:TARGET%は機嫌良さげにピコピコとリボンを羽ばたかせると、唇に唇を重ねてきた
				ENDIF
			;焦らし無し
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%の舌を吸うと、%CALLNAME:TARGET%の%RAND_SPLIT("リボンがふるっと震えた/膣は嬉しそうにきゅぅっと陰茎を締め付けてきた")%…
				CALL FADEOUT(4, "（…これ）")
				CALL FADEIN(8, "（気持ちいい…）")
		ENDSELECT
		RETURN 1
	ENDIF
		
	;=================================================
	;相思相愛
	;=================================================
	IF 口上記入チェック(0) && AFFECTION("相思相愛")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;後ろから
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
		RETURN 1
	ENDIF
	
	;=================================================
	;前から
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		[SKIPSTART]
		;地の文
		;%CALLNAME:TARGET%は%CALLNAME:MASTER%の顔に近づき、
		;唇に唇を重ねた…
		;口上ここ
		;空行
		;さらに追加行動
		
		;地の文恥ずかしがった場合
		;%CALLNAME:TARGET%は強引に%CALLNAME:MASTER%の唇を奪うと
		;%CALLNAME:MASTER%の口内を舌で蹂躙した…
		;空行
		;%CALLNAME:MASTER%はこみ上げる快感を隠せず、恥ずかしがって顔を背けた…
		;リアクション口上はここ
		[SKIPEND]
		
		[SKIPSTART]
		;ディープキスに断定してしまうのは勿体ない バードキスはリアクション側に移転
		PRINTFORMW 「%RAND_SPLIT("ね？目、瞑って/ほら、ちゅーしなさい/キス、するわよ")%」
		PRINTFORMW 「んっ！」
		PRINTFORM 「んっ
		CALL HEARTBW
		PRINTFORMW 「んっんっ」
		PRINTFORM 「んー
		CALL HEARTBW
		
		;キス絶頂腰砕けこの時点ではリアクション内でのキス描写があるので一工夫必要
		PRINTFORMD キスを終え瞳を開けると、%CALLNAME:TARGET%のリボンは
		PRINTFORM くてっ
		PRINTFORMDW と力なく垂れ下がっていた
		
		;追加行動の事情から手を使えないので無し
		PRINTFORMDW %CALLNAME:MASTER%の頬に%CALLNAME:TARGET%の手が添えられる

		;追加行動の事情から手を使えないので無し
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の首に手を回して
		
		IF TALENT:TARGET:サド
			IF !RAND:5
				;あくまであなた受動のキス
				;PRINTFORMW 「あんたからも、して」
				;PRINTFORMDW %CALLNAME:TARGET%はひとしきり%CALLNAME:MASTER%の唇を味わうと首に腕を回し更なる口づけを求めてきた
			ELSE
				PRINTFORMW 「しちゃった」
				PRINTFORMDW %CALLNAME:TARGET%はにこにこしながら%CALLNAME:MASTER%の唇を指でなぞった
			ENDIF
		ELSE
		ENDIF
		[SKIPEND]
		IF IS_CONDITION(TARGET, "退屈")
			PRINTFORMW 「退屈してきたからちゅーしなさい」
			PRINTFORMW 「ほら」
		ELSE
			PRINTFORMW 「%RAND_SPLIT("ね？目、瞑って/ほら、ちゅーしなさい/するわよ")%」
			PRINTFORM 「んっ
			CALL HEARTBW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_1, 媚薬キス
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教対象に口移しで媚薬を飲ませる
;-------------------------------------------------
;RESULT値を2として@KOJO_USE_KXの媚薬等使用口上を呼び出さない設定をデフォルトとしておきます
;必要に応じて戻り値を設定してください
;-------------------------------------------------
IF 口上記入チェック(100) && !TFLAG:ACT派生 && GETBIT(TFLAG:媚薬等使用, 1)
	;媚薬使用口上処理済チェック(媚薬使用の地の文カットする際はTFLAG:媚薬等使用地の文スキップのコメントを解除)
	CLEARBIT TFLAG:媚薬等使用口上, 1
	;CLEARBIT TFLAG:媚薬等使用地の文スキップ, 1
	;=================================================
	;性交中（調教者挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交中
		;体位を派生処理
		SELECTCASE TEQUIP:性交中
			;正常位					頭をつかんで情熱的なキスをした…
			CASE 1
				PRINTFORMW 
			;後背位					振り向かせ唇を奪った…
			CASE 2
				PRINTFORMW 
			;騎乗位					下から突き上げながら情熱的に口づけした…
			CASE 3
				PRINTFORMW 
			;対面座位				ぎゅっと抱きしめるとそのまま唇を奪った…
			CASE 4
				PRINTFORMW 
			;背面座位				振り向かせそのまま唇を奪った…
			CASE 5
				PRINTFORMW 
			;アナルセックス			アヌスを犯しながらキスしてきた…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;=================================================
	;性交奉仕中（調教対象挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交奉仕中
		;焦らし度による派生			TCVAR:MASTER:イきそうで囲んではいないので、CASEELSEは通常の状態を意味する
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		[SKIPSTART]
		;地の文
		;%CALLNAME:TARGET%は%CALLNAME:MASTER%の顔に近づき、
		;唇に唇を重ねた…
		;口上ここ
		;%CALLNAME:TARGET%は自ら媚薬を飲んで、キスしながらそれを口移しで%CALLNAME:MASTER%に飲ませた…
		[SKIPEND]
		;言い回しに課題が残る
		PRINTFORML 「口開けて？濃いのするんだから」
		PRINTFORM 「私の唾液、飲んで
		CALL HEARTBW
		RETURN 2
	ENDIF
ENDIF

;────────────────────────────────────
;ACT16, 性器を擦り合う
;────────────────────────────────────
;・貝合わせ/素股する
;・調教対象にペニスがある場合はACT派生 == 1「素股する」へ派生する
;・調教対象がペニスバンド装着中は、ACT派生 == 2「ペニスバンド素股」になる
;・ターン方針が"ハード", "異常だと発生しない
;・調教対象が女の子でバイブ装着中だと発生しない
;・TEQUIP(オナホール/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教者にペニスがあると発生しない（奉仕系の素股強制になる）
;-------------------------------------------------
@KOJO_ACT_K1_16
;-------------------------------------------------
;・TFLAG:ACT派生（=0,貝あわせ/=1,素股/=2,ペニスバンド素股）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_0, 貝合わせ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;地の文は調教対象の下半身服装毎の分岐あり。必要に応じて拡張してください
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_1, 素股する
;━━━━━━━━━━━━━━━━━━━━━━━━
;・女性器と太股で男性器を挟んで愛撫する
;・調教対象にペニスがあると、必ず素股するになる
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;焦らし度派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 「まだ射精しちゃ駄目よ？」
				PRINTFORMW 「くす。頑張れるだけ頑張んなさい」
			;焦らし終了
			CASE 1
				PRINTFORMW 「焦らすのは、この位にしておこうかしら」
				PRINTFORMW 「あんたの先っぽが、私の入り口に当たってるの、分かるかしら？」
				PRINTFORMW 「我慢しないでいいわよ？」
			;焦らし無し
			CASEELSE
				PRINTFORMW 「あんたが私の下で射精しちゃうとこ。見ててあげるわ♪」
				PRINTFORMW %CALLNAME:TARGET%は発情しきった目で%CALLNAME:MASTER%のペニスの先端を食い入るように見つめている
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT == TFLAG:前ACT
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW 「このまま、擦り付けてたら挿入っちゃいそうね…」
			PRINTFORMW 「挿入れたい？」
			PRINTFORMW 「%RAND_SPLIT("私は挿入れたいわ/先っぽだけ、挿入れちゃおうかしら…")%」
			PRINTFORMDW %CALLNAME:TARGET%は膣穴で%CALLNAME:MASTER%の亀頭を押し潰しながら腰を上下に揺すっている
		;その他
		ELSE
			PRINTFORMW 「あんた、ふともも好きだもんね？前に膝枕してあげた時、惚けてたもの♪」
			PRINTFORMW 「あんたの性癖に付き合ってあげるんだから、感謝しなさいよ」
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;地の文は調教対象の下半身服装毎の分岐あり。必要に応じて拡張してください
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW 「あんたの%RAND_SPLIT("/ちんちん/おちんちん")%は、乗られると元気になるのよね」
			PRINTFORMW 「うふふっ♪押し潰されておちんちん嬉しそうね」
		;その他
		ELSE
			PRINTFORMW 「太腿で挟んであげるから、あんたは大人しくしてなさい」
			PRINTFORMW 「何押し込もうとしてるのよ。勝手に捻じ込んだら怒るわよ」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_2, ペニスバンド素股
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教対象がペニスバンド装着中は、ペニスバンド素股になる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;地の文は調教対象の下半身服装毎の分岐あり。必要に応じて拡張してください
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT17, パイズリする
;────────────────────────────────────
;・TFLAG:お仕置きフラグがたっていると発生しない
;・ターン方針が"ハード", "異常"の場合は発生しない
;・TEQUIP(オナホール/三角木馬)装着中には発生しない
;・調教対象にペニスがない場合は発生しない
;・調教者がオトコの場合は発生しない
;・現在ACT派生は存在しないが将来の改変に備え、ACT派生 == 0とする
;-------------------------------------------------
@KOJO_ACT_K1_17
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT17_0, パイズリする
;━━━━━━━━━━━━━━━━━━━━━━━━
	;ローションの使用を伴う
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;地の文は調教者の胸のサイズで分岐します。
	;素質を変更する事が可能なので、必要に応じて拡張するのも良いでしょう
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORML 「こんなのはどうかしら？」
		PRINTFORMW 「いつも私の胸、意識してたものね？」
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;=================================================
	;巨乳焦らし終了時パイズリフェラに派生 口が使えない状況での言動に注意
	;=================================================
	;地の文
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%のペニスから手を離した…
	;
	;%CALLNAME:TARGET%は豊満な胸からかろうじて顔をのぞかせた%CALLNAME:MASTER%の亀頭に
	;口づけし、射精の瞬間を待ち構えた…
	;口上ここ
	;
	;%CALLNAME:TARGET%は%CALLNAME:MASTER%の体にローションを塗った…
	;%CALLNAME:MASTER%の体というよりは自らの胸にな気がする
	
	IF 口上記入チェック(100) && TALENT:巨乳 && TCVAR:MASTER:焦らし度 == 1
		;効果音につき「」（）無し
		CALL FADEOUT(2, "あむっ")
		CALL FADEIN(4, "（だしちゃえ）")
		RETURN 1
	ENDIF
		
	;=================================================
	;調教対象がイキそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度によって分岐
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 「ほれほれ♪おっぱいで挟むの、気に入ったかしら？」
		PRINTFORM 「%RAND_SPLIT("ひぃひぃ言いながら堪えても/必死に我慢しても")%…おっぱいには勝てないわよ？んふふ
		CALL HEARTBW
		RETURN 1
	ENDIF
		[SKIPSTART]
		;---------------------------------------------------
		;地の文には着衣の状態による分岐が存在しますが、書き分けも面倒なので必要に応じてコメントアウトを外すなり
		;望む形に改変するなりして使用して下さい
		;---------------------------------------------------
		;干渉する衣服が存在する時
		IF TRUECHECK(TEQUIP:上半身下着, TEQUIP:上半身上着, TEQUIP:全身上着) > 0 || (TEQUIP:MASTER:下半身下着 && !TALENT:壁抜け) || CFLAG:衣装 == 3
			;露出状況により分岐
			SELECTCASE TEQUIP:着衣露出 & 6
				;干渉箇所全露出時
				CASE 6
					PRINTFORMW 
				;調教対象性器露出時
				CASE 4
					PRINTFORMW 
				;調教者胸露出時
				CASE 2
					PRINTFORMW 
				;干渉箇所非露出時
				CASEELSE
					PRINTFORMW 
			ENDSELECT
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		[SKIPEND]
		;他に分岐を設けないのなら、ここは書いておきましょう
		;分岐を設けるならここのPRINTは空行を生むので削除しましょう
		
	;=================================================
	;ここに着衣パイズリを起てよう;貧乳さらし分岐も必要になる
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		IF IS_CONDITION(TARGET, "情欲")
			PRINTFORM 「おっぱいで押し潰してあげるわ
			CALL HEARTBW
			PRINTFORMW 「ほれほれ、%CALLNAME:TARGET%のおっぱいまんこ\@ TCVAR:MASTER:イきそう ? でいっちゃいなさい # 気持ちいい？ \@」
		ELSE
			IF !RAND:2
				PRINTFORMW 「おっぱいの時間よ」
				PRINTFORMW 「んふふ…ほれほれ。おちんちん幸せに%RAND_SPLIT("なっちゃいなさい/してあげるわ")%」
			ELSE
				PRINTFORMW 「おっぱいで押し潰してあげるわ」
				PRINTFORM 「気持ちいい？挟んでてあげるから、動いてもいいわよ
				CALL HEARTBW
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT18, 足コキする
;────────────────────────────────────
;・足コキする
;・調教対象にペニスがない場合は発生しない
;・異常系から派生する場合もある
;・TFLAG:お仕置きフラグがたっていると発生しない
;・今日の方針が"イチャイチャ"だと発生しない
;・TEQUIP(オナホール/三角木馬)装着中は発生しない
;・性交中/性交奉仕中は発生しない
;・調教者に足が無いと発生しない
;・調教対象が女の子だと発生しない
;・現在ACT派生は存在しないが将来の改変に備え、ACT派生 == 0とする
;-------------------------------------------------
@KOJO_ACT_K1_18
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT18, 足コキする
;━━━━━━━━━━━━━━━━━━━━━━━━	
	;足コキは作者の趣味ではないようで、地の文は割りと手抜きだったり
	;逆に言えば、色々とやっても地の文との整合性が崩れる心配はあまりないので、好きなようにやると良いでしょう
;=================================================
;初回
;=================================================
IF 口上記入チェック(100) && FIRSTACT(0)
	PRINTFORMW 「足がいいのね？あんたは踏まれて喜ぶ変態だって、私の勘が言ってるのよ」
	PRINTFORMW 「情けない声上げちゃって…しょうがないんだから♪」
	;初回に限ることもなさそうな言動
	PRINTFORMW 「あんたの情けないおちんちん、私が世話してあげるわ♪」
	RETURN 1
ENDIF
	
	;地の文にTCVAR:MASTER:焦らし度等の分岐は存在しない箇所
	;=================================================
	;調教対象がイキそう 両足で%CALLNAME:MASTER%のペニスを荒々しくしごいてきた…
	;=================================================
IF 口上記入チェック(100) && !TFLAG:ACT派生
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		IF POLICY("今日") == "子作り"
			PRINTFORMW 「ほらほら…私の事%RAND_SPLIT("妊娠させ/孕ませ")%たいんでしょ？おまんこに中出ししたいんでしょ？」
			PRINTFORMW 「だったら足でなんて…いっちゃ駄目よね？んふふ…」
			PRINTFORM 「頑張りなさい
			CALL HEARTBW
			RETURN 1
		ENDIF
	ENDIF

;=================================================
	;連続実行 愉快そうに%CALLNAME:MASTER%を見下ろしながらペニスをぐりぐりと踏みつけている…
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT == TFLAG:前ACT
		;顔面騎乗中以外
		IF !TEQUIP:顔面騎乗 || !TEQUIP:顔面騎乗アナル
			IF TEQUIP:下半身上着
				PRINTFORML
				PRINTFORMDW %CALLNAME:TARGET%はスカートの裾をつまみあげた
				PRINTFORMW 「目がやらしいわよ？」
				PRINTFORMW 「あんたって%RAND_SPLIT("本当、/ほんと、/、うふふ…")%%RAND_SPLIT("助平/すけべ/えっち")%なんだから」
				PRINTFORMW 「ま…あんたに%RAND_SPLIT("女として/そういう目で/いやらしい目で")%見られるのは、%RAND_SPLIT("悪い気はし/嫌じゃ/満更でも")%ないわ」
				RETURN 1
			ENDIF
		ENDIF
	ENDIF
	
	;地の文に派生は設けてありませんが、これは割りと必要と思われるので
	;=================================================
	;靴下履いたまま足コキ(デフォルトで靴下履いていないキャラは表示されない)
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:靴下
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;調教者にTALENT:壁抜けがない場合、布越しになる
	;=================================================
	;下半身服装が邪魔
	;=================================================
	IF 口上記入チェック(0) && PANTSCHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		;冷ややかな視線が足りない
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「何踏まれて喜んでるのよ、この%RAND_SPLIT("駄目/馬鹿")%ちんちん」
			CASE 1
				PRINTFORMW 「んふふ…%RAND_SPLIT("えいえい/ぐりぐり/ほれほれ")%♪…んふふ♪」
			CASEELSE
				PRINTFORMW 「うふふ♪あんたが好きな事、一杯してあげるわ」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT19, 指挿入れ(ACT19_0, 指挿入れ/ACT19_1, Ｇスポット刺激/ACT19_2, バイブ出し入れ)
;────────────────────────────────────
;・調教対象がふたなり若しくはオンナの場合に発生
;・調教対象が処女の場合Ｇスポット刺激等に派生しない＆快Ｖにマイナス補正
;・バイブ装備中はバイブ出し入れに派生
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・TEQUIP(三角木馬/ペニスバンド)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教対象がオトコだと発生しない
;・調教対象がふたなりの場合、焦らし中には発生しない
;-------------------------------------------------
;※調教対象が処女ではない
;※連続実行の場合
;※イきそうor快Ｃが5000以上の場合、調教者の愛撫スキルに応じて
;※勃起度が低い場合、調教者の愛撫スキルに応じて
;※要強制勃起の場合
;・上記条件の場合Ｇスポット刺激に派生する
;-------------------------------------------------
@KOJO_ACT_K1_19
;-------------------------------------------------
;・TFLAG:ACT派生(0=指挿入れ/1=Ｇスポット刺激/2=バイブ出し入れ)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_0, 指挿入れ 膣口に指先をあてがい、そっと奥へ押し込んだ…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;顔面騎乗中
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;位置前後分岐は必要に応じて
	;何処かで統合してしまうのも良いと思います
	;=================================================
	;後ろから
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;前から
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_1, Ｇスポット刺激 指を進入させＧスポットを直接刺激した…
;━━━━━━━━━━━━━━━━━━━━━━━━
;イきそうor快Ｃが5000以上の場合、調教者の愛撫スキルに応じてＧスポット刺激
;Ｇスポット責めに派生すると前立腺刺激扱いになる
;-------------------------------------------------
;調教対象がふたなりの場合でも発生するので、必要に応じてPENIS(MASTER)なり!PENIS(MASTER)で分岐を設けると良いと思います
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;後ろから
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;前から
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_2, バイブ出し入れ Ｇスポットを抉るように激しく動かした…
;━━━━━━━━━━━━━━━━━━━━━━━━
;地の文自体に派生はありませんが、@TARGET_DESCRIPTIONでの状況描写が入る可能性があります
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT27, 押し倒す
;────────────────────────────────────
;・押し倒す/押し倒し終了
;・既に押し倒している場合、解放になる
;・TFLAG:お仕置きフラグがたっていると発生しない
;・今日の方針が"イチャイチャ"だと発生しない
;・TEQUIP:ペニスバンド装着中には発生しない
;・焦らし中には発生しない
;-------------------------------------------------
@KOJO_ACT_K1_27
;-------------------------------------------------
;TFLAG:ACT派生（=0,押し倒す/=1,押し倒し終了）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_0, 押し倒す %CALLNAME:MASTER%を押し倒した…
;━━━━━━━━━━━━━━━━━━━━━━━━
;地の文に派生はありませんが、@TARGET_DESCRIPTIONで状況描写が入る可能性があります
;-------------------------------------------------
;性交奉仕は発生しないがパイズリは発生する
;押し倒し搾精は発生しないあんたをイかせる事が多い大体手淫が来る
;-------------------------------------------------
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;-------------------------------------------------
	;状態分岐
	;-------------------------------------------------
	IF 口上記入チェック(100) && IS_CONDITION(TARGET, "情欲")
		PRINTFORMDW がばぁっ！
		;描写と呼吸を切り離さないよう空行のコメントアウト
		;PRINTFORML
		PRINTFORMW 「%RAND_SPLIT("ふーっ…ふーっ…/はーっ…はーっ…はーっ…")%」
		PRINTFORML
		PRINTFORMDW %CALLNAME:TARGET%の吐息は熱く情欲に染まっている
		RETURN 1
	ENDIF
	IF 口上記入チェック(100) && IS_CONDITION(TARGET, "怒り")
		PRINTFORMDL %CALLNAME:TARGET%は怒気を孕んだ目をしている
		RETURN 1
	ENDIF
	IF 口上記入チェック(100) && IS_CONDITION(TARGET, "退屈")
		PRINTFORMW 「眠くなって来たわ…あんた布団になりなさい」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;条件無し
	;-------------------------------------------------
	IF 口上記入チェック(100)
		SELECTCASE RAND:4
			CASE 0
				PRINTFORML 「これからどうされちゃうか、分かるわよね？」
				PRINTFORML 「んふふ、可愛がってあげるから覚悟しなさい」
			CASE 1
				;子猫を捕まえるかのように。フは４つ
				PRINTFORML 「んふふふふ…」
				PRINTFORMDL %CALLNAME:TARGET%は含み笑いをしながら%CALLNAME:MASTER%に覆い被さってきた
				;なによ抵抗するのこいつめ！
				PRINTFORML 「んー？」
				;子猫を捕まえたかのように。フは５つ
				PRINTFORML 「んふふふふふ♪」
			CASE 2
				CALL FADEIN(12, "「抵抗しなければ、手荒な真似は…」")
				CALL FADEOUT(8, "「いやまあその…」")
				PRINTFORMW 「とにかく、観念しなさい」
			CASE 3
				PRINTFORMW 「大人しくしなさい」
				PRINTFORML 「大人しくしてても、手荒な真似はするけど…」
				CALL FADEOUT(12, "「そこは、うん…」")
				CALL FADEIN(4, "「観念しなさい」")
			;死に分岐
			CASEELSE
				PRINTFORML 「これからどうされちゃうか、分かるわよね？んふふふふ…」
				PRINTFORMW 「わからないってとぼけても、許してあげないわよ」
				;%CALLNAME:TARGET%さんに徹底凌辱されるエロ同人下さい
				PRINTFORMW 「ら・ん・ぼ・う♪するのよ。エロ同人みたいに」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_1, 押し倒し終了		%CALLNAME:MASTER%を自由にした…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	PRINTFORMW 「はい。おしまい」
	RETURN 1
ENDIF
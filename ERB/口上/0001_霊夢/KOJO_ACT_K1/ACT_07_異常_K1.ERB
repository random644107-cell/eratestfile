;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 異常系アクション口上ファイル
;
;	異常系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;異常系 ACT70～75
;========================================================================
;・TFLAG:ACT分類 == 7に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "異常")で参照する事が可能
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------


;────────────────────────────────────
;ACT73, 放置プレイ
;────────────────────────────────────
;・調教者が調教対象を放置する
;・ターン方針が"ソフト", "ノーマル", "異常"だと発生しない
;・(異常系ACTでターン方針"異常"で弾いているのはミスと思われ)
;・今日の方針が"イチャイチャ"の場合は発生しない
;・押し倒し中には発生しない
;・性交中/性交奉仕中には発生しない
;・羞恥系から発生する場合もある(v1.010現在、羞恥系から派生した場合にのみ発生と思われ)
;・顔面騎乗系は解除される
;-------------------------------------------------
@KOJO_ACT_K1_73
;-------------------------------------------------
;・ACTの派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 放置プレイ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文に分岐なし
;-------------------------------------------------
IF 口上記入チェック(100)
	;%CALLNAME:TARGET%はただ%CALLNAME:MASTER%を放置してなにもしない…
	;=================================================
	;調教者自慰中
	;=================================================
	IF 口上記入チェック(0) && GETBIT(TEQUIP:自慰中, 1)
		;調教対象も自慰中
		IF GETBIT(TEQUIP:自慰中, 0)
			PRINTFORMW 
		;調教者のみ自慰中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象自慰中
	;=================================================
	IF 口上記入チェック(0) && GETBIT(TEQUIP:自慰中, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP系分岐はアイマスク以外にもバイブ、アナルバイブ、アナルビーズ等が考えられます
	;必要に応じて拡張してください
	;=================================================
	;アイマスク装着中
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:アイマスク
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT75, ペニスバンド装着
;────────────────────────────────────
;・調教者が双頭ペニスバンドを身に着け、調教対象に見せつける
;・調教者が処女である場合処女喪失になる
;・性交中/性交奉仕中には発生しない
;・調教者にペニスがある場合には発生しない
;・既に装着していた場合は解除となる
;・顔面騎乗系は解除される
;-------------------------------------------------
@KOJO_ACT_K1_75
;-------------------------------------------------
;・TFLAG:ACT派生(0=装着/1=解除)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_0, ペニスバンド装着
;━━━━━━━━━━━━━━━━━━━━━━━━
;・ペニスバンド装備中は快Ｃが快Ｖに変換
;・変換率…快Ｃを20％にし、残りの値にＶ感覚+1/Ｃ感覚+1を掛けて２で割って快Ｖに加算する
;・地の文はACT派生、及び処女であるか否かで分岐する
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;調教者の初期設定が処女でない場合は(基本的に)不要な箇所です
	;FIRSTACTのイベント番号を揃えて、処女以外の場合と重複しないようになっていますが
	;この箇所が必要でない場合は削除してしまった方が良いと思います
	;=================================================
	;初回「調教者が処女」
	;=================================================
	IF 口上記入チェック(0) && TALENT:TARGET:処女 && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;調教者が処女でない場合はここで初回判定をします
	;=================================================
	;初回「調教者が処女でない場合」
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_1, ペニスバンド解除
;━━━━━━━━━━━━━━━━━━━━━━━━
;・身に着けているペニスバンドを取り外す
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 性交奉仕系アクション口上ファイル
;
;	性交奉仕系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;性交奉仕系 ACT95～103
;========================================================================
;・TFLAG:ACT(95=正常位させる/96=後背位させる/97=対面座位させる/98=背面座位させる/99=アナルセックスさせる/103=逆レイプ)
;・TFLAG:ACT分類 == 10に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "性奉")で参照する事が可能
;・原則として調教対象→調教者の挿入となる
;・体位の連携によってTFLAG:ACT派生で派生する
;・各ACTに於ける主導権は@SEX_INITIATIVEにて定義される
;-------------------------------------------------
;※性交中、性交奉仕中からの派生について…
;　一部のACT派生はACT_VARIATIONで定義されつつも、ACT_ABLEで弾かれている現状です
;　ものによっては現在の仕様自体が怪しい気もしますが、この件についての本体側からの回答が得られていません
;　いずれ手が入る可能性もあるので、注釈をつけた上で枠は設けておきます
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT95, 正常位させる
;────────────────────────────────────
;・調教者が調教対象に正常位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・背面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_95にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_95
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT95, 正常位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(100) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			;ムードが足りないので再考
			;PRINTFORMW %CALLNAME:MASTER%がやり方が分からないと正直に話すと%CALLNAME:TARGET%はリボンをパタパタと羽ばたかせた
			;PRINTFORML
			;PRINTFORMW 「大丈夫、私もした事ないけれど…こう、真っ直ぐ」
			;PRINTFORMW 「ぐぐーっと行けば…なんとかなるわよ」
			;PRINTFORML 
			;PRINTFORMW 大雑把だった
			
			PRINTFORMW 「最初から上手くやろうと思わなくていいわ」
			PRINTFORMW 「思う侭にやれば、まずはそれでいいから」
			PRINTFORMW 「来て」
		[SKIPSTART]
		コメントアウトしておきます@/L
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(100) && TFLAG:抜かず
		PRINTFORMW 「まだ、足りない？いいわよ」
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが正常位させる)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		;SELECTCASE TCVAR:MASTER:焦らし度
		;	;焦らし中
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;焦らし終了
		;	CASE 1
		;		PRINTFORMW 
		;	;通常の連続実行
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		SIF TCVAR:MASTER:イきそう
			PRINTFORMW （%RAND_SPLIT("いきそうなおちんちん、出し入れされてる/射精する時の表情してる/奥でぐりぐりしてる…これ、膣内に射精するつもりね")%）
		PRINTFORMDW %CALLNAME:TARGET%は瞳を潤ませている
		;RETURNはさせず追加描写の形でいいかも
		;RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 5(背面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("対面座位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
		;体位特有の風味が出ていないので再考する
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORM 「まだ、だめ
				CALL HEARTBW
			;焦らし終了
			CASE 1
				SELECTCASE !RAND:2
					CASE 0
						PRINTFORM 「あんたの好きに、%RAND_SPLIT("/好きに")%していいわよ
						CALL HEARTBW
					CASEELSE
						;霊夢さんが中に出して！
						
						PRINTFORMW 「このまま%RAND_SPLIT("膣内に/")%、膣内に射精して」
				ENDSELECT
			;通常の連続実行
			CASEELSE
				PRINTFORMW 「前からの方が安心できるわね」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 5
		PRINTFORMW 「お尻よりも、こっちの方がいいわね」
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		PRINTFORMW 「いっちゃいそうなの我慢してるあんたも可愛いわよ」
		PRINTFORM 「でも、そろそろ限界そうね？んふふ
		CALL HEARTBW
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%が寝床に腰を下ろし、自らの陰唇を指で割り開く
				PRINTFORML 
				PRINTFORM 「来て
				CALL HEARTBW
				PRINTFORMW 「挿入れる前に射精しちゃったら怒るわよ？」
			CASE 1
				PRINTFORMW 「それじゃあ、おやすみなさい」
				PRINTFORML 
				PRINTFORMW 「冗談だからそんな泣きそうな顔しないでよ」
				PRINTFORMW 「%RAND_SPLIT("私だってしたいんだから/最後までしていいから")%、えっちしましょ」
			CASEELSE
				PRINTFORMW 「それじゃあ、おやすみなさい」
				PRINTFORML 
				PRINTFORMDW %CALLNAME:TARGET%が寝床に横になってしまう
				PRINTFORML 
				PRINTFORMW 「…あんた%RAND_SPLIT("へっぽこ/鈍感/奥手")%だから念のために言っておくけど、」
				CALL FADEIN(12, "「襲っていいわよ」")
			ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%が腕を%CALLNAME:MASTER%に伸ばす
		PRINTFORMW 「ほら、来なさいよ」
		PRINTFORMW 「私は横になってるから、おちんちんでしっかりとご奉仕しなさい」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT96,後背位させる
;────────────────────────────────────
;・調教者が調教対象に後背位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・対面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_96にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_96
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT96,後背位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(100) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
		PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%のお尻を掴み、縦筋に亀頭を押し当てた
		PRINTFORMW 「もうちょっと上…そこにぐーっと押し込んで」
		;言われてみたいがムードが足りない。後背位ならそれでいいのかもしれない
		;PRINTFORM 「あんたの童貞ちんぽで%CALLNAME:TARGET%の事、非処女にして
		CALL HEARTBW
		[SKIPSTART]
		コメントアウトしておきます@/L
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 「優しくしなさいよ」
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが後背位させる)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			;CASE IS > 1
			;	PRINTFORMW 
			;焦らし終了
			CASE 1
				;中は駄目って言ってるでしょちゃんと中に出しなさい
				PRINTFORMW 「だめっ、なかだめっ、だめっ、なかだしだめぇ」
			;通常の連続実行
			CASEELSE
			;	PRINTFORMW 
				RETURN 0
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 4(対面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("正常位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 「今度は後ろから、ね」
				PRINTFORMW 「私の気が済むまでは、射精しちゃ駄目だから」
			;焦らし終了
			CASE 1
				;野生と品性。下品さ直球さの塩梅がいまひとつ
				PRINTFORMW 「最後は後ろからね」
				PRINTFORMW 「種付け交尾で射精しちゃいなさい」
			;通常の連続実行
			CASEELSE
				;からしが気になる。あまりカタカナは使いたくない
				;PRINTFORMW 「今度は、後ろからして」
				PRINTFORMW 「今度は後ろから、ね」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;退屈状態が活かせていないので何か変化球。イメージプレイに走る。嫌がる恥ずかしがるに対応できるようなもの。
	;=================================================
	;%CALLNAME:TARGET%が退屈
	;=================================================
	IF 口上記入チェック(25) && IS_CONDITION(TARGET, "退屈")
		PRINTFORMW 「たまには趣向を変えてみようかしら」
		PRINTFORMW 「あんたがご主人様やりなさい」
		PRINTFORMW 「簡単よ。私の事好き放題犯してくれればいいわ」
		PRINTFORMW 「いつもと変わらない？うっさいわね」
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		PRINTFORML 「もう駄目って表情ね？んふふ♪」
		PRINTFORMW 「出したい？出したいわよね？おちんちん跳ねさせて精液びゅっびゅっしたいわよね？」
		PRINTFORML
		PRINTFORMW 「いいわよ」
		PRINTFORMW 「来て♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(100)
		IF !RAND:2
			PRINTFORMW 「\@ IS_CONDITION(TARGET, "情欲") ? こーび # 交尾 \@しましょ」
			PRINTFORMDW %CALLNAME:TARGET%の安産型のお尻が%CALLNAME:MASTER%の中の雄を誘う
		ELSE
			;後背秘貝開帳
			PRINTFORMW 「きて」
			PRINTFORMDW %CALLNAME:TARGET%は自ら秘肉を割り広げ、%CALLNAME:MASTER%を誘っている
		ENDIF
		
		;恥じらい不足、課題が残る
		PRINTFORMW 「%CALLNAME:TARGET%のおまんこにちんちん捩じ込んで」
		;12から短縮
		CALL FADEIN(6, "「た」")
		CALL FADEIN(6, "「ね」")
		CALL FADEIN(6, "「つ」")
		CALL FADEIN(6, "「け」")
		PRINTFORMW 「して♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT97, 対面座位させる
;────────────────────────────────────
;・調教者が調教対象に対面座位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・背面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_97にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_97
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT97, 対面座位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(100) && TFLAG:抜かず
		PRINTFORMW 「まだ、放してあげないんだから」
		[SKIPSTART]
		;地の文と被る為コメントアウト
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の体に腕を回し抱き着いてきた
		[SKIPEND]
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%を潤んだ瞳で見つめながら腰をくねらせている
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(対面座位させるから)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		;SELECTCASE TCVAR:MASTER:焦らし度
		;	;焦らし中
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;焦らし終了
		;	CASE 1
		;		PRINTFORMW 
		;	;通常の連続実行
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 2(後背位させる)/TEQUIP:性交奉仕中 == 5(背面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("正常位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;PRINTFORMW 「もう射精そう？あんたの事、一番近くで見ててあげるわ」
		PRINTFORMW 「もう射精そう？あんたがいっちゃう所、見ててあげるわ」
		PRINTFORML 「ほらこうやって、一番近くでね」
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%%RAND_SPLIT("にしっかりと抱き着いてきた/のおでこにおでこをくっつけてきた/に笑顔を見せてくれた")%
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(100)
		SELECTCASE RAND:2
			;%CALLNAME:MASTER%攻め
			CASE 0
				PRINTFORMW 「あんたの腕の中、あったかいわね」
				CALL FADEOUT(8, "「んふふ♪」")
				;PRINTFORML
				;この手の描写は初回限定が望ましい //こんな感じでどうでしょうか？ @/L //提案ありがとうございます@宿禰
				SIF FIRSTACT(80)
					PRINTFORMDW 横暴に振る舞う%CALLNAME:TARGET%を腕の中に抱いてみれば、その体はか細く、小さかった
			;%CALLNAME:TARGET%攻め
			CASEELSE
				;PRINTFORMW 「あったまりましょ」
				PRINTFORM 「ぬくぬくしましょ
				CALL HEARTBW
				CALL FADEOUT(8, "「うふふ♪」")
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%に抱き着いてきた
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT98,背面座位させる
;────────────────────────────────────
;・調教者が調教対象に背面座位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・正常位、対面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_98にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_98
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT98,背面座位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;処女・童貞
	;=================================================
	;IF 口上記入チェック(0) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
	;	;調教者が処女＆調教対象が童貞
	;	IF TALENT:処女 && TALENT:MASTER:童貞
	;		PRINTFORMW 
	;	;調教者が処女
	;	ELSEIF TALENT:処女
	;		PRINTFORMW 
	;	;調教対象が童貞
	;	ELSE
	;		PRINTFORMW 
	;	ENDIF
	;	RETURN 1
	;ENDIF
	
	;ソース入手数 体感発生率 共に高い優遇コマンド
	;ご主人様にとって好ましい状況を意識する 背中から寄りかかられ、かつ、挿入失敗の目がある
	
	;あなたへの好感度[1589]霊夢への好感度[1666]童貞処女にて踏み切る。
	;テスト時の状況は気力切れ恥ずかしがる一択。実行後あなたがイきそうなまま調教を終了。ひとまずこれをモデルにする。
	;かなり早期。相思相愛の前に発生すると考えた方が良い。
	;一線越えない霊夢に一線を越えさせる難題。
	;体位を活かしどちらが超えたか曖昧な境界線上の灰色を表現してみる。
	
	;ずかずかとマヨヒガに押し入る異変解決時の問答無用さは、あなたに拒否権が残っている為に使えそうにもない
	;処女性と強引さと抵抗の余地と選ばれるであろう恥ずかしがる
	;「『挿入しろ』とは言ったが『挿入しろ』とは言ってない。」は成立するだろうか？
	IF 口上記入チェック(100) && TALENT:処女
		;Ｃへの刺激の確保
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の膝の上に腰かけるとお尻で陰茎を押し潰し、%CALLNAME:MASTER%の表情を確かめながら腰をくねらせた
		CALL FADEOUT(8, "  ちゅく…")
		;ここで一捻りするとどうだろう。あなた主導を仄めかす。
		PRINTFORMDW %CALLNAME:MASTER%の陰茎は限界まで嵩を増し、
		PRINTFORMDW 限界まで膨らみ濡れた亀頭が%CALLNAME:TARGET%の濡れそぼった膣口を押し広げる
		CALL FADEOUT(8, "  …ごくっ")
		PRINTFORMW 「此処、ね…？」
		;小刻みに速めていく
		CALL FADEIN(6, "  ちゅく…")
		CALL FADEOUT(6, "  「んっ…」")
		;あなたの消極性を残しつつ胸に手を促す。
		PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:MASTER%の手を取り胸に抱くと、濡れそぼった膣口で亀頭の先が浅く咥え込んだままに
		PRINTFORMDW もじもじと腰を揺すり亀頭の先を捏ねた
		CALL FADEIN(4, "  ちゅく…")
		CALL FADEIN(4, "  ちゅくちゅっ…")
		CALL FADEOUT(6, "  「…んっんっ」")
		PRINTFORMW 「こうしてると、挿入っちゃいそうね」
		;合意を求めるは別にコマンドがある
		;結局霊夢が入れたのかあなたが入れたのか曖昧に
		;同意を求めつつ合意は取らず命じたようで命じていないようにも聞こえる一文字
		PRINTFORMW 「ね。」
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが背面座位させる)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		;SELECTCASE TCVAR:MASTER:焦らし度
		;	;焦らし中
		;	CASE IS > 1
		;		PRINTFORMW 
		;	;焦らし終了
		;	CASE 1
		;		PRINTFORMW 
		;	;通常の連続実行
		;	CASEELSE
		;		PRINTFORMW 
		;ENDSELECT
		;=================================================
		;退屈
		;=================================================
		IF 口上記入チェック(25) && IS_CONDITION(TARGET, "退屈")
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%%RAND_SPLIT("の胸にすっぽりと収まったまま/にもたれかかり")%寝息を立てている…
			RETURN 1
		ENDIF
		
		IF 口上記入チェック(100)
			PRINTFORMW 「この体勢だと、私からできる事ってあまりないのよね」
			;頂戴が硬い気がする
			;PRINTFORMW 「あんたが気持ちよくして頂戴」
			PRINTFORMW 「ね、気持ちよくして」
			RETURN 1
		ENDIF
	ENDIF
	
	;TEQUIP:性交奉仕中 == 1(正常位させる)/TEQUIP:性交奉仕中 == 4(対面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("後背位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
		[SKIPSTART]
		コメントアウトしておきます@/L
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
		ENDSELECT
		[SKIPEND]
		PRINTFORMW 「よいしょっと…腕、首に回して？」
		PRINTFORMW 「えへへ♪」
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		PRINTFORML 「あんたの事だから、座られるのが一番気持ちいいわよね？」
		PRINTFORMW 「挿れてあげるから、もうちょっとだけ%RAND_SPLIT("頑張り/辛抱し/我慢し")%なさい」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORML 「よいしょっ」
		PRINTFORMW 「あんたの座り心地、中々悪くないわよ」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT99, アナルセックスさせる
;────────────────────────────────────
;・調教者が調教対象にアヌスへの挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・ターン方針が"ソフト"でＡ性交奉仕中でない場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・Ｖ性交奉仕中からは派生しない
;・調教者のＡ感覚が2以下の場合は発生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;-------------------------------------------------
@KOJO_ACT_K1_99
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ａ性交系から/3=Ｖ性交奉仕系から/4=Ｖ性交系から)
;※v1.010現在、ACT派生2から4はACT_ABLEで規制されているので機能していない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT99, アナルセックスさせる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生1(連続)、ACT派生2 TO 5(Ａ性交/Ｖ性交奉仕/Ｖ性交)、その他で分岐
;・各派生内でペニスバンドの有無で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0)
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTがアナルセックスさせる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交奉仕系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT103, 逆レイプ
;────────────────────────────────────
;・調教者が騎乗位で強引に性交渉に及ぶ
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(TARGET)"
;-------------------------------------------------
;・他の性交奉仕系と異なり、調教対象は抵抗できない
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_103にて行われる
;-------------------------------------------------
@KOJO_ACT_K1_103
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;・v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;ACT103, 逆レイプ
;━━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(100) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 「あんたの初めて、貰うわよ」
			PRINTFORML 「代わりに私の初めてあげるんだから、いいわよね？」
		[SKIPSTART]
		コメントアウトしておきます@/L
		;調教者が処女
		ELSEIF TALENT:処女
			;%CALLNAME:MASTER%が初期状態で非童貞な場合もある
			PRINTFORMW 「私の初めて、あんたが受け取りなさい」
			PRINTFORMW 「私は、あんたが良いのよ」
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		[SKIPEND]
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	;射精してから抜かずに続けた場合、体位変更時にここに来る
	IF 口上記入チェック(100) && TFLAG:抜かず
		PRINTFORMW 「私がしてあげるから、あんたは楽にしてなさい」
		PRINTFORML 「私が満足するまでは、付き合ってもらうわよ」
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが逆レイプ)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
		;=================================================
		;ハードにしてに応じる追加描写
		;=================================================
		IF 口上記入チェック(100) && IS_PREVCOMNAME("もっとハードにして")
			PRINTFORM 「お望み通り、激しくしてあげる
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の両肩を抑え込みながら激しく腰を打ち付けてきた
			IF EXP:TARGET:Ｖ経験 < 10
				CALL FADEOUT(4, "「んんっ！！」")
				PRINTFORMDL （まだ、痛むわね…）
			ENDIF
		ENDIF
		;=================================================
		;焦らし度による派生
		;=================================================
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				IF TFLAG:お仕置きフラグ
					PRINTFORMW 「まだ射精しちゃ駄目よ？これはお仕置きなんだから」
					PRINTFORMW 「いっぱい犯してあげるんだから簡単に終わらせないで」
				ELSE
					IF IS_CONDITION(TARGET, "情欲")
						PRINTFORML 「まだおあずけなんだから」
						PRINTFORMDW %CALLNAME:TARGET%は楽しそうに微笑んだ
						PRINTFORM 「ほら、頑張れ頑張れ
						CALL HEARTBW
					ENDIF
						PRINTFORMW 「まだ射精しちゃ駄目よ」
						PRINTFORML 「私より先にいったら…追いかけてやるんだから…」
				ENDIF
			;焦らし終了
			CASE 1
				IF PALAM:快Ｖ > 8500
					PRINTFORMW 「私もそろそろ…いいわよ」
					PRINTFORML 「中に出して」
				ELSE
					PRINTFORMW 「もうらめ？むりやりらめ？ぐっぽぐっぽらめぇ？」
					CALL FADEOUT(4, "「くすっ」")
					PRINTFORM 「%RAND_SPLIT("いいから/駄目じゃないから")%私の膣内で、射精しちゃいなさい
					CALL HEARTB
				ENDIF
			;通常の連続実行
			CASEELSE
				IF 口上記入チェック(100) && PALAM:快Ｖ > 8500 && !TCVAR:MASTER:イきそう
					PRINTFORMW 「だめ…わたし…だめ…」
					PRINTFORMDW %CALLNAME:TARGET%は絶頂寸前で堪えながらゆっくりと腰を動かしている
				ENDIF
				IF 口上記入チェック(100) && IS_CONDITION(MASTER, "情欲")
					PRINTFORMW 「無理矢理えっち気持ちい？気持ちいいんだ？」
					PRINTFORML 「ふーん」
					PRINTFORMDL %CALLNAME:TARGET%は情欲にとろける%CALLNAME:MASTER%を満足げに見下している
				ENDIF
				IF 口上記入チェック(100)
					PRINTFORMW 「私の下で喘ぐあんたを見てると、嬉しくなるわ」
					PRINTFORM 「もっとしてあげる
					CALL HEARTBW
				ENDIF
		ENDSELECT
		RETURN 1
	ENDIF
	;どの体位からも派生する可能性あり
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				;PRINTFORMW 
				RETURN 0
			;焦らし終了
			;CASE 1
				;PRINTFORMW 
				;RETURN 0
			;焦らしなし
			CASEELSE
				;シューティング要素を織り込むのは無理があるのかもしれない
				PRINTFORML 「準備はいいわよ。あんたの%RAND_SPLIT("子種/せーし/ちんちんぢる")%は、しっかりと上部回収してあげるわ」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;=================================================
		;妊娠中 孕ませについて言及しない ひとまず条件なしに送る
		;=================================================
		IF 口上記入チェック(0) && TALENT:妊娠
			[SKIPSTART]
			;PRINTFORMW 「どうせ射精すなら、ここがいいわよね？」
			;矛盾の可能性有コメントアウト
			;PRINTFORMW 「お望み通り挿入れてあげるから、もう少しだけがんばりなさい」
			[SKIPEND]
			RETURN 1
		ENDIF
		;=================================================
		;霊夢怒りの中出させ
		;=================================================
		IF 口上記入チェック(100) && IS_CONDITION(TARGET, "怒り")
			PRINTFORML 「いいからとっとと射精しなさい！」
			PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:MASTER%の肩を押さえつけながら腰を打ち付けてきた
			RETURN 1
		ENDIF
		;=================================================
		;調教対象がイきそう妊娠中でない
		;=================================================
		IF 口上記入チェック(100) && !TALENT:妊娠
			;記入箇所ミスの予感
			;♪は無い方が冷淡さが出せる
			PRINTFORMW 「あんたのこと%RAND_SPLIT("妊娠するまで/孕むまで/あかちゃん出来ちゃうまで/何度も")%何度でも、犯してあげるわ」
			PRINTFORM 「うふふ
			CALL HEARTBW
			PRINTFORMW 「%RAND_SPLIT("あんたのおちんちんは、私/旦那様のおちんちんは、お嫁さん")%を孕ませる為にあるんだものね？」
			;夫婦臭を強調する為あなた表記
			PRINTFORMW 「%RAND_SPLIT("覚悟を決めなさい？あ・な・た/♪覚悟を決めなさい？うふふ…/あんたの事だから、嬉しいでしょ？ほれほれ♪")%」
			RETURN 1
		ENDIF
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%怒り
	;=================================================
	IF 口上記入チェック(100) && IS_CONDITION(TARGET, "怒り")
		PRINTFORMW 「いいからとっととヤらせなさい！」
	ENDIF
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(100)
		;おっぱい
		IF TALENT:巨乳 && !RAND:3
			PRINTFORMDW ローアングルから見上げる揺れる乳房から、%CALLNAME:MASTER%は目を逸らすことができない
		;言いたい事は通じるが落ち着きのある推敲を要する
		;ELSEIF !TALENT:貧乳 && !RAND:3
		;	PRINTFORMDW %CALLNAME:TARGET%の下乳が下乳がおっぱいでおっぱいで%CALLNAME:MASTER%いっぱいいっぱいだ
		;リボン
		ELSEIF !RAND:2
			PRINTFORML 「あんたの子種、全部搾り取ってあげるわ」
			PRINTFORMW 「濃いのいっぱい射精さないと…%RAND_SPLIT("承知しないんだから/堪忍しないわよ？")%」
			PRINTFORML
			PRINTFORMDW %CALLNAME:TARGET%がリボンをパタパタと羽ばたかせる
		;黒髪の強調 真面目さ本気さ真剣さを出す
		ELSE
			PRINTFORMW 「搾り取ってあげるから、覚悟して」
			PRINTFORMDW %CALLNAME:TARGET%は麗しい黒髪を垂らしながら、%CALLNAME:MASTER%を真剣な眼差しで見下している
		ENDIF
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;未定稿、没鎬
	;=================================================
		;アライメント正
		IF ALI(0)
		;アライメント負
		ELSE
			;関係悪化は控えめに
			;PRINTFORMW 「すぐには終わらせないけど、%CALLNAME:MASTER%は天井のシミでも数えてなさい」
			
			;IF TALENT:MASTER:掃除の違いがわかる
			;	PRINTFORMDL その言葉に、素直に天井を観察すると
			;	PRINTFORMDL 怠りがちな天井の掃除もよく行き届いていた…
			;ENDIF
			
			;IF TALENT:MASTER:おしりしゅごい
			;	PRINTFORMDL その言葉に逆らい視線を横に向けると枕元のローションとティッシュが目に入った
			;	PRINTFORMDL %CALLNAME:MASTER%のお尻はキュッとなった
			;ENDIF
			
			;薄い本を持ち出すのは無し
			;PRINTFORML 「これでも読んでなさい」
			;PRINTFORML 
			;PRINTFORMDL 霊夢から渡された薄い本には霊夢との甘い生活が描かれている
			;PRINTFORMDL 霊夢さん。こういうの、すきなの？
			
			;霊夢さんち広いよ
			;PRINTFORMDL 四畳半の空の下、%CALLNAME:TARGET%に犯され\@ TIME ? ずぶずぶと夜の夢に沈む # 襖の隙間から細く広がる空を望む \@
		ENDIF
	;=================================================
	;挿入時に調教対象がイきそうであるという状況。条件が特定できずコメントアウト
	;=================================================
	PRINTFORML 「もう射精そう？挿れてあげるからもうちょっとだけ%RAND_SPLIT("頑張り/辛抱し/我慢し")%なさい」
	PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%%RAND_SPLIT("にしっかりと抱き着いてきた/のおでこにおでこをくっつけてきた/に笑顔を見せてくれた")%
	PRINTFORMDW %CALLNAME:TARGET%に見惚れているうちに%CALLNAME:MASTER%の一物は%CALLNAME:TARGET%の膣に飲み込まれていった
	;こちらのPRINTFORMLは残した方が挿入中と挿入完了のタイムラグが出てくれるかも
	PRINTFORML 
	PRINTFORMW 「全部挿入った…ちゃんと我慢できたわね」
	PRINTFORMW 「偉いわよ」
	RETURN 1
	
	PRINTFORMDW 今にも射精してしまいそうな陰茎を%CALLNAME:TARGET%の女陰がゆっくりと飲み込んでいく
	PRINTFORML
	PRINTFORM 「ん…%RAND_SPLIT("ぜんぶ/一番奥まで/あんたのおちんちん")%挿入っちゃった
	CALL HEARTBW
	RETURN 1
	;=================================================
	;挿入即射精 %CALLNAME:TARGET%が情欲でエロエロ
	;=================================================
	IF 口上記入チェック(100) && IS_CONDITION(TARGET, "情欲") && TCVAR:MASTER:イきそう
		PRINTFORMDW %CALLNAME:TARGET%は足をM字に開き、%CALLNAME:MASTER%の陰茎を膣口に当て、舌なめずりした
		PRINTFORMW 「もう射精していいわよ」
		PRINTFORMW 「挿入れてくとこ見ながら、いっちゃいなさい」
		PRINTFORMDW %CALLNAME:TARGET%はゆっくりと腰を降ろしていった
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%が情欲 性交奉仕同時絶頂
	;=================================================
	IF 口上記入チェック(100) && IS_CONDITION(TARGET, "情欲") && PALAM:快Ｖ > 8500
		PRINTFORMW 「注ぎ込んで！中出しで%CALLNAME:TARGET%のおまんこ」
		PRINTFORMW 「いかせて！」
	ENDIF
	;=================================================
	;REACTとの調整が必要
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の陰茎を最奥まで咥え込むと嬉しそうに下腹部を撫でた
		PRINTFORMW 「私の膣内にあんたの%RAND_SPLIT("あんたが/が/、")%入ってる」
	ENDIF

	;=================================================
	;体位変更逆レイプ
	;=================================================
	IF 口上記入チェック(100)
		;PRINTFORMW 「さっきより硬いのは気の所為かしらね？んふふ」
		;PRINTFORML 「あんたって、%RAND_SPLIT("ほんと、/")%受けよね」
	ENDIF
		
	IF 口上記入チェック(100)
		;PRINTFORMW 「お腹の中で大きくなってきた。射精しそうなのね」
	ENDIF
	[SKIPEND]
ENDIF

;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 ショップイベント口上ファイル
;
;	ショップから呼び出される口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;//暫定版@/L(2015/01/15)

;========================================================================
;ショップイベント口上
;========================================================================

;────────────────────────────────────
;EV101, 衣装変更
;────────────────────────────────────
;・ショップから衣装変更を行った場合に表示
;-------------------------------------------------
@KOJO_EVENT_K1_101(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=
;　一の位…（0, もう着てる、1, 着替えた、2, 恥ずかしくて着られない、3, 嫌がって着ない、9, 持ってない）
;　十の位…（0, 通常、10, 半裸、20, 全裸、30, 下着無し）
;　百の位…コスプレのために保留
;　千の位…（0, 調教者, 1000, 調教対象）
;　例（調教対象が恥ずかしがって全裸にできなかった＝1022）
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が着替えた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;通常になった
		CASE 1
			PRINTFORML 「これが一番私らしいかしらね？」
			PRINTFORML
			RETURN 1
		;半裸になった
		CASE 11
			;初回
			PRINTFORML 「もっと、大人っぽい下着の方が好き？」
			PRINTFORML
			;PRINTFORMW 「下脱ぎが良い？あんたも遠慮が無くなってきたわね」
			;PRINTFORMW 「いい傾向だと思うけど、駄目。下着が無いって、恥ずかしいんだから」
			RETURN 1
		;全裸になった
		CASE 21
			PRINTFORML 「この方が話が早いわね」
			PRINTFORMDL %CALLNAME:TARGET%はさりげなく両腕で胸と秘所を隠している
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 2
	SELECTCASE ARG
		;半裸だって恥ずかしい
		CASE 12
			PRINTFORML （下着、新調した方がいいかしら…）
			PRINTFORML
			RETURN 1
		;普段から全裸はちょっと……
		CASE 22
			PRINTFORML 「ああもう、このすけべは…」
			CALL FADEOUT(12, "もにょもにょ…")
			PRINTFORMDL %CALLNAME:MASTER%の視線を気にしてか、%CALLNAME:TARGET%は着替えを止めてしまった
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG < 1000 && (ARG % 10) == 3
	SELECTCASE ARG
		;半裸は嫌
		CASE 13
			PRINTFORML 
			PRINTFORML 
			RETURN 1
		;普段から全裸なんて嫌
		CASE 23
			PRINTFORML 
			PRINTFORML 
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が着替えるのをやめた
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%の衣装はすでに[　　]に設定されています
;------------------------------------------------
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 0
	SELECTCASE ARG
		;通常のまま
		CASE 0
			;少女推敲中
			PRINTFORML 「ああもう、このすけべは…」
			CALL FADEIN(8, "もにょもにょ……")
			PRINTFORMDL %CALLNAME:MASTER%の視線を気にしてか、%CALLNAME:TARGET%は着替えを止めてしまった
			PRINTFORML
			RETURN 1
		;半裸のまま
		CASE 10
			PRINTFORML 「もう着ていいかしら？」
			PRINTFORML
			RETURN 1
		;全裸のまま
		CASE 20
			PRINTFORML 「寒いんだから、はやく暖めなさいよ」
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象を着替えさせた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;通常になった
		CASE 1001
			PRINTFORML 「これが一番あんたらしいわね」
			;着衣プレイ好きの場合
			IF TALENT:着衣プレイ好き
				CALL FADEIN(12, "「その、」")
				CALL FADEOUT(12, "「似合ってるわよ」")
				PRINTFORMDL %CALLNAME:TARGET%の頬が赤い…
				PRINTFORMDL %CALLNAME:TARGET%は普段着姿の%CALLNAME:MASTER%にも欲情しているようだ…
				PRINTFORML
			ENDIF
			RETURN 1
		;半裸になった
		CASE 1011
			;全裸→半裸に対応できない
			;PRINTFORML 「ほら、脱ぎなさいよ」
			[SKIPSTART]
			;着衣から半裸へ　条件設定が未だ
			PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:MASTER%の衣服をひんむこうとしたが、洋服に慣れない%CALLNAME:TARGET%は勢い余って下着ごとずり下げてしまった…
			CALL FADEIN(4, "「あっ…」")
			CALL FADEOUT(4, "「ごっ…」")
			PRINTFORMW 「…ごめんなさい」
			PRINTFORML 
			PRINTFORMDL %CALLNAME:TARGET%は目を丸くした後、大人しく%CALLNAME:MASTER%の下着を元に戻した…
			PRINTFORMDL 強い立場にいたとしても素直に謝る事の出来る%CALLNAME:TARGET%に%CALLNAME:MASTER%は好印象を持った…
			PRINTFORML
			[SKIPEND]
			RETURN 1
		;全裸になった
		CASE 1021
			PRINTFORMW 「こうして改めてみると…」
			PRINTFORML （いいカラダしてるわよね）
			PRINTFORML 
			PRINTFORMDL %CALLNAME:TARGET%は露わになった%CALLNAME:MASTER%の腕にときめいている…
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG >= 1000 && (ARG & 10) == 2
	SELECTCASE ARG
		;半裸だって恥ずかしい
		CASE 1012
			;知らなかったのか？ワープ巫女からは逃げられない。が、無理にはひん剥かない。
			PRINTFORML 脱がせようとする%CALLNAME:TARGET%の手に%CALLNAME:MASTER%が後ずさりすると%CALLNAME:TARGET%との追いかけっこが始まった…
			PRINTFORML 何処まで逃げても目の前に現れる%CALLNAME:TARGET%に%CALLNAME:MASTER%の心は今にも折れそうだ…
			PRINTFORML
			RETURN 1
		;普段から全裸なんて恥ずかしい
		CASE 1022
			PRINTFORML 
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG >= 1000 && (ARG & 10) == 3
	SELECTCASE ARG
		;半裸は嫌
		CASE 1013
			PRINTFORML 
			PRINTFORML 
			RETURN 1
		;普段から全裸なんて嫌
		CASE 1023
			PRINTFORML 
			PRINTFORML 
			RETURN 1
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象を着替えさせるのをやめた FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%の衣装はすでに[　　]に設定されています
;------------------------------------------------
IF 口上記入チェック(0) && ARG >= 1000 && (ARG % 10) == 0
	SELECTCASE ARG
		;通常のまま
		CASE 1000
			IF TALENT:着衣プレイ好き
				;性交判定に不利なため見送り
				;CALL FADEIN(8, "こくこく")
				;PRINTFORMDW %CALLNAME:TARGET%は何故か首を縦に振った…
				PRINTFORML
				PRINTFORML
			ENDIF
			RETURN 1
		;半裸のまま
		CASE 1010
			PRINTFORML 
			PRINTFORML 
			RETURN 1
		;全裸のまま
		CASE 1020
			PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:MASTER%の肌を
			CALL FADEOUT(4, "ちらちら")
			PRINTFORMDL と観察している
			PRINTFORML
			RETURN 1
	ENDSELECT
ENDIF

;────────────────────────────────────
;EV102, 調教者変更
;────────────────────────────────────
;・調教者を交代させたときに表示
;-------------------------------------------------
@KOJO_EVENT_K1_102(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 口上主が他のキャラに任せた、1, 口上主が調教者になった)
;旧調教者は「%CALLNAME:SHOP_OLDTARGET%」、新調教者は「%CALLNAME:SHOP_NEWTARGET%」で参照できる
;口上主はTARGETで参照
;-------------------------------------------------
;・反抗刻印は調教者毎に保存されるので、MARK:MASTER:反抗刻印などとしてしまうと0にしかならないので注意
;・これは屈服刻印も同様です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;他の調教者に交代
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 0
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORML 「そう。それじゃあ…」
		PRINTFORML 「私はここに居るから」
		;お茶に口をつけるまでの間を空ける為に遅め
		CALL FADEOUT(8, "…ずぞぞっ")
		PRINTFORML
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまが調教者になった
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORML 「んふふ♪」
		PRINTFORMW 「やっぱり%CALLNAME:TARGET%ちゃんが一番可愛い？」
		PRINTFORMDL %CALLNAME:TARGET%は機嫌良さそうだ…その気質は宙に浮かれている…
		PRINTFORML
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;EV110, 魔力使用
;────────────────────────────────────
;・魔力を消費して何らかの特典を購入したときに表示
;-------------------------------------------------
@KOJO_EVENT_K1_110(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 魔力使用前、1, 調教技巧、2, 知識を得る前、3, 調合知識取得、4, 魅惑取得、5, 禁断の知識取得、6, 道具使い取得、
;	7, 技術を得る前、8, 舌使い取得、9, 器用な指先取得、10, 縛り上手、11, サド消去/サド取得、12, 汚れ無視取得、 38, 着衣プレイ好き、39, 腰使い)
;イベント口上：アイテム
;引数内訳…
;ARG=(13, アイテム練成前、14, ローター練成、15,バイブ練成、16, アナルビーズ練成、 17, ペニスバンド練成、 18, アナルバイブ練成
; 19, クリキャップ練成、 20, ニプルキャップ練成、21, オナホール練成)
;イベント口上：責め具錬成
;引数内訳…
;ARG=(22, 責め具練成前、23, アイマスク練成、24, 鞭練成、 25, 縄練成、 26, ボールギャグ練成、27,  針練成、28, 浣腸セット練成
; 29, 三角木馬練成、 30, 大鏡練成、 31, 首輪練成、 32, 道具をカスタマイズした)
;その他イベント
;引数内訳…
;ARG=(33, ふたなり化/戻す、34, 巨乳化/戻す、35, 貧乳化/戻す、36, 精力の限界値上昇)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま魔力使用前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 0
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(0)
		PRINTFORML 「ええと、お米券…お米券は…」
		PRINTFORMDL 数分後、四つん這いになってうなだれる%CALLNAME:TARGET%の姿があった
		PRINTFORML
		RETURN 1
	ENDIF
	RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまの調教技巧がアップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま知識を得る前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 2
	PRINTFORMW 
	RETURN 1
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;魅惑の%CALLNAME:TARGET%さま
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 4
	[SKIPSTART]
	;=================================================
	;あなたがサドの場合
	;=================================================
	;巫女さんに日本刀
	;サドあなたをお誘いしてみる霊夢さん
	;アクセサリを見せびらかす女性でもない
	;おもちゃを自慢する子供でもない
	;しかしらしさがいまひとつ出せない
	;あざとさが強く自然体な素直さとミスマッチ
	IF TALENT:MASTER:サド
		;PRINTFORMDL %CALLNAME:TARGET%は手に日本刀を握りしめている
		;PRINTFORMDL どこから調達したのかと聞くと廃刀令の影響で日本刀の値崩れは激しいとの説明を受けた
		PRINTFORMDL %CALLNAME:TARGET%が後ろ手に刀を握りながら%CALLNAME:MASTER%の顔をのぞきこんできた
		PRINTFORMW 「どう？その、犯したくなってくるかしら」
		PRINTFORMDL 
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の嗜虐心を刺激しようと試みたようだが%CALLNAME:MASTER%にそんなものはない…
		PRINTFORMDL 
		;企ててはコケる茨霊夢さん可愛い
		PRINTFORMDL 四つん這いで項垂れる%CALLNAME:TARGET%に魅了された%CALLNAME:MASTER%は、心ここにあらずといった表情で%CALLNAME:TARGET%をなでなでした
		PRINTFORMDL 空振りした真心はホーミングがかかり、不自然な角度からではあるものの
		PRINTFORMDL %CALLNAME:TARGET%の魅力は確かに伝わった
		PRINTFORML 
		PRINTFORMDL %CALLNAME:TARGET%は一時的に[魅惑]を得た
		PRINTFORML
		RETURN 1
	ENDIF
	[SKIPEND]
	;=================================================
	;あなたがマゾの場合
	;=================================================
	;猫巫女霊夢さんと虐待されるマゾあなた
	IF TALENT:MASTER:ドＭ
		PRINTFORML 「にゃーん」
		PRINTFORMDW %CALLNAME:TARGET%は真っ赤になりプルプルしながら猫の真似をした
		PRINTFORML 
		PRINTFORMDL ………
		PRINTFORMW ………
		PRINTFORML 
		PRINTFORMW 「…にゃ、にゃーん」
		PRINTFORML 
		PRINTFORMDL ………
		PRINTFORML ………
		PRINTFORMDW ………
		PRINTFORML 
		PRINTFORMDL 沈黙に耐えきれなくなった%CALLNAME:TARGET%は高速移動で逃げていった
		PRINTFORMDL 猫に虐待される生活を想い、%CALLNAME:MASTER%の被虐心は静かに燃え盛った…
		RETURN 1
	ENDIF
	
		[SKIPSTART]
		;霊夢が着衣中
		;衣服供給源をアリスにするか紫にするか悩ましい所 保護者の子作り奨励が欲しいので紫に断定
		PRINTFORMDW 紅白の着物に身を包んだ%CALLNAME:TARGET%が蝶の様に袖をはためかせる…
		PRINTFORML 「紫に貰ったの。んふふ♪たまにはこういうのもいいでしょ？」
		PRINTFORMW 「どう？似合うかしら」
		PRINTFORMW %CALLNAME:TARGET%に魅了されている自覚に%CALLNAME:MASTER%の頬は熱を帯びていった…
		
		;ツリ目霊夢。ゆかりんの真似
		PRINTFORML 「あんまりべたべたしてると飽きられちゃうかしら」
		PRINTFORML 「んー。こんな感じかしら？」
		PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:TARGET%を惚れ直させようと、鏡の前で悪戦苦闘している…
		[SKIPEND]
	
	;=================================================
	;それ以外の場合
	;=================================================
	IF 口上記入チェック(100)
		;描写不足につき加筆
		PRINTFORMDL 洗面所に入ると湯上り姿の%CALLNAME:TARGET%と直面した
		PRINTFORML 
		PRINTFORMW ほこほこ
		PRINTFORML 
		PRINTFORML 「お風呂、先に頂いたわよ」
		PRINTFORMW 「おふとん%RAND_SPLIT("、あっためておくから/でまってるから")%」
		PRINTFORMDL %CALLNAME:TARGET%の%RAND_SPLIT("言葉に/濡れた髪に/下ろした髪に/上気した頬に/残り湯に/うなじに/使用済みの手拭いに")%%CALLNAME:MASTER%の胸は高鳴った…
		;PRINTFORML 
		;PRINTFORMDL %CALLNAME:TARGET%は一時的に[魅惑]を得た
	ENDIF
	PRINTFORML
	PRINTFORMD なんてことのない日常のひとコマに%CALLNAME:TARGET%を意識してしまい%CALLNAME:MASTER%は湯船の中にずぶずぶと沈んでいく
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま素質を取得
;━━━━━━━━━━━━━━━━━━━━━━━━
;・必要に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 口上記入チェック(100) && (INRANGE(ARG, 3, 12) || GROUPMATCH(ARG, 38, 39))
	SELECTCASE ARG
		[SKIPSTART]
		腰使いは39です
		ちなみにこの部分の処理はBONUS_GAIN.ERB内にあります@/L
		[SKIPEND]
		;腰使い
		CASE 39
			PRINTFORMDW %CALLNAME:TARGET%は徐に%CALLNAME:MASTER%を蹴り倒すと、%CALLNAME:MASTER%に跨り腰使いの練習を始めた
			;地の分で十分
			;PRINTFORMDL %CALLNAME:TARGET%は[腰使い]を取得した
			;PRINTFORML
			RETURN 1
		;腰使い以外の場合はRETURN 0しておきます@/L
		CASEELSE
			RETURN 0
	ENDSELECT
	;RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまアイテム錬成前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 13
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまアイテム練成
;━━━━━━━━━━━━━━━━━━━━━━━━
;・必要に応じて個別の条件に切り分けて下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && INRANGE(ARG, 14, 21)
	SELECTCASE ARG
		;オナホール
		CASE 21
			PRINTFORML 「これはちくわ……これはちくわ……」
			PRINTFORMDL 何やら不思議な呪文が聞こえてきた
			PRINTFORMDL しかしながら、悲しい事に%CALLNAME:TARGET%の直感は無情にも「食べられません」と告げている…
			PRINTFORML
			RETURN 1
		;オナホール以外の場合はRETURN 0しておきます@/L
		CASEELSE
			RETURN 0
	ENDSELECT
	;RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま責め具練成前
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 22
	PRINTFORMW 
	PRINTFORML
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま責め具錬成
;━━━━━━━━━━━━━━━━━━━━━━━━
;・必要に応じて個別の条件に切り分けて下さい
;------------------------------------------------
IF 口上記入チェック(100) && INRANGE(ARG, 23, 31)
	SELECTCASE ARG
		;アイマスク
		;CASE 23
		;鞭
		;CASE 24
		;縄　%CALLNAME:TARGET%の場合鬼縛陣　専用地の文持ち
		;%CALLNAME:TARGET%さんが良い子過ぎてSM系が埋まらない。いっその事SMグッズを自前で用意させるのはどうだろう。
		;鞭=紙垂 縄目隠し=札 木馬=賽銭箱 針=信仰針 口枷=ドロワ オナホ=袖、髪飾り
		CASE 25
			PRINTFORMDW %CALLNAME:TARGET%はせっせとお札を作っている…
			PRINTFORMDL …嫌な予感しかしない
			PRINTFORML
			RETURN 1
		;ボールギャグ
		;CASE 26
		;針
		;CASE 27
		;大鏡
		CASE 30
			PRINTFORML 「ふぉーゆーっ！」
			PRINTFORMDW 何やら%CALLNAME:TARGET%が鏡の前で構えを取っている
			PRINTFORMW 「ああこれ？昔ね。流行したのよ」
			PRINTFORML
			RETURN 1
		;首輪
		;犬でも猫でも%CALLNAME:MASTER%でも、首輪は初手で着けてしまうのがジャスティス
		;CASE 31
		;条件にヒットしなかった場合はRETURN 0を返しておきます@/L
		CASEELSE
			RETURN 0
	ENDSELECT
	;RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま道具をカスタマイズ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 32
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さまふたなり化orふたなり消去
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 33
	;=================================================
	;ふたなり化
	;=================================================
	IF 口上記入チェック(100) && TALENT:TARGET:ふたなり
		PRINTFORMW 「あんたね…こんなもの付けさせた以上は、私の代わりに…」
		;男あなたでも同一のリアクションむしろ男あなた想定
		CALL FADEIN(12, "「産んで貰うわよ？」")
		PRINTFORMDL %CALLNAME:TARGET%の目は真剣そのものだ…
		PRINTFORML
		RETURN 1
	ENDIF
	
	;=================================================
	;ふたなり消去
	;=================================================
	IF 口上記入チェック(100) && !TALENT:TARGET:ふたなり
		PRINTFORMW 「それでいいのよ。やっぱりあんたは搾り取ってあげないと、気が済まないもの」
		PRINTFORML 「うふ♪…たっぷりと可愛がってあげるから、覚悟しなさい♪」
		PRINTFORML
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま巨乳化or通常に戻す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 34
	;=================================================
	;巨乳化
	;=================================================
	IF 口上記入チェック(100) && TALENT:TARGET:巨乳
		[SKIPSTART]
		FIRSTEVENT等のイベント番号は同じイベント内"@KOJO_EVENT_K1_110(ARG)"では分ける必要があります
		例えばこの箇所の場合、%CALLNAME:TARGET%さま魔力使用前でFIRSTEVENT(0)が立ってしまうので、(0)では決して真になることはありません
		とりあえずARGの番号を割り振っておきます@/L
		[SKIPEND]
		;初回
		IF FIRSTEVENT(34)
			PRINTFORML 「この大きさなら…」
			PRINTFORMDW %CALLNAME:TARGET%がグッと握りこぶしを作る
			PRINTFORML
			PRINTFORMDW …しばらくして、四つん這いになってうなだれる%CALLNAME:TARGET%の姿があった
			PRINTFORML 「紫には勝てなかったわ…」
			PRINTFORML
			;初回時のみRETURNするようにしておきます。初回時以外は0が返ります@/L
			RETURN 1
		ELSE
			PRINTFORML 「胸元がきついわね…ん？んふふ…♪」
			PRINTFORMDW %CALLNAME:MASTER%の視線に気付いた%CALLNAME:TARGET%は、腋を僅かに引き延ばし%CALLNAME:MASTER%を揶揄い始めた
			PRINTFORMW 「ここ？ここにおちんちん突き込みたい？」
			PRINTFORML 「あんたってホント、%RAND_SPLIT("助平/すけべ/えっち")%なんだから♪」
			PRINTFORML
			RETURN 1
		ENDIF
		;RETURN 1
	ENDIF
	
	;=================================================
	;元に戻す
	;=================================================
	IF 口上記入チェック(100) && !TALENT:TARGET:巨乳
		PRINTFORMW 「そうね。私も、元のままの私を愛して欲しいわ」
		PRINTFORML
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%さま貧乳化or通常に戻す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 35
	;=================================================
	;貧乳化
	;=================================================
	IF 口上記入チェック(100) && TALENT:TARGET:貧乳
		PRINTFORMDL %CALLNAME:TARGET%が%CALLNAME:MASTER%の視線を察しサラシを解く
		PRINTFORMDW %RAND_SPLIT("広くなった胸元/ぶかぶかの腋")%から胸の先端が見え…
		PRINTFORMDW …見えそうで見えない
		PRINTFORML
		PRINTFORML 「んふ♪」
		PRINTFORMW 「この%RAND_SPLIT("助平/すけべ/ロリコン")%」
		PRINTFORML 「こういうの、好きなんだ？」
		PRINTFORML 「ほれほれ♪」
		PRINTFORML
		PRINTFORMDL %CALLNAME:TARGET%は%CALLNAME:MASTER%を散々に揶揄うと満足したようだ
		PRINTFORML
		RETURN 1
	ENDIF
	
	;=================================================
	;元に戻す
	;=================================================
	IF 口上記入チェック(100) && !TALENT:TARGET:貧乳
		PRINTFORML 「そうね。私も、元のままの私を愛して欲しいわ」
		PRINTFORML
		RETURN 1
	ENDIF
ENDIF

;─────────────────
;EV111, ご主人様として新しい調教者を雇った
;────────────────────────────────────
@KOJO_EVENT_K1_111(ARG)
;-------------------------------------------------
;・新しい調教者がTARGETになっているため、口上主はTARGETではないので注意
;・口上主を参照したい場合はFLAG:ご主人様で
;-------------------------------------------------
;戻り値（1,表示、2,表示して加入したキャラの加入口上をスキップ）
;-------------------------------------------------
IF 口上記入チェック(0)
	;//NO:TARGETで雇った相手を参照することが可能です
	;CSTR:(FLAG:ご主人様):呼び方リスト = 元の名前_呼ばせたい名前/%CSTR:(FLAG:ご主人様):呼び方リスト%
	PRINTFORMW 
	PRINTFORML
	RETURN 1
ENDIF

;────────────────────────────────────
;EV112, %CALLNAME:TARGET%さまが調教者として加入した
;────────────────────────────────────
@KOJO_EVENT_K1_112(ARG)
;-------------------------------------------------
;・ご主人様の名前は「FLAG:ご主人様」で参照できる
;・ご主人様が雇用口上で戻り値2を返していると表示されない
;・口上主はTARGET
;-------------------------------------------------
IF 口上記入チェック(100)
	;%CALLNAME:TARGET%が来ても助けてくれない。現実は非情である。
	;調教対象の反抗刻印Lv5以上
	IF MARK:(FLAG:ご主人様):反抗刻印 >= 5
		PRINTFORMW 「そこまでよ！」
		PRINTFORML
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「そこまでよ！」
		;%CALLNAME:MASTER%に言っているのかご主人様に言っているのかはぼかす
		PRINTFORMW 「随分楽しそうなこと。してるわね」
		PRINTFORML
	ENDIF
	RETURN 1
ENDIF
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 性交奉仕系個別リアクション口上ファイル
;
;	性交奉仕系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;性交奉仕系
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT分類(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT分類:性交奉仕系の場合、奉仕系COM(COM40~COM44)を中心の選択肢となりますが
;・状況によってはその他のものが選択肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める必要はありません
;・必要と思うものを見極め、絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、全て拾ってしまって汎用リアクションに流れません
;・調教対象の反応に関わらず、同じリアクションしかしなくなってしまいます
;・要するにこれは、想定されたシステムの破壊に繋がります
;-------------------------------------------------
;　REACTの地の文はACT_MESSAGE.ERBのTRAIN_MESSAGE2_{TFLAG:ACT}で処理されます
;　地の文は受容の場合はTFLAG:REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)
;　拒絶した場合はREACT分類(暴れる/逃げる)とREACT分類:拒否に分かれます
;　各REACT分類内でTFLAG:REACT負荷(自然な行動/無理のない行動/無理をした行動)で派生
;　REACT分類が(受け入れる/積極的従う/平然とやる)の場合は、TCVAR:MASTER:イきそう、TCVAR:MASTER:焦らし度等で細かく描写されます
;　ACT_MESSAGE.ERB REACTION_MESSAGE.ERBを参照しながらの作業をお勧めします
;-------------------------------------------------
;各REACTでは想定される反応を網羅してあります。必要に応じて分岐を増減して使用して下さい
;COMによってはREACT派生でREACT分類が変化する事に注意
;各COMの派生は基本的にREACT派生1を条件決めうちの小分岐にし、REACT派生0はその他扱いで条件なしとしています
;手間を掛けたくない場合はその他のみを書いて、派生に関わらず当該箇所を補完する事ができます
;またIF 口上記入チェック(0) && TFLAG:REACT派生 < 1とする事で派生した場合は汎用リアクションへ流すなどという事も可能です
;派生は将来増える可能性もあるので != 1などとするよりは < 1とした方が無難であると考えます
;-------------------------------------------------
;・COM20.腰を振るについて…
;・COM20, 腰を振るはACTによってREACT分類が変化
;・逆レイプの場合はREACT分類:受け入れる。その他性交奉仕系TではREACT分類:積極的従う扱いとなる
;・この変化は関数@SEX_INITIATIVEにて行われる
;-------------------------------------------------

;────────────────────────────────────
;REACT95, 正常位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K1_95
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;・TEQUIP:性交奉仕中が立っている際にREACT分類が暴れるだと拒絶扱いになる(即ち、性交奉仕系ACT以外でも拒絶処理となる場合がある)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「私からしないと、駄目かしら？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「射精しちゃ駄目」
				PRINTFORMW 「…駄目だけど、嫌じゃないからね」
			CASE 1
				PRINTFORMW 「だしちゃだめ」
				PRINTFORMW 「…だめだけど、むりやりなかだしするのは構わないわ」
			CASEELSE
				CALL FADEOUT(28, "（駄目って、いいって事なんだけど…こいつ鈍感だから、わかってなさそうね）")
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		IF !RAND:2
			PRINTFORMW 「あんたの恥ずかしがってる表情、丸見えよ」
			CALL FADEIN(12, "いい眺めね")
			CALL FADEOUT(8, "んふふ")
		ELSE
			PRINTFORMW 「ほら、恥ずかしがってないで来なさいよ」
			CALL FADEOUT(20, "「私だって、恥ずかしいんだから」")
			PRINTFORMW 「もっと、がっついてくれると…満更じゃないわ」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 「そんなに嫌？」
	PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%に不安げな目を向けた
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「我慢しているあんたの顔。可愛いわよ」
		PRINTFORMDW %CALLNAME:TARGET%はいぢわるな笑みを浮かべると、ねっとりと腰を動かし%CALLNAME:MASTER%を追い詰めていった
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 「蕩けた顔しちゃって♪%CALLNAME:TARGET%のなか、気持ちいい？」
	PRINTFORMW 「んふふ♪もっと気持ちよくなっちゃいなさい」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
	[SKIPSTART]
	;余裕が有り過ぎて少女らしさを欠く為コメントアウト
	;快晴、春の陽気、少女らしさ、義務教育受けてなさそう。霊夢と調教の食い合わせは、悪い。
	;PRINTFORMW 「うん。続けていいわよ」
	;PRINTFORMW 「もっと気持ちよくなっちゃいなさい」
	[SKIPEND]
	PRINTFORMW 「続けていいから、もっと気持ちよくなっちゃいなさい」
	CALL FADEOUT(8, "（気持ちいいんだ）")
	PRINTFORM 「…えへへ
	CALL HEARTBW
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("腰を振る")
	;んきゅって言わせたい この辺りが適当か
	PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の抽挿に、口を押さえて声にならない声を漏らしている
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
	
	IF 口上記入チェック(100) && PALAM:快Ｖ > 8500
		PRINTFORMW 「%RAND_SPLIT("焦らさないで/お願い")%、気持ち良くして」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「受け止めてあげるから、もっとがっついていいわよ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 「んっ。もっと、もっと！」
	[SKIPSTART]
	;体位特有の味をもっと出したい
	;PRINTFORMW 「ずっとこうされたかったの、あんたに覆い被られて、組み敷かれて、抱きしめられて」
	;正常位を男女逆転騎乗位と解釈するとどうだろう
	[SKIPEND]
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛情を込めてやる")
	IF !RAND:3
		PRINTFORMW 「これ…心地良いわ…もっとして？」
		[SKIPSTART]
		;ここは描写しない方が良さそう
		PRINTFORMDW %CALLNAME:TARGET%はうっとりとした表情で%CALLNAME:MASTER%に身を任せている
		[SKIPEND]
	ELSEIF !RAND:2
		PRINTFORM 「んふふ
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%は覆いかぶさる%CALLNAME:MASTER%の頬を両手で挟み見つめてきた
	ELSE
		;ここは%CALLNAME:MASTER%表記
		PRINTFORM 「%CALLNAME:MASTER%
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%はだっこをねだる幼子のように両手を伸ばしてきた
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 「淡々とされるのは、面白くないわね…」
	PRINTFORMW 「あんたはするよりも、される方がいいのかしらね？」
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(100) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			;%CALLNAME:TARGET%さん不敵に笑う
			PRINTFORMW 「このまま、いっちゃいたい？どぷどぷ精液出したい？」
			PRINTFORMW 「子種汁\@ TALENT:TARGET:妊娠 ? 注ぎ込みたい # 注ぎ込んで、私を孕ませたい \@？」
			PRINTFORMDW %CALLNAME:TARGET%は不敵に笑うと激しく腰を動かし始めた
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 「まだ駄目よ？もう少し楽しみたいんだから、我慢しなさい」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		[SKIPSTART]
		;派生1,イかせてあげるに移転。「ふーんと聞き流す」の「ふーん」を活用
		PRINTFORMW 「このまま、いっちゃいたい？どぷどぷ精液出したい？」
		PRINTFORMW 「子種注いで、私を孕ませたい？」
		[SKIPEND]
		PRINTFORMW 「ふーん？いきたんだ？」
		PRINTFORMDW %CALLNAME:TARGET%は蕩けた視線で%CALLNAME:MASTER%を見つめながら荒げている
		RETURN 1
	ENDIF
ENDIF
;REACT95, 正常位させる

;────────────────────────────────────
;REACT96, 後背位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K1_96
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他・ACT開始時の拒絶
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORML 「女に恥をかかせないでよ」
		PRINTFORMDW %CALLNAME:TARGET%はふくれっ面で%CALLNAME:MASTER%を睨んでいる
		PRINTFORMW 「私からしないと駄目かしら？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		;素直になれるの分岐あたりで矛盾しないだろうか
		PRINTFORMW 「%RAND_SPLIT("セキニン取りますって言う/素直になれる/好きって言う")%まで、%RAND_SPLIT("なかだし/膣内射精/出")%しちゃ駄目よ」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 「%RAND_SPLIT("セキニン取りますって言う/素直になれる/好きって言う")%まで、出しちゃ駄目よ」
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「前からの方が好き？」
				PRINTFORMW 「前からすると、あんたに%RAND_SPLIT("表情/感じてるとこ/涎出てるとこ")%見られちゃうから…」
				;消え入るような声を表現したいが、fadeoutはなにか違う気がする。やっと絞り出した声としてFADEINに変更
				CALL FADEIN(12, "「私が恥ずかしいのよ…」")
			CASEELSE
				PRINTFORMW 「獣の交尾みたいで恥ずかしいかしら？」
				PRINTFORMW 「そんなに恥ずかしがらなくても、せっかく二人きりなんだから、ね？\@ IS_CONDITION(MASTER, "情欲") ? 繁殖しましょ # 楽しみましょ \@」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 「交尾みたいで嫌？」
	PRINTFORMW 「ケダモノなあんたにはこれでいいのよ」
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	[SKIPSTART]
	;こづくりを前面に出し過ぎると赤い座布団運びを連想する為コメントアウト
	;もう取ってる。もう産ませている際に矛盾する為コメントアウト
	;改善の余地が残る
	PRINTFORMDW %CALLNAME:MASTER%は責任を取る%RAND_SPLIT("覚悟をすると/決意を固めると")%%CALLNAME:TARGET%の腰を掴み根元まで陰茎を捻じ込んだ
	;%CALLNAME:MASTER%のちんぽは果たして奥まで届くのか
	PRINTFORMW 「そう…奥までしっかり挿入れなさい」
	
	;後ろ手に両手掴みは地の文と矛盾する
	
	;受けあなたが攻めを受け入れるプレイ
	[SKIPEND]
	;腰を振るより消極的なあなた主導 
	IF IS_CONDITION(MASTER, "情欲")
		;霊夢さんが気持ちいい！
		PRINTFORML 「あんたにされちゃうの、気持ちいい」
	ELSE
		;霊夢さんがもっとハードにして
		PRINTFORML 「もっと激しく%RAND_SPLIT("ても大丈夫だから/してもいいから")%」
	ENDIF
	
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 「気持ち良い？おまんこ気持ちい？」
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("腰を振る")
	[SKIPSTART]
	IF !RAND:2
		;脱衣分岐が必要
		PRINTFORMDW 白い背中に広がる黒髪が艶めかしい
		
		;和室に音の反響は合わない気がしてきた
		PRINTFORMDW %CALLNAME:TARGET%の腰をがっちりと掴み、お尻に腰を打ち付ける度、
		PRINTFORMDW リボンが蝶のように舞い遊び、獣のような喘ぎ声が部屋に響いた
		;調教中の言動とすり合わせ 激しくの移転の為コメントアウト
		;PRINTFORMW 「んんっ！激しくなってきたわね」
		;PRINTFORMW 「一生懸命腰振っちゃって、もっと激しくしてもいいわよ」
		PRINTFORMW 「んんっ！一生懸命腰振っちゃって」
		;射精時との擦り合わせが必要
		PRINTFORMW 「あんたの性欲、全部ぶつけていいわ」
		
		;あんたの%CALLNAME:TARGET%（所有）が使いたいが、「の」と相性が悪い条件の調整も必要
		;PRINTFORMW 「もっと%RAND_SPLIT("あんた/れいむ")%の\@ TALENT:TARGET:妊娠 ? 孕み穴 # おまんこ \@使い込んで」
		;尻に欲情したケダモノ風
		PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%のお尻を鷲掴みにしながら必死に腰を打ち付けた
		PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%の足を抱え上げ必死に腰を打ち付けた
		RETURN 1
	ENDIF
	[SKIPEND]
	IF IS_CONDITION(MASTER, "情欲")
		PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%の腰を掴み固定しながら腰を打ち付けた
		PRINTFORML 
		PRINTFORMW 「こら、がっつきすぎ」
		RETURN 1
	ENDIF
	
	IF IS_CONDITION(TARGET, "情欲")
		PRINTFORMDW %CALLNAME:TARGET%の口元を涎が伝う
		RETURN 1
	ENDIF
	
	;隠語は口にすれども喘ぎ声は必死に抑える
	IF PALAM:快Ｖ > 8500
		IF !RAND:2
			PRINTFORM 「～～
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%は枕に顔を埋めている
		ELSE
			PRINTFORM 「～～
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%は枕に顔を埋めながらも%CALLNAME:MASTER%の腰の動きに合わせ甘い声を漏らしている
		ENDIF
		RETURN 1
	ENDIF
	
	IF 口上記入チェック(100)
		;調教中の発言とのすりあわせのためコメントアウト
		;PRINTFORMW 「んんっ！激しくなってきたわね」
		;PRINTFORMW 「一生懸命腰振っちゃって、もっと激しくしてもいいわよ」
		PRINTFORMW 「んんっ！一生懸命腰振っちゃって」
		RETURN 1
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		IF !IS_CONDITION(MASTER, "情欲") && IS_CONDITION(TARGET, "情欲")
			PRINTFORML 「お願い。焦らさないで」
			PRINTFORMW 「もっと、もっとおちんちん、ずこずこして」
			;苛立ちが上がる箇所におねだりさせられて悔しいご主人様を仕込むのはどうだろうか
			SIF TCVAR:MASTER:イきそう
				PRINTFORMW 「中に出して！」
		ELSEIF IS_CONDITION(TARGET, "情欲")
			PRINTFORMW 「何我慢してるのよ」
			PRINTFORMW 「ほら、して？」
		ELSE
			PRINTFORMW 「お願い。焦らさないで」
			PRINTFORMDW %CALLNAME:TARGET%は媚びるような目で%CALLNAME:MASTER%を見つめてきた
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「前からの方がいいかしら？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 「んんっ！激しくなってきたわね」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 「必死に理性を保とうとしてるわね」
	PRINTFORMW 「わかるわよ。腰の動き、優しいもの」
	
	;安産型のお尻を愛でるプレイはどうだろう
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 「もっと理性飛ばして、夢中になって。して欲しいんだけど」
	PRINTFORMW 「前からの方がいいかしら？」
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(100) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 「いきたいなら、自分で腰振って気持ちよくなりなさい」
			PRINTFORMW 「%CALLNAME:TARGET%のおまんこで、おなにーしなさい」
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 「射精したらあんた、すぐ%RAND_SPLIT("へばっちゃう/くてってなっちゃう")%じゃない」
			PRINTFORMW 「私だって気持ちよくして欲しいんだから、%RAND_SPLIT("まだだめよ/我慢して")%」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「どうしようかしら？…んふふ」
		PRINTFORMW 「イきたいなら自分で腰振って、気持ちよくなりなさい」
		RETURN 1
	ENDIF
ENDIF
;REACT96, 後背位させる

;────────────────────────────────────
;REACT97, 対面座位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K1_97
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;対面座位のいちゃこら感を保ちつつ姿勢を活かし拒絶する
		PRINTFORMDW %CALLNAME:MASTER%は妨害を試み%CALLNAME:TARGET%の腋に両手を差し込み、擽った
		PRINTFORMW 「ちょっ！」
		PRINTFORML 「ふっあはっ」
		PRINTFORMW 「やめ！くふっ！あははは！やめ！」
		PRINTFORMW 「や・め・な・さ・い！」
		PRINTFORMDL 怒られた
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「必死に我慢しちゃって、いい眺めね」
		PRINTFORMW 「あんたの膝、私の特等席ね」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	;半騎乗位として扱うのはどうだろう
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛情を込めてやる")
	SELECTCASE RAND:2
		CASE 0
			PRINTFORM 「いっぱい愛して、%RAND_SPLIT("もっと/いっぱい")%%CALLNAME:TARGET%の事、可愛がって
			CALL HEARTBW
		CASEELSE
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%に抱き着きながら満足そうにしている
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW ちゅ
	PRINTFORML ちゅっ
	PRINTFORMW ちゅっちゅっ
	PRINTFORMDW 素面を保とうとする%CALLNAME:MASTER%に%CALLNAME:TARGET%が唇を押し付ける度に、%CALLNAME:MASTER%の頬は緩んでいった…
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(100) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		;イかせるのやめて焦らす展開に進行する可能性あり
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 「んー？んふふ、どうしようかしら」
			PRINTFORMDL %CALLNAME:TARGET%は繋がったまま%CALLNAME:MASTER%の背に腕を回すと腰の動きを抑え、ゆったりとカラダをすりつけた
		;派生2,イかせてあげない
		ELSE
			;寝起きのまどろみのように「？」 「…」ではなく「、」で長すぎず
			PRINTFORMW 「んー？もうちょっと、」
			PRINTFORMDL %CALLNAME:TARGET%はぴっとりとひっつくと腰の動きを止めてしまった
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT97, 対面座位させる

;────────────────────────────────────
;REACT98, 背面座位させる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K1_98
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && V_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
;素直にシンプルに　確率低め
IF 口上記入チェック(25) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 「わたしも」
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 「ひゃっ！」
	PRINTFORMDW %CALLNAME:MASTER%が積極的に%CALLNAME:TARGET%の%RAND_SPLIT("耳を甘噛みす/うなじに口付け/肩を愛撫す")%ると、霊夢の口から可愛い悲鳴が上がった
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMDL %CALLNAME:MASTER%は%CALLNAME:TARGET%の髪に鼻を埋めながら労わるように%CALLNAME:TARGET%の全身を解すように奉仕した…
	PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%にしなだれかかり、時折その可憐な唇から弛緩した声を漏らした
	;緩みすぎたのでコメントアウト
	;PRINTFORMW 「あ”あ”～生き返るわ～」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT98, 背面座位させる

;────────────────────────────────────
;REACT99, アナルセックスさせる・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ａ性交系から/3=Ｖ性交奉仕系から/4=Ｖ性交系から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る
;苦痛系反応
;	COM:暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K1_99
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・REACT分類が暴れる/拒否の場合はキャンセル扱い
;・ACTがキャンセルとなった場合はTEQUIP:性交奉仕中もリセット
;・その他のREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的やる)の場合は受容扱いとなる
;・調教対象が童貞でも童貞喪失にはならない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が拒絶した場合(REACT分類が暴れる/拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_COMGRONAME("暴れる/拒否")
	;ACTION_APPLY2でTEQUIPは解除されているので
	;現在の@V_SEXの仕様(TEQUIP:性交奉仕中を参照している)だと死に分岐になると思われます
	;ACT派生で括る事で相似形の結果が得られそうですが、関数が手入れされる可能性を考慮してとりあえずこのままという事で
	;=================================================
	;既に挿入していた場合
	;=================================================
	;IF 口上記入チェック(0) && A_SEX(MASTER)
	;	PRINTFORMW
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF


;-----------------------------
;以下、COM毎の分岐
;-----------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1はREACT分類:暴れる扱いなので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
;確率の抑制
IF 口上記入チェック(25) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 「あんたの腕の中、あったかい」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT99, アナルセックスさせる

;────────────────────────────────────
;REACT103, 逆レイプ・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:悲鳴を上げる/暴れる/怯える
;性交系反応
;	COM:腰を振る
;奉仕系反応
;	COM:消極的やる/積極的やる/愛情を込めてやる/平然とやる
;その他反応
;	COM:イかせて！
;-------------------------------------------------
@KOJO_REACT_K1_103
;-------------------------------------------------
;・地の文はREAC分類で分岐
;・他の性交奉仕系と異なり、調教対象は拒絶できない
;・COM20,腰を振るはREACT分類:受け入れるにセットされる
;・調教対象が童貞なら童貞喪失、調教者が処女なら処女喪失が起こる
;・COM:嫌がる/暴れるはREACT分類:暴れる(即ち、暴れが通る状況)にはならない
;-------------------------------------------------
;・逆レイプは挙動及びCOMの傾向がちょっと異なります
;-------------------------------------------------

;逆レイプは拒絶できないので、拒絶系の分岐は設置しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;焦らし中
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 > 1 && IS_COMGRONAME("積極的従う/平然と従う/受け入れる")
	;=================================================
	;REACT分類が積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("積極的従う/平然と従う")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;すぐにでもイきそうな状態のまま、腰を振って奉仕させられている…
		PRINTFORMW 「%RAND_SPLIT("我慢できちゃうのも、/余裕が出てきたかしら？少し")%面白くないわね」
		PRINTFORMW 「もっと激しくしないと駄目かしら」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類が受け入れる
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("受け入れる")
		;%CALLNAME:MASTER%は%CALLNAME:TARGET%に快感をコントロールされ
		;イくことができない…
		PRINTFORMW 「んふふ…おねだりが聞きたいわ」
		PRINTFORM 「とろけた顔で
		PRINTFORMD %RAND_SPLIT("ちんちんじる搾り取って/ちんちんぐっぽぐっぽして/膣内は駄目")%！
		PRINTFORM って言うまで、射精させてあげない
		CALL HEARTBW
		RETURN 1
	ENDIF
ENDIF

;-----------------------------
;以下、COM毎の分岐
;-----------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("許しを乞う")
	;派生1の場合はREACT分類:なし
	;=================================================
	;意見を出すなんていい度胸だね！(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;=================================================
		;あなた衰弱
		;=================================================
		;あなたにも霊夢にも制御の自由はない
		IF IS_CONDITION(MASTER, "衰弱")
		;PRINTFORML 「そうね」
		;PRINTFORML 「あんたとは太く長い付き合いにするんだし、許してやるか」
		;霊夢さん比較対象を知らない
		;PRINTFORML 「あんたのは細く短いけど」
			IF IS_CONDITION(TARGET, "情欲")
				PRINTFORM 「ごめんなさい。こし、とまんない
				CALL HEARTBW
			ELSE
				PRINTFORM 「もうちょっとだけ。ね
				CALL HEARTBW
				PRINTFORMDL もうちょっとがいつまで経っても終わらない…
			ENDIF
			RETURN 1
		ENDIF
		
		;=================================================
		;あなた疲弊
		;=================================================
		IF IS_CONDITION(MASTER, "疲弊")
			PRINTFORMW 「」
			PRINTFORMW 「%RAND_SPLIT("何を情けない事言ってるのよ/もう一回。ね？/あんたのここは、もっとしたいみたいよ？/まだ、硬いじゃない/まだ、できるでしょ？/………")%」
			CALL FADEIN(6, "ゆっさゆっさ")
			CALL FADEOUT(6, "「んっんっ」")
			;速める
			CALL FADEIN(4, "ゆっさゆっさ")
			CALL FADEOUT(4, "「んっんっ」")
			RETURN 1
		ENDIF
		
		;=================================================
		;それ以外
		;=================================================
		PRINTFORMW 
		PRINTFORMW 「何を許してほしいのかしら？ほら、言ってみなさい」
		PRINTFORMW 「ほらほら」
		PRINTFORM 「
		PRINTFORMD %RAND_SPLIT("れいぷ/ごーかん/なかだし")%らめぇ
		PRINTFORMW って」
		PRINTFORMW 「もっとあんたの情けない声、聞かせて？聞かせなさい♪」
		PRINTFORML 
		PRINTFORMDW %CALLNAME:TARGET%は楽しそうだ
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「そんなに恥ずかしい？私だって恥ずかしいから%RAND_SPLIT("大丈夫/平気/おあいこ")%よ」
		RETURN 1
	ENDIF
ENDIF

;逆レイプの場合、派生1、REACT分類:暴れるになる事はないので考慮しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
	IF IS_CONDITION(MASTER, "鬱屈")
		PRINTFORMW 「こんなにも尽くしてあげてるのに…釈然としないわ…」
		PRINTFORMW 「どんな事ならしてもいいの？ね。教えて」
		IF IS_CONDITION(TARGET, "鬱屈")
			CALL FADEOUT(12, "「お願いだから」")
			PRINTFORMW 「お願いだから」
			PRINTFORMDW %CALLNAME:TARGET%の頬を雫が伝っていった…
		ENDIF
	ELSE
		PRINTFORMW 「そんなに、嫌？」
		PRINTFORMDW %CALLNAME:TARGET%が悲しげに表情を曇らせる…
		PRINTFORMW 「…ううん」
		PRINTFORMW 「私らしくなかったわね」
		PRINTFORMW 「あんたは私の下でひんひん言ってればいいのよ」
		PRINTFORMW 「観念してヤられなさい！」
		PRINTFORMDW %CALLNAME:TARGET%は目に涙を滲ませながらも、懸命に腰を振っている…
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORML 「うん？何かおかしいかしら？」
	PRINTFORMDW %CALLNAME:TARGET%の頭上に？が浮かぶ
	PRINTFORMDW 何事にもとらわれない%CALLNAME:TARGET%が、こうして自分に介入してくるのがおかしいといえばおかしい
	PRINTFORML
	;PRINTFORMDW %CALLNAME:TARGET%が拗ねたような目を%CALLNAME:MASTER%を向ける
	PRINTFORMDW %CALLNAME:TARGET%に%RAND_SPLIT("額/頬/鼻先")%をつつかれた
	PRINTFORMW 「おかしくなんかないわよ。私だって%RAND_SPLIT("/適齢期な/孕み頃な/女の子な/年頃な/お赤飯来てる")%んだから…」
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	IF 口上記入チェック(100) && PENIS(MASTER) && TCVAR:MASTER:イきそう
		PRINTFORMDW %CALLNAME:TARGET%は射精を我慢する%CALLNAME:MASTER%の姿に口元を吊り上げ\@ IS_CONDITION(TARGET, "情欲") ? ると、脚をＭの字に開き、精液を搾り出そうと #  \@腰の動きを速めた
		RETURN 1
	ENDIF
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		IF !RAND:2
			PRINTFORMW 「…なんだか、変な癖がついちゃいそうで怖いわ」
		ELSE
			PRINTFORMDW %CALLNAME:TARGET%は生唾を飲み込むと、無言で腰を打ち付けてきた
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「いつまで我慢できるかしら？んふふ…ほら頑張れ頑張れ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 「声、出ちゃってるわよ？」
	PRINTFORMW 「気持ちいい？\@ TALENT:妊娠 ? %CALLNAME:TARGET%の孕み穴 # 無理矢理こづくり \@気持ちいい？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
	;語尾の？はあった方がいいものか。ACT側で？REACT側では無い方がいいかもしれない
	SELECTCASE RAND:4
		CASE 0
			PRINTFORMW 「…あんたも、動いていいわよ」
		CASE 1
			PRINTFORMW 「んふ、正直でよろしい♪」
			PRINTFORMW 「自分で単純だとは思うけど、やっぱり嬉しいわ」
		CASE 2
			PRINTFORM 「んふふ♪いっぱい、えっちしましょ
			CALL HEARTBW
		CASE 3
			PRINTFORMW 「ん。続けるわね？」
	ENDSELECT
	;霊夢さんなりの愛情表現
	;えっちが気持ちよくないとも受け取れる為コメントアウト
	;PRINTFORM 「私も、あんたの作るご飯が一番おいしいわ
	;CALL HEARTBW
	RETURN 1
ENDIF

;逆レイプの場合はREACT分類:受け入れるになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("腰を振る")
	;%CALLNAME:MASTER%はイきそうになったの後に表示される事に注意
	;焦らし時に問題が出るかもしれない
	[SKIPSTART]
	;ハートのみでは物足りない
	PRINTFORM 「
	CALL HEARTBW
	PRINTFORML
	PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%%RAND_SPLIT("の手を掴み/に覆いかぶさり")%、%CALLNAME:MASTER%の突き上げを受け止めている
	PRINTFORM 「必死に腰振っちゃって、私の膣内で射精したいのね」
	[SKIPEND]
	;-------------------------------------------------
	;%CALLNAME:MASTER%イきそう
	;-------------------------------------------------
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		PRINTFORM 「あんたのおちんちん、あんたのおちんちんが、
		PRINTFORMD 射精したい射精したい
		PRINTFORM って、私の中で暴れてる
		CALL HEARTBW
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;%CALLNAME:TARGET%イきそう
	;-------------------------------------------------
	IF 口上記入チェック(100) && PALAM:快Ｖ > 8500
		PRINTFORMW 「私、もう%RAND_SPLIT("少し/駄目そう")%だから」
		PRINTFORM 「いかせて
		CALL HEARTBW
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;%CALLNAME:TARGET%情欲
	;-------------------------------------------------
	IF 口上記入チェック(0) && IS_CONDITION(TARGET, "情欲")
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;条件無し
	;-------------------------------------------------
	IF 口上記入チェック(100)
		PRINTFORM 「あんたがしてくれるの、素直に嬉しいわ
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%%RAND_SPLIT("の手を掴み/に覆いかぶさり")%、%CALLNAME:MASTER%の突き上げを受け止めている
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	;道具責め中の苦痛が反映されるとの事なので変更が必要
	;PRINTFORMW 「痛いのを我慢するのは私の方だと思うのだけど」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「あんたはこれでも咥えてなさい」
				PRINTFORMDW %CALLNAME:TARGET%は自分の下着を%CALLNAME:MASTER%の口に押し込んできた
			CASEELSE
				PRINTFORMW 「うっさいわね。ちんちん大きくしたら和姦よ！」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;逆レイプの場合暴れが通る事はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生2,制止による暴れ失敗扱い。派生1は汎用リアクションへ
;-------------------------------------------------
IF 口上記入チェック(100) && IS_NOWCOMNAME("暴れる") && TFLAG:REACT派生 == 2
	IF IS_CONDITION(TARGET, "情欲")
		PRINTFORML 「ほら動かないで」
		PRINTFORML 「すぐに気持ち良くするから」
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の両肩を押さえ犯してきた
		RETURN 1
	ENDIF
	PRINTFORMW 「…そんなに、嫌？」
	PRINTFORMDW %CALLNAME:TARGET%が悲しげに表情を曇らせる
	PRINTFORML
	PRINTFORML 「…ううん」
	PRINTFORMW 「私らしくなかったわね」
	PRINTFORMW 「あんたは私の下でひんひん言ってればいいのよ」
	PRINTFORMW 「観念して犯られなさい！」
	PRINTFORMDW %CALLNAME:TARGET%は目尻に涙を滲ませながら、%RAND_SPLIT("健気に/懸命に/愛情を求め")%腰を振っている
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		IF IS_CONDITION(TARGET, "情欲")
			;余裕が無いので無意味に不安を煽る
			;有意義な情緒操作は素直な霊夢には不適切
			PRINTFORMW 「怖くないから。怖くないから。」
		ELSE
			PRINTFORMW 「怖がらなくていいわ。私がするから、じっとしてなさい」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;焦らし中には選択不能。TCVAR:MASTER:イきそうが立つ事によってCOMABLEを抜けてくる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM70, イかせて！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("イかせて！")
	;ACTに於いてTCVAR:MASTER:焦らし度 == 1(イかせる)であっても、@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
	;現状でこの変化を拾う術はない
	;=================================================
	;REACT派生1/REACT派生2
	;=================================================
	IF 口上記入チェック(100) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		;派生1,イかせてあげる
		IF TFLAG:REACT派生 == 1
			;>@COM70でTCVAR:MASTER:焦らし度 > 1(焦らす)になる場合あり
			;どちらでも通じる当たり障りの無い方向に軌道修正
			;PRINTFORMW 「いいわよ。一杯射精しちゃいなさい」
			;PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の精を絞り出そうと腰の動きを速めていった
			IF !RAND:2
				PRINTFORM 「んふ
				CALL HEARTBW
			ELSE
				;霊夢さんが恥ずかしがる
				;霊夢さんにとっては全編和姦。いいね？
				PRINTFORML 「なんだか、あんたを無理矢理犯している『みたい』で恥ずかしくなってきたわ」
				PRINTFORMDL 事此処に至って%CALLNAME:TARGET%は恥ずかしがり動きを止めてしまった
				;%CALLNAME:TARGET%の膣内に、%CALLNAME:MASTER%は精液を注ごうと必死に腰を突き上げた
			ENDIF
		;派生2,イかせてあげない
		ELSE
			PRINTFORMW 「もうちょっと繋がっていたいんだから、我慢して」
		ENDIF
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「ふーんと聞き流す」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「もういきたい？無理矢理されて、射精したい？」
		PRINTFORMW 「んふふ…%RAND_SPLIT("まだ駄目よ/だーめ")%」
		RETURN 1
	ENDIF
ENDIF
;REACT103, 逆レイプ
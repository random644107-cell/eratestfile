;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 愛撫系個別リアクション口上ファイル
;
;	愛撫系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;愛撫系 REACT10～19 & REACT27
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT分類(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT分類:愛撫系の場合、基本的に愛撫系COM(COM10~COM16)が選択肢となりますが
;・状況によってはその会話系、苦痛系、その他のものが選択肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める必要はありません
;・必要と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT及びACT派生内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、リアクションに関わらず、全て拾ってしまって汎用リアクションに流れません
;・要するに、想定されたシステムの破壊に繋がります
;-------------------------------------------------
;　基本的に地の文はTFLAG:REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐し、
;　各REACT分類内でTFLAG:REACT負荷(自然な行動/無理のない行動/無理をした行動)で派生
;　ACT_MESSAGE.ERBを参照しつつの作業をお勧めします
;-------------------------------------------------
;　取り敢えずACT派生毎に枠を切り分けてありますが、一つに纏めた上で汎用的な記述で手間を減らすという手もあります
;　正直、やり方はいくらでもあるので、以下に示す方法は一つの例と思ってください
;-------------------------------------------------

;────────────────────────────────────
;REACT10, 手で愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=愛撫/1=手淫/2=ペニスバンド手淫)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K1_10
;-------------------------------------------------
;・ACTTION_APPLY2の処理なし
;・地の文はACT派生毎にREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0においては可能性のあるCOMを一通り網羅しておきます
;必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;=================================================
	;COM6, もっとハードにして(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("もっとハードにして")
		;-------------------------------------------------
		;「この恥知らずの豚め！」(お仕置きポイント加算)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(100)
			PRINTFORMW 「痛くないのかしら…それとも、痛いのが気持ちいいのかしら？」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 「気持ちいい？気持ちいいのよね？」
		;状況に恥ずかしがる
		ELSE
			;恥ずかしい状況の確保
			SIF TCVAR:MASTER:勃起度 <= 1000 && !RAND:2
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の皮を弄っている
			PRINTFORMW 「恥ずかしがってるあんたを見るのも、悪くないわ」
			PRINTFORMW 「続けるわよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
			;コメントアウトしてRETURN 0しておきます@/L
			;PRINTFORM 
			RETURN 0
		;嫌がった					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 「そんなに嫌がることないでしょ。本当に嫌？」
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔を覗き込みながら手を動かしている
			PRINTFORMW 「そんなに嫌じゃない…わよね？」
			;この箇所でRETURNするようにしておきます@/L
			RETURN 1
		ENDIF
		;RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 「そんな強がり言って…うふふ。あんた判りやすいもの」
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			SELECTCASE RAND:2
				CASE 0
					PRINTFORMW 「上手くなってあげるから、あんたの気持ちいいの、教えて？」
				CASEELSE
					PRINTFORMW 「これから上手くなるわよ」
					PRINTFORMDW %CALLNAME:TARGET%はさらっと受け流して行為を続けている
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 「な、何よ」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			;コメントアウトしRETURN 0しておきます@/L
			;PRINTFORMW 
			RETURN 0
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 「気持ちいいのよね？んふふ」
			;この箇所ででRETURNしておきます@/L
			RETURN 1
		ENDIF
		;RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 「んふふ…もっと気持ちよくなっちゃいなさい」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 「んふふ♪素直でよろしい」
		RETURN 1
	ENDIF
	
	;派生1.派生2の場合はREACT分類:消極的従う扱い
	;=================================================
	;COM32, 暴れる(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
		;-------------------------------------------------
		;暴れ失敗(REACT分類:消極的従う)
		;-------------------------------------------------
		IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
			[SKIPSTART]
			※内容を書き分けるならコメントアウトを解除して使用して下さい
			;縄拘束による暴れ失敗
			IF TFLAG:REACT派生 == 1
				PRINTFORMW 
			;制止による暴れ失敗
			ELSE
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
			PRINTFORMW 「痛かったかしら？」
			RETURN 1
		ENDIF
		
		;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;PRINTFORMW 「大人しくしなさい」
			;PRINTFORMW 腋で挟まれると%CALLNAME:MASTER%は途端に大人しくなった
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 手淫
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1は派生0と同じ種類のものを違う形で提示してみます
;まぁこんなやりかたもあるという事で
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			;PRINTFORMW 
		;状況に恥ずかしがる
		ELSE
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の余った皮をつまみ、引き伸ばしてきた
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 「息、荒くなってるわよ？」
			PRINTFORMDW %CALLNAME:TARGET%は不敵に笑いながら手淫を続けている
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			SELECTCASE RAND:2
				CASE 0
					PRINTFORMW 「上手くなってあげるから、あんたの気持ちいいの、教えて？」
				CASEELSE
					PRINTFORMW 「これから上手くなるわよ」
					PRINTFORMDW %CALLNAME:TARGET%はさらっと受け流して行為を続けている
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM32, 暴れる(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
		;暴れ失敗(REACT分類:消極的従う)
		IF GROUPMATCH(TFLAG:REACT派生, 1, 2)
			PRINTFORMW 
		;暴れ成功
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;手間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;=================================================
	;REACT分類4, 受け入れる＆COM, 快感を受け入れる/気持ち良い！
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
		;前から対面手淫中に%CALLNAME:MASTER%が快感を受け入れる%CALLNAME:TARGET%が情欲でエスカレートさせる
		IF TCVAR:MASTER:位置前後 == 1
			IF IS_CONDITION(TARGET, "情欲")
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の陰茎をまじまじと見つめながら行為を続けている
				CALL FADEOUT(20, "（どうしよう…舐めたくなってきたわ…）")
				CALL FADEOUT(4, "ちら")
				PRINTFORMDW ;%CALLNAME:TARGET%は陰茎に口を近づけると視線でお伺いを立ててきた
				IF !IS_CONDITION(MASTER, "怒り")
					CALL FADEIN(12, "（いいわよね？怒ってないわよね？）")
					TCVAR:行動予約 = 12
				ENDIF
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	;=================================================
	;REACT分類4, 受け入れる＆COM, 快感を受け入れる/気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("もっとハードにして/快感を受け入れる/気持ち良い！")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類2, 平然と従う＆COM, 不敵に笑う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う") && IS_NOWCOMNAME("不敵に笑う")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COMが妨害/哀願に派生したらCOMへ流す
	;&& TFLAG:REACT派生 < 1自体はなくてもREACT分類の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT分類2, 消極的従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う") && IS_NOWCOMNAME("嫌がる/快感を我慢する") && TFLAG:REACT派生 < 1
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, ペニスバンド手淫
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0/1を参考に拡張するなり、統合するなり
;そんな訳でここは最小限にしておきます
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT10, 手で愛撫

;────────────────────────────────────
;REACT11, 胸愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=胸愛撫/1=乳首吸い/2=揉み合い/3=乳首責め)
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
;・地の文はACT派生2, 揉み合いとその他で分岐
;・ACTION_APPLY2に於いて、ACT派生2の場合は受容、拒否の判定処理あり
;・ACT派生2, 揉み合いにおいてREACT分類(消極的従う/平然と従う/積極的従う/受け入れる)以外は"拒否"扱い
;-------------------------------------------------
;　派生毎にまともにやると手間が尋常ではないので、ACT派生分岐の上層で汎用処理したり
;　各ACT派生内のCOM分岐等は一部のものに絞ってしまうのが良いと思います
;　またACT派生で区切らずに、大分岐でまとめてしまう手もありますが、手の込んだ事をやるには不向きであると考えます
;-------------------------------------------------
@KOJO_REACT_K1_11
;-------------------------------------------------
;決まったやりかたなどというものはないので、以下に示すものはあくまで一つの例ということで
;分岐の増減、COMに流すのか否かの判断は作者の判断に委ねます
;-------------------------------------------------

;*************************************************
;COM単体の分岐 ACT派生の上層で処理
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2は挙動が異なるので条件で弾く
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる") && TFLAG:ACT派生 !=2
	;=================================================
	;快感に対して恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1を書いた場合はREACT派生0, 状況に対して恥ずかしがる扱い
	;派生1を放置した場合はここで補完し、&& TFLAG:REACT派生 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
	;ACT派生2の場合はACT派生2分岐内のIS_COMGRONAME("拒否")で処理
	;=================================================
	;嫌がって妨害した(REACT分類6, 暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1 && TFLAG:ACT派生 !=2
		RETURN 1
	ENDIF
	
	;REACT派生1を書いた場合はREACT派生0, 嫌がった(REACT分類1, 消極的従う)扱い
	;派生1を放置した場合はここで補完し、&& TFLAG:REACT派生 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;強がって(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1を書いた場合はREACT派生0, 未熟なテクニックに対して(REACT分類:平然と従う)扱い
	;派生1を放置した場合はここで補完し、&& TFLAG:REACT派生 < 1などとしてCOMに流してしまうのもあり
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;以下、ACT派生毎の分岐
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 胸愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 乳首吸い
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者に素質(幼稚/舌使い)がない場合は発生する確立は低い
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, 揉み合い
;━━━━━━━━━━━━━━━━━━━━━━━━
;奉仕系COMが選択肢となる場合あり
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;要求に応じなかった
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("拒否")
		;恥ずかしがって
		IF PALAM:MASTER:恥情 > PALAM:MASTER:反抗
			PRINTFORMW 
		;嫌がって
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-----------------------------------------------------------------
	;以下、COMを並列化
	;手間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;-----------------------------------------------------------------
	
	;=================================================
	;REACT分類4, 受け入れる＆COM, 快感を受け入れる/気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("快感を受け入れる/気持ち良い！")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;REACT分類2, 平然と従う＆COM, 不敵に笑う/平然とやる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う") && IS_NOWCOMNAME("不敵に笑う/平然とやる")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類2, 積極的従う＆COM, 積極的やる/愛情を込めてやる
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う") && IS_NOWCOMNAME("積極的やる/愛情を込めてやる")
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COM嫌がって妨害/哀願に派生したらCOMへ流す
	;&& TFLAG:REACT派生 < 1自体はなくてもREACT分類の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT分類2, 消極的従う
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う") && IS_NOWCOMNAME("嫌がる/快感を我慢する") && TFLAG:REACT派生 < 1
		;消費気力による派生
		SELECTCASE GET_COM_RANK()
			;自然な行動
			CASE "自然な行動"
				PRINTFORMW 
			;無理のない行動
			CASE "無理のない行動"
				PRINTFORMW 
			;無理をした行動
			CASE "無理をした行動"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生3, 乳首責め
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者に素質(幼稚/舌使い)がない場合は発生する確立が高い
;調教対象がオトコの場合はここにしか落ちない
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT11, 胸愛撫

;────────────────────────────────────
;REACT12, 口で愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=クンニ/1=フェラ)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_12
;-------------------------------------------------
;・地の文はACT派生毎にREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;・派生毎に区切ってありますが、例えばACT派生1のみ書いてあとは汎用に投げるという事もできます
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, クンニ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0においては参考として可能性のあるCOMを網羅しておきます
;必要に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;COM6, もっとハードにして(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
		;-------------------------------------------------
		;「この恥知らずの豚め！」(お仕置きポイント加算)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
		;状況に恥ずかしがる
		ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
		;嫌がった					REACT分類"消極的従う"
		ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
		;派生:耐久					REACT分類"消極的従う"
		ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, フェラ
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1は派生0と同じ種類のものを違う形で提示してみます
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 「気持ちよさそうだし、続けるわよ？」
			CALL FADEOUT(8, "あむっ")
			;ここに水音が欲しい
			;CALL FADEIN(値, "文章")
		;状況に恥ずかしがる
		ELSE
			;口淫中の発言には課題が残る
			CALL FADEOUT(8, "ぷはっ")
			PRINTFORMW 「こんな事されて、恥ずかしい？」
			PRINTFORMW 「おちんちん間近で見られて、おちんちんの皮もぐもぐされて、恥ずかしいんだ？んふ」
			PRINTFORMW 「続けるわよ？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の言葉に色が混じっている事に気を良くし、更に%CALLNAME:MASTER%の陰茎を吸いたてた
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%を睨むと軽く歯を立ててきた
			PRINTFORMW 「調子に乗るな」
		ENDIF
		RETURN 1
	ENDIF
	
	;手間を厭わないのであれば、IFなり三項なりを使って内部で書き分けるのもあり
	;=================================================
	;REACT分類4, 受け入れる＆気持ち良い！単独分岐
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("受け入れる")  && IS_NOWCOMNAME("気持ち良い！")
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					RETURN 0
				;焦らし継続中にここを通り矛盾が出る
				;焦らし終了
				CASE 1
					PRINTFORMDW %CALLNAME:MASTER%の言葉に%CALLNAME:TARGET%は目を細めると、一生懸命に頭を前後し始めた
				;焦らし無し
				CASEELSE
					PRINTFORMDW %CALLNAME:TARGET%は陰茎を咥えながら%CALLNAME:MASTER%の目に視線を合わせ、優しく微笑んだ
			ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類4, もっとハードにして　確率で行動予約させてみる
	;=================================================
	IF 口上記入チェック(50) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("もっとハードにして") && TFLAG:REACT派生 < 1
		IF !RAND:2
				PRINTFORMW %CALLNAME:MASTER%のおねだりに%CALLNAME:TARGET%は気を良くしたようだ
			TCVAR:行動予約 = 12
		ElSE
			;擬音につき「」の消去
			CALL FADEOUT(8, "…ぷはっ")
			PRINTFORMW 「私にばっかりさせてないで、あんたもして」
			CALL FADEOUT(8, "ね？お願い")
			CALL FADEIN(8, "あむっ…")
			;フェラから69顔面騎乗に進展
			TCVAR:行動予約 = 51
		ENDIF
	ENDIF
	;=================================================
	;REACT分類4, 受け入れる＆COM, 快感を受け入れる/もっとハードにして
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("受け入れる") && IS_NOWCOMNAME("快感を受け入れる/もっとハードにして")
		IF IS_CONDITION(MASTER, "情欲") && TCVAR:MASTER:イきそう
			PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%の頭を掴むと喉奥に射精するべく陰茎を捻じ込んだ
			PRINTFORMDW %CALLNAME:TARGET%は%RAND_SPLIT("なすがままに/%CALLNAME:MASTER%の望むまま")%強引に口内を犯されている
		ELSEIF TCVAR:MASTER:イきそう
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の腰に腕を回し深く咥え込んできた
		ELSE
			PRINTFORMDW %CALLNAME:MASTER%が%CALLNAME:TARGET%の頭を撫でると%CALLNAME:TARGET%は幸せそうに目を細めた
			PRINTFORMDW 気をよくしたのか%CALLNAME:TARGET%の奉仕は次第に激しさを増していった
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類2, 平然と従う＆COM, 不敵に笑う
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("平然と従う") && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW （これじゃどっちが受けかわからないわね…）
		CALL FADEOUT(10, "（ま、いいわ…）")
		RETURN 1
	ENDIF
	
	;COMが妨害/哀願に派生したらCOMへ流す
	;&& TFLAG:REACT派生 < 1自体はなくてもREACT分類の決めうちで同様の結果を得られますが、仕様の可視化ということで
	;=================================================
	;REACT分類2, 消極的従う
	;=================================================
	IF 口上記入チェック(50) && IS_COMGRONAME("消極的従う") && IS_NOWCOMNAME("嫌がる/快感を我慢する") && TFLAG:REACT派生 < 1
		;;消費気力による派生
		;SELECTCASE GET_COM_RANK()
		;	;自然な行動
		;	CASE "自然な行動"
		;		PRINTFORMW 
		;	;無理のない行動
		;	CASE "無理のない行動"
		;		PRINTFORMW 
		;	;無理をした行動
		;	CASE "無理をした行動"
		;		PRINTFORMW 
		;ENDSELECT
		;RETURN 1
		;=================================================
		;フェラに対し消極的 反応が薄い場合
		;=================================================
		IF 口上記入チェック(50) && IS_COMGRONAME("消極的従う") && IS_NOWCOMNAME("嫌がる/快感を我慢する") && TFLAG:REACT派生 < 1
			;=================================================
			;子作り時に勃起 性交奉仕に発展
			;=================================================
			IF 口上記入チェック(100) && TCVAR:勃起度  >= 1000 && TCVAR:今日の満足ボーナス == 7 && POLICY("今日") == "子作り"
				IF !RAND:3
					CALL FADEOUT(12, "（…反応が今一つね）")
					PRINTFORMW （…意地悪はこのくらいにして、してあげようかしら？）
					TCVAR:行動予約 = 103
				ELSEIF !RAND:2
					PRINTFORMW （…必死になって我慢しちゃって）
					CALL FADEOUT(12, "（…しょうがないわね）")
					TCVAR:行動予約 = 103
				ELSE
					CALL FADEOUT(12, "（…反応が今一つね）")
					PRINTFORMW （…たまには好きにさせてあげようかしら？）
					TCVAR:行動予約 = 95
				ENDIF
			;=================================================
			;それ以外 パイズリに発展
			;=================================================
			ELSE
				CALL FADEIN(12, "（…反応が今一つね？）")
				CALL FADEOUT(12, "（…この贅沢者め）")
				TCVAR:行動予約 = 17
			ENDIF
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT12, 口で愛撫

;────────────────────────────────────
;REACT13, アナル愛撫・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生（0=アナル愛撫/1=前立腺刺激/2=アナルバイブ出し入れ）
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_13
;-------------------------------------------------
;・地の文はACT派生で分岐。ACT派生1, 2分岐内の派生なし
;・ACT派生0, アナル愛撫のみREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で派生処理される
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, アナル愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0においては可能性のあるCOMを網羅しておきます
;必要に応じて分岐を増減、ないし構造を改変して使用して下さい
;-------------------------------------------------
;・TFLAG:ACT派生（0=アナル愛撫/1=前立腺刺激/2=アナルバイブ出し入れ）
;おしりいぢめ屈服狙い 強制勃起狙い フェラの一環 ふたなり状態なら挿入も絡んでくることに注意
;-------------------------------------------------
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;=================================================
	;COM7, 許しを乞う(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
		;派生1の場合はREACT分類:なし
		;-------------------------------------------------
		;意見を出すなんていい度胸だね！(お仕置きポイント加算)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 恥ずかしがる(REACT分類"消極的従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
		;快感に恥ずかしがる
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;状況に恥ずかしがる
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 嫌がる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
		;嫌がって妨害した			REACT分類"暴れる"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;嫌がった					REACT分類"消極的従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
		;強がって					REACT分類"消極的従う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;未熟なテクニックに対して	REACT分類"平然と従う"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類"平然と従う")
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			SELECTCASE ABL:MASTER:Ａ感覚
				;お尻の穴ほじられるのが嫌
				CASE 0 TO 2
					PRINTFORMW 「お尻は嫌？嫌よね。でも私は嫌じゃないわ」
				;おひりの穴で気持ちよくなっちゃうのやらぁ
				CASEELSE
					;いっちゃう？きちゃう？
					PRINTFORML 「お尻でいっちゃうの嫌？」
					CALL FADEOUT(12, "「いいじゃない」")
					PRINTFORML 
					CALL FADEIN(8, "「似合ってるわよ？」")
					;サド追加 ACTと合わせて二回目になりうる
					;いいじゃないと肯定して笑顔で変態断定 否定のニュアンスは含めない
					SIF TALENT:サド
						CALL FADEIN(8, "「この変態」")
			ENDSELECT
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			;指摘ありがとうございました。ついでによりよいものを目指し調整コメントアウト
			;PRINTFORMW 「お尻弄るたびおちんちん跳ねさせちゃって」
			;CALL FADEOUT(4, "「くす」")
			;PRINTFORMW 「痛い？それとも気持ちいい？」
			;気持ちいい！と言わせる誘導
			;PRINTFORMW 「言ってくれなくても分かるわよ？でもね、%RAND_SPLIT("言って/聞かせて")%欲しいわ」
			;長さとFADEOUTがミスマッチ。２連続FADEOUTは空行が不自然になる。コメントアウト
			;PRINTFORMW 「あんたがお尻で気持ち良くなっちゃう変態だって、もうばれちゃってるんだから」
			;PRINTFORMW 「%RAND_SPLIT("言いなさい/観念しなさい")%♪」
			
			;こういう場合はIFではなくSELECTCASEで。またRANDではなくRANDREACTを使う手もあります@/L
			SELECTCASE RAND:3
				CASE 0
					PRINTFORMW 「お尻弄るたびおちんちん跳ねさせちゃって」
					CALL FADEOUT(4, "  くす")
					PRINTFORML 「気持ちい？」
				CASE 1
					;ACT側の「どうしようもない%RAND_SPLIT("変態/わ")%ね？」に合わせお尻で気持ちいい変態ですと自供させる
					PRINTFORML 「お尻弄るたびおちんちん跳ねさせちゃって」
					PRINTFORMW 「痛い？それとも気持ちいい？」
					PRINTFORMW 「言ってくれなくても分かるわよ。あんたのことだもの」
					;上のあんたのことだものにかかるか 下の言わせたいわにかかるか曖昧な笑顔
					CALL FADEOUT(4, "  にこっ")
					;あなたの言葉を待っている霊夢を仕込む
					PRINTFORMW 「%RAND_SPLIT("/わかるけど/でもね")%、%RAND_SPLIT("言って欲しいわ/聞かせて/言わせたいわ/ちゃんと言葉にして欲しいわ")%」
				CASE 2
					PRINTFORML 「あんたがお尻で気持ち良くなっちゃう変態だって事、もうばれちゃってるんだから」
					PRINTFORMW 「%RAND_SPLIT("気持ちいい！って言っちゃいなさい/観念しなさい")%♪」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;COM15, 快感を受け入れる
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 「お尻が良いだなんて、あんたって%RAND_SPLIT("ほんと/本当")%…」
		CALL FADEIN(12, "「駄目ねぇ…」")
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORM 「お尻が
		PRINTFORMD 気持ち良い！
		PRINTFORMW だなんて、%RAND_SPLIT("ほんと/本当")%…」
		CALL FADEIN(12, "「駄目ねぇ…」")
		CALL FADEIN(8, "「あんた」")
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 前立腺刺激
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がオトコの場合のみに派生。地の文の派生なし
;ACT派生0を参考に拡張するなり、統合するなり
;そんな訳でここは最小限にしておきます
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	
	;REACTIONに関わらず以下の地の文が表示される
	;直腸から前立腺をこねくり回される感覚に%CALLNAME:MASTER%の男性器は膨張し、
	;渦を巻くような強烈な射精感がこみ上げてくる…
	
	;=================================================
	;COM14, 快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;派生:哀願					REACT分類"許しを乞う"
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;派生:耐久					REACT分類"消極的従う"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる/COM16, 気持ち良い！
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる/気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, アナルバイブ出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;地の文はTALENT:MASTER:オトコか否かで分岐
;ACT派生0を参考に拡張するなり、統合するなり
;そんな訳でここは最小限にしておきます
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;調教対象がオトコ
	;=================================================
	;直腸から前立腺をこねくり回される感覚に%CALLNAME:MASTER%の男性器は膨張し、
	;渦を巻くような強烈な射精感がこみ上げてくる…
	;-------------------------------------------------
	IF 口上記入チェック(0) && TALENT:MASTER:オトコ
		;-------------------------------------------------
		;COM14, 快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			;派生:哀願					REACT分類"許しを乞う"
			IF TFLAG:REACT派生 == 1
				PRINTFORMW 
			;派生:耐久					REACT分類"消極的従う"
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;COM15, 快感を受け入れる/COM16, 気持ち良い！
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる/気持ち良い！")
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	;%CALLNAME:MASTER%は便意のような異様な快感に悶えた…
	;-------------------------------------------------
	IF 口上記入チェック(0)
		;-------------------------------------------------
		;COM14, 快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			;派生:哀願					REACT分類"許しを乞う"
			IF TFLAG:REACT派生 == 1
				PRINTFORMW 
			;派生:耐久					REACT分類"消極的従う"
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;COM15, 快感を受け入れる/COM16, 気持ち良い！
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる/気持ち良い！")
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT13, アナル愛撫

;────────────────────────────────────
;REACT14, アナル舐め・個別リアクション
;────────────────────────────────────
;・ACT派生なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_14
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;・ACT派生はないのでCOM分岐で完全に区切ります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類"消極的従う")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;快感に恥ずかしがる
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;状況に恥ずかしがる
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;嫌がって妨害した			REACT分類"暴れる"
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;嫌がった					REACT分類"消極的従う"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;強がって					REACT分類"消極的従う"
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;未熟なテクニックに対して	REACT分類"平然と従う"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類"平然と従う")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;派生:哀願					REACT分類"許しを乞う"
	IF TFLAG:REACT派生 == 1
		PRINTFORMW 
	;派生:耐久					REACT分類"消極的従う"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類"受け入れる")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類"受け入れる")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT14, アナル舐め

;────────────────────────────────────
;REACT15, キス・個別リアクション
;────────────────────────────────────
;・ACT派生なし。ACTION_APPLY2の処理なし
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;その他反応
;	COM:自慰し始める
;-------------------------------------------------
@KOJO_REACT_K1_15
;-------------------------------------------------
;・地の文はREACT分類(積極的従う/受け入れる/平然と従う/許しを乞う/消極的従う)で分岐
;・TEQUIP:性交中は体位毎、TEQUIP:性交奉仕中はTCVAR:MASTER:焦らし度で派生する
;・可能性のあるものを一通り置いておきます。必要に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「うっさい馬鹿」
				PRINTFORMW 「えっちくない口づけも、たまにはいいでしょ」
				PRINTFORMDW %CALLNAME:TARGET%は拗ねている
			CASE 1
				PRINTFORMW 「それじゃあ、やりなおしね」
				CALL FADEOUT(2, "ちゅ")
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の唇に軽く唇を重ねてきた
				PRINTFORML
				PRINTFORMW 「もっとやりなおしたい？」
			CASEELSE
				PRINTFORMW 「うっさい馬鹿」
				PRINTFORMW 「べろちゅーの方が、良かったかしら」
		ENDSELECT
		RETURN 1
		;積極的やるは発生しないので%CALLNAME:MASTER%から舌を入れる展開をいれるならここ
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「いいわよ」
		PRINTFORMDW %CALLNAME:TARGET%は再び%CALLNAME:MASTER%の口を自らの口で塞いだ
		
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;口元押さえてもじもじ%CALLNAME:TARGET%に恥ずかしがるで返す箇所
		;一見強気内心おめめぐるぐるか、あるいは口元押さえたままチラチラ
		;%CALLNAME:TARGET%が強気に答える（無理をした行動）というのはどうだろう
		IF FIRSTACT(0)
			CALL FADEOUT(4, "（…しちゃった）")
			PRINTFORMDW %CALLNAME:TARGET%は口元を押さえたまま視線を上げ%CALLNAME:MASTER%をチラチラ観察し、再び視線を下げた
		ELSE
			PRINTFORMW 「顔真っ赤よ？」
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:TARGET%で%RAND_SPLIT("恥ずかしいらしく、耳まで真っ赤になっ/白い頬を紅くし")%ている
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		;本気で嫌がられて悲しい。は他の箇所で使っているのでなにか一工夫欲しい
		;PRINTFORMDW %CALLNAME:TARGET%は悲しそうな表情を浮かべた
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		IF !RAND:2
			PRINTFORMW 「キスだけじゃ嫌。そう解釈させて貰うわ」
			PRINTFORMW 「帳尻は…後から合わせるから、大丈夫よ」
			CALL FADEOUT(8, "「問題ないわ」")
		ELSE
			PRINTFORMW 「嫌がられても無理矢理するまでよ」
			CALL FADEOUT(4, "「絶対」")
			PRINTFORMW 「絶対、やめてあげない%RAND_SPLIT("/んだから")%」
			PRINTFORML
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔をがっしりと掴むと無理矢理唇を貪ってきた
			RETURN 1
		ENDIF
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("不敵に笑う")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		PRINTFORMW 「顔真っ赤よ？」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「したいからしたのよ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 「何よ」
	PRINTFORMDW %CALLNAME:MASTER%はベタ惚%CALLNAME:TARGET%を冷やかした
	PRINTFORMW 「うっさい馬鹿」
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;啄木鳥運動で耐久%CALLNAME:MASTER%を崩す
		IF !RAND:2
			PRINTFORMW 「駄目」
			CALL FADEIN(2, "ちゅ")
			PRINTFORMW 「許してなんて」
			CALL FADEOUT(2, "ちゅ…ちゅ…")
			PRINTFORMW 「あげない」
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%にくちづけを繰り返した
		ELSE
			CALL FADEIN(2, "「んふふ…」")
			CALL FADEOUT(2, "ちゅ…")
			CALL FADEIN(2, "ちゅ…")
			CALL FADEOUT(2, "ちゅ…")
			PRINTFORMDW %CALLNAME:TARGET%は堪える%CALLNAME:MASTER%がもう駄目になるまで口付けを続けるつもりのようだ
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 「んふふ♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 「うん。またしましょ」
	PRINTFORMDW %CALLNAME:TARGET%は上機嫌ににっこりと笑った
	RETURN 1
ENDIF
;REACT15, キス

;────────────────────────────────────
;REACT16, 性器を擦り合う・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生（=0,貝あわせ/=1,素股/=2,ペニスバンド素股）
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K1_16
;-------------------------------------------------
;・地の文はACT派生毎にREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う, 消極的従う)で分岐
;・必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 貝あわせ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, 素股
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, ペニスバンド素股
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;快感を我慢する
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;快感を受け入れる
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF
;REACT16, 性器を擦り合う

;────────────────────────────────────
;REACT17, パイズリする・個別リアクション
;────────────────────────────────────
;・ACT派生なし
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_17
;-------------------------------------------------
;・地の文はCOM:腰を振る、REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐
;・愛撫系COMを網羅。必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「こうかしら？」
		;ACT側と被るので加筆
		PRINTFORMDW %CALLNAME:TARGET%は更に%CALLNAME:MASTER%の陰茎を押し潰してきた
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("不敵に笑う")
	;ご主人様表記の排斥
	;PRINTFORMW 「これじゃ、どっちがご主人様か分からないわね…まあいいわ…」
	PRINTFORMW （これじゃ、私がさせられてるみたい%RAND_SPLIT("/で面白くないわ")%ね…）
	CALL FADEOUT(4, "（まあいいわ…）")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	PRINTFORMW 「必死に我慢しちゃって♪」
	PRINTFORM 「うりうり
	CALL HEARTBW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 「ほれほれ…気持ちいい？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 「もう%RAND_SPLIT("参った/降参")%かしら？」
	PRINTFORMDW %CALLNAME:TARGET%は勝ち誇ったような笑みを浮かべている…
	RETURN 1
ENDIF

;焦らし時には選択不可
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("腰を振る")
	;=================================================
	;イきそう状態で焦らし終了/焦らし無し
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう && TCVAR:MASTER:焦らし度 < 2
		;=================================================
		;巨乳焦らし終了時はパイズリフェラに派生 状況的にパイズリイラマチオ 喋るには厳しい
		;=================================================
		IF 口上記入チェック(100) && TALENT:巨乳
			CALL FADEOUT(8, "「んっ！？」")
			CALL FADEIN(8, "（好き放題してくれちゃって…）")
			PRINTFORMW （…それが嬉しい辺り、私も参っちゃってるわね…）
			RETURN 1
		ENDIF
		;=================================================
		;その他パイズリ射精
		;=================================================
		IF 口上記入チェック(100)
			PRINTFORMW 「%RAND_SPLIT("必死に腰振っちゃって/んふ♪")%」
			PRINTFORM 「好きにさせてたら、おっぱいに中出しされちゃうわね
			CALL HEARTBW
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「自分から気持ちよくなっちゃうなんて、そんなに%RAND_SPLIT("これが気に入った/おっぱいが好きな/おっぱいとえっちしたかった")%のかしら？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「変な声出てるわよ？」
		RETURN 1
	ENDIF
ENDIF

;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT17, パイズリする

;────────────────────────────────────
;REACT18, 足コキする・個別リアクション
;────────────────────────────────────
;・ACT派生なし
;-------------------------------------------------
;想定されるCOM
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K1_18
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;・必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
	;=================================================
	;快感に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;所有者は%CALLNAME:TARGET%です
		;PRINTFORMW 「恥ずかしがってもあんたのおちんちんは、ぐいぐい押し返してきてるわよ？」
		PRINTFORMW 「恥ずかしがってもおちんちんは、ぐいぐい押し返してきてるわよ？」
		PRINTFORMW 「%RAND_SPLIT("悪い/駄目な")%おちんちんね？」
	RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「何言ってるのよ。あんたのちんちんはもっと踏んで欲しいって言ってるわよ？」
		PRINTFORMW 「あんたより私の方が、あんたのこと解ってるんだから」
		PRINTFORMW 「んふ！」
		PRINTFORMW 「さあ観念なさい！」
		PRINTFORMDW %CALLNAME:TARGET%は舌なめずりしながら%CALLNAME:MASTER%への愛撫を続けている
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「だーめ。このまま足でいっちゃいなさい」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMDW %CALLNAME:TARGET%は足コキを受け入れた%CALLNAME:MASTER%を認めると足の動きを止めてしまった
	PRINTFORML 
	PRINTFORMW 「はい
	CALL HEARTBW
	PRINTFORMDW %CALLNAME:TARGET%は舌なめずりすると両足の土踏まずで穴を作った
	PRINTFORMDW %CALLNAME:TARGET%の脚に魅了された%CALLNAME:MASTER%は自ら快楽を貪っている
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良い！")
	CALL FADEOUT(8, "「はいはい」")
	PRINTFORMDW %CALLNAME:TARGET%は苦笑しながらも足での愛撫を続けてくれた
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「いい反応ね？」
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;昇天脚
		PRINTFORMW （足が好きなのかそれとも私の手が下手なのか…なんだか腹が立ってきたわ）
		PRINTFORMDW %CALLNAME:TARGET%は不機嫌そうに%CALLNAME:MASTER%を睨みつけている
		RETURN 1
	ENDIF
ENDIF
;REACT18, 足コキする

;────────────────────────────────────
;REACT19, 指挿入れ
;────────────────────────────────────
;・TFLAG:ACT派生(0=指挿入れ/1=Ｇスポット刺激/2=バイブ出し入れ)
;-------------------------------------------------
;想定されるCOM
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K1_19
;-------------------------------------------------
;地の文はACT派生0のみREACT分類で分岐
;その他はTALENT:MASTER:ふたなりであるか否かで派生するのみ
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 指挿入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はREACT分類(受け入れる/積極的従う/平然と従う/許しを乞う, 消極的従う)で分岐
;・愛撫系COMを網羅。必要に応じて分岐を増減して使用して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;COM10, 恥ずかしがる(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("恥ずかしがる")
		;-------------------------------------------------
		;快感に恥ずかしがる
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「今の状況に恥ずかしがる」扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;派生1の場合はREACT分類:暴れる扱い
	;=================================================
	;COM11, 嫌がる(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
		;-------------------------------------------------
		;嫌がって妨害した(REACT分類:暴れる)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM12, 下手だと罵る
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		;-------------------------------------------------
		;快感を感じながら(REACT分類:消極的従う)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 不敵に笑う(REACT分類:平然と従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1の場合はREACT分類:許しを乞う扱い
	;=================================================
	;COM14, 快感を我慢する(消極的従う)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
		;-------------------------------------------------
		;哀願(REACT分類:許しを乞う)
		;-------------------------------------------------
		IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;派生1を書いた場合はREACT派生0,「耐久」扱い
		;書かなかった場合はREACT派生に関わらず、この箇所を補完します
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 快感を受け入れる(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 気持ち良い！(REACT分類:受け入れる)
	;=================================================
	IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1, Ｇスポット刺激
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は調教対象がふたなりか否かで分岐
;・REACT分類による分岐はなく、内容は汎用的なもの
;-------------------------------------------------
;とりあえず最小限の構成にしておきます。必要に応じて分岐を追加して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;膣内から前立腺をこねくり回される感覚に%CALLNAME:MASTER%の男性器は膨張し、
	;渦を巻くような強烈な射精感がこみ上げてくる…
	;=================================================
	;調教対象がふたなり
	;=================================================
	IF 口上記入チェック(0) && TALENT:MASTER:ふたなり
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%はＧスポットを指でえぐられる感覚に
	;髪を振り乱して悶えた…
	;=================================================
	;調教対象にペニス無し
	;=================================================
	IF 口上記入チェック(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生2, バイブ出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文は調教対象がふたなりか否かで分岐
;・REACT分類による分岐はなく、内容は汎用的なもの
;-------------------------------------------------
;とりあえず最小限の構成にしておきます。必要に応じて分岐を追加して下さい
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;膣内から前立腺をこねくり回される感覚に%CALLNAME:MASTER%の男性器は膨張し、
	;渦を巻くような強烈な射精感がこみ上げてくる…
	;=================================================
	;調教対象がふたなり
	;=================================================
	IF 口上記入チェック(0) && TALENT:MASTER:ふたなり
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%はＧスポットをバイブにえぐられる感覚に
	;髪を振り乱して悶えた…
	;=================================================
	;調教対象にペニス無し
	;=================================================
	IF 口上記入チェック(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;快感を受け入れる
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;快感を我慢する
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;────────────────────────────────────
;REACT27, 押し倒す・個別リアクション
;────────────────────────────────────
;TFLAG:ACT派生（=0,押し倒す/=1,押し倒し終了）
;-------------------------------------------------
;想定されるCOM
;会話系反応
;	COM:気弱く応答する/無愛想に応答する/愛想よく応答する/強気に応答する/許しを乞う/気持ち良くして
;苦痛系反応
;	COM:怯える
;-------------------------------------------------
@KOJO_REACT_K1_27
;-------------------------------------------------
;地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;ACT派生毎の分岐はなしかつ、内容的にはACT派生0のもの
;ACT派生1の場合は整合性が崩れると思われる
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 押し倒す
;━━━━━━━━━━━━━━━━━━━━━━━━
;この箇所は個別のCOM毎に記述するには適さないと思われるので
;REACT分類で分けておきます
;-------------------------------------------------
IF 口上記入チェック(100) && !TFLAG:ACT派生
	;=================================================
	;REACT分類受け入れる
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("受け入れる")
		PRINTFORMW 「抱き%RAND_SPLIT("つか/締め")%ないでよ…%RAND_SPLIT("恥ずかしいじゃない/苦しいでしょ/動けないじゃない")%」
		PRINTFORMW 「…%RAND_SPLIT("少し/ちょっと")%だけ%RAND_SPLIT("だからね/なんだからね？")%」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT分類積極的従う/平然と従う
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("積極的従う/平然と従う")
		IF AFFECTION("相思相愛", TARGET)
			PRINTFORMW 「きゃ…」
			PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%を引き倒した
			PRINTFORMDW %CALLNAME:TARGET%はされるがままに%CALLNAME:MASTER%の胸の中に納まり硬直している
			PRINTFORML （あっ）
			PRINTFORM 「～～
			CALL HEARTBW
			PRINTFORMW 「……何すんのよ」
			PRINTFORMDW %CALLNAME:TARGET%は存分に%CALLNAME:MASTER%の%RAND_SPLIT("体温を交換して/腕の中を十分に堪能して/腕の中を楽しんで")%から抗議してきた
		ELSE
			PRINTFORMW 「きゃ…」
			PRINTFORMDW %CALLNAME:MASTER%は%CALLNAME:TARGET%を引き倒した
			PRINTFORMDW %CALLNAME:TARGET%はされるがままに%CALLNAME:MASTER%の胸の中に納まり硬直している
			PRINTFORMW 「……何すんのよ」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;REACT分類許しを乞う
	;=================================================
	IF 口上記入チェック(100) && (IS_COMGRONAME("許しを乞う") || IS_NOWCOMNAME("怯える"))
		[SKIPSTART]
		;描写が淡泊すぎるのでコメントアウト
		;PRINTFORMW 「なんか…興奮してきたわ」
		;COM許しを乞うではなくREACT分類許しを乞う系。無言で怯える際に矛盾。コメントアウト
		;PRINTFORMW 「%RAND_SPLIT("涙声になっ/慌て")%ちゃって♪」
		[SKIPEND]
		[SKIPSTART]
		;描写の調整につきコメントアウト
		;押し倒す→怯える時、「観念して」が被るのでコメントアウト
		PRINTFORMW 「そんなに怯えなくても、何も取って食おうって訳じゃ…」
		PRINTFORMW 「無くもなかったわ」
		PRINTFORMW 「さあ！観念して食べられちゃいなさい！」
		[SKIPEND]
		PRINTFORMW 「そんなに怯えなくても、何も取って食おうって訳じゃ…」
		CALL FADEIN(8, "「無くもなかったわね」")
		RETURN 1
	ENDIF
;=================================================
	;REACT分類消極的従う
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("消極的従う")
		PRINTFORMW 「本当にやめて欲しい？それとも、やめて欲しくない？」
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生0, 押し倒し終了
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生1,押し倒しと処理が共通なのでちょっとアレな感じの箇所
;何もせずにRETURN 1だけ返すのもアリな気もします
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	RETURN 1
ENDIF
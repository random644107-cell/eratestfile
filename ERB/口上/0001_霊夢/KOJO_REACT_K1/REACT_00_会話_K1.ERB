;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 会話系個別リアクション口上ファイル
;
;	会話系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;会話系 REACT0～9
;========================================================================
;・1231版よりTFLAG:REACT負荷が導入され、選択したCOMが自然な行動かどうかが参照できるように
;・0127版以降のリアクション後の地の文は、基本的にREACT負荷で分岐するようになっています
;・TFLAG:REACT負荷(0=自然な行動/1=無理のない行動/2=無理をした行動)
;-------------------------------------------------
;　会話系はあまり汎用処理には向かないので、汎用リアクション(COM)に流さず個別リアクション(REACT)
;　で完結させてしまうのが望ましいと考えます
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 今の気分を聞く・個別リアクション
;────────────────────────────────────
;・ACT派生への代入は調教対象のリアクションに応じて@ACTION_APPLY2_0にて行われる
;-------------------------------------------------
@KOJO_REACT_K1_0
;-------------------------------------------------
;・TFLAG:ACT派生(0=歯切れ悪く答えた/1=勇気を出して強く当たった/2=皮肉/3=はっきりと今の気持ちを告げた
;・4=まあまあ悪くはない/5=気持ち良いことしたい)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,0 歯切れ悪く質問に答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が消極的従うで、リアクションが皮肉を言う以外の場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 0
	;自然な行動
	;=================================================
	;REACT負荷0, 質問にはっきり答えることができなかった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 歯切れ悪く質問に答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 出掛かった言葉を飲み込み答えを濁した
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「自分でもよく解ってないのかしら」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,1 勇気を出して強く当たった
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが強気に応答するの場合
;REACT分類は"拒否"にセットされる
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;自然な行動
	;=================================================
	;REACT負荷0, 厳しい言葉を浴びせた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 勇気を出して強く当たった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 目を逸らし口では強気に振る舞った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「威勢がいいわね」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,2 皮肉「今の気分は、と聞かれても」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが皮肉を言うの場合
;REACT分類は"拒否"にセットされる
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
	;自然な行動
	;=================================================
	;REACT負荷0, 余裕の笑みで肩をすくめ「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 少し考えてから「今の気分は、と聞かれても」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, かろうじて皮肉交じりに答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「まだまだ余裕がありそうね」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,3 はっきりと今の気持ちを告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 3
	;=================================================
	;妊娠 逆レイプで子供出来ちゃってどんな気持ちどんな気持ち もうすぐ生まれそう
	;=================================================
	IF 口上記入チェック(0) && TALENT:妊娠
		RETURN 1
	ENDIF
	;自然な行動
	;=================================================
	;REACT負荷0, 一緒に居られることが嬉しいと素直に口にした
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "自然な行動"
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「ふーん」
				PRINTFORML
				PRINTFORMW 「…おゆはん。期待していいわよ」
			CASEELSE
				PRINTFORMW 「私もよ」
				PRINTFORMW 「ずっと先の事だけど…私がおばあちゃんになっても、その言葉が聞きたいわ」
			ENDSELECT
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, はっきりと今の気持ちを告げた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 「ほんと？」
		PRINTFORMDW %CALLNAME:TARGET%は期待の籠った目で%CALLNAME:MASTER%を見つめている
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 機嫌を損ねないよう、取り繕って答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 「無理してるでしょ」
		PRINTFORML
		PRINTFORMW 「言霊ってあるのよ」
		PRINTFORMW 「その言葉、本当にしてあげるわ」
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,4 まあまあ悪くはない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が平然と従うの場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 4
	;自然な行動
	;=================================================
	;REACT負荷0, そう悪く思ってはいないと答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "自然な行動"
		;%CALLNAME:MASTER%が調教される時、ご主人様もまた調教されている。%CALLNAME:TARGET%さんが無理をした積極的行動
		IF AFFECTION("溺愛")
			CALL FADEOUT(8, "「そ、そう…」")
			PRINTFORML 「処で、良いお茶菓子があるんだけど…」
			CALL FADEOUT(8, "そわそわ")
		PRINTFORMDW %CALLNAME:TARGET%は唐突にお茶菓子を勧めてきた
			
		ELSE
		ENDIF
		PRINTFORMW 「肯定的に捉えておくわ」
		PRINTFORMW 「…お茶菓子、あるわよ？」
		PRINTFORMDW %CALLNAME:TARGET%は唐突にお茶菓子を勧めてきた
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「まあまあ悪くはない」と言った
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 「はっきりしないわねー」
		;ここに顔面騎乗分岐を作ろう
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 無理をした行動
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理をした行動"
		[SKIPSTART]
		コメントアウトしておきます@/L
		;実は積極			視線を外し、悪くはないよと呟いた
		IF COMCOR_POSI()
		;実は嫌				精一杯の強がりで、まあまあ悪くないと答えた
		ELSE
			PRINTFORMW 
		ENDIF
		[SKIPEND]
		PRINTFORMW 「そんな事言って、無理してるでしょ」
		PRINTFORMW 「私には素直になれない？」
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,5 気持ち良いことしたい
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が受け入れるの場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 5
	;自然な行動
	;=================================================
	;REACT負荷0, もっと気持ち良くなりたいと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 気持ち良いことしたい気分だと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 媚びるように敢えて淫らな行為をねだった
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「気持ちの良いことって、その…そういう意味よね…」
		PRINTFORMW 「な、なにってその、えっちな…その…」
		PRINTFORML
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:TARGET%で煮え切らないようだ
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT分類に属さないCOM毎の分岐
;-------------------------------------------------
[SKIPSTART]
通常の場合リアクションの地の文は@TRAIN_MESSAGE2と@REACTION_MESSAGEで処理されますが
以下の部分は@TRAIN_MESSAGE2の条件から漏れる為、@REACTION_MESSAGEでのみ処理されます
COM分岐に於いても当然TFLAG:REACT派生は機能しますし、@REACTION_MESSAGE内でもREACT負荷毎に書き分けられています
SELECTCASE GET_COM_RANK()
	;自然な行動
	CASE "自然な行動"
		PRINTFORMW 
	;無理のない行動
	CASE "無理のない行動"
		PRINTFORMW 
	;無理をした行動
	CASE "無理をした行動"
		;実はまんざらでもない
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
上記のような分岐を設ける事によって、本体側の変化に対応する事は可能ですが
テンプレに於いてデフォルトで設けるには手間がかかり過ぎると考える故に、基本的にREACT内のCOM分岐には枠を設けない方針です
汎用的な内容にしてもよし、また部分的に汎用リアクション(COM)に流しても良いと思います
まあREACT負荷以外にも拡張する方法はいくらでもあったりするので、その辺りの判断は各口上作者に委ねたいと思います
3BM/4MBでも書くから兎に角考えられる要素を全部入れてくれ！的な要望がもしあれば、その時はまた考えたいなと
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
;今の気分を聞くでのCOM沈黙するはTFLAG:REACT派生 == 1(質問に答えない)扱いとなりお仕置きポイントが加算される
;-------------------------------------------------
IF 口上記入チェック(100) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 「………」
	PRINTFORMDL %CALLNAME:MASTER%が沈黙を続けると%CALLNAME:TARGET%は責めるでもなく%CALLNAME:MASTER%の目を見つめてきた
	PRINTFORMDW まるで、心の奥底まで覗き込まれているかのような心地になってくる
	PRINTFORML
	PRINTFORMW 「…黙っていると、苦しくなるわよ？」
	PRINTFORMW 「ま、いいわ」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;記入箇所ミス。駆け足で書いていた時期の名残り。
		;PRINTFORMW 「駄目よ。大人しく退治されなさい」
		PRINTFORMW 「%RAND_SPLIT("駄目よ。大人しく答えて頂戴/教えて/ね。聞かせて？")%」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「人を妖怪みたいに…」
		PRINTFORMDW 妖怪よりも怖いと言いたくなったが、それは言わない方がいいだろう
		RETURN 1
	ENDIF
ENDIF
;REACT0, 今の気分を聞く

;────────────────────────────────────
;REACT1, 自分への気持ちを聞く・個別リアクション
;────────────────────────────────────
;・ACT派生への代入は調教対象のリアクションに応じて@ACTION_APPLY2_1にて行われる
;-------------------------------------------------
@KOJO_REACT_K1_1
;-------------------------------------------------
;・TFLAG:ACT派生(0=嫌いじゃない…かも知れない…/1=そんなことあるわけない！/2=困るねぇ…好きじゃないといったら怒る？
;・3=もう酷いことしなければ、もっと好きになれるかもしれない/4=はっきりと好きと告げた?5=もっとすごいことしてくれば、もっと好きになれるかも)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「嫌いじゃない…かも知れない…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が消極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 0
	;自然な行動
	;=================================================
	;REACT負荷0, どうしても嫌いとは言えなかった
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 「私は好き」
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 「私は好き」
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 本心を隠すように目を伏せ、「嫌いじゃない…かも知れない…」と答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 「うん」
		PRINTFORMW 「その続きが分かったら、聞かせてもらうわ」
		PRINTFORMDW %CALLNAME:TARGET%は思いの他、%CALLNAME:MASTER%の返答に満足そうだ
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「そんなことあるわけない！」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが強気に応答するの場合
;REACT分類は"拒否"にセットされる
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;自然な行動
	;=================================================
	;REACT負荷0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 「私は好き」
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「ふーん」
		PRINTFORMW 「今はそれでもいいわ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 皮肉「困るねぇ…好きじゃないといったら怒る？」
;━━━━━━━━━━━━━━━━━━━━━━━━
;リアクションが皮肉を言うの場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 2
	;自然な行動
	;=================================================
	;REACT負荷0, 「好きになれるわけがない！」と強く答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "自然な行動"
		;すっとぼけた防御崩し
		PRINTFORML 「わかったわ」
		PRINTFORMDW %CALLNAME:TARGET%は目を伏せ
		PRINTFORMW 「羊羹、私の分もあげるわよ」
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「そんなことあるわけない！」と言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 「私は好き」
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「うーん…まだまだ余裕がありそうねぇ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「もう酷いことしなければ、もっと好きになれるかもしれない」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うで、調教対象好意が少ないか、REACT分類が平然と従う
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 3
	;自然な行動
	;=================================================
	;REACT負荷0, 「もっと好きになりたいから、酷いことはしないで」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「もう酷いことしなければ、好きになれるかもしれない」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 機嫌を伺うように視線を向けた後、「酷いことばかりする人は嫌い」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「そんな事言ったって駄目よ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, はっきり好きだと告げた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うで、調教対象好意が1000以上
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 4
	;負荷2は弾いた方が何かと都合がよさそうなのでとりあえず
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTREACT(4) && TFLAG:REACT負荷 < 2
		;かみしめるように長く４から１２へ変更
		CALL FADEOUT(12, "「うん」")
		;短いフェードインでキッパリとを表現できないものか
		CALL FADEIN(2, "「私も好き」")
		RETURN 1
	ENDIF
	
	;=================================================
	;妊娠 もうすぐ生まれそう
	;=================================================
	IF 口上記入チェック(100) && TALENT:妊娠 && CFLAG:妊娠 >= 15
		PRINTFORMDW %CALLNAME:MASTER%は妊娠している%CALLNAME:TARGET%が一番かわいいと答えた
		RETURN 1
	ENDIF
	;自然な行動
	;=================================================
	;REACT負荷0, 愛想よく笑って大好きだと答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "自然な行動"
		SELECTCASE RAND:2
			CASE 0
				CALL FADEOUT(4, "「むぅ……」")
				PRINTFORMDW %CALLNAME:TARGET%はムスッとして顔をそらしてしまった
				PRINTFORMW 「なんかずるいわ。それ反則よ」
			CASEELSE
				PRINTFORML 「なんだか…ずるいわね…」
				CALL FADEIN(8, "ごにょごにょ…")
		ENDSELECT
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, はっきりと好きと告げた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理のない行動"
		;あえて選択するところに意義を感じるので自然な行動より上位と解釈
		CALL FADEOUT(4, "「ふーん…」")
		PRINTFORMDW %CALLNAME:TARGET%は真っ赤になってしまった
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 作り笑いを浮かべて、好きだよと答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 「表情が硬いわよ」
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の頬をつん！とつついた
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		;上部を埋めたので死に分岐。だがそれがいい
		PRINTFORMW 「どこが好き？」
		PRINTFORMDW 全体の雰囲気
		PRINTFORML
		PRINTFORMDW 強いて挙げれば、腋
		PRINTFORMDW %CALLNAME:TARGET%様、大不満
		PRINTFORML
		PRINTFORMDW 腋と横乳と黒髪とお尻とお茶とホーミング性能
		PRINTFORMD %CALLNAME:TARGET%は
		PRINTFORM 「まあそれなら許す」
		PRINTFORMDW と頷いた
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「もっとすごいことしてくれれば、もっと好きになれるかも」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が受け入れるの場合
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 5
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(5)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;自然な行動
	;=================================================
	;REACT負荷0, 「もっとすごいことしてくれば、もっと好きになれるかも」と答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「もっと好きになるように、もっと可愛がって欲しい」と答えた
	;=================================================
	IF 口上記入チェック(100) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 「そう。良い子ね」
		PRINTFORMW なでなで
		IF IS_CONDITION(TARGET, "情欲")
		ENDIF
		PRINTFORMW 「もっと？しょうがないわね」
		;言わない方が良いかもしれないのでコメントアウト
		;PRINTFORMW 「ま、いいわ。素直なあんたに免じてあげる」
		PRINTFORMW なでなで
		PRINTFORMW なでなで
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 「好きになれるくらい、すごいことしてくれればいい」と答えた
	;=================================================
	[SKIPSTART]
	PRINTFORML 「凄い事って何？正直者の死大回転？それならできるわよ」
	PRINTFORMDW 本気で凄い霊夢さんに%CALLNAME:MASTER%の気力は大きく削がれ屈服しそうになった…
	[SKIPEND]
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「すごいこと…」
		PRINTFORMDW %CALLNAME:TARGET%は『すごいこと』について思案し、考えるのをやめた
		PRINTFORMW 「いつもどおり、勘で、なんとかなるわよね」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT分類に属さないCOM毎の分岐
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
;自分への気持ちを聞くの場合はTFLAG:REACT派生 == 1(質問に答えない)扱いとなりお仕置きポイントが加算される
;-------------------------------------------------
IF 口上記入チェック(100) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 「堅く守るとかえって破りやすいものよ」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「駄目よ、ほら教えなさい」
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔を覗き込んだ
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「怖がらなくていいわよ」
		PRINTFORMW 「怖がったってやることは一緒なんだから」
		RETURN 1
	ENDIF
ENDIF
;REACT1, 自分への気持ちを聞く

;────────────────────────────────────
;REACT2, 性についての話をする・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K1_2
;-------------------------------------------------
;　派生する可能性のあるCOMはとりあえずREACT派生分岐を設けていますが
;　&& TFLAG:REACT派生 < 1などとして汎用リアクションで派生を拾う手もあります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 「ここで黙らないでよ…恥ずかしくなってきたわ…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめてと言った
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 0
		;PRINTFORMDW %CALLNAME:MASTER%は「お、おう…」と答えた
		PRINTFORMW 「何よその、気のない返事」
		PRINTFORMW 「ひょっとして私、何か間違ったかしら…」
		;PRINTFORMDW ソースは？
		;PRINTFORMW 「紫」
		;PRINTFORMDW 胡散臭い事この上なかった
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「こういう話、苦手だったかしら？」
		[SKIPSTART]
		;入手ソースの確認が必要
		PRINTFORML 
		PRINTFORMDW 霊夢は性的でない話に切り替えてくれた
		PRINTFORMDW %CALLNAME:TARGET%はチルノの話題に切り替えてきた…
		PRINTFORMDW 霊夢はいいお母さんになると%CALLNAME:MASTER%は心の中で確信した
		PRINTFORMDW %CALLNAME:TARGET%は小鈴の話題に切り替えてきた…
		PRINTFORMDW 霊夢はいい保護者になると%CALLNAME:MASTER%は心の中で確信した
		[SKIPEND]
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 気持ち良くして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良くして")
	;=================================================
	;お仕置きポイント加算 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「今の話、実践%RAND_SPLIT("/したいのね/したいんだ/したいの")%？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("恥ずかしがる")
	;TEQUIPによって快SOURCEが入っている場合に派生
	;=================================================
	;REACT派生1, 快楽に恥ずかしがる
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 状況に恥ずかしがる
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 0
		PRINTFORML 「あんたの奥手なトコ、嫌いじゃないわよ？でもね」
		PRINTFORMW 「それじゃ話が進まないの」
		PRINTFORMW 「私はね。あんたとの関係を進めたいのよ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「話の通りにされるの、想像したかしら？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 自慰し始める
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("自慰し始める")
	;=================================================
	;REACT派生1, 「勝手にオナニーするな」
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		PRINTFORMDW %CALLNAME:MASTER%はえっちな言葉を紡ぐ%CALLNAME:TARGET%をオカズに陰茎を扱いた…
		PRINTFORMW 「なに%RAND_SPLIT("/勝手にしこしこ/勝手にぬきぬき/自慰")%してるのよ」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 「勝手にオナニーしてるけどいいか」
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 0
		PRINTFORMW 「人の話を聞きながら何してるのよあんた」
		RETURN 1
	ENDIF
ENDIF
;REACT2, 性についての話をする

;────────────────────────────────────
;REACT3, 優しく慰める・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_K1_3
;-------------------------------------------------
;　派生する可能性のあるCOMにはREACT派生分岐を設けていますが
;　&& TFLAG:REACT派生 < 1などとして汎用リアクションで派生を拾う手もあります
;-------------------------------------------------
;　とりあえず会話系COMを全て網羅してありますが
;　10番台の愛撫系COM、50番台以降のその他COM等も状況によっては選択可能になります
;　必要に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMDW %CALLNAME:TARGET%は責めるでもなく、%CALLNAME:MASTER%の顔を覗き込んでいる
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		PRINTFORMW 「それは駄目…もうちょっとしたらやめてあげるわ」
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 0
		PRINTFORMW 「うん」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 無愛想に応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("無愛想に応答する")
		PRINTFORMW 「うん」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 愛想良く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛想よく応答する")
	PRINTFORMW 「元気が出たかしら？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 強気に応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("強気に応答する")
	PRINTFORMW 「うーん…何か間違えたかしら」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 「元気、出たかしら？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 「ふーん」
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 0
		PRINTFORMW 「元気出たかしら？」
		
		PRINTFORML 「ふーん。乱暴されたいんだ」
		IF TALENT:サド
			PRINTFORMD %CALLNAME:TARGET%は
			PRINTFORM それもいいかしら
			PRINTFORMDL と呟いた…
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		PRINTFORMW 「素直に受け止めてもらえては、いないみたいね…」
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「まだ駄目」
		PRINTFORMW 「…もうちょっとしたら、やめてあげるわ」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 気持ち良くして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気持ち良くして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「いいわよ」
		RETURN 1
	ENDIF
ENDIF
;REACT3, 優しく慰める

;────────────────────────────────────
;REACT4, 厳しく脅す・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理無し。SOURCE_MESSAGEでの汎用処理のみ
;-------------------------------------------------
@KOJO_REACT_K1_4
;-------------------------------------------------
;%CALLNAME:MASTER%をピチュらせたら[点]アイテムになるかしら？
;エクステンドアイテムです（強気）
;[Ｐ]になります（強気）うっさい[P]
;1[P]になります（弱気）あんたと私は2[P]の関係よ異論は認めないわ
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 「…少しは堪えたかしら」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 0
		PRINTFORMW 「…少しは堪えたかしら」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("皮肉を言う")
		PRINTFORMW 「…まだまだ余裕がありそうね？」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「許してほしい？」
		PRINTFORMDW %CALLNAME:TARGET%は満面の不気味な笑みを浮かべている
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「今にも泣きそうな顔して…そんな表情しても、許してあげないわよ」
		RETURN 1
	ENDIF
ENDIF
;REACT4, 厳しく脅す

;────────────────────────────────────
;REACT5, 衣装の変更を要求する・個別リアクション
;────────────────────────────────────
;・調教対象のリアクションによって一枚脱ぐ、二枚脱ぐ、脱がないに分かれる
;・REACT分類が"消極的従う", "受け入れる"の場合は一枚脱ぐ
;・REACT分類が"積極的従う", "平然と従う"の場合は二枚脱ぐ
;・その他の場合は脱がない(PALAM恥情/反抗の数値によって、恥ずかしくて脱がない、嫌がって脱がないに分かれる)
;-------------------------------------------------
@KOJO_REACT_K1_5
;-------------------------------------------------
;・TFLAG:ACT派生(0=服を脱ぐように命令/1=服を着るように命令)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 服を脱ぐように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 0
	;=================================================
	;一枚脱いだ(消極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う/受け入れる")
		;-------------------------------------------------
		;最後の一枚を脱いで全裸になった
		;-------------------------------------------------
		IF 口上記入チェック(0) && (TEQUIP:MASTER:アクセサリ + TEQUIP:MASTER:靴下 + TEQUIP:MASTER:下半身下着 + TEQUIP:MASTER:上半身下着 + TEQUIP:MASTER:下半身上着 + TEQUIP:MASTER:上半身上着 + TEQUIP:MASTER:全身上着) == 0
			;REACT分類受け入れる			全裸になり、期待と興奮で肌が火照るのを感じた
			IF IS_COMGRONAME("受け入れる")
				PRINTFORMW 
			;REACT分類消極的従う			仕方なく最後の一枚を脱ぎ、全裸になった
			ELSE
				PRINTFORMW 
			ENDIF
			[SKIPSTART]
			;勃起していない場合。皮
			SIF TCVAR:MASTER:勃起度 < 1000
				PRINTFORMW 
			[SKIPEND]
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;緩やかな動きで服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("受け入れる")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;仕方なく服を一枚脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;その他・条件なし
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;二枚脱いだ(積極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		;-------------------------------------------------
		;全裸になった
		;-------------------------------------------------
		IF 口上記入チェック(0) && (TEQUIP:MASTER:アクセサリ + TEQUIP:MASTER:靴下 + TEQUIP:MASTER:下半身下着 + TEQUIP:MASTER:上半身下着 + TEQUIP:MASTER:下半身上着 + TEQUIP:MASTER:上半身上着 + TEQUIP:MASTER:全身上着) == 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;積極的に脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;手際よく脱ぎ捨てた
		;-------------------------------------------------
		IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;その他・条件なし
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;脱がなかった
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("拒否")
		;恥ずかしくて脱げなかった
		IF PALAM:MASTER:恥情 > PALAM:MASTER:反抗
			PRINTFORMW 
		;嫌がって脱がなかった
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;一枚着た(消極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う/受け入れる")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;二枚着た(積極的反応)
	;=================================================
	IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う/平然と従う")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;地の文では"嫌がって"という扱い
	;=================================================
	;着なかった
	;=================================================
	IF 口上記入チェック(100) && IS_COMGRONAME("拒否")
		PRINTFORMW 「そんなに嫌がっても風邪ひかれて看病するのは、私なのよ」
		PRINTFORMW 「無理矢理着せるわ」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 衣装の変更を要求する

;────────────────────────────────────
;REACT6, 合意を求める・個別リアクション
;────────────────────────────────────
;・調教対象のリアクションに応じてREACT分類毎に派生する
;-------------------------------------------------
@KOJO_REACT_K1_6
;-------------------------------------------------
;手間を厭わないのであれば、TALENT:MASTER:オトコ等で内部で分けるのもアリかもしれません
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 小さい声で了承する
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が消極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("消極的従う")
	;自然な行動
	;=================================================
	;REACT負荷0, 覚悟を決めたように、小さい声で了承した
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷0, 小さい声で了承した	
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		;実は積極			恥ずかしさに消え入りそうな声で了承した
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				悔しさに顔を歪めながらも了承した
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 好きにすればいいと答えた
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が平然と従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("平然と従う")
	;自然な行動
	;=================================================
	;REACT負荷0, 別に嫌じゃないと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 好きにすればいいと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		;実は積極			したいならいいと言った
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				勝手にすればいいと答えた
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 意を決して捧げてもいいと告げる
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が積極的従うの場合
;-------------------------------------------------
IF 口上記入チェック(0) && IS_COMGRONAME("積極的従う")
	;自然な行動
	;=================================================
	;REACT負荷0, 初めてをあげられて嬉しいと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 意を決して捧げてもいいと告げた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, もう守れないならばせめて優しくされるようにと同意した
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 気持ち良くしてくれるんだったら初めてをあげてもいい
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類が受け入れるの場合
;-------------------------------------------------
IF 口上記入チェック(100) && IS_COMGRONAME("受け入れる")
	;自然な行動
	;=================================================
	;REACT負荷0, 初めてを捧げるから気持ち良くして欲しいと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 気持ち良くしてくれるんだったら初めてをあげてもいいと答えた
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 初めてを捧げろと言うなら、辛さを忘れられるくらい気持ち良くしてと言った
	;=================================================
	IF 口上記入チェック(0) && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 合意しない
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT分類がその他の場合(その他のREACT分類の場合、@ACTION_APPLY2_6にて"拒否"にセットされる)
;-------------------------------------------------
IF 口上記入チェック(100) && IS_COMGRONAME("拒否")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTREACT(4)
		PRINTFORMW 「いいわよ。あんたの覚悟が決まるまで、もう少しだけ待ってあげる」
		PRINTFORMW 「…先に、私が我慢できなくなったら」
		PRINTFORMW 「その時は無理矢理、干乾びるまで犯すから」
		PRINTFORMW 「あんまり待たせないでよ？」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「私は%CALLNAME:MASTER%が欲しいわ」
		PRINTFORMDW %CALLNAME:TARGET%は婚姻届を突き付けてきた
		RETURN 1
	ENDIF
ENDIF
;REACT6, 合意を求める

;────────────────────────────────────
;REACT7, お仕置きと宣言する・個別リアクション
;────────────────────────────────────
;・宣言してもお仕置きが発生するとは限らない
;-------------------------------------------------
@KOJO_REACT_K1_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"気弱く応答する"は一定条件でREACT分類"許しを乞う"に派生する
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめてと言った
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生0, 「ふーん」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	;少女的に考えて対応できないというのも有りな気がしてきた。
	CALL FADEOUT(4, "「えっ」")
	CALL FADEIN(8, "「もっと？」")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う") 
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		;お仕置き中の言動との擦り合わせが必要
		PRINTFORMW 「さあ覚悟しなさい」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	;キャー%CALLNAME:TARGET%さん素敵抱いて！な決め台詞をこう。ビシっと
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT7, お仕置きと宣言する

;────────────────────────────────────
;REACT8, 怒鳴る・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K1_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 怒鳴る

;────────────────────────────────────
;REACT9, 不気味に笑う・個別リアクション
;────────────────────────────────────
;・TRAIN_MESSAGE2の処理なし
;-------------------------------------------------
@KOJO_REACT_K1_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「うふ、うふ、うふふふふふふふ…」
		RETURN 1
	ENDIF
ENDIF

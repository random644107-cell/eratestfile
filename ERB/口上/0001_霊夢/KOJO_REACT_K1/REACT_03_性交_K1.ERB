;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 性交系個別リアクション口上ファイル
;
;	性交系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;性交系 REACT30～35
;========================================================================
;・各REACT内の分け方としてTFLAG:REACT分類(IS_COMGRONAME)やSELECTCOM(IS_NOWCOMNAME)が考えられます
;・ただ、範囲の広い条件で拾ってしまうのは基本的にお勧めしません
;・ACT分類:性交系の場合、基本的に愛撫系COM(COM10~COM16)、性交系COM(COM20~COM22)、苦痛系COM(COM30~COM33)が選択肢となりますが
;・状況によってはその他のものが選択肢に含まれる可能性もあります
;・リアクション(REACT)で処理されなかったものは汎用リアクション(COM)で補完されるので
;・無理に全て埋める必要はありません
;・必要と思うものを絞り込み、COMとの連携を考えて効率よく構築すべきです
;-------------------------------------------------
;・各REACT内は、&& IS_NOWCOMNAMEなり、&& IS_COMGRONAME等の条件で決めうちしましょう
;・ここに条件無し分岐があると、リアクションに関わらず、全て拾ってしまって汎用リアクションに流れません
;・調教対象の反応に関わらず、同じリアクションしかしなくなってしまいます
;・要するにこれは、想定されたシステムの破壊に繋がります
;-------------------------------------------------
;　REACTの地の文はACT_MESSAGE.ERBのTRAIN_MESSAGE2_{TFLAG:ACT}で処理されます
;　基本的に地の文はTFLAG:REACT分類(受け入れる/積極的従う/平然と従う/許しを乞う/消極的従う)で分岐し、
;　各REACT分類内でTFLAG:REACT負荷(自然な行動/無理のない行動/無理をした行動)で派生
;　固有のTRAIN_MESSAGE2がない場合は、REACTION_MESSAGEでCOM毎に汎用処理されます
;　ACT_MESSAGE.ERB REACTION_MESSAGE.ERBを参照しながらの作業をお勧めします
;-------------------------------------------------
;各REACTでは想定される反応を網羅してあります。必要に応じて分岐を増減して使用して下さい
;COMによってはREACT派生でREACT分類が変化する事に注意
;各COMの派生は基本的にREACT派生1を条件決めうちの小分岐にし、REACT派生0はその他扱いで条件なしとしています
;手間を掛けたくない場合はその他のみを書いて、派生に関わらず当該箇所を補完する事ができます
;またIF 口上記入チェック(0) && TFLAG:REACT派生 < 1とする事で派生した場合は汎用リアクションへ流すなどという事も可能です
;派生は将来増える可能性もあるので != 1などとするよりは < 1とした方が無難であると考えます
;-------------------------------------------------
;性交系のACT派生はACT開始時のシチュエーションの違いを表すものであり
;内容が大きく異なるものではない。という訳でREACTに於いてはACT派生で分けない方針
;とはいえ、派生で分けてみるのも試みとしては面白いかも知れません
;-------------------------------------------------

;────────────────────────────────────
;REACT30, 正常位・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続実行/2=他のＶ性交から/3=Ａ性交から/4=Ｖ性交奉仕から/5=Ａ性交奉仕から)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:もっとハードにして
;愛撫系反応
;	COM:嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る/中に出して！/中に出さないで！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K1_30
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う. 平然と従う/許しを乞う/消極的従う)で分岐
;・キャンセル処理はACT_MESSAGE_OTHERSにて汎用処理
;・主導権は調教者(SEX_INITIATIVE(TARGET))
;-------------------------------------------------

;MASTERが処女なら、処女喪失が起こる。この処理は@ACTION_APPLY2_30にて行われる
;REACT分類が暴れるでキャンセル処理が呼び出された際は処女喪失、童貞喪失は回避
;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失/童貞喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TRUECHECK(TCVAR:MASTER:処女喪失, TCVAR:童貞喪失) > 0
	;=================================================
	;調教対象が処女喪失＆調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失 && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/TEQUIP:調教者ペニスバンドなどで書き分けても良いでしょう
	;=================================================
	;調教対象が処女喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;「この恥知らずの豚め！」(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生1の場合はREACT分類:暴れる扱い
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生1の場合はREACT分類:許しを乞う扱い
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
;主導権によってREACT分類が変化する事に注意
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 中に出して！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("中に出して！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 中に出さないで！正常位
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出さないで！")
	;=================================================
	;中に出す(REACT分類:なし)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		SELECTCASE RAND:10
			CASE 0
				;和姦
				PRINTFORMW 「それならこの足は何よ？」
				PRINTFORMW 「がっちりと組みついてくれちゃって、可愛いわよ」
			CASE 1
				;両親のいない%CALLNAME:TARGET%さんは無条件に受け入れて欲しい
				PRINTFORMW 「ごめんね？中に出すから、受け入れて」
			CASE 2
				;支配欲強め
				PRINTFORMW 「駄目って言われてやめるなら、こんな事初めっからしないわよ」
			CASE 3
				;妊娠目的
				PRINTFORMW 「外に出したらできないじゃない」
				PRINTFORMW 「いいからあんたは、孕みなさい」
			CASE 4
				;ばっさり
				PRINTFORMW 「駄目」
			CASE 5
				;無理矢理
				PRINTFORMW 「それっておねだりよね？いいわよ」
				PRINTFORMDW %CALLNAME:TARGET%の動きが激しさを増す
			CASE 6
				;言葉責め
				PRINTFORMW 「そんな事言って、本当は中に出されたいのよね？無理矢理されたいのよね？」
				CALL FADEOUT(8, "くす")
				PRINTFORMW 「ほら、こうすると」
				PRINTFORMDW %CALLNAME:TARGET%が%CALLNAME:MASTER%に圧し掛かかってくる
				PRINTFORML
				PRINTFORMW 「きゅぅっ！と締め付けてくるわよ？」
				PRINTFORMW 「本当は、出されたいのよね？」
			CASE 7
				PRINTFORM 「%RAND_SPLIT("……/可愛いんだから…/ええ")%
				CALL HEARTBW
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の話を聞いているのか、怪しい返事をした
			CASE 8
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の口を口で塞ぐと、必死に腰を打ち付けた
				PRINTFORMDW %CALLNAME:MASTER%の言葉は%CALLNAME:TARGET%の口に飲み込まれていった
			CASEELSE
				;言葉責め無理矢理
				PRINTFORMW 「そんな事言って、あんたの目は出してって言ってるんだけど」
				CALL FADEOUT(20, "まあいいわ")
				PRINTFORMW 「%RAND_SPLIT("本音/腹の内")%はあんたのお腹に聞けばいいわね」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「中に出さない」(REACT分類:許しを乞う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT30, 正常位

;────────────────────────────────────
;REACT31, 後背位・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続実行/2=他のＶ性交から/3=Ａ性交から/4=Ｖ性交奉仕から/5=Ａ性交奉仕から)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:恥ずかしがる/嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る/中に出して！/中に出さないで！
;苦痛系反応
;	COM:暴れる
;-------------------------------------------------
@KOJO_REACT_K1_31
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う\消極的従う)で分岐
;・キャンセル処理はACT_MESSAGE_OTHERSにて汎用処理
;・主導権は調教者(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失/童貞喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTERが処女なら、処女喪失が起こる。この処理は@ACTION_APPLY2_30にて行われる
;REACT分類が暴れるでキャンセル処理が呼び出された際は処女喪失、童貞喪失は回避
;-------------------------------------------------
IF 口上記入チェック(0) && TRUECHECK(TCVAR:MASTER:処女喪失, TCVAR:童貞喪失) > 0
	;=================================================
	;調教対象が処女喪失＆調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失 && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/TEQUIP:調教者ペニスバンドなどで書き分けても良いでしょう
	;=================================================
	;調教対象が処女喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;派生1の場合はREACT分類:なし
	;=================================================
	;意見を出すなんていい度胸だね！(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 中に出して！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("中に出して！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 中に出さないで！後背位
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出さないで！")
	;=================================================
	;中に出す(REACT分類:なし)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		;性交共通部分へ移転予定
		SELECTCASE RAND:7
			CASE 0
				PRINTFORM 「%RAND_SPLIT("……/可愛いんだから…/ええ")%
				CALL HEARTBW
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の話を聞いているのか、怪しい返事をした
			CASE 1
				;両親のいない%CALLNAME:TARGET%さんは無条件に受け入れて欲しい
				PRINTFORMW 「ごめんね？中に出すから、受け入れて」
			CASE 2
				;支配欲強め
				PRINTFORMW 「駄目って言われてやめるなら、こんな事初めっからしないわよ」
			CASE 3
				;妊娠目的
				PRINTFORMW 「外に出したらできないじゃない」
				PRINTFORMW 「いいからあんたは、孕みなさい」
			CASE 4
				;ばっさり
				PRINTFORMW 「駄目」
			CASE 5
				;無理矢理
				PRINTFORMW 「それっておねだりよね？いいわよ」
				PRINTFORMDW %CALLNAME:TARGET%の動きが激しさを増す
			CASEELSE
				;言葉責め
				PRINTFORMW 「そんな事言って、本当は中に出されたいのよね？無理矢理されたいのよね？」
				CALL FADEOUT(8, "くす")
				PRINTFORMW 「ほら、こうすると」
				PRINTFORMDW %CALLNAME:TARGET%が%CALLNAME:MASTER%に圧し掛かかってくる
				PRINTFORML
				PRINTFORMW 「きゅぅっ！と締め付けてくるわよ？」
				PRINTFORMW 「本当は、出されたいのよね？」
		ENDSELECT
		RETURN 1
	ENDIF

	;派生1を書いた場合はREACT派生0,「中に出さない」(REACT分類:許しを乞う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「ええ、いいわよ。外出しもしてあげるわ」
			CASEELSE
				PRINTFORMW 「しょうがないわね…」
				PRINTFORMW 「%RAND_SPLIT("…あんた、がっかりしてない？/何、残念そうな顔してるのよ")%」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 「いじらしくて、素敵よ」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT31, 後背位

;────────────────────────────────────
;REACT32, 騎乗位・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続実行/2=他のＶ性交から/3=Ａ性交から/4=Ｖ性交奉仕から/5=Ａ性交奉仕から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:嫌がる/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る/中に出して！/中に出さないで！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;奉仕系反応
;	COM:拒否する/消極的やる/積極的やる/愛情を込めてやる/平然とやる
;-------------------------------------------------
@KOJO_REACT_K1_32
;-------------------------------------------------
;・地の文はREACT分類(暴れる, 逃げる/拒否/受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;・キャンセル処理はACT_MESSAGE_OTHERSにて汎用処理
;・他の性交系とは違い、奉仕系反応(COM40番台)も出る事に注意
;・キャンセルが成立するのは他の性交系と異なり、REACT分類が暴れる/拒否の場合である
;・主導権は調教対象(SEX_INITIATIVE(MASTER))
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失/童貞喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTERが処女なら、処女喪失が起こる。この処理は@ACTION_APPLY2_30にて行われる
;REACT分類が暴れるでキャンセル処理が呼び出された際は処女喪失、童貞喪失は回避
;-------------------------------------------------
IF 口上記入チェック(0) && TRUECHECK(TCVAR:MASTER:処女喪失, TCVAR:童貞喪失) > 0
	;=================================================
	;調教対象が処女喪失＆調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失 && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/TEQUIP:調教者ペニスバンドなどで書き分けても良いでしょう
	;=================================================
	;調教対象が処女喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「必死に我慢しても、いくまで許してあげないわよ」
		PRINTFORM 「あんたが自分から腰振っていっちゃう所、いっちゃう所見せて
		CALL HEARTBW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORM 「涎出てるわよ？%RAND_SPLIT("本当/ほんと")%、%RAND_SPLIT("だらし/しょうが/情け")%ないんだから
	CALL HEARTBW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT騎乗位の場合、REACT分類は積極的従うになる
;-------------------------------------------------
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	;ここに情欲分岐
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 中に出して！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出して！")

	PRINTFORMW 「私の子種、%RAND_SPLIT("おまんこ/子宮/お腹")%に注ぎ込んで欲しいの？」
	PRINTFORMW 「%RAND_SPLIT("そんなことしたら、赤ちゃんできちゃうけど…/妊娠したいの？")%」
	PRINTFORM 「…本気にするわよ
	CALL HEARTBW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 中に出さないで！騎乗位
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出さないで！")
	;=================================================
	;中に出す(REACT分類:なし)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		PRINTFORMW 「だったら、腰…止めなさいよ」
		PRINTFORMW 「搾り取られちゃう」
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「中に出さない」(REACT分類:許しを乞う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「しょうがないわね…」
		PRINTFORMW 「%RAND_SPLIT("…あんた、がっかりしてない？/何、残念そうな顔してるのよ")%」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 拒否する(REACT分類:拒否)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("拒否する")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 消極的やる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("消極的やる")
	;=================================================
	;消極的なのが気に入らない(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「消極的だけどいいか」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 積極的やる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("積極的やる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 愛情を込めてやる(REACT分類:積極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("愛情を込めてやる")
	PRINTFORMW 「両手、貸してなさい…こうした方が動きやすいわ」
	PRINTFORMDW %CALLNAME:TARGET%が%CALLNAME:MASTER%の手を求め、指を絡めてくる
	PRINTFORM 「%RAND_SPLIT("何回でも/何度だって")%射精してあげるから、%RAND_SPLIT("二人で気持ちよくなり/一緒に気持ちよくなり/一杯し")%ましょ
	CALL HEARTBW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 平然とやる(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("平然とやる")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT32, 騎乗位

;────────────────────────────────────
;REACT33, 対面座位・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続実行/2=他のＶ性交から/3=Ａ性交から/4=Ｖ性交奉仕から/5=Ａ性交奉仕から)
;-------------------------------------------------
;想定されるCOM…
;愛撫系反応
;	COM:嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る/中に出して！/中に出さないで！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_33
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う. 平然と従う/許しを乞う/消極的従う)で分岐
;・キャンセル処理はACT_MESSAGE_OTHERSにて汎用処理
;・主導権は調教者(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失/童貞喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTERが処女なら、処女喪失が起こる。この処理は@ACTION_APPLY2_30にて行われる
;REACT分類が暴れるでキャンセル処理が呼び出された際は処女喪失、童貞喪失は回避
;-------------------------------------------------
IF 口上記入チェック(0) && TRUECHECK(TCVAR:MASTER:処女喪失, TCVAR:童貞喪失) > 0
	;=================================================
	;調教対象が処女喪失＆調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失 && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/TEQUIP:調教者ペニスバンドなどで書き分けても良いでしょう
	;=================================================
	;調教対象が処女喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 中に出して！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("中に出して！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 中に出さないで！対面座位
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出さないで！")
	;=================================================
	;中に出す(REACT分類:なし)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		[SKIPSTART]
		;性交共通部分へ移転予定
		;体位による差異を出したいので書き直しコメントアウト
		SELECTCASE RAND:7
			CASE 0
				PRINTFORM 「%RAND_SPLIT("……/可愛いんだから…/ええ")%
				CALL HEARTBW
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の話を聞いているのか、怪しい返事をした
			CASE 1
				;両親のいない%CALLNAME:TARGET%さんは無条件に受け入れて欲しい
				PRINTFORMW 「ごめんね？中に出すから、受け入れて」
			CASE 2
				;支配欲強め
				PRINTFORMW 「駄目って言われてやめるなら、こんな事初めっからしないわよ」
			CASE 3
				;妊娠目的
				PRINTFORMW 「外に出したらできないじゃない」
				PRINTFORMW 「いいからあんたは、孕みなさい」
			CASE 4
				;ばっさり
				PRINTFORMW 「駄目」
			CASE 5
				PRINTFORMW 「だったら、腰…止めなさいよ」
				PRINTFORMW 「搾り取られちゃう」
			CASEELSE
				;言葉責め無理矢理
				PRINTFORMW 「そんな事言って、あんたの目は出してって言ってるんだけど」
				CALL FADEOUT(20, "まあいいわ")
				PRINTFORMW 「%RAND_SPLIT("本音/腹の内")%はあんたのお腹に聞けばいいわね」
		ENDSELECT
		[SKIPEND]
		SELECTCASE RAND:3
			CASE 0
				CALL FADEOUT(8, "「そんな事言っても」")
				PRINTFORMW 「あんたの腕も、おまんこも、苦しいくらい抱きしめてきて」
				CALL FADEIN(8, "「外出しだめ」")
				PRINTFORMW 「って言ってるわよ？」
			CASE 1
				PRINTFORMW 「だったら、腰…止めなさいよ」
				PRINTFORMW 「搾り取られちゃう」
			CASEELSE
				;言葉責め無理矢理
				PRINTFORMW 「そんな事言って、あんたの目は出してって言ってるんだけど」
				CALL FADEOUT(20, "まあいいわ")
				PRINTFORMW 「%RAND_SPLIT("本音/腹の内")%はあんたのお腹に聞けばいいわね」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「中に出さない」(REACT分類:許しを乞う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT33, 対面座位

;────────────────────────────────────
;REACT34, 背面座位・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続実行/2=他のＶ性交から/3=Ａ性交から/4=Ｖ性交奉仕から/5=Ａ性交奉仕から)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る/中に出して！/中に出さないで！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_34
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;・キャンセル処理はACT_MESSAGE_OTHERSにて汎用処理
;・主導権は調教者(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失/童貞喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTERが処女なら、処女喪失が起こる。この処理は@ACTION_APPLY2_30にて行われる
;REACT分類が暴れるでキャンセル処理が呼び出された際は処女喪失、童貞喪失は回避
;-------------------------------------------------
IF 口上記入チェック(0) && TRUECHECK(TCVAR:MASTER:処女喪失, TCVAR:童貞喪失) > 0
	;=================================================
	;調教対象が処女喪失＆調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失 && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/TEQUIP:調教者ペニスバンドなどで書き分けても良いでしょう
	;=================================================
	;調教対象が処女喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;派生1の場合はREACT分類:なし
	;=================================================
	;意見を出すなんていい度胸だね！(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 中に出して！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("中に出して！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 中に出さないで！背面座位
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && IS_NOWCOMNAME("中に出さないで！")
	;=================================================
	;中に出す(REACT分類:なし)
	;=================================================
	IF 口上記入チェック(100) && TFLAG:REACT派生 == 1
		SELECTCASE RAND:5
			CASE 0
				PRINTFORM 「%RAND_SPLIT("……/可愛いんだから…/ええ")%
				CALL HEARTBW
				PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の話を聞いているのか、怪しい返事をした
			CASE 1
				;両親のいない%CALLNAME:TARGET%さんは無条件に受け入れて欲しい
				PRINTFORMW 「ごめんね？中に出すから、受け入れて」
			CASE 2
				;支配欲強め
				PRINTFORMW 「駄目って言われてやめるなら、こんな事初めっからしないわよ」
			CASE 3
				;妊娠目的
				PRINTFORMW 「外に出したらできないじゃない」
				PRINTFORMW 「いいからあんたは、孕みなさい」
			CASEELSE
				;ばっさり
				PRINTFORMW 「駄目」だめ
		ENDSELECT
	ENDIF
	;派生1を書いた場合はREACT派生0,「中に出さない」(REACT分類:許しを乞う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT34, 背面座位

;────────────────────────────────────
;REACT35, アナルセックス・個別リアクション
;────────────────────────────────────
;・TFLAG:ACT派生(0=通常/1=連続実行/3=Ｖ性交から/4=Ｖ性交奉仕から/5=Ａ性交奉仕から)
;-------------------------------------------------
;想定されるCOM…
;会話系反応
;	COM:許しを乞う
;愛撫系反応
;	COM:嫌がる/下手だと罵る/快感を我慢する/快感を受け入れる/気持ち良い！
;性交系反応
;	COM:腰を振る/中に出して！/中に出さないで！
;苦痛系反応
;	COM:痛みを我慢する/悲鳴を上げる/暴れる
;-------------------------------------------------
@KOJO_REACT_K1_35
;-------------------------------------------------
;・地の文はREACT分類(受け入れる/積極的従う, 平然と従う/許しを乞う/消極的従う)で分岐
;・キャンセル処理はACT_MESSAGE_OTHERSにて汎用処理
;・主導権は調教者(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;処女喪失/童貞喪失
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTERが処女なら、処女喪失が起こる。この処理は@ACTION_APPLY2_30にて行われる
;REACT分類が暴れるでキャンセル処理が呼び出された際は処女喪失、童貞喪失は回避
;-------------------------------------------------
IF 口上記入チェック(0) && TRUECHECK(TCVAR:MASTER:処女喪失, TCVAR:童貞喪失) > 0
	;=================================================
	;調教対象が処女喪失＆調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失 && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/TEQUIP:調教者ペニスバンドなどで書き分けても良いでしょう
	;=================================================
	;調教対象が処女喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:処女喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が童貞喪失
	;=================================================
	IF 口上記入チェック(0) && TCVAR:童貞喪失
		PRINTFORMW 
		RETURN 1
	ENDIF
;条件にヒットしなければ汎用リアクションに流す
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う(REACT分類:許しを乞う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("許しを乞う")
	;派生1の場合はREACT分類:なし
	;=================================================
	;意見を出すなんていい度胸だね！(お仕置きポイント加算)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「ふーん」(調教対象の意見を黙殺)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1の場合はREACT分類:暴れる扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 嫌がる(REACT分類:消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("嫌がる")
	;=================================================
	;嫌がって妨害した(REACT分類:暴れる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「嫌がるがACTは妨害せず」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 下手だと罵る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("下手だと罵る")
	;=================================================
	;快感を感じながら(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「実際に下手な場合」(REACT分類:平然と従う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 不敵に笑う(REACT分類:平然と従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("不敵に笑う")
	PRINTFORMW 
	RETURN 1
ENDIF

;派生1の場合はREACT分類:許しを乞う扱い
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 快感を我慢する(消極的従う)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を我慢する")
	;=================================================
	;哀願(REACT分類:許しを乞う)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「耐久」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 快感を受け入れる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("快感を受け入れる")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 気持ち良い！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("気持ち良い！")
	PRINTFORMW 
	RETURN 1
ENDIF

;主導権によってREACT分類が変化する事に注意
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 腰を振る(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("腰を振る")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 中に出して！(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("中に出して！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 中に出さないで！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("中に出さないで！")
	;=================================================
	;中に出す(REACT分類:なし)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「中に出さない」(REACT分類:許しを乞う)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 痛みを我慢する(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("痛みを我慢する")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる(REACT分類:受け入れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("悲鳴を上げる")
	;=================================================
	;恐怖による悲鳴
	;=================================================
	IF 口上記入チェック(0) && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1を書いた場合はREACT派生0,「痛みによる悲鳴」扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;派生1.派生2の場合はREACT分類:消極的従う扱い
;REACT分類が暴れるであった場合は性交はキャンセルになる
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 暴れる(REACT分類:暴れる)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && IS_NOWCOMNAME("暴れる")
	;=================================================
	;暴れ失敗(REACT分類:消極的従う)
	;=================================================
	IF 口上記入チェック(0) && GROUPMATCH(TFLAG:REACT派生, 1, 2)
		[SKIPSTART]
		※内容を書き分けるならコメントアウトを解除して使用して下さい
		;縄拘束による暴れ失敗
		IF TFLAG:REACT派生 == 1
			PRINTFORMW 
		;制止による暴れ失敗
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;手間を掛けたくないなら条件分岐させず、まとめてしまうと良いです
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;派生1, 2を書いた場合はREACT派生0,「暴れ成功」(REACT分類:暴れる)扱い
	;書かなかった場合はREACT派生に関わらず、この箇所を補完します
	;ただCOM32暴れるの場合、REACT派生によって内容が大きく異なるので、書き分ける方が望ましいと考えます
	;=================================================
	;その他
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT35, アナルセックス
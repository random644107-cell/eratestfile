;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	霊夢口上 調教終了イベント口上ファイル
;
;	調教終了イベント口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 調教終了口上
;========================================================================
;・@EVENTCOMENDから呼び出される
;・口上主が調教終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に口上を表示
;・ARG:0の場合は固有の地の文なし。口上表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと口上表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:日常制御	調教終了時状況(1=通常/2=精力0/3=一回休み/4=失神した)
;-------------------------------------------------
@KOJO_EVENT_K1_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 調教対象が疲れている、2, 調教対象が失神した)
;-------------------------------------------------
IF TALENT:Ｖ敏感 == 1
	PRINTFORML
	PRINTFORMDW 子作りセックスに反応していた%CALLNAME:TARGET%の膣は、元の感度に戻った
	TALENT:Ｖ敏感 = 0
ENDIF
IF TALENT:魅惑 == 1
	PRINTFORML
	PRINTFORMDW %CALLNAME:TARGET%の湯上り状態と共に魅惑は解除されました
	TALENT:魅惑 = 0
ENDIF

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・失神による調教終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・調教対象消耗度に応じて疲労/失神に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→口上の表示
;・EVENT201, 介抱(日常イベント)の箇所を書く場合は、上手く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	PRINTFORMW 「そろそろ切り上げましょうか」
	PRINTFORMW 「倒れるまでして欲しかった？そんなに焦らないでも、私は」
	CALL FADEIN(20, "「逃がさないわよ♪」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が失神した
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 2
	PRINTFORMDW %CALLNAME:TARGET%は意識を失った%CALLNAME:MASTER%を担ぎ上げた
	RETURN 1
ENDIF

;────────────────────────────────────
;屈服終了　今回で取得したか？を参照しないと意味がない
;────────────────────────────────────
;【屈服:25】膣内射精取得初回
IF 口上記入チェック(100) && GETBIT(CFLAG:刻印, 25) && FIRSTEVENT(125)
	PRINTFORML 「ね。どうだった？何ってその」
	CALL FADEOUT(6, "  もじもじ")
	PRINTFORML 「こづくり。」
	PRINTFORML 「こづくりえっち、好きになれそう？」
	RETURN 1
ENDIF
;【屈服:11】キス取得初回
IF 口上記入チェック(100) && GETBIT(CFLAG:刻印, 11) && FIRSTEVENT(111)
	CALL FADEOUT(4, "  ちら")
	PRINTFORMDL %CALLNAME:TARGET%は行為を切り上げようとしつつも、
	CALL FADEOUT(4, "  ちら")
	PRINTFORMDL ひっきりなしに%CALLNAME:MASTER%の口元に視線を向けてきた…
	PRINTFORML 
	PRINTFORMDL 自らの唇に指をあてる%CALLNAME:TARGET%
	PRINTFORML 「口付けしたんだから、これで私達[恋人]よね」
	PRINTFORMW 「いいでしょ、夫婦兼恋人よ。」
	CALL FADEIN(6, "「んふふ♪」")
	PRINTFORMW 「ほら、私達ってそういう時期が無かったじゃない」
	PRINTFORML 
	PRINTFORMDW %CALLNAME:TARGET%は調教を切り上げると汗を流しに浴室に向かった…
	;見えない後ろ姿に聞こえる鼻歌弾む足取りに合わせて少女綺想曲
	CALL FADEOUT(12, "「らんらんらーらーらー♪ららんらーらーら♪」")
	RETURN 1
ENDIF
;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・通常の調教終了時の口上
;・地の文なし。PRINTLによる空行の後に口上表示
;・枠毎にイベント番号を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;大満足ボーナスを得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:今回で大満足
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(10)
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象の精力0
	;=================================================
	IF 口上記入チェック(100) && FLAG:日常制御 == 2
		PRINTFORML 「今日も楽しかったわ」
		PRINTFORMDL %CALLNAME:TARGET%はツヤツヤとしながら%CALLNAME:MASTER%ににっこりとした笑顔を向けた
		RETURN 1
	ENDIF

	;=================================================
	;できちゃった　孕ませてから産むまで物足りない為展開強化
	;=================================================
	IF 口上記入チェック(0) && CFLAG:妊娠 == 10
		PRINTFORMDL 
		RETURN 1
	ENDIF

	IF 口上記入チェック(100) && CFLAG:妊娠 == 4
		PRINTFORMDW %CALLNAME:TARGET%は体温計のようなものを手渡してきた
		PRINTFORMDL 体温計のようなものには丸に縦線が二つ並び表示されている…
		PRINTFORML 
		PRINTFORMDL どうやら明日と明後日は晴れるようだ…
		PRINTFORMDL %CALLNAME:TARGET%には陽がよく似合う
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
	;何の話だったか忘失
	;ここでリターンさせ下部を大満足していない%CALLNAME:TARGET%さんに仕立て上げる
	RETURN 1
ENDIF

;*************************************************
;以下、好感度による派生
;*************************************************

;EVENT11あたりで任意のKFLAGを立てて条件にしてやると話の辻褄は合わせやすいです
;━━━━━━━━━━━━━━━━━━━━━━━━
;相思相愛（好意3000以上・好感度3000以上）
;-------------------------------------------------
;・互いに好き合っている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("相思相愛")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(20)
		PRINTFORMDW %CALLNAME:MASTER%の部屋を出ようとし、一度振り向き口元をほころばせる
		PRINTFORML 「幸せそうな表情しちゃって。」
		RETURN 1
	ENDIF
	[SKIPSTART]
	;=================================================
	;ふたなり%CALLNAME:TARGET%さんが大満足していないかつ性交屈服獲得済み【屈服:22】膣内射精された
	;=================================================
		;=================================================
		;%CALLNAME:MASTER%の処女を貰ってご機嫌
		;=================================================
		
		;=================================================
		;%CALLNAME:MASTER%がオトコかつアナル開発済みの場合お尻を使われる
		;=================================================
		
	[SKIPEND]
	;=================================================
	;大満足していないかつ性交奉仕済み
	;=================================================
	;%CALLNAME:TARGET%とエッチした事ある場合の条件設定が思いつかないので孕ませ後のご褒美に位置付ける
	;大満足は発生しなくとも毎晩満足はさせる。サティスファクション！
	IF 口上記入チェック(100)
		PRINTFORML 「うーん。ここからはあんたの好きにしていいわ」
		PRINTFORMDW %CALLNAME:TARGET%はまだ満足していないようだ…
		PRINTFORML 
		;%CALLNAME:MASTER%が%CALLNAME:TARGET%に惚れている事を暗示
		PRINTFORMDL %CALLNAME:MASTER%は%CALLNAME:TARGET%が満足するまでゆっくりと後戯を楽しんだ
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		RETURN 1
	ENDIF
ENDIF

;好感度の伸び的に溺愛の後になると思われます
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教完了（好意2000以上）
;-------------------------------------------------
;・調教対象が口上主にもうメロメロ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("調教完了")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(30)
		PRINTFORMDW 今の関係を一方的に築かれてから随分と長い時が過ぎたように思う
		PRINTFORMDW 涼しげな%CALLNAME:TARGET%の横顔ばかり見ていた以前を顧みる
		PRINTFORMDW この関係が始まってから、色々な%CALLNAME:TARGET%の表情を見ることができた
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;溺愛（好感度2000以上）
;-------------------------------------------------
;・調教者の方が調教対象にはまってしまい、深かったり倒錯した愛を注いでいる
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("溺愛")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(40)
		PRINTFORMDW 部屋を出ようとした%CALLNAME:TARGET%が振り返る
		;ここはあなた表記の方が雰囲気が出る
		PRINTFORMW 「ね。%CALLNAME:MASTER%」
		;一度目は聞き逃しかねない。
		CALL FADEOUT(20, "「好きよ」")
		;大事な事なので二回目ははっきりと。
		CALL FADEIN(20, "「好き」")
		PRINTFORML
		PRINTFORMDW %CALLNAME:TARGET%はそう告げると%CALLNAME:MASTER%の返事も聞かず、そそくさと行ってしまった
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教済み（好意1000以上）
;-------------------------------------------------
;・調教対象が口上主に調教されるのを幸せに感じている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("調教済み")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(50)
		PRINTFORMDW %CALLNAME:TARGET%は何故こんな事をしでかしたのだろう
		PRINTFORMDL 本人に訊ねた処で
		;ここは桃色口上色
		PRINTFORMW 「勘よ」
		PRINTFORMDL の一言で片づけられてしまうだろうか
		PRINTFORMDW そう苦笑いする
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;嫌悪（好意-100未満）
;-------------------------------------------------
;・調教者に対し嫌悪感を抱いている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("嫌悪")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(60)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;・心に傷を作られ、ご主人様の姿を見るだけで怯えてしまう
;・トラウマの種は「%ACTSTR(CFLAG:MASTER:8)%」で表示できる
;　例）PRINTFORMW 「もう%ACTSTR(CFLAG:MASTER:8)%は嫌ですって？」

;━━━━━━━━━━━━━━━━━━━━━━━━
;恐怖（好意200未満・トラウマLv3以上）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("恐怖")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(70)
		;コメディ色が強いのでコメントアウト
		;PRINTFORMW 「もう『連日素麺』は嫌？…五箱も送りつけてきた奴に言いなさい」
		;PRINTFORML
		;PRINTFORMDW %CALLNAME:MASTER%は光の消えた瞳でおそうめんをすすりあげた…
		;PRINTFORML
		;PRINTFORMDW ずぞっぞぞっ…
		;PRINTFORMW ちゅるっ…
		;PRINTFORML
		;PRINTFORMDW 山山葵がツンと%CALLNAME:MASTER%の鼻腔を刺激すると…涙が零れた…
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;反抗（好意0以下・反抗刻印Lv1以上）
;-------------------------------------------------
;・心を開かない奴隷に苛つくご主人様
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && AFFECTION("反抗")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTEVENT(80)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;目覚め（好意500以上）
;-------------------------------------------------
;・調教されるのもむしろ好きになってきたかも知れない…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("目覚め")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(90)
		PRINTFORMDW ろくでもない始まり方ではあったが、その甲斐あって二人の関係は確かな進展を見せていた
		PRINTFORMDW 元々悪しからずは思ってはいたとはいえ、%CALLNAME:TARGET%の事が頭から離れない
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;順応（好意100以上・好感度100以上）
;-------------------------------------------------
;・丁寧に調教を進められて、思ったより嫌じゃないかも…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && AFFECTION("順応")
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(100)
		PRINTFORMDW 少しづつだが、今の状況に好意的な解釈を持つようになってきた自分がいる
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;序盤（条件なし）
;-------------------------------------------------
;・ここまでの条件にヒットしなかった場合
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(110)
		PRINTFORMW 「大抵の事は上手くできるんだけど…色事はそうもいかないみたいね…」
		PRINTFORMW 「気長にやっていきましょ」
		PRINTFORMW 「これからも、長い付き合いになるんだから…」
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「今日も楽しかったわよ」
		PRINTFORMW 「また来るわ」
		RETURN 1
	ENDIF
ENDIF